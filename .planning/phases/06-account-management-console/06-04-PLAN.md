---
phase: 06
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - apps/admin-web/src/pages/DashboardPage.tsx
  - apps/admin-web/src/pages/UsersPage.tsx
  - apps/admin-web/src/main.tsx
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02
  - MGMT-06

must_haves:
  truths:
    - "概览页展示 4 个统计卡片：已注册用户、活跃设备、注册 Gateway、近 7 天审计事件"
    - "用户页展示用户表格：邮箱/注册时间/登录次数/登录失败次数/最后登录/活跃设备数"
    - "用户表格有加载态（Skeleton）、错误态（重试按钮）、空态（暂无用户）"
    - "用户表格支持分页（20 条/页）"
    - "页面使用 UI-SPEC.md 规定的 dark 主题 CSS 变量和字体"
  artifacts:
    - path: "apps/admin-web/src/pages/DashboardPage.tsx"
      provides: "概览页，4 个 Card 组件展示统计数字"
    - path: "apps/admin-web/src/pages/UsersPage.tsx"
      provides: "用户管理页，Table + Skeleton + Pagination"
    - path: "apps/admin-web/src/main.tsx"
      provides: "路由入口更新，PlaceholderPage 替换为真实页面"
  key_links:
    - from: "apps/admin-web/src/pages/DashboardPage.tsx"
      to: "apps/admin-web/src/lib/admin-api.ts"
      via: "getDashboardStats(token) 在 useEffect 中调用"
      pattern: "getDashboardStats"
    - from: "apps/admin-web/src/pages/UsersPage.tsx"
      to: "apps/admin-web/src/lib/admin-api.ts"
      via: "listUsers(token, page) 在 useEffect 中调用"
      pattern: "listUsers"
---

<objective>
实现管理控制台的 Dashboard（概览）页和 Users（用户）页，替换 Plan 01 中的 PlaceholderPage 占位符。

Purpose: 让管理员登录后能立即看到账户系统的整体状态（4 项统计），并浏览用户列表及登录分析数据。
Output: /admin/dashboard 展示 4 个统计卡片；/admin/users 展示可分页的用户表格。
</objective>

<execution_context>
@/Users/dream/code/tether/.planning/phases/06-account-management-console/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/phases/06-account-management-console/06-UI-SPEC.md
@/Users/dream/code/tether/.planning/phases/06-account-management-console/06-PATTERNS.md

<interfaces>
<!-- Plan 01 创建的接口，executor 直接使用 -->

From apps/admin-web/src/lib/admin-api.ts (Plan 01 创建):
```typescript
export type AdminUser = {
  id: string; email: string; createdAt: number;
  loginCount: number; failedLoginCount: number;
  lastLoginAt: number | null; activeDeviceCount: number;
};

export async function getDashboardStats(token: string): Promise<{
  totalUsers: number; activeDevices: number;
  registeredGateways: number; auditEventsLast7Days: number;
}>;

export async function listUsers(token: string, page?: number): Promise<{
  users: AdminUser[]; total: number;
}>;
```

From apps/admin-web/src/hooks/use-admin-auth.ts (Plan 01 创建):
```typescript
export function useAdminAuth(): {
  managementAuth: AuthStorageRecord<ManagementIdentity> | null;
  authReady: boolean;
  loginManagement: (input: { email: string; password: string }) => Promise<void>;
  logoutManagement: () => void;
};
```

From 06-UI-SPEC.md — Dashboard 页面规格:
- 布局：2 列统计卡片网格（≥768px），单列移动端
- 4 个卡片：已注册用户、活跃设备、注册 Gateway、近 7 天审计事件
- 每个卡片：Card 组件，数字 24px/600（display），标签 12px/600/muted
- 数字是页面第一视觉焦点

From 06-UI-SPEC.md — Users 页面规格:
- 表格列：邮箱 / 注册时间 / 登录次数 / 登录失败次数 / 最后登录 / 活跃设备数
- 分页：20 条/页，shadcn Pagination
- 加载态：Skeleton 行（高度 48px，行数与 page size 相同）
- 错误态：错误消息 + "重试" 按钮
- 空态文案：暂无用户 / 当前工作区还没有注册用户。

From 06-UI-SPEC.md — shadcn 组件:
- Card (已在 apps/admin-web/src/components/ui/card.tsx)
- Table: 需要新增 apps/admin-web/src/components/ui/table.tsx
- Badge: 需要新增 apps/admin-web/src/components/ui/badge.tsx
- Skeleton: 需要新增 apps/admin-web/src/components/ui/skeleton.tsx
- Pagination: 需要新增 apps/admin-web/src/components/ui/pagination.tsx

shadcn 组件复制源：apps/web 没有这些组件（仅有 button/card/input/label/form）。
需要从 shadcn 官方 CLI 安装到 apps/admin-web：
```bash
cd apps/admin-web
npx shadcn@latest add table badge skeleton pagination
```

From 06-UI-SPEC.md — Copywriting:
- 空态 users: "暂无用户" / "当前工作区还没有注册用户。"
- 加载态: "加载中…"
- 分页: 上一页 / 下一页 / 第 {n} 页，共 {total} 条
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装缺少的 shadcn 组件 + DashboardPage</name>
  <read_first>
    - apps/admin-web/components.json （确认 shadcn 配置正确）
    - apps/admin-web/src/components/ui/button.tsx （确认 ui 组件目录存在）
    - apps/admin-web/src/lib/admin-api.ts （getDashboardStats 签名）
    - apps/admin-web/src/hooks/use-admin-auth.ts （useAdminAuth hook）
  </read_first>
  <files>
    apps/admin-web/src/components/ui/table.tsx,
    apps/admin-web/src/components/ui/badge.tsx,
    apps/admin-web/src/components/ui/skeleton.tsx,
    apps/admin-web/src/components/ui/pagination.tsx,
    apps/admin-web/src/pages/DashboardPage.tsx
  </files>
  <action>
**步骤 1：安装 shadcn 组件**
```bash
cd /Users/dream/code/tether/apps/admin-web
npx shadcn@latest add table badge skeleton pagination --yes
```
这会在 src/components/ui/ 下生成 table.tsx、badge.tsx、skeleton.tsx、pagination.tsx 文件。

如果 shadcn CLI 报错（如 components.json 配置问题），手动创建这些组件：
- skeleton.tsx:
```typescript
import { cn } from '@/lib/utils.js';
function Skeleton({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) {
  return <div className={cn('animate-pulse rounded-md bg-muted', className)} {...props} />;
}
export { Skeleton };
```
- badge.tsx: 参照 shadcn 官方 badge 组件模式（variant=default/secondary/destructive/outline）
- table.tsx: 参照 shadcn 官方 table 组件（Table, TableHeader, TableBody, TableRow, TableHead, TableCell, TableCaption）
- pagination.tsx: 参照 shadcn 官方 pagination 组件

**步骤 2：创建 DashboardPage.tsx**

```typescript
import * as React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '../components/ui/card.js';
import { getDashboardStats } from '../lib/admin-api.js';
import { useAdminAuth } from '../hooks/use-admin-auth.js';

type Stats = {
  totalUsers: number;
  activeDevices: number;
  registeredGateways: number;
  auditEventsLast7Days: number;
};

export function DashboardPage() {
  const { managementAuth } = useAdminAuth();
  const [stats, setStats] = React.useState<Stats | null>(null);
  const [error, setError] = React.useState<string | null>(null);

  React.useEffect(() => {
    if (!managementAuth?.accessToken) return;
    getDashboardStats(managementAuth.accessToken)
      .then(setStats)
      .catch((err: unknown) => setError(err instanceof Error ? err.message : 'load_failed'));
  }, [managementAuth]);

  const statItems: Array<{ label: string; value: number | string }> = stats
    ? [
        { label: '已注册用户', value: stats.totalUsers },
        { label: '活跃设备', value: stats.activeDevices },
        { label: '注册 Gateway', value: stats.registeredGateways },
        { label: '近 7 天审计事件', value: stats.auditEventsLast7Days }
      ]
    : [
        { label: '已注册用户', value: '—' },
        { label: '活跃设备', value: '—' },
        { label: '注册 Gateway', value: '—' },
        { label: '近 7 天审计事件', value: '—' }
      ];

  if (error) {
    return (
      <p className="text-destructive text-sm">数据加载失败，请检查网络连接后重试。</p>
    );
  }

  return (
    <div>
      <h1 className="text-lg font-semibold mb-6">概览</h1>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
        {statItems.map(item => (
          <Card key={item.label}>
            <CardHeader className="pb-2">
              <CardTitle className="text-xs font-semibold text-muted-foreground uppercase tracking-wide">
                {item.label}
              </CardTitle>
            </CardHeader>
            <CardContent>
              <p className="text-2xl font-semibold">{item.value}</p>
            </CardContent>
          </Card>
        ))}
      </div>
    </div>
  );
}
```

遵循 UI-SPEC.md 规定：
- 统计数字 text-2xl（对应 24px）+ font-semibold（600）
- 卡片标签 text-xs（12px）+ font-semibold + text-muted-foreground
- 2 列网格（sm 及以上），移动端单列
  </action>
  <verify>
    <automated>
      ls /Users/dream/code/tether/apps/admin-web/src/components/ui/table.tsx &&
      ls /Users/dream/code/tether/apps/admin-web/src/components/ui/badge.tsx &&
      ls /Users/dream/code/tether/apps/admin-web/src/components/ui/skeleton.tsx &&
      ls /Users/dream/code/tether/apps/admin-web/src/components/ui/pagination.tsx &&
      grep -c 'getDashboardStats' /Users/dream/code/tether/apps/admin-web/src/pages/DashboardPage.tsx &&
      grep -c '已注册用户\|活跃设备\|注册 Gateway\|近 7 天审计事件' /Users/dream/code/tether/apps/admin-web/src/pages/DashboardPage.tsx
    </automated>
  </verify>
  <done>
    4 个 shadcn 组件文件存在；DashboardPage.tsx 包含 getDashboardStats 调用和 4 个统计标签。
  </done>
</task>

<task type="auto">
  <name>Task 2: UsersPage + main.tsx 路由更新</name>
  <read_first>
    - apps/admin-web/src/pages/DashboardPage.tsx （刚创建的，了解页面组件格式）
    - apps/admin-web/src/components/ui/table.tsx （Table 组件 API）
    - apps/admin-web/src/components/ui/skeleton.tsx （Skeleton 组件 API）
    - apps/admin-web/src/components/ui/pagination.tsx （Pagination 组件 API）
    - apps/admin-web/src/lib/admin-api.ts （AdminUser type + listUsers 签名）
    - apps/admin-web/src/main.tsx （Plan 01 创建的，需要替换 PlaceholderPage）
  </read_first>
  <files>
    apps/admin-web/src/pages/UsersPage.tsx,
    apps/admin-web/src/main.tsx
  </files>
  <action>
**apps/admin-web/src/pages/UsersPage.tsx** — 新建（完整实现）：

功能：
1. mount 时调用 `listUsers(token, page)` 获取用户列表
2. 加载中：渲染 20 行 Skeleton（每行高度 h-12）
3. 加载失败：显示错误消息 + "重试" 按钮（重新 fetch）
4. 空列表：显示空态文案
5. 正常：Table 展示 6 列数据 + Pagination

```typescript
import * as React from 'react';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '../components/ui/table.js';
import { Skeleton } from '../components/ui/skeleton.js';
import { Button } from '../components/ui/button.js';
import { listUsers, type AdminUser } from '../lib/admin-api.js';
import { useAdminAuth } from '../hooks/use-admin-auth.js';

const PAGE_SIZE = 20;

function formatDate(ms: number | null): string {
  if (!ms) return '—';
  return new Date(ms).toLocaleString('zh-CN', { hour12: false });
}

export function UsersPage() {
  const { managementAuth } = useAdminAuth();
  const [users, setUsers] = React.useState<AdminUser[]>([]);
  const [total, setTotal] = React.useState(0);
  const [page, setPage] = React.useState(1);
  const [loading, setLoading] = React.useState(true);
  const [error, setError] = React.useState<string | null>(null);

  const fetchUsers = React.useCallback((p: number) => {
    if (!managementAuth?.accessToken) return;
    setLoading(true);
    setError(null);
    listUsers(managementAuth.accessToken, p)
      .then(result => {
        setUsers(result.users);
        setTotal(result.total);
      })
      .catch((err: unknown) => setError(err instanceof Error ? err.message : 'load_failed'))
      .finally(() => setLoading(false));
  }, [managementAuth]);

  React.useEffect(() => { fetchUsers(page); }, [fetchUsers, page]);

  const totalPages = Math.max(1, Math.ceil(total / PAGE_SIZE));

  return (
    <div>
      <h1 className="text-lg font-semibold mb-6">用户</h1>

      {error && (
        <div className="flex items-center gap-3 mb-4">
          <p className="text-destructive text-sm">数据加载失败，请检查网络连接后重试。</p>
          <Button variant="outline" size="sm" onClick={() => fetchUsers(page)}>重试</Button>
        </div>
      )}

      <div className="rounded-md border">
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead className="text-xs font-semibold">邮箱</TableHead>
              <TableHead className="text-xs font-semibold">注册时间</TableHead>
              <TableHead className="text-xs font-semibold">登录次数</TableHead>
              <TableHead className="text-xs font-semibold">登录失败次数</TableHead>
              <TableHead className="text-xs font-semibold">最后登录</TableHead>
              <TableHead className="text-xs font-semibold">活跃设备数</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {loading
              ? Array.from({ length: PAGE_SIZE }).map((_, i) => (
                  <TableRow key={i}>
                    {Array.from({ length: 6 }).map((_, j) => (
                      <TableCell key={j}><Skeleton className="h-4 w-full" /></TableCell>
                    ))}
                  </TableRow>
                ))
              : users.length === 0
              ? (
                  <TableRow>
                    <TableCell colSpan={6} className="text-center py-12 text-muted-foreground">
                      <p className="font-medium">暂无用户</p>
                      <p className="text-xs mt-1">当前工作区还没有注册用户。</p>
                    </TableCell>
                  </TableRow>
                )
              : users.map(user => (
                  <TableRow key={user.id}>
                    <TableCell className="text-sm">{user.email}</TableCell>
                    <TableCell className="text-sm text-muted-foreground">{formatDate(user.createdAt)}</TableCell>
                    <TableCell className="text-sm">{user.loginCount}</TableCell>
                    <TableCell className="text-sm text-destructive">{user.failedLoginCount}</TableCell>
                    <TableCell className="text-sm text-muted-foreground">{formatDate(user.lastLoginAt)}</TableCell>
                    <TableCell className="text-sm">{user.activeDeviceCount}</TableCell>
                  </TableRow>
                ))
            }
          </TableBody>
        </Table>
      </div>

      {!loading && total > 0 && (
        <div className="flex items-center justify-between mt-4 text-sm text-muted-foreground">
          <span>第 {page} 页，共 {total} 条</span>
          <div className="flex gap-2">
            <Button variant="outline" size="sm" disabled={page <= 1} onClick={() => setPage(p => p - 1)}>
              上一页
            </Button>
            <Button variant="outline" size="sm" disabled={page >= totalPages} onClick={() => setPage(p => p + 1)}>
              下一页
            </Button>
          </div>
        </div>
      )}
    </div>
  );
}
```

**apps/admin-web/src/main.tsx** — 读取 Plan 01 创建的内容，将 DashboardPage 和 UsersPage 的 PlaceholderPage 替换为真实组件：

1. 添加 import：
```typescript
import { DashboardPage } from './pages/DashboardPage.js';
import { UsersPage } from './pages/UsersPage.js';
```

2. 将路由中 `/admin/dashboard` 的 `<PlaceholderPage title="概览" />` 替换为 `<DashboardPage />`
3. 将路由中 `/admin/users` 的 `<PlaceholderPage title="用户" />` 替换为 `<UsersPage />`
4. 保留其他路由的 PlaceholderPage（DevicesPage、GatewaysPage、AuditPage 由 Plan 05 替换）
  </action>
  <verify>
    <automated>
      grep -c 'listUsers\|AdminUser' /Users/dream/code/tether/apps/admin-web/src/pages/UsersPage.tsx &&
      grep -c '暂无用户\|Skeleton\|TableRow\|TableCell' /Users/dream/code/tether/apps/admin-web/src/pages/UsersPage.tsx &&
      grep -c '上一页\|下一页' /Users/dream/code/tether/apps/admin-web/src/pages/UsersPage.tsx &&
      grep -c 'DashboardPage\|UsersPage' /Users/dream/code/tether/apps/admin-web/src/main.tsx &&
      cd /Users/dream/code/tether/apps/admin-web && pnpm typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    UsersPage.tsx 包含 listUsers 调用、Skeleton 加载态、空态文案、分页按钮；main.tsx 已 import DashboardPage 和 UsersPage 并使用；pnpm typecheck 通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardPage/UsersPage → admin-api.ts | 通过 accessToken 读取只读数据，无写操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | DashboardPage 展示用户总数 | accept | 统计数字本身不包含 PII；只有登录的管理用户才能看到 |
| T-06-04-02 | Information Disclosure | UsersPage 展示用户邮箱 | accept | 管理控制台的核心功能就是查看用户信息；只有通过 management token 鉴权的管理用户才能访问 |
| T-06-04-03 | Spoofing | 过期 token 加载数据 | mitigate | API 返回 401 时，admin-api.ts requestJson 抛出 Error；页面显示错误态；AdminAuthContext 在 401 时清空 localStorage 并重定向 /admin/login |
</threat_model>

<verification>
1. `pnpm typecheck`（在 apps/admin-web 目录）— 退出码 0
2. 开发服务器运行时，访问 /admin/dashboard — 展示 4 个统计卡片（数字加载后显示）
3. 访问 /admin/users — 展示用户表格，加载中显示 Skeleton，空数据时显示空态
4. `/admin/users` 页面有分页控件，点击"下一页"时 page 递增
</verification>

<success_criteria>
- DashboardPage 展示 4 个统计卡片，数字使用 text-2xl 样式
- UsersPage 展示 6 列用户表格，含 Skeleton 加载态和空态
- 分页控件显示"第 N 页，共 M 条"
- pnpm typecheck 通过
</success_criteria>

<output>
完成后创建 .planning/phases/06-account-management-console/06-04-SUMMARY.md
</output>
