---
phase: 06
plan: 03
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - apps/server/app/service/admin/devices.ts
  - apps/server/app/service/admin/gateways.ts
  - apps/server/app/service/admin/audit.ts
  - apps/server/app/controller/admin/devices.ts
  - apps/server/app/controller/admin/gateways.ts
  - apps/server/app/controller/admin/audit.ts
  - apps/server/app/service/storage.ts
  - apps/server/app/router/admin.ts
autonomous: true
requirements:
  - MGMT-03
  - MGMT-04
  - MGMT-05

must_haves:
  truths:
    - "GET /admin/api/devices 返回分页设备列表（含所属用户邮箱、在线状态、最后在线时间、是否吊销）"
    - "POST /admin/api/devices/:id/revoke 成功吊销设备，revoked_at 写入 devices 表，同时撤销该设备的所有 active refresh tokens"
    - "GET /admin/api/gateways 返回分页 Gateway 列表（含最后认证时间、在线状态）"
    - "DELETE /admin/api/gateways/:id/unlink 删除 Gateway 记录（取消链接）"
    - "GET /admin/api/audit 支持按 userId/action/deviceId/gatewayId/from/to 筛选，分页返回审计事件"
    - "所有端点无 management token 时返回 401"
    - "revoke/unlink 操作记录 audit event"
  artifacts:
    - path: "apps/server/app/service/admin/devices.ts"
      provides: "listAdminDevices, revokeAdminDevice service 函数（revokeAdminDevice 含撤销 refresh tokens 逻辑）"
    - path: "apps/server/app/service/admin/gateways.ts"
      provides: "listAdminGateways, unlinkAdminGateway service 函数"
    - path: "apps/server/app/service/admin/audit.ts"
      provides: "listAdminAuditEvents service 函数（支持 userId/action/deviceId/gatewayId/from/to 多条件筛选）"
    - path: "apps/server/app/service/storage.ts"
      provides: "loadAllDevices, revokeDeviceById, revokeRefreshTokensByDeviceId, loadAllGateways, deleteGatewayById, loadAuditEventsFiltered 新增函数"
    - path: "apps/server/app/router/admin.ts"
      provides: "devices/gateways/audit 路由注册"
  key_links:
    - from: "apps/server/app/controller/admin/devices.ts"
      to: "apps/server/app/service/admin/devices.ts"
      via: "revokeAdminDevice 调用 revokeDeviceById 写 revokedAt + revokeRefreshTokensByDeviceId 撤销 tokens"
      pattern: "revokeAdminDevice\|revokedAt\|revokeRefreshTokensByDeviceId"
    - from: "apps/server/app/service/admin/audit.ts"
      to: "apps/server/app/service/storage.ts"
      via: "loadAuditEventsFiltered 支持 deviceId/gatewayId 参数"
      pattern: "deviceId\|gatewayId\|loadAuditEventsFiltered"
    - from: "apps/server/app/controller/admin/gateways.ts"
      to: "apps/server/app/service/admin/gateways.ts"
      via: "unlinkAdminGateway 调用 deleteGatewayById 删除记录"
      pattern: "unlinkAdminGateway\|deleteGatewayById"
---

<objective>
扩展 apps/server 管理 API 层，添加设备管理、Gateway 管理和审计日志查询端点（含筛选）。同时在 storage.ts 补充必要的批量查询和写操作函数。设备吊销操作同时撤销该设备的所有 active refresh tokens，防止已吊销设备继续刷新 token。

Purpose: 为 Plan 05 的 DevicesPage、GatewaysPage、AuditPage 提供完整的 API 支撑。
Output: /admin/api/devices、/admin/api/gateways、/admin/api/audit 端点完整可用，revoke/unlink 操作正确写库并记录 audit 事件，audit 支持 deviceId/gatewayId 筛选维度。
</objective>

<execution_context>
@/Users/dream/code/tether/.planning/phases/06-account-management-console/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/phases/06-account-management-console/06-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/06-account-management-console/06-PATTERNS.md

<interfaces>
<!-- 从 Plan 02 和现有 storage.ts 提取的关键接口 -->

From apps/server/app/service/runtime.ts — DeviceRecord (当前缺少 revokedAt 字段):
```typescript
export type DeviceRecord = {
  id: string; accountId: string; workspaceId: string;
  userId?: string; adminUserId?: string; name: string; platform: string;
  createdAt: number; updatedAt: number;
  // 注意：runtime.ts 中 DeviceRecord 没有 revokedAt 字段
  // 但 storage.ts deviceFromRow 函数从 devices.revoked_at 列映射
  // revokedAt 实际上在 storage.ts 返回的对象中存在，只是 type 未声明
};
```

从 sql/001_init.sql 确认：
- devices.revoked_at DATETIME DEFAULT NULL — 非空表示已吊销
- gateways 表没有 unlinked_at 列，"取消链接" = DELETE FROM gateways WHERE id = ?
- refresh_tokens 表包含 device_id 列，可据此查找并撤销某设备的所有 active tokens

From apps/server/app/service/storage.ts — saveDevice 函数（已有，可用于写 revokedAt）:
实际上 saveDevice 做的是 INSERT，不是 UPDATE。
需要新增 revokeDeviceById 函数：
```sql
UPDATE devices SET revoked_at = NOW(), updated_at = NOW() WHERE id = ?
```

revokeRefreshTokensByDeviceId 函数（需新增，撤销设备关联的所有 active refresh tokens）：
```sql
UPDATE refresh_tokens SET revoked = TRUE WHERE device_id = ? AND revoked = FALSE
```

From apps/server/app/service/storage.ts — loadAuditEvents (现有，无筛选):
```typescript
export async function loadAuditEvents(): Promise<AuditEventRecord[]> {
  const [rows] = await execute('SELECT * FROM audit_events ORDER BY id ASC');
  return (rows as Record<string, unknown>[]).map(auditEventFromRow);
}
```
需要新增 loadAuditEventsFiltered（支持 userId/eventType/deviceId/gatewayId/from/to/limit/offset）。

From apps/server/app/service/storage.ts — auditEventFromRow:
audit_events.event_type 列 → AuditEventRecord.action 字段的映射在 auditEventFromRow 中。
audit_events 表包含 device_id 和 gateway_id 列（来自 SQL schema）。

From apps/server/app/service/audit.ts — recordAuditEvent (已有，用于记录 revoke/unlink 操作):
```typescript
export async function recordAuditEvent(input: Omit<AuditEventRecord, 'id' | 'createdAt'>) { ... }
```

From Plan 02 - admin-auth middleware:
```typescript
import { requireManagementToken } from '../../middleware/admin-auth.js';
// 在每个 controller 方法第一行调用
const identity = requireManagementToken(this.ctx.get('authorization'), this.app.config);
```

401 触发条件: error.message === 'missing_token' || error.message === 'wrong_token_class'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: storage.ts 追加设备/Gateway/审计批量查询函数（含 deviceId/gatewayId 筛选 + 设备 token 撤销）</name>
  <read_first>
    - apps/server/app/service/storage.ts （末尾部分，了解现有函数格式后追加；重点看 loadDeviceById、saveDevice、loadGatewayById、loadAuditEvents、auditEventFromRow、deviceFromRow 的实现）
    - apps/server/app/service/runtime.ts （DeviceRecord 完整 type，了解 revokedAt 是否在 type 中）
    - apps/server/sql/001_init.sql （devices/gateways/audit_events/refresh_tokens 表列名，确认 revoked_at、device_id、event_type 等列）
  </read_first>
  <files>
    apps/server/app/service/storage.ts
  </files>
  <action>
在 storage.ts 末尾（Phase 6 Admin Management Queries 注释块之后）继续追加以下函数（外科手术式修改，只追加）：

```typescript
// --- Phase 6 Admin: Devices ---

export async function loadAllDevices(limit = 20, offset = 0): Promise<Array<DeviceRecord & { revokedAt: number | null; userEmail: string | null }>> {
  const [rows] = await execute(
    `SELECT d.*, u.email AS user_email
     FROM devices d
     LEFT JOIN users u ON d.user_id = u.id
     WHERE d.token_class = 'normal_client_access'
     ORDER BY d.created_at DESC
     LIMIT ? OFFSET ?`,
    [limit, offset]
  );
  return (rows as Record<string, unknown>[]).map(row => ({
    ...deviceFromRow(row),
    revokedAt: row.revoked_at ? sqlDateToMs(row.revoked_at) : null,
    userEmail: typeof row.user_email === 'string' ? row.user_email : null
  }));
}

export async function countDevices(): Promise<number> {
  const [rows] = await execute(
    "SELECT COUNT(*) AS count FROM devices WHERE token_class = 'normal_client_access'"
  );
  const row = (rows as Record<string, unknown>[])[0];
  return Number(row?.count ?? 0);
}

export async function revokeDeviceById(id: string): Promise<void> {
  await execute(
    'UPDATE devices SET revoked_at = NOW(), updated_at = NOW() WHERE id = ?',
    [id]
  );
}

export async function revokeRefreshTokensByDeviceId(deviceId: string): Promise<void> {
  // 撤销该设备关联的所有 active refresh tokens，防止已吊销设备继续刷新
  await execute(
    'UPDATE refresh_tokens SET revoked = TRUE WHERE device_id = ? AND revoked = FALSE',
    [deviceId]
  );
}

// --- Phase 6 Admin: Gateways ---

export async function loadAllGateways(limit = 20, offset = 0): Promise<GatewayRecord[]> {
  const [rows] = await execute(
    'SELECT * FROM gateways ORDER BY created_at DESC LIMIT ? OFFSET ?',
    [limit, offset]
  );
  return (rows as Record<string, unknown>[]).map(gatewayFromRow);
}

export async function countGateways(): Promise<number> {
  const [rows] = await execute('SELECT COUNT(*) AS count FROM gateways');
  const row = (rows as Record<string, unknown>[])[0];
  return Number(row?.count ?? 0);
}

export async function deleteGatewayById(id: string): Promise<void> {
  await execute('DELETE FROM gateways WHERE id = ?', [id]);
}

// --- Phase 6 Admin: Audit (filtered) ---

export async function loadAuditEventsFiltered(params: {
  userId?: string;
  eventType?: string;
  deviceId?: string;
  gatewayId?: string;
  fromMs?: number;
  toMs?: number;
  limit: number;
  offset: number;
}): Promise<AuditEventRecord[]> {
  const conditions: string[] = [];
  const values: unknown[] = [];

  if (params.userId) {
    conditions.push('user_id = ?');
    values.push(params.userId);
  }
  if (params.eventType) {
    conditions.push('event_type = ?');
    values.push(params.eventType);
  }
  if (params.deviceId) {
    conditions.push('device_id = ?');
    values.push(params.deviceId);
  }
  if (params.gatewayId) {
    conditions.push('gateway_id = ?');
    values.push(params.gatewayId);
  }
  if (params.fromMs) {
    conditions.push('created_at >= FROM_UNIXTIME(? / 1000)');
    values.push(params.fromMs);
  }
  if (params.toMs) {
    conditions.push('created_at <= FROM_UNIXTIME(? / 1000)');
    values.push(params.toMs);
  }

  const where = conditions.length > 0 ? `WHERE ${conditions.join(' AND ')}` : '';
  const sql = `SELECT * FROM audit_events ${where} ORDER BY id DESC LIMIT ? OFFSET ?`;
  values.push(params.limit, params.offset);

  const [rows] = await execute(sql, values);
  return (rows as Record<string, unknown>[]).map(auditEventFromRow);
}

export async function countAuditEventsFiltered(params: {
  userId?: string;
  eventType?: string;
  deviceId?: string;
  gatewayId?: string;
  fromMs?: number;
  toMs?: number;
}): Promise<number> {
  const conditions: string[] = [];
  const values: unknown[] = [];

  if (params.userId) { conditions.push('user_id = ?'); values.push(params.userId); }
  if (params.eventType) { conditions.push('event_type = ?'); values.push(params.eventType); }
  if (params.deviceId) { conditions.push('device_id = ?'); values.push(params.deviceId); }
  if (params.gatewayId) { conditions.push('gateway_id = ?'); values.push(params.gatewayId); }
  if (params.fromMs) { conditions.push('created_at >= FROM_UNIXTIME(? / 1000)'); values.push(params.fromMs); }
  if (params.toMs) { conditions.push('created_at <= FROM_UNIXTIME(? / 1000)'); values.push(params.toMs); }

  const where = conditions.length > 0 ? `WHERE ${conditions.join(' AND ')}` : '';
  const [rows] = await execute(`SELECT COUNT(*) AS count FROM audit_events ${where}`, values);
  const row = (rows as Record<string, unknown>[])[0];
  return Number(row?.count ?? 0);
}
```

注意：
- `deviceFromRow`、`gatewayFromRow`、`auditEventFromRow`、`sqlDateToMs` 都是 storage.ts 内的 private 函数，可直接调用
- `DeviceRecord`、`GatewayRecord`、`AuditEventRecord` 从 runtime.ts import
- `revokeRefreshTokensByDeviceId` 的 SQL 假设 refresh_tokens 表有 `device_id` 列和 `revoked` BOOLEAN 列；如果列名不同（如 `is_revoked`），读取 SQL schema 后修正
  </action>
  <verify>
    <automated>
      grep -v '^#' /Users/dream/code/tether/apps/server/app/service/storage.ts | grep -c 'loadAllDevices\|revokeDeviceById\|revokeRefreshTokensByDeviceId\|loadAllGateways\|deleteGatewayById\|loadAuditEventsFiltered\|countAuditEventsFiltered' &&
      grep -v '^#' /Users/dream/code/tether/apps/server/app/service/storage.ts | grep -c 'deviceId.*?\|gatewayId.*?' &&
      cd /Users/dream/code/tether/apps/server && pnpm typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    storage.ts 包含 loadAllDevices、countDevices、revokeDeviceById、revokeRefreshTokensByDeviceId、loadAllGateways、countGateways、deleteGatewayById、loadAuditEventsFiltered、countAuditEventsFiltered 共 9 个新函数；loadAuditEventsFiltered 支持 deviceId 和 gatewayId 可选参数；pnpm typecheck 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: devices/gateways/audit service + controller + 路由注册（设备吊销含 token 撤销）</name>
  <read_first>
    - apps/server/app/service/admin/users.ts （Plan 02 创建的，参照 MySQL/内存双模式格式）
    - apps/server/app/controller/admin/users.ts （Plan 02 创建的，参照 controller 错误处理格式）
    - apps/server/app/service/audit.ts （recordAuditEvent 函数签名）
    - apps/server/app/service/runtime.ts （runtimeStore().devices，.gateways，.auditEvents 结构）
    - apps/server/app/router/admin.ts （Plan 02 创建的，需要追加 devices/gateways/audit 路由）
    - apps/server/app/service/storage.ts （刚新增的 loadAllDevices、revokeRefreshTokensByDeviceId 等函数）
  </read_first>
  <files>
    apps/server/app/service/admin/devices.ts,
    apps/server/app/service/admin/gateways.ts,
    apps/server/app/service/admin/audit.ts,
    apps/server/app/controller/admin/devices.ts,
    apps/server/app/controller/admin/gateways.ts,
    apps/server/app/controller/admin/audit.ts,
    apps/server/app/router/admin.ts
  </files>
  <action>
**apps/server/app/service/admin/devices.ts** — 新建：
```typescript
import type { AuthConfig } from '../auth.js';
import { runtimeStore } from '../runtime.js';
import { mysqlModeEnabled, loadAllDevices, countDevices, revokeDeviceById, revokeRefreshTokensByDeviceId, loadDeviceById } from '../storage.js';
import { recordAuditEvent } from '../audit.js';

export async function listAdminDevices(_config: AuthConfig, page: number, limit: number) {
  if (mysqlModeEnabled()) {
    const offset = (page - 1) * limit;
    const devices = await loadAllDevices(limit, offset);
    const total = await countDevices();
    return {
      devices: devices.map(d => ({
        id: d.id, name: d.name, platform: d.platform,
        userId: d.userId ?? null, userEmail: d.userEmail,
        status: d.revokedAt ? 'revoked' : 'active' as const,
        lastSeenAt: null // devices 表当前无 last_seen_at 列
      })),
      total
    };
  }
  const store = runtimeStore();
  const all = [...store.devices.values()];
  const total = all.length;
  const paginated = all.slice((page - 1) * limit, page * limit);
  return {
    devices: paginated.map(d => ({
      id: d.id, name: d.name, platform: d.platform,
      userId: d.userId ?? null, userEmail: null,
      status: 'active' as const, lastSeenAt: null
    })),
    total
  };
}

export async function revokeAdminDevice(
  deviceId: string,
  adminUserId: string,
  accountId: string,
  workspaceId: string
) {
  if (mysqlModeEnabled()) {
    const device = await loadDeviceById(deviceId);
    if (!device) throw new Error('not_found');
    // 1. 标记设备为已吊销
    await revokeDeviceById(deviceId);
    // 2. 撤销该设备的所有 active refresh tokens（防止已吊销设备继续刷新 token）
    await revokeRefreshTokensByDeviceId(deviceId);
    // 3. 记录审计事件
    await recordAuditEvent({
      accountId, workspaceId, adminUserId,
      action: 'admin.device.revoked',
      tokenClass: 'management_access',
      payload: { deviceId }
    });
    return { ok: true };
  }
  const store = runtimeStore();
  const device = store.devices.get(deviceId);
  if (!device) throw new Error('not_found');
  store.devices.set(deviceId, { ...device, revokedAt: Date.now() } as typeof device);
  return { ok: true };
}
```

**apps/server/app/service/admin/gateways.ts** — 新建：
```typescript
import type { AuthConfig } from '../auth.js';
import { runtimeStore } from '../runtime.js';
import { mysqlModeEnabled, loadAllGateways, countGateways, deleteGatewayById, loadGatewayById } from '../storage.js';
import { recordAuditEvent } from '../audit.js';

export async function listAdminGateways(_config: AuthConfig, page: number, limit: number) {
  if (mysqlModeEnabled()) {
    const offset = (page - 1) * limit;
    const gateways = await loadAllGateways(limit, offset);
    const total = await countGateways();
    return {
      gateways: gateways.map(g => ({
        id: g.id,
        lastSeenAt: g.lastSeenAt,
        status: g.status
      })),
      total
    };
  }
  const store = runtimeStore();
  const all = [...store.gateways.values()];
  const total = all.length;
  const paginated = all.slice((page - 1) * limit, page * limit);
  return {
    gateways: paginated.map(g => ({ id: g.id, lastSeenAt: g.lastSeenAt, status: g.status })),
    total
  };
}

export async function unlinkAdminGateway(
  gatewayId: string,
  adminUserId: string,
  accountId: string,
  workspaceId: string
) {
  if (mysqlModeEnabled()) {
    const gateway = await loadGatewayById(gatewayId);
    if (!gateway) throw new Error('not_found');
    await deleteGatewayById(gatewayId);
    await recordAuditEvent({
      accountId, workspaceId, adminUserId,
      action: 'admin.gateway.unlinked',
      tokenClass: 'management_access',
      payload: { gatewayId }
    });
    return { ok: true };
  }
  const store = runtimeStore();
  if (!store.gateways.has(gatewayId)) throw new Error('not_found');
  store.gateways.delete(gatewayId);
  return { ok: true };
}
```

**apps/server/app/service/admin/audit.ts** — 新建，支持 deviceId/gatewayId 筛选维度：
```typescript
import type { AuthConfig } from '../auth.js';
import { runtimeStore } from '../runtime.js';
import { mysqlModeEnabled, loadAuditEventsFiltered, countAuditEventsFiltered } from '../storage.js';

export async function listAdminAuditEvents(
  _config: AuthConfig,
  params: {
    page: number;
    limit: number;
    userId?: string;
    action?: string;
    deviceId?: string;
    gatewayId?: string;
    from?: string;  // ISO date string or timestamp ms string
    to?: string;
  }
) {
  const fromMs = params.from ? Number(params.from) || new Date(params.from).getTime() || undefined : undefined;
  const toMs = params.to ? Number(params.to) || new Date(params.to).getTime() || undefined : undefined;

  if (mysqlModeEnabled()) {
    const offset = (params.page - 1) * params.limit;
    const events = await loadAuditEventsFiltered({
      userId: params.userId,
      eventType: params.action,
      deviceId: params.deviceId,
      gatewayId: params.gatewayId,
      fromMs: isNaN(fromMs ?? NaN) ? undefined : fromMs,
      toMs: isNaN(toMs ?? NaN) ? undefined : toMs,
      limit: params.limit,
      offset
    });
    const total = await countAuditEventsFiltered({
      userId: params.userId,
      eventType: params.action,
      deviceId: params.deviceId,
      gatewayId: params.gatewayId,
      fromMs: isNaN(fromMs ?? NaN) ? undefined : fromMs,
      toMs: isNaN(toMs ?? NaN) ? undefined : toMs
    });
    return {
      events: events.map(e => ({
        id: e.id, action: e.action,
        userId: e.userId ?? null, adminUserId: e.adminUserId ?? null,
        deviceId: e.deviceId ?? null, gatewayId: e.gatewayId ?? null,
        createdAt: e.createdAt, payload: e.payload
      })),
      total
    };
  }
  const store = runtimeStore();
  let all = store.auditEvents;
  if (params.userId) all = all.filter(e => e.userId === params.userId);
  if (params.action) all = all.filter(e => e.action === params.action);
  if (params.deviceId) all = all.filter(e => e.deviceId === params.deviceId);
  if (params.gatewayId) all = all.filter(e => e.gatewayId === params.gatewayId);
  if (fromMs && !isNaN(fromMs)) all = all.filter(e => e.createdAt >= fromMs);
  if (toMs && !isNaN(toMs)) all = all.filter(e => e.createdAt <= toMs);
  const total = all.length;
  const paginated = all.slice((params.page - 1) * params.limit, params.page * params.limit);
  return {
    events: paginated.map(e => ({
      id: e.id, action: e.action,
      userId: e.userId ?? null, adminUserId: e.adminUserId ?? null,
      deviceId: e.deviceId ?? null, gatewayId: e.gatewayId ?? null,
      createdAt: e.createdAt, payload: e.payload
    })),
    total
  };
}
```

**apps/server/app/controller/admin/devices.ts** — 新建：
```typescript
import { Controller } from 'egg';
import { requireManagementToken } from '../../middleware/admin-auth.js';
import { listAdminDevices, revokeAdminDevice } from '../../service/admin/devices.js';

export default class AdminDevicesController extends Controller {
  public async index(): Promise<void> {
    try {
      requireManagementToken(this.ctx.get('authorization'), this.app.config);
      const query = this.ctx.query as Record<string, string | undefined>;
      const page = Math.max(1, Number(query.page ?? '1'));
      const limit = Math.min(100, Math.max(1, Number(query.limit ?? '20')));
      this.ctx.body = await listAdminDevices(this.app.config, page, limit);
    } catch (error) {
      this.ctx.status = error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401 : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'list_devices_failed' };
    }
  }

  public async revoke(): Promise<void> {
    try {
      const identity = requireManagementToken(this.ctx.get('authorization'), this.app.config);
      const { id } = this.ctx.params as Record<string, string>;
      await revokeAdminDevice(id, identity.adminUserId, identity.accountId, identity.workspaceId);
      this.ctx.body = { ok: true };
    } catch (error) {
      this.ctx.status = error instanceof Error && error.message === 'not_found' ? 404
        : error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401
        : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'revoke_device_failed' };
    }
  }
}
```

**apps/server/app/controller/admin/gateways.ts** — 新建：
```typescript
import { Controller } from 'egg';
import { requireManagementToken } from '../../middleware/admin-auth.js';
import { listAdminGateways, unlinkAdminGateway } from '../../service/admin/gateways.js';

export default class AdminGatewaysController extends Controller {
  public async index(): Promise<void> {
    try {
      requireManagementToken(this.ctx.get('authorization'), this.app.config);
      const query = this.ctx.query as Record<string, string | undefined>;
      const page = Math.max(1, Number(query.page ?? '1'));
      const limit = Math.min(100, Math.max(1, Number(query.limit ?? '20')));
      this.ctx.body = await listAdminGateways(this.app.config, page, limit);
    } catch (error) {
      this.ctx.status = error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401 : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'list_gateways_failed' };
    }
  }

  public async unlink(): Promise<void> {
    try {
      const identity = requireManagementToken(this.ctx.get('authorization'), this.app.config);
      const { id } = this.ctx.params as Record<string, string>;
      await unlinkAdminGateway(id, identity.adminUserId, identity.accountId, identity.workspaceId);
      this.ctx.body = { ok: true };
    } catch (error) {
      this.ctx.status = error instanceof Error && error.message === 'not_found' ? 404
        : error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401
        : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'unlink_gateway_failed' };
    }
  }
}
```

**apps/server/app/controller/admin/audit.ts** — 新建，透传 deviceId/gatewayId query 参数：
```typescript
import { Controller } from 'egg';
import { requireManagementToken } from '../../middleware/admin-auth.js';
import { listAdminAuditEvents } from '../../service/admin/audit.js';

export default class AdminAuditController extends Controller {
  public async index(): Promise<void> {
    try {
      requireManagementToken(this.ctx.get('authorization'), this.app.config);
      const query = this.ctx.query as Record<string, string | undefined>;
      const page = Math.max(1, Number(query.page ?? '1'));
      const limit = Math.min(200, Math.max(1, Number(query.limit ?? '50')));
      this.ctx.body = await listAdminAuditEvents(this.app.config, {
        page, limit,
        userId: query.userId,
        action: query.action,
        deviceId: query.deviceId,
        gatewayId: query.gatewayId,
        from: query.from,
        to: query.to
      });
    } catch (error) {
      this.ctx.status = error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401 : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'list_audit_failed' };
    }
  }
}
```

**apps/server/app/router/admin.ts** — 在 Plan 02 创建的文件基础上，追加 devices/gateways/audit 路由（读取当前内容后，在 "Plan 03 会添加" 注释行处替换为实际路由）：
```typescript
  router.get('/admin/api/devices',                  controller.admin.devices.index);
  router.post('/admin/api/devices/:id/revoke',      controller.admin.devices.revoke);
  router.get('/admin/api/gateways',                 controller.admin.gateways.index);
  router.delete('/admin/api/gateways/:id/unlink',   controller.admin.gateways.unlink);
  router.get('/admin/api/audit',                    controller.admin.audit.index);
```
  </action>
  <verify>
    <automated>
      ls /Users/dream/code/tether/apps/server/app/service/admin/devices.ts &&
      ls /Users/dream/code/tether/apps/server/app/service/admin/gateways.ts &&
      ls /Users/dream/code/tether/apps/server/app/service/admin/audit.ts &&
      ls /Users/dream/code/tether/apps/server/app/controller/admin/devices.ts &&
      ls /Users/dream/code/tether/apps/server/app/controller/admin/gateways.ts &&
      ls /Users/dream/code/tether/apps/server/app/controller/admin/audit.ts &&
      grep -v '^#' /Users/dream/code/tether/apps/server/app/service/admin/devices.ts | grep -c 'revokeRefreshTokensByDeviceId' &&
      grep -v '^#' /Users/dream/code/tether/apps/server/app/service/admin/audit.ts | grep -c 'deviceId\|gatewayId' &&
      grep -v '^#' /Users/dream/code/tether/apps/server/app/router/admin.ts | grep -c 'devices.*revoke\|gateways.*unlink\|audit.*index' &&
      cd /Users/dream/code/tether/apps/server && pnpm typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    6 个新 service/controller 文件创建完毕；devices.ts 的 revokeAdminDevice 函数包含 revokeRefreshTokensByDeviceId 调用；audit.ts service 和 controller 都透传 deviceId/gatewayId 参数；router/admin.ts 包含 devices/gateways/audit 路由；pnpm typecheck 通过无 TS 错误。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → /admin/api/devices/:id/revoke | 需要 management token，revoke 是不可撤销操作 |
| HTTP → /admin/api/gateways/:id/unlink | 需要 management token，DELETE 操作不可恢复 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | POST /admin/api/devices/:id/revoke | mitigate | requireManagementToken 验证；loadDeviceById 确认存在性后再写 revokedAt；同步撤销所有 active refresh tokens；操作记录 audit_events |
| T-06-03-02 | Tampering | DELETE /admin/api/gateways/:id/unlink | mitigate | requireManagementToken 验证；loadGatewayById 确认存在性后再删除；操作记录 audit_events |
| T-06-03-03 | Information Disclosure | GET /admin/api/audit 返回 payload 字段 | mitigate | recordAuditEvent 已在写入时通过 maskSensitivePayload 过滤敏感字段；查询结果不需要额外 mask |
| T-06-03-04 | Denial of Service | GET /admin/api/audit 无 limit 上限 | mitigate | controller 强制 limit = Math.min(200, ...) |
| T-06-03-05 | Repudiation | revoke/unlink 操作无审计记录 | mitigate | revokeAdminDevice 和 unlinkAdminGateway 均在成功后调用 recordAuditEvent |
| T-06-03-06 | Elevation of Privilege | 已吊销设备仍可使用 refresh token | mitigate | revokeAdminDevice 同时调用 revokeRefreshTokensByDeviceId，确保已吊销设备无法继续刷新 token |
</threat_model>

<verification>
1. `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4800/admin/api/devices` — 401
2. `curl -s -H "Authorization: Bearer <valid_mgmt_token>" http://127.0.0.1:4800/admin/api/gateways` — 200 含 gateways 数组
3. `curl -s -H "Authorization: Bearer <valid_mgmt_token>" "http://127.0.0.1:4800/admin/api/audit?limit=5"` — 200 含 events 数组
4. `curl -s -H "Authorization: Bearer <valid_mgmt_token>" "http://127.0.0.1:4800/admin/api/audit?deviceId=xxx"` — 200 含按 deviceId 筛选的结果
5. `pnpm typecheck`（在 apps/server 目录）— 退出码 0
</verification>

<success_criteria>
- /admin/api/devices 返回设备列表，字段含 status（active/revoked）
- POST /admin/api/devices/:id/revoke 将 devices.revoked_at 设为当前时间，并撤销该设备所有 active refresh tokens
- /admin/api/gateways 返回 Gateway 列表
- DELETE /admin/api/gateways/:id/unlink 删除 gateways 表记录
- /admin/api/audit 支持 userId/action/deviceId/gatewayId/from/to 筛选
- 所有端点未授权返回 401
- revoke/unlink 操作产生 audit_events 记录
</success_criteria>

<output>
完成后创建 .planning/phases/06-account-management-console/06-03-SUMMARY.md
</output>
