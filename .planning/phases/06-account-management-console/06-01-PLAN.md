---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-web/package.json
  - apps/admin-web/vite.config.ts
  - apps/admin-web/tsconfig.json
  - apps/admin-web/index.html
  - apps/admin-web/components.json
  - apps/admin-web/src/styles.css
  - apps/admin-web/src/lib/utils.ts
  - apps/admin-web/src/lib/admin-api.ts
  - apps/admin-web/src/contexts/admin-auth-context.tsx
  - apps/admin-web/src/hooks/use-admin-auth.ts
  - apps/admin-web/src/components/ui/button.tsx
  - apps/admin-web/src/components/ui/card.tsx
  - apps/admin-web/src/components/ui/input.tsx
  - apps/admin-web/src/components/ui/label.tsx
  - apps/admin-web/src/components/ui/form.tsx
  - apps/admin-web/src/components/layout/AdminLayout.tsx
  - apps/admin-web/src/main.tsx
autonomous: true
requirements:
  - WEBUI-01
  - MGMT-01

must_haves:
  truths:
    - "apps/admin-web 是独立的 pnpm workspace 包，可独立 dev/build"
    - "访问 /admin/* 路由时，若 localStorage 中无有效 management token，自动跳转 /admin/login"
    - "AdminLayout 渲染左侧导航（5 个菜单项）+ 顶部 header（页面标题 + 已登录邮箱）"
    - "admin-api.ts 提供基于 requestJson 的 API 封装，通过 Authorization: Bearer 传 token"
    - "visual token（CSS 变量）与 apps/web 保持一致（dark 主题）"
  artifacts:
    - path: "apps/admin-web/package.json"
      provides: "@tether/admin-web pnpm 包定义，dev 端口 4792"
    - path: "apps/admin-web/src/main.tsx"
      provides: "React 应用入口，挂载 AdminAuthProvider + React Router + AdminLayout nested routes"
    - path: "apps/admin-web/src/components/layout/AdminLayout.tsx"
      provides: "路由级 auth guard + sidebar + header + Outlet"
    - path: "apps/admin-web/src/lib/admin-api.ts"
      provides: "requestJson 封装 + /admin/api/* 请求函数（listUsers, listAdmins）"
    - path: "apps/admin-web/src/contexts/admin-auth-context.tsx"
      provides: "AdminAuthContext，读写 localStorage key tether:web:managementAuth"
  key_links:
    - from: "apps/admin-web/src/components/layout/AdminLayout.tsx"
      to: "localStorage tether:web:managementAuth"
      via: "useAdminAuth().managementAuth 检查，null 时 Navigate to /admin/login"
      pattern: "Navigate.*admin/login"
    - from: "apps/admin-web/src/lib/admin-api.ts"
      to: "apps/server /admin/api/*"
      via: "vite proxy /admin/api -> http://127.0.0.1:4800"
      pattern: "requestJson.*admin/api"
---

<objective>
创建 apps/admin-web 包的完整基础设施：pnpm 包配置、Vite + shadcn 设置、auth context、AdminLayout（含路由级 auth guard）和 admin-api.ts 请求封装。

Purpose: 为 Phase 6 管理控制台所有页面提供可复用的基础层，后续的 Plan 04/05 页面可直接在此基础上开发。
Output: 可运行的 apps/admin-web 开发服务器，访问 /admin/* 路由能看到带 sidebar/header 的布局框架（页面内容为 Outlet 占位），未登录时重定向到 /admin/login。
</objective>

<execution_context>
@/Users/dream/code/tether/.planning/phases/06-account-management-console/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/PROJECT.md
@/Users/dream/code/tether/.planning/ROADMAP.md
@/Users/dream/code/tether/.planning/phases/06-account-management-console/06-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/06-account-management-console/06-PATTERNS.md
@/Users/dream/code/tether/.planning/phases/06-account-management-console/06-UI-SPEC.md

<interfaces>
<!-- 从 apps/web 提取的关键模式，executor 直接使用，无需再探索 -->

From apps/web/package.json (exact version numbers to replicate):
```json
{
  "dependencies": {
    "@hookform/resolvers": "^5.2.2",
    "@radix-ui/react-label": "^2.1.7",
    "@radix-ui/react-slot": "^1.2.3",
    "@vitejs/plugin-react": "^5.0.2",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-hook-form": "^7.62.0",
    "react-router-dom": "^7.9.1",
    "tailwind-merge": "^3.3.1",
    "zod": "^4.1.11",
    "typescript": "^5.8.3",
    "vite": "^7.1.5"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.13",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "tailwindcss": "^4.1.13"
  }
}
```

From apps/web/src/lib/api.ts (requestJson 函数，admin-api.ts 直接复用):
```typescript
type RequestOptions = RequestInit & { token?: string };

async function requestJson<T>(path: string, options: RequestOptions = {}): Promise<T> {
  const headers = new Headers(options.headers);
  if (!headers.has('content-type') && options.body) {
    headers.set('content-type', 'application/json');
  }
  if (options.token) {
    headers.set('Authorization', `Bearer ${options.token}`);
  }
  const response = await fetch(path, { ...options, headers });
  const body = await response.json().catch(() => undefined) as { error?: string } | T | undefined;
  if (!response.ok) {
    const message = body && typeof body === 'object' && 'error' in body && typeof body.error === 'string'
      ? body.error
      : `request_failed_${response.status}`;
    throw new Error(message);
  }
  return body as T;
}
```

From apps/web/src/contexts/auth-context.tsx (localStorage key + readStorage 模式):
```typescript
const MANAGEMENT_STORAGE_KEY = 'tether:web:managementAuth';

function readStorage<T>(key: string): AuthStorageRecord<T> | null {
  const raw = window.localStorage.getItem(key);
  if (!raw) return null;
  try {
    const parsed = JSON.parse(raw) as AuthStorageRecord<T>;
    if (typeof parsed.accessToken === 'string' && typeof parsed.refreshToken === 'string') {
      return parsed;
    }
  } catch { return null; }
  return null;
}

function writeStorage<T>(key: string, value: AuthStorageRecord<T> | null) {
  if (!value) { window.localStorage.removeItem(key); return; }
  window.localStorage.setItem(key, JSON.stringify(value));
}
```

From apps/web/src/main.tsx (RequireAdminAuth pattern):
```typescript
function RequireAdminAuth({ children }: { children: React.ReactNode }) {
  const location = useLocation();
  const { authReady, managementAuth } = useAuth();
  if (!authReady) return null;
  if (!managementAuth) {
    return <Navigate replace to="/admin/login" state={{ from: location.pathname }} />;
  }
  return <>{children}</>;
}
```

From apps/web/src/styles.css (CSS 变量，完整复制到 admin-web):
```css
@import "tailwindcss";
:root {
  color-scheme: dark;
  font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: #101214;
  color: #e8ecef;
  --background: 210 10% 8%;
  --foreground: 210 12% 92%;
  --card: 215 12% 11%;
  --card-foreground: 210 12% 92%;
  --popover: 215 12% 11%;
  --popover-foreground: 210 12% 92%;
  --primary: 205 100% 78%;
  --primary-foreground: 210 10% 6%;
  --secondary: 215 12% 16%;
  --secondary-foreground: 210 12% 92%;
  --muted: 215 10% 18%;
  --muted-foreground: 210 8% 62%;
  --accent: 205 100% 78%;
  --accent-foreground: 210 10% 6%;
  --destructive: 0 72% 60%;
  --destructive-foreground: 0 0% 98%;
  --border: 215 12% 23%;
  --input: 215 12% 27%;
  --ring: 215 12% 40%;
  --radius: 0.375rem;
}
body { margin: 0; min-height: 100vh; }
#root { height: 100vh; display: flex; overflow: hidden; }
```

ManagementIdentity type (from apps/web/src/lib/api.ts):
```typescript
export type ManagementIdentity = {
  accountId: string;
  workspaceId: string;
  adminUserId: string;
  deviceId?: string;
  tokenClass: 'management_access';
  expiresAt: number;
  jti: string;
};

export type AuthStorageRecord<TIdentity> = {
  accessToken: string;
  refreshToken: string;
  identity?: TIdentity;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: apps/admin-web 包脚手架（配置文件 + shadcn 基础组件 + CSS tokens）</name>
  <read_first>
    - apps/web/package.json （精确版本号）
    - apps/web/vite.config.ts （完整 Vite 配置）
    - apps/web/tsconfig.json （tsconfig 结构）
    - apps/web/components.json （shadcn 配置）
    - apps/web/src/styles.css （CSS 变量 tokens）
    - apps/web/src/lib/utils.ts （cn 函数）
    - apps/web/src/components/ui/button.tsx （复制到 admin-web）
    - apps/web/src/components/ui/card.tsx
    - apps/web/src/components/ui/input.tsx
    - apps/web/src/components/ui/label.tsx
    - apps/web/src/components/ui/form.tsx
    - tsconfig.base.json （extends 根路径）
    - pnpm-workspace.yaml （确认 apps/* 已覆盖）
  </read_first>
  <files>
    apps/admin-web/package.json,
    apps/admin-web/vite.config.ts,
    apps/admin-web/tsconfig.json,
    apps/admin-web/index.html,
    apps/admin-web/components.json,
    apps/admin-web/src/styles.css,
    apps/admin-web/src/lib/utils.ts,
    apps/admin-web/src/components/ui/button.tsx,
    apps/admin-web/src/components/ui/card.tsx,
    apps/admin-web/src/components/ui/input.tsx,
    apps/admin-web/src/components/ui/label.tsx,
    apps/admin-web/src/components/ui/form.tsx
  </files>
  <action>
创建 apps/admin-web 包，使用与 apps/web 完全相同的 tech stack。

**package.json** — name 为 "@tether/admin-web"，dev 端口 4792，preview 端口 4793。去掉 @xterm/* 相关依赖。其余依赖版本与 apps/web/package.json 保持一致（见 context interfaces）。scripts 使用相同的 dev/build/typecheck/preview 命令，仅端口不同。

**vite.config.ts** — 复制 apps/web/vite.config.ts 结构，但 server.proxy 只保留：
```typescript
proxy: {
  '/admin/api': { target: 'http://127.0.0.1:4800' },
  '/api/admin': { target: 'http://127.0.0.1:4800' }
}
```

**tsconfig.json** — 与 apps/web/tsconfig.json 完全相同，直接复制。

**index.html** — 与 apps/web/index.html 结构相同，title 改为 "Tether Admin"，script src 指向 `/src/main.tsx`。

**components.json** — 与 apps/web/components.json 完全相同（style=default, baseColor=neutral, cssVariables=true, rsc=false）。

**src/styles.css** — 复制 apps/web/src/styles.css 的 CSS 变量 tokens 部分（@import tailwindcss + :root {} + body/html 基础样式）。移除 apps/web 中针对 #root 的 grid-template-rows 设置，改为：
```css
#root { height: 100vh; display: flex; overflow: hidden; }
```

**src/lib/utils.ts** — 与 apps/web/src/lib/utils.ts 相同：
```typescript
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
```

**src/components/ui/button.tsx, card.tsx, input.tsx, label.tsx, form.tsx** — 原样复制 apps/web/src/components/ui/ 下同名文件内容，不修改任何代码。

完成后运行 `cd apps/admin-web && pnpm install` 安装依赖。
  </action>
  <verify>
    <automated>
      ls /Users/dream/code/tether/apps/admin-web/package.json &&
      grep -c '"@tether/admin-web"' /Users/dream/code/tether/apps/admin-web/package.json &&
      grep -c '4792' /Users/dream/code/tether/apps/admin-web/package.json &&
      ls /Users/dream/code/tether/apps/admin-web/src/components/ui/button.tsx &&
      ls /Users/dream/code/tether/apps/admin-web/src/components/ui/card.tsx &&
      ls /Users/dream/code/tether/apps/admin-web/src/lib/utils.ts &&
      grep -c 'tailwindcss' /Users/dream/code/tether/apps/admin-web/src/styles.css
    </automated>
  </verify>
  <done>
    apps/admin-web/ 目录存在，package.json name 为 "@tether/admin-web"，dev 端口 4792；5 个 shadcn UI 组件文件复制完毕；styles.css 包含 CSS 变量 tokens。
  </done>
</task>

<task type="auto">
  <name>Task 2: admin-auth-context + use-admin-auth hook + admin-api.ts + AdminLayout + main.tsx 入口</name>
  <read_first>
    - apps/web/src/contexts/auth-context.tsx （localStorage 读写模式，management 侧逻辑）
    - apps/web/src/hooks/use-auth.ts （hook 结构）
    - apps/web/src/lib/api.ts （requestJson + ManagementIdentity + AuthStorageRecord types）
    - apps/web/src/main.tsx （RequireAdminAuth pattern，路由结构参考）
    - apps/admin-web/src/styles.css （确认已创建）
  </read_first>
  <files>
    apps/admin-web/src/contexts/admin-auth-context.tsx,
    apps/admin-web/src/hooks/use-admin-auth.ts,
    apps/admin-web/src/lib/admin-api.ts,
    apps/admin-web/src/components/layout/AdminLayout.tsx,
    apps/admin-web/src/main.tsx
  </files>
  <action>
**src/contexts/admin-auth-context.tsx** — 仅包含 management 侧逻辑（不含 normalAuth）。

类型定义（从 apps/web/src/lib/api.ts 复制，不 import apps/web）：
```typescript
type ManagementIdentity = {
  accountId: string; workspaceId: string; adminUserId: string;
  deviceId?: string; tokenClass: 'management_access'; expiresAt: number; jti: string;
};
type AuthStorageRecord<TIdentity> = {
  accessToken: string; refreshToken: string; identity?: TIdentity;
};
```

Storage key: `'tether:web:managementAuth'`（与 apps/web 保持一致，共享已登录状态）。

Context value shape:
```typescript
type AdminAuthContextValue = {
  managementAuth: AuthStorageRecord<ManagementIdentity> | null;
  authReady: boolean;
  loginManagement: (input: { email: string; password: string }) => Promise<void>;
  logoutManagement: () => void;
};
```

实现：
- `authReady` 初始为 false，mount 时读 localStorage，读完后设为 true
- `loginManagement` 调用 `POST /api/admin/auth/login` 获取 `{ accessToken, refreshToken, adminUser }` 并写入 localStorage
- `logoutManagement` 清空 localStorage key，设 managementAuth 为 null
- 导出 `AdminAuthContext`（createContext）和 `AdminAuthProvider`（包含 value + children）

**src/hooks/use-admin-auth.ts**：
```typescript
import * as React from 'react';
import { AdminAuthContext } from '../contexts/admin-auth-context.js';
export function useAdminAuth() {
  const context = React.useContext(AdminAuthContext);
  if (!context) throw new Error('useAdminAuth must be used within <AdminAuthProvider>');
  return context;
}
```

**src/lib/admin-api.ts** — 复制 requestJson 内部函数（不导出），再导出 /admin/api/* 请求函数：
```typescript
export async function listUsers(token: string, page = 1) {
  return requestJson<{ users: AdminUser[]; total: number }>(
    `/admin/api/users?page=${page}&limit=20`, { token });
}
export async function listAdmins(token: string) {
  return requestJson<{ admins: AdminUserItem[] }>('/admin/api/admins', { token });
}
export async function listDevices(token: string, page = 1) {
  return requestJson<{ devices: AdminDevice[]; total: number }>(
    `/admin/api/devices?page=${page}&limit=20`, { token });
}
export async function revokeDevice(token: string, deviceId: string) {
  return requestJson<{ ok: true }>(
    `/admin/api/devices/${encodeURIComponent(deviceId)}/revoke`,
    { method: 'POST', token });
}
export async function listGateways(token: string, page = 1) {
  return requestJson<{ gateways: AdminGateway[]; total: number }>(
    `/admin/api/gateways?page=${page}&limit=20`, { token });
}
export async function unlinkGateway(token: string, gatewayId: string) {
  return requestJson<{ ok: true }>(
    `/admin/api/gateways/${encodeURIComponent(gatewayId)}/unlink`,
    { method: 'DELETE', token });
}
export async function listAuditEvents(token: string, params: {
  page?: number; limit?: number; userId?: string; action?: string;
  from?: string; to?: string;
} = {}) {
  const qs = new URLSearchParams();
  qs.set('page', String(params.page ?? 1));
  qs.set('limit', String(params.limit ?? 50));
  if (params.userId) qs.set('userId', params.userId);
  if (params.action) qs.set('action', params.action);
  if (params.from) qs.set('from', params.from);
  if (params.to) qs.set('to', params.to);
  return requestJson<{ events: AdminAuditEvent[]; total: number }>(
    `/admin/api/audit?${qs.toString()}`, { token });
}
export async function getDashboardStats(token: string) {
  return requestJson<{
    totalUsers: number; activeDevices: number;
    registeredGateways: number; auditEventsLast7Days: number;
  }>('/admin/api/dashboard/stats', { token });
}
```

在文件顶部定义必要的 DTO 类型（不依赖 @tether/core，独立定义）：
```typescript
export type AdminUser = { id: string; email: string; createdAt: number; loginCount: number; failedLoginCount: number; lastLoginAt: number | null; activeDeviceCount: number; };
export type AdminUserItem = { id: string; email: string; role: string; status: string; createdAt: number; };
export type AdminDevice = { id: string; name: string; platform: string; userId: string | null; userEmail: string | null; status: 'active' | 'revoked'; lastSeenAt: number | null; };
export type AdminGateway = { id: string; lastSeenAt: number | null; status: 'online' | 'offline' | 'unlinked'; };
export type AdminAuditEvent = { id: number; action: string; userId: string | null; adminUserId: string | null; deviceId: string | null; gatewayId: string | null; createdAt: number; payload: Record<string, unknown>; };
```

**src/components/layout/AdminLayout.tsx** — 实现 UI-SPEC.md 中的布局：
- 先检查 authReady（false 时返回 null）
- managementAuth 为 null 时 `<Navigate replace to="/admin/login" />`
- 通过后渲染三栏布局：sidebar（宽 220px，bg=card）+ 主区域（flex-1 flex-col）
  - header（高 52px，bg=secondary，border-bottom，显示页面标题 + 已登录邮箱）
  - content area（flex-1 overflow-y-auto p-6，渲染 `<Outlet />`）
- sidebar 5 个 nav items，使用 `<NavLink>` 实现 active 状态（active: text-primary + border-l-2 border-primary + bg-primary/5）
- Nav items（顺序）: 概览→/admin/dashboard, 用户→/admin/users, 设备→/admin/devices, Gateway→/admin/gateways, 审计→/admin/audit

**src/main.tsx** — React 应用入口（从头编写，不复制 apps/web/src/main.tsx）：
```typescript
import * as React from 'react';
import { createRoot } from 'react-dom/client';
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { AdminAuthProvider } from './contexts/admin-auth-context.js';
import { AdminLayout } from './components/layout/AdminLayout.js';
import './styles.css';

// 页面占位（Plan 04/05 会替换这些）
function PlaceholderPage({ title }: { title: string }) {
  return <div className="text-muted-foreground">{title} 页面（开发中）</div>;
}

const root = document.getElementById('root')!;
createRoot(root).render(
  <React.StrictMode>
    <BrowserRouter>
      <AdminAuthProvider>
        <Routes>
          <Route element={<AdminLayout />}>
            <Route index path="/admin/dashboard" element={<PlaceholderPage title="概览" />} />
            <Route path="/admin/users" element={<PlaceholderPage title="用户" />} />
            <Route path="/admin/devices" element={<PlaceholderPage title="设备" />} />
            <Route path="/admin/gateways" element={<PlaceholderPage title="Gateway" />} />
            <Route path="/admin/audit" element={<PlaceholderPage title="审计" />} />
          </Route>
          <Route path="*" element={<Navigate replace to="/admin/dashboard" />} />
        </Routes>
      </AdminAuthProvider>
    </BrowserRouter>
  </React.StrictMode>
);
```

注意：此 main.tsx 不包含 LoginPage，因为 auth guard 会重定向到 /admin/login（该路由在 apps/web 中已存在，Phase 5 已实现）。
  </action>
  <verify>
    <automated>
      grep -c 'AdminAuthContext' /Users/dream/code/tether/apps/admin-web/src/contexts/admin-auth-context.tsx &&
      grep -c 'tether:web:managementAuth' /Users/dream/code/tether/apps/admin-web/src/contexts/admin-auth-context.tsx &&
      grep -c 'useAdminAuth' /Users/dream/code/tether/apps/admin-web/src/hooks/use-admin-auth.ts &&
      grep -c 'listUsers\|listDevices\|listGateways\|listAuditEvents\|getDashboardStats' /Users/dream/code/tether/apps/admin-web/src/lib/admin-api.ts &&
      grep -c 'Navigate.*admin/login' /Users/dream/code/tether/apps/admin-web/src/components/layout/AdminLayout.tsx &&
      grep -c 'AdminAuthProvider' /Users/dream/code/tether/apps/admin-web/src/main.tsx &&
      cd /Users/dream/code/tether/apps/admin-web && pnpm typecheck
    </automated>
  </verify>
  <done>
    admin-auth-context 导出 AdminAuthContext 和 AdminAuthProvider；use-admin-auth hook 正确封装 context；admin-api.ts 包含所有 5 类请求函数和对应 DTO 类型；AdminLayout 实现 sidebar/header/auth-guard；main.tsx 挂载 AdminAuthProvider + BrowserRouter + AdminLayout nested routes；`pnpm typecheck` 通过无 TS 错误。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /admin/api/* | 浏览器发送 management token，通过 Vite proxy 到 apps/server |
| localStorage → AdminAuthContext | 从 localStorage 读取 management token，用于所有 API 请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Spoofing | AdminLayout auth guard | mitigate | 检查 localStorage token 存在性 + authReady 状态；任何 401 响应清空 token 并重定向 /admin/login |
| T-06-01-02 | Information Disclosure | admin-api.ts requestJson | mitigate | token 仅通过 Authorization header 传输，不放入 URL query 参数 |
| T-06-01-03 | Tampering | localStorage management token | accept | management token 是 JWT，服务端验证签名；localStorage 被篡改只影响发起者自身，不影响服务端授权决策 |
| T-06-01-04 | Information Disclosure | Vite dev proxy | accept | proxy 仅用于 dev 环境；生产部署时 admin-web 直接请求 server；不在代理层添加额外鉴权 |
</threat_model>

<verification>
1. `ls apps/admin-web/package.json` — 文件存在
2. `grep -c '"@tether/admin-web"' apps/admin-web/package.json` — 输出 1
3. `grep -c '4792' apps/admin-web/package.json` — 输出 1（dev 端口）
4. `ls apps/admin-web/src/components/ui/{button,card,input,label,form}.tsx` — 5 个文件均存在
5. `grep -c 'tether:web:managementAuth' apps/admin-web/src/contexts/admin-auth-context.tsx` — 输出 ≥1
6. `grep -c 'Navigate.*admin/login' apps/admin-web/src/components/layout/AdminLayout.tsx` — 输出 ≥1
7. `cd apps/admin-web && pnpm typecheck` — 退出码 0
</verification>

<success_criteria>
- apps/admin-web 包可独立安装依赖（pnpm install）
- TypeScript 检查无错误（pnpm typecheck）
- /admin/* 路由未登录时重定向到 /admin/login（auth guard 生效）
- sidebar 有 5 个导航项，header 展示已登录邮箱
- admin-api.ts 含所有 6 类 API 函数（listUsers, listAdmins, listDevices, revokeDevice, listGateways, unlinkGateway, listAuditEvents, getDashboardStats）
- CSS 变量与 apps/web 一致（dark 主题）
</success_criteria>

<output>
完成后创建 .planning/phases/06-account-management-console/06-01-SUMMARY.md
</output>
