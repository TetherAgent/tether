---
phase: 06
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/app/middleware/admin-auth.ts
  - apps/server/app/service/admin/users.ts
  - apps/server/app/service/admin/admins.ts
  - apps/server/app/controller/admin/users.ts
  - apps/server/app/controller/admin/admins.ts
  - apps/server/app/service/storage.ts
  - apps/server/app/router/admin.ts
  - apps/server/app/router.ts
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02
  - MGMT-06

must_haves:
  truths:
    - "所有 /admin/api/* 路由都通过 admin-auth.ts 中间件验证 management token"
    - "GET /admin/api/users 返回分页用户列表（包含登录次数、最后登录时间、活跃设备数）"
    - "GET /admin/api/admins 返回管理用户列表"
    - "POST /admin/api/admins 创建新管理用户"
    - "DELETE /admin/api/admins/:id 删除管理用户"
    - "GET /admin/api/dashboard/stats 返回 4 项统计数字"
    - "缺少 management token 或 token 类型错误时返回 401"
  artifacts:
    - path: "apps/server/app/middleware/admin-auth.ts"
      provides: "requireManagementToken 函数，验证 management_access token class"
    - path: "apps/server/app/service/admin/users.ts"
      provides: "listAdminUsers, dashboardStats service 函数"
    - path: "apps/server/app/service/admin/admins.ts"
      provides: "listAdminManagers, createAdminManager, deleteAdminManager service 函数"
    - path: "apps/server/app/controller/admin/users.ts"
      provides: "AdminUsersController.index, AdminUsersController.dashboard"
    - path: "apps/server/app/controller/admin/admins.ts"
      provides: "AdminAdminsController.index, create, destroy"
    - path: "apps/server/app/service/storage.ts"
      provides: "loadAllUsers, loadAllAdminUsers, countDevicesByUserId 新增函数"
    - path: "apps/server/app/router/admin.ts"
      provides: "/admin/api/* 路由注册"
  key_links:
    - from: "apps/server/app/router/admin.ts"
      to: "apps/server/app/router.ts"
      via: "在 router.ts 中 import adminRouter 并调用"
      pattern: "adminRouter\|admin.*router"
    - from: "apps/server/app/controller/admin/users.ts"
      to: "apps/server/app/middleware/admin-auth.ts"
      via: "每个 controller 方法第一行调用 requireManagementToken"
      pattern: "requireManagementToken"
---

<objective>
在 apps/server 中建立管理 API 基础层：admin-auth 中间件、dashboard 统计、用户列表（含登录分析）、管理用户 CRUD。同时扩展 storage.ts 增加批量查询函数。

Purpose: 为 Plan 04 的 Dashboard 页和 Users 页提供 API 支撑；为整个管理 API 提供统一鉴权中间件。
Output: /admin/api/users、/admin/api/admins、/admin/api/dashboard/stats 端点正确响应，未授权时返回 401。
</objective>

<execution_context>
@/Users/dream/code/tether/.planning/phases/06-account-management-console/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/phases/06-account-management-console/06-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/06-account-management-console/06-PATTERNS.md

<interfaces>
<!-- 从 apps/server 提取的关键接口，executor 直接使用 -->

From apps/server/app/middleware/auth.ts (admin-auth.ts 的参照，完整内容):
```typescript
import type { AuthTokenClass } from '@tether/core';
import { verifyToken, type AuthConfig } from '../service/auth';

export function bearerTokenFromHeader(headerValue: string | undefined): string {
  if (!headerValue || !headerValue.startsWith('Bearer ')) {
    throw new Error('missing_token');
  }
  return headerValue.slice(7).trim();
}

export function requireTokenClass(
  headerValue: string | undefined,
  config: AuthConfig,
  expected: AuthTokenClass[]
) {
  const token = bearerTokenFromHeader(headerValue);
  const payload = verifyToken(token, config);
  if (!expected.includes(payload.tokenClass)) {
    throw new Error('wrong_token_class');
  }
  return payload;
}
```

From apps/server/app/controller/admin-auth.ts (controller 错误处理模式):
```typescript
// request body 读取
const body = this.ctx.request.body as Record<string, string | undefined>;
// 错误响应
this.ctx.status = error instanceof Error && error.message === 'not_found' ? 404 : 400;
this.ctx.body = { error: error instanceof Error ? error.message : 'operation_failed' };
// 401 用于 auth 错误
this.ctx.status = 401;
this.ctx.body = { error: 'unauthorized' };
```

From apps/server/app/service/audit.ts (MySQL/内存双模式):
```typescript
import { runtimeStore } from './runtime';
import { mysqlModeEnabled } from './storage';

export async function someListFn(config: AuthConfig) {
  if (mysqlModeEnabled()) {
    // MySQL query
  }
  return [...runtimeStore().someMap.values()];
}
```

From apps/server/app/router.ts (路由注册模式):
```typescript
import type { Application } from 'egg';
export default (app: Application): void => {
  const { router, controller } = app;
  router.get('/admin/api/users', controller.admin.users.index);
};
```

From apps/server/app/service/runtime.ts — 关键 types:
```typescript
export type UserRecord = {
  id: string; accountId: string; workspaceId: string; email: string;
  passwordHash: string; status: 'active' | 'disabled'; createdAt: number; updatedAt: number;
};
export type AdminUserRecord = {
  id: string; accountId: string; workspaceId: string; email: string;
  passwordHash: string; role: 'super_admin' | 'admin'; status: 'active' | 'disabled';
  createdAt: number; updatedAt: number;
};
export type DeviceRecord = {
  id: string; accountId: string; workspaceId: string;
  userId?: string; adminUserId?: string; name: string; platform: string;
  createdAt: number; updatedAt: number;
};
export type AuditEventRecord = {
  id: number; accountId: string; workspaceId?: string;
  userId?: string; adminUserId?: string; deviceId?: string; gatewayId?: string;
  sessionId?: string; action: string; tokenClass?: string;
  failureReason?: string; ip?: string; userAgent?: string;
  payload: Record<string, unknown>; createdAt: number;
};
```

From apps/server/sql/001_init.sql — audit_events 表结构:
- 字段: id, account_id, workspace_id, user_id, admin_user_id, device_id, gateway_id,
         session_id, token_class, jti, event_type, actor_type, actor_id, payload_json, created_at, updated_at
- 注意: SQL 列名是 event_type，runtime 中 AuditEventRecord.action 是内存侧的映射，
         service/storage.ts 在 auditEventFromRow 中负责 event_type → action 的字段映射

From apps/server/app/service/storage.ts — 现有批量查询（缺少，需新增）:
- 目前有 loadUserByEmail, loadUserById，但没有 loadAllUsers
- 目前有 loadAdminUserByEmail, loadAdminUserById，但没有 loadAllAdminUsers
- 没有 countDevicesByUserId、loadAllDevices、loadAllGateways
- 需要在 storage.ts 末尾新增这些函数
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin-auth 中间件 + storage.ts 批量查询函数</name>
  <read_first>
    - apps/server/app/middleware/auth.ts （完整内容，admin-auth.ts 参照）
    - apps/server/app/service/storage.ts （末尾，了解现有函数格式后追加新函数）
    - apps/server/app/service/runtime.ts （UserRecord, AdminUserRecord, DeviceRecord types）
    - apps/server/app/service/audit.ts （MySQL/内存双模式的 if-branch 模式）
  </read_first>
  <files>
    apps/server/app/middleware/admin-auth.ts,
    apps/server/app/service/storage.ts
  </files>
  <action>
**apps/server/app/middleware/admin-auth.ts** — 新建文件：
```typescript
import { requireTokenClass } from './auth.js';
import type { AuthConfig } from '../service/auth.js';

export function requireManagementToken(
  headerValue: string | undefined,
  config: AuthConfig
) {
  return requireTokenClass(headerValue, config, ['management_access']);
}
```

注意导入路径使用 `.js` 扩展名（与项目其他 middleware 一致）。

**apps/server/app/service/storage.ts** — 在文件末尾追加以下新函数（外科手术式修改，只追加，不修改任何现有代码）：

```typescript
// --- Phase 6 Admin Management Queries ---

export async function loadAllUsers(limit = 20, offset = 0): Promise<UserRecord[]> {
  const [rows] = await execute(
    'SELECT * FROM users ORDER BY created_at DESC LIMIT ? OFFSET ?',
    [limit, offset]
  );
  return (rows as Record<string, unknown>[]).map(userFromRow);
}

export async function countUsers(): Promise<number> {
  const [rows] = await execute('SELECT COUNT(*) AS count FROM users');
  const row = (rows as Record<string, unknown>[])[0];
  return Number(row?.count ?? 0);
}

export async function loadAllAdminUsers(limit = 100, offset = 0): Promise<AdminUserRecord[]> {
  const [rows] = await execute(
    'SELECT * FROM admin_users ORDER BY created_at ASC LIMIT ? OFFSET ?',
    [limit, offset]
  );
  return (rows as Record<string, unknown>[]).map(adminUserFromRow);
}

export async function countActiveDevices(): Promise<number> {
  const [rows] = await execute(
    "SELECT COUNT(*) AS count FROM devices WHERE revoked_at IS NULL AND token_class = 'normal_client_access'"
  );
  const row = (rows as Record<string, unknown>[])[0];
  return Number(row?.count ?? 0);
}

export async function countRegisteredGateways(): Promise<number> {
  const [rows] = await execute('SELECT COUNT(*) AS count FROM gateways');
  const row = (rows as Record<string, unknown>[])[0];
  return Number(row?.count ?? 0);
}

export async function countAuditEventsLast7Days(): Promise<number> {
  const [rows] = await execute(
    'SELECT COUNT(*) AS count FROM audit_events WHERE created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)'
  );
  const row = (rows as Record<string, unknown>[])[0];
  return Number(row?.count ?? 0);
}

export async function loadUserLoginStats(userId: string): Promise<{
  loginCount: number;
  failedLoginCount: number;
  lastLoginAt: number | null;
}> {
  const [loginRows] = await execute(
    "SELECT COUNT(*) AS count FROM audit_events WHERE user_id = ? AND event_type = 'auth.login.success'",
    [userId]
  );
  const [failRows] = await execute(
    "SELECT COUNT(*) AS count FROM audit_events WHERE user_id = ? AND event_type = 'auth.login.failed'",
    [userId]
  );
  const [lastRows] = await execute(
    "SELECT created_at FROM audit_events WHERE user_id = ? AND event_type = 'auth.login.success' ORDER BY created_at DESC LIMIT 1",
    [userId]
  );
  const loginCount = Number((loginRows as Record<string, unknown>[])[0]?.count ?? 0);
  const failedLoginCount = Number((failRows as Record<string, unknown>[])[0]?.count ?? 0);
  const lastRow = (lastRows as Record<string, unknown>[])[0];
  const lastLoginAt = lastRow?.created_at ? sqlDateToMs(lastRow.created_at) : null;
  return { loginCount, failedLoginCount, lastLoginAt };
}

export async function countActiveDevicesByUserId(userId: string): Promise<number> {
  const [rows] = await execute(
    "SELECT COUNT(*) AS count FROM devices WHERE user_id = ? AND revoked_at IS NULL",
    [userId]
  );
  const row = (rows as Record<string, unknown>[])[0];
  return Number(row?.count ?? 0);
}

export async function deleteAdminUserById(id: string): Promise<void> {
  await execute('DELETE FROM admin_users WHERE id = ?', [id]);
}
```

注意：`sqlDateToMs` 和 `userFromRow`、`adminUserFromRow` 是 storage.ts 中的 private 函数，已在文件内定义，直接调用即可。
  </action>
  <verify>
    <automated>
      ls /Users/dream/code/tether/apps/server/app/middleware/admin-auth.ts &&
      grep -c 'requireManagementToken' /Users/dream/code/tether/apps/server/app/middleware/admin-auth.ts &&
      grep -c 'loadAllUsers\|countUsers\|loadAllAdminUsers\|countActiveDevices\|countRegisteredGateways\|countAuditEventsLast7Days' /Users/dream/code/tether/apps/server/app/service/storage.ts &&
      cd /Users/dream/code/tether/apps/server && pnpm typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    admin-auth.ts 存在并导出 requireManagementToken；storage.ts 末尾追加了至少 8 个新导出函数；pnpm typecheck 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: users/admins service + controller + admin router 注册</name>
  <read_first>
    - apps/server/app/controller/admin-auth.ts （controller 模式，错误处理格式）
    - apps/server/app/service/audit.ts （MySQL/内存双模式参照）
    - apps/server/app/service/runtime.ts （runtimeStore 结构，users/adminUsers map）
    - apps/server/app/router.ts （路由注册格式，确认如何 import adminRouter）
    - apps/server/app/middleware/admin-auth.ts （刚创建的 requireManagementToken）
    - apps/server/app/service/storage.ts （刚新增的批量查询函数）
  </read_first>
  <files>
    apps/server/app/service/admin/users.ts,
    apps/server/app/service/admin/admins.ts,
    apps/server/app/controller/admin/users.ts,
    apps/server/app/controller/admin/admins.ts,
    apps/server/app/router/admin.ts,
    apps/server/app/router.ts
  </files>
  <action>
**apps/server/app/service/admin/users.ts** — 新建（MySQL/内存双模式）：
```typescript
import type { AuthConfig } from '../auth.js';
import { runtimeStore } from '../runtime.js';
import {
  mysqlModeEnabled,
  loadAllUsers,
  countUsers,
  loadUserLoginStats,
  countActiveDevicesByUserId,
  countActiveDevices,
  countRegisteredGateways,
  countAuditEventsLast7Days
} from '../storage.js';

export async function listAdminUsers(_config: AuthConfig, page: number, limit: number) {
  if (mysqlModeEnabled()) {
    const offset = (page - 1) * limit;
    const users = await loadAllUsers(limit, offset);
    const total = await countUsers();
    const items = await Promise.all(users.map(async (u) => {
      const stats = await loadUserLoginStats(u.id);
      const activeDeviceCount = await countActiveDevicesByUserId(u.id);
      return {
        id: u.id,
        email: u.email,
        createdAt: u.createdAt,
        loginCount: stats.loginCount,
        failedLoginCount: stats.failedLoginCount,
        lastLoginAt: stats.lastLoginAt,
        activeDeviceCount
      };
    }));
    return { users: items, total };
  }
  const store = runtimeStore();
  const all = [...store.users.values()];
  const total = all.length;
  const paginated = all.slice((page - 1) * limit, page * limit);
  return {
    users: paginated.map(u => ({
      id: u.id, email: u.email, createdAt: u.createdAt,
      loginCount: 0, failedLoginCount: 0, lastLoginAt: null, activeDeviceCount: 0
    })),
    total
  };
}

export async function getDashboardStats(_config: AuthConfig) {
  if (mysqlModeEnabled()) {
    const [totalUsers, activeDevices, registeredGateways, auditEventsLast7Days] = await Promise.all([
      countUsers(),
      countActiveDevices(),
      countRegisteredGateways(),
      countAuditEventsLast7Days()
    ]);
    return { totalUsers, activeDevices, registeredGateways, auditEventsLast7Days };
  }
  const store = runtimeStore();
  return {
    totalUsers: store.users.size,
    activeDevices: [...store.devices.values()].filter(d => !d.revokedAt).length,
    registeredGateways: store.gateways.size,
    auditEventsLast7Days: 0
  };
}
```

**apps/server/app/service/admin/admins.ts** — 新建：
```typescript
import type { AuthConfig } from '../auth.js';
import { runtimeStore } from '../runtime.js';
import { mysqlModeEnabled, loadAllAdminUsers, deleteAdminUserById, loadAdminUserById } from '../storage.js';
import { recordAuditEvent } from '../audit.js';

export async function listAdminManagers(_config: AuthConfig) {
  if (mysqlModeEnabled()) {
    const items = await loadAllAdminUsers();
    return {
      admins: items.map(u => ({
        id: u.id, email: u.email, role: u.role, status: u.status, createdAt: u.createdAt
      }))
    };
  }
  const store = runtimeStore();
  return {
    admins: [...store.adminUsers.values()].map(u => ({
      id: u.id, email: u.email, role: u.role, status: u.status, createdAt: u.createdAt
    }))
  };
}

export async function deleteAdminManager(
  id: string,
  adminUserId: string,
  accountId: string,
  workspaceId: string
) {
  if (mysqlModeEnabled()) {
    const target = await loadAdminUserById(id);
    if (!target) throw new Error('not_found');
    await deleteAdminUserById(id);
    await recordAuditEvent({
      accountId, workspaceId, adminUserId,
      action: 'admin.admin_user.deleted',
      tokenClass: 'management_access',
      payload: { targetAdminUserId: id }
    });
    return { ok: true };
  }
  const store = runtimeStore();
  if (!store.adminUsers.has(id)) throw new Error('not_found');
  store.adminUsers.delete(id);
  return { ok: true };
}
```

**apps/server/app/controller/admin/users.ts** — 新建：
```typescript
import { Controller } from 'egg';
import { requireManagementToken } from '../../middleware/admin-auth.js';
import { listAdminUsers, getDashboardStats } from '../../service/admin/users.js';

export default class AdminUsersController extends Controller {
  public async index(): Promise<void> {
    try {
      const identity = requireManagementToken(this.ctx.get('authorization'), this.app.config);
      const query = this.ctx.query as Record<string, string | undefined>;
      const page = Math.max(1, Number(query.page ?? '1'));
      const limit = Math.min(100, Math.max(1, Number(query.limit ?? '20')));
      this.ctx.body = await listAdminUsers(this.app.config, page, limit);
    } catch (error) {
      this.ctx.status = error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401 : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'list_users_failed' };
    }
  }

  public async dashboard(): Promise<void> {
    try {
      requireManagementToken(this.ctx.get('authorization'), this.app.config);
      this.ctx.body = await getDashboardStats(this.app.config);
    } catch (error) {
      this.ctx.status = error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401 : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'dashboard_failed' };
    }
  }
}
```

**apps/server/app/controller/admin/admins.ts** — 新建：
```typescript
import { Controller } from 'egg';
import { requireManagementToken } from '../../middleware/admin-auth.js';
import { listAdminManagers, deleteAdminManager } from '../../service/admin/admins.js';
import { registerManagementUser } from '../../service/auth.js';

export default class AdminAdminsController extends Controller {
  public async index(): Promise<void> {
    try {
      requireManagementToken(this.ctx.get('authorization'), this.app.config);
      this.ctx.body = await listAdminManagers(this.app.config);
    } catch (error) {
      this.ctx.status = error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401 : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'list_admins_failed' };
    }
  }

  public async create(): Promise<void> {
    try {
      requireManagementToken(this.ctx.get('authorization'), this.app.config);
      const body = this.ctx.request.body as Record<string, string | undefined>;
      const result = await registerManagementUser({
        email: body.email ?? '',
        password: body.password ?? '',
        displayName: body.displayName,
        deviceName: body.deviceName ?? 'admin-web',
        platform: body.platform ?? 'web',
        ip: this.ctx.ip,
        userAgent: this.ctx.get('user-agent')
      }, this.app.config);
      this.ctx.status = 201;
      this.ctx.body = result;
    } catch (error) {
      this.ctx.status = error instanceof Error && error.message === 'email_already_registered' ? 409
        : error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401
        : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'create_admin_failed' };
    }
  }

  public async destroy(): Promise<void> {
    try {
      const identity = requireManagementToken(this.ctx.get('authorization'), this.app.config);
      const { id } = this.ctx.params as Record<string, string>;
      await deleteAdminManager(id, identity.adminUserId, identity.accountId, identity.workspaceId);
      this.ctx.body = { ok: true };
    } catch (error) {
      this.ctx.status = error instanceof Error && error.message === 'not_found' ? 404
        : error instanceof Error && (error.message === 'missing_token' || error.message === 'wrong_token_class') ? 401
        : 400;
      this.ctx.body = { error: error instanceof Error ? error.message : 'delete_admin_failed' };
    }
  }
}
```

**apps/server/app/router/admin.ts** — 新建：
```typescript
import type { Application } from 'egg';

export default (app: Application): void => {
  const { router, controller } = app;

  router.get('/admin/api/dashboard/stats', controller.admin.users.dashboard);
  router.get('/admin/api/users',           controller.admin.users.index);
  router.get('/admin/api/admins',          controller.admin.admins.index);
  router.post('/admin/api/admins',         controller.admin.admins.create);
  router.delete('/admin/api/admins/:id',   controller.admin.admins.destroy);
  // Plan 03 会添加 devices/gateways/audit 路由
};
```

**apps/server/app/router.ts** — 在现有文件末尾追加对 adminRouter 的调用（外科手术修改，只追加）：

在文件末尾的 `router.get('/api/audit', ...)` 行之后，追加：
```typescript
  // Phase 6: Admin Management API
  const adminRouterModule = await import('./router/admin.js');
  adminRouterModule.default(app);
```

实际上 Egg 中不支持 async default 导出，需要用同步方式。正确方式是在 router.ts 文件顶部 import，然后在 default 函数中调用：
```typescript
import adminRouter from './router/admin.js';
// 在 export default 函数末尾调用
adminRouter(app);
```

读取 apps/server/app/router.ts 的当前内容，在文件顶部添加 import，并在函数末尾添加 `adminRouter(app)` 调用。
  </action>
  <verify>
    <automated>
      ls /Users/dream/code/tether/apps/server/app/middleware/admin-auth.ts &&
      ls /Users/dream/code/tether/apps/server/app/service/admin/users.ts &&
      ls /Users/dream/code/tether/apps/server/app/service/admin/admins.ts &&
      ls /Users/dream/code/tether/apps/server/app/controller/admin/users.ts &&
      ls /Users/dream/code/tether/apps/server/app/controller/admin/admins.ts &&
      ls /Users/dream/code/tether/apps/server/app/router/admin.ts &&
      grep -c 'adminRouter\|admin.*router\|router/admin' /Users/dream/code/tether/apps/server/app/router.ts &&
      cd /Users/dream/code/tether/apps/server && pnpm typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    所有 service/controller/router 文件创建完毕；router.ts 已 import adminRouter 并调用；`pnpm typecheck` 通过无 TS 错误。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → /admin/api/* | 所有管理 API 入口，必须携带有效 management_access token |
| controller → service | 鉴权完成后的内部调用，service 不再验证 token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Spoofing | requireManagementToken | mitigate | 调用 requireTokenClass(['management_access'])，JWT 签名验证由 verifyToken 保证；token class 不匹配返回 401 |
| T-06-02-02 | Elevation of Privilege | POST /admin/api/admins | mitigate | 创建新管理用户前先验证 management_access token；任何人都不能在未登录的情况下注册新管理账号 |
| T-06-02-03 | Tampering | DELETE /admin/api/admins/:id | mitigate | 需要有效 management token；目标用户不存在时返回 404；操作记录到 audit_events |
| T-06-02-04 | Information Disclosure | GET /admin/api/users 返回 passwordHash | mitigate | service 层的 map 函数只返回 id/email/createdAt/stats，不返回 passwordHash 字段 |
| T-06-02-05 | Denial of Service | GET /admin/api/users 无 limit 上限 | mitigate | controller 强制 limit = Math.min(100, ...) 防止超大查询 |
</threat_model>

<verification>
1. `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:4800/admin/api/users` — 返回 401（无 token）
2. `curl -s -H "Authorization: Bearer <valid_mgmt_token>" http://127.0.0.1:4800/admin/api/users` — 返回 200 含 users 数组
3. `curl -s http://127.0.0.1:4800/admin/api/dashboard/stats` — 返回 401
4. `pnpm typecheck`（在 apps/server 目录）— 退出码 0
</verification>

<success_criteria>
- /admin/api/users 未授权返回 401，有效 management token 返回 { users: [...], total: N }
- /admin/api/dashboard/stats 返回 { totalUsers, activeDevices, registeredGateways, auditEventsLast7Days }
- /admin/api/admins GET/POST/DELETE 端点均正确鉴权
- storage.ts 新增函数不破坏现有 TypeScript 编译
- audit_events 中记录 admin.admin_user.deleted 事件
</success_criteria>

<output>
完成后创建 .planning/phases/06-account-management-console/06-02-SUMMARY.md
</output>
