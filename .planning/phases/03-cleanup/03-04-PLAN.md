---
phase: 3
plan: 04
title: Web Display, Docs, and End-to-End Cleanup Verification
type: implementation
wave: 4
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - apps/web/src/main.tsx
  - AI_CONTEXT.md
  - PROJECT.md
  - docs/current/gateway-supervisor.md
  - docs/current/relay-mvp.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STACK.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02]
must_haves:
  truths:
    - Web UI does not default missing transport to tmux.
    - Current docs describe PTY event stream as the only active transport.
    - Phase 3 closes only after static scans, typecheck, tests, and minimal PTY E2E verification.
  artifacts:
    - apps/web/src/main.tsx
    - AI_CONTEXT.md
    - PROJECT.md
    - docs/current/gateway-supervisor.md
    - docs/current/relay-mvp.md
  key_links:
    - Web legacy display matches Gateway's 409 legacy unsupported behavior.
    - Current docs and codebase maps are updated after runtime tmux removal.
---

<objective>
Update the remaining user-facing surfaces and documentation so tmux is no longer presented as a live fallback, then run the full static, type, test, and PTY E2E verification gate.
</objective>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Web UI implies legacy sessions are still controllable | medium | Show unknown/historical transport as legacy and disable stop/control affordances for non-running PTY sessions. |
| Long-term docs keep telling future phases to preserve tmux fallback | medium | Update current docs and root context to PTY-only active transport while preserving historical notes. |
| Phase closes without proving tmux is gone | high | Run static scans, typecheck, tests, and a minimal PTY E2E with Gateway/session creation/input/stop. |
</threat_model>

<tasks>
<task id="03-04-T01" type="execute">
  <name>Show legacy transport honestly in Web session cards and views</name>
  <read_first>
    <file>apps/web/src/main.tsx</file>
    <file>.planning/phases/03-cleanup/03-CONTEXT.md</file>
  </read_first>
  <files>
    <file>apps/web/src/main.tsx</file>
  </files>
  <action>
    Replace the Web session card fallback `{session.transport ?? 'tmux'}` with a helper that returns:
    `PTY` for `pty-event-stream`;
    `legacy` for `tmux`;
    `unknown` for missing or unrecognized transport.
    Ensure `SessionView` does not treat missing transport as tmux. If direct snapshot returns HTTP 409 for a legacy session, show a clear status string such as `旧 tmux 会话已不再支持`.
    Do not add new Web control modes or auth UI in this phase.
  </action>
  <verify>Run the task-specific acceptance criteria below and the plan verification commands.</verify>
  <acceptance_criteria>
    <criterion>`apps/web/src/main.tsx` does not contain `session.transport ?? 'tmux'`.</criterion>
    <criterion>`apps/web/src/main.tsx` contains `legacy` or `旧 tmux 会话已不再支持` for historical transport display.</criterion>
    <criterion>`pnpm --filter @tether/web typecheck` exits 0.</criterion>
  </acceptance_criteria>
  <done>All acceptance criteria for this task pass.</done>
</task>

<task id="03-04-T02" type="execute">
  <name>Update current architecture docs from tmux fallback to PTY-only active transport</name>
  <read_first>
    <file>AI_CONTEXT.md</file>
    <file>PROJECT.md</file>
    <file>docs/current/gateway-supervisor.md</file>
    <file>docs/current/relay-mvp.md</file>
    <file>.planning/phases/03-cleanup/03-CONTEXT.md</file>
  </read_first>
  <files>
    <file>AI_CONTEXT.md</file>
    <file>PROJECT.md</file>
    <file>docs/current/gateway-supervisor.md</file>
    <file>docs/current/relay-mvp.md</file>
  </files>
  <action>
    Update long-term/current docs to state that tmux runtime fallback has been removed and PTY event stream is the only active transport.
    Preserve historical context where needed by saying tmux rows may remain in SQLite as lost/legacy records.
    Remove command examples that tell users to run `--transport tmux`.
    Do not edit frozen or working draft documents unless a current doc directly links them as authoritative for present behavior.
  </action>
  <verify>Run the task-specific acceptance criteria below and the plan verification commands.</verify>
  <acceptance_criteria>
    <criterion>`PROJECT.md` does not contain `--transport tmux` as a current command.</criterion>
    <criterion>`AI_CONTEXT.md` states that PTY event stream is the only active transport.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` does not describe tmux as a live fallback.</criterion>
    <criterion>`docs/current/relay-mvp.md` does not describe Relay sessions as tmux-backed.</criterion>
  </acceptance_criteria>
  <done>All acceptance criteria for this task pass.</done>
</task>

<task id="03-04-T03" type="execute">
  <name>Refresh codebase maps after tmux runtime deletion</name>
  <read_first>
    <file>.planning/codebase/ARCHITECTURE.md</file>
    <file>.planning/codebase/INTEGRATIONS.md</file>
    <file>.planning/codebase/STACK.md</file>
    <file>.planning/phases/03-cleanup/03-CONTEXT.md</file>
  </read_first>
  <files>
    <file>.planning/codebase/ARCHITECTURE.md</file>
    <file>.planning/codebase/INTEGRATIONS.md</file>
    <file>.planning/codebase/STACK.md</file>
  </files>
  <action>
    Update the codebase map docs so they no longer list `apps/gateway/src/tmux.ts`, `--transport tmux`, `tmux fallback`, or tmux executable helpers as current architecture.
    Keep historical notes only if clearly marked as historical or removed.
    Preserve safety rules that say subprocesses must use list-form arguments; do not cite deleted tmux code as the example after this phase.
  </action>
  <verify>Run the task-specific acceptance criteria below and the plan verification commands.</verify>
  <acceptance_criteria>
    <criterion>`.planning/codebase/ARCHITECTURE.md` does not list `apps/gateway/src/tmux.ts` as a current component.</criterion>
    <criterion>`.planning/codebase/INTEGRATIONS.md` does not document `--transport tmux` as a current CLI flag.</criterion>
    <criterion>`.planning/codebase/STACK.md` does not say tmux is required for a current fallback.</criterion>
  </acceptance_criteria>
  <done>All acceptance criteria for this task pass.</done>
</task>

<task id="03-04-T04" type="execute">
  <name>Run static scans, typecheck, tests, and minimal PTY E2E</name>
  <read_first>
    <file>package.json</file>
    <file>PROJECT.md</file>
    <file>apps/cli/src/main.ts</file>
    <file>apps/gateway/src/daemon.ts</file>
    <file>.planning/phases/03-cleanup/03-CONTEXT.md</file>
  </read_first>
  <files>
    <file>.planning/phases/03-cleanup/03-04-PLAN.md</file>
  </files>
  <action>
    Run the full Phase 3 verification:
    `rg -n "apps/gateway/src/tmux.ts|--transport|capturePane|createAgentSession|assertTmuxAvailable|sessionExists|showStatusMessage|spawn\\('tmux'|spawn\\(\"tmux\"" apps packages PROJECT.md AI_CONTEXT.md docs/current .planning/codebase`
    and inspect results. Runtime source and current docs must have no active tmux references; historical references must be marked historical/legacy.
    Run `pnpm typecheck`.
    Run `pnpm test`.
    Run a minimal PTY E2E: start a Gateway with API session creation enabled, create a PTY session using a harmless provider command or `/bin/cat` test path, attach or send input once, stop the session, and confirm no tmux dependency is used.
    Record any command failures in the phase summary when executing Phase 3; do not mark the phase complete with skipped verification unless the reason and residual risk are explicit.
  </action>
  <verify>Run the task-specific acceptance criteria below and the plan verification commands.</verify>
  <acceptance_criteria>
    <criterion>`test ! -f apps/gateway/src/tmux.ts` exits 0.</criterion>
    <criterion>`rg -n "from './tmux\\.js'|capturePane|createAgentSession|assertTmuxAvailable|sessionExists|showStatusMessage|spawn\\('tmux'|spawn\\(\"tmux\"" apps packages` has no matches.</criterion>
    <criterion>`rg -n -- "--transport" apps packages PROJECT.md AI_CONTEXT.md docs/current .planning/codebase` has no matches except historical/frozen references explicitly marked non-current.</criterion>
    <criterion>`pnpm typecheck` exits 0.</criterion>
    <criterion>`pnpm test` exits 0.</criterion>
    <criterion>Minimal PTY E2E creates, interacts with, and stops a PTY session without tmux.</criterion>
  </acceptance_criteria>
  <done>All acceptance criteria for this task pass.</done>
</task>
</tasks>

<verification>
- Run `pnpm --filter @tether/web typecheck`.
- Run `pnpm typecheck`.
- Run `pnpm test`.
- Run the static scans listed in task `03-04-T04`.
- Run the minimal PTY E2E listed in task `03-04-T04`.
</verification>

<must_haves>
## Must Haves

### Truths
- CLEAN-01 requires no current runtime/doc path to expose tmux fallback.
- CLEAN-02 requires retained transport fields to be documented as historical/future extension, not active tmux support.
- D-01, D-03, D-04, D-11, D-14, D-16, D-17, and D-18 must be visible in final verification.

### Checks
- Web does not default unknown transport to tmux.
- Current docs no longer advertise `--transport tmux`.
- Static scans, typecheck, tests, and PTY E2E all pass or are explicitly reported with blockers.
</must_haves>

<success_criteria>
- User-facing surfaces describe PTY-only active transport.
- Documentation and codebase maps match the implementation.
- The phase has a reproducible verification record.
</success_criteria>
