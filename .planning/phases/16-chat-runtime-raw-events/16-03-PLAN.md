---
phase: "16-chat-runtime-raw-events"
plan: "03"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/server/app/service/runtimeSyncRepository.ts
  - apps/server/app/controller/runtime-sync.ts
  - apps/server/app/service/chatEventsRepository.ts
  - apps/server/app/controller/chat-events.ts
  - apps/server/app/router.ts
  - apps/server/config/config.default.ts
  - apps/server/app/controller/chat.ts
  - apps/server/app/service/chatRepository.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "runtime-sync controller 收到 scope.transport='chat' 时调用 upsertChatRuntimeEvent 而非 upsertRuntimeEvent"
    - "upsertChatRuntimeEvent 在同一事务中写 gateway_runtime_chats_events 和更新 gateway_chat_messages.raw_json"
    - "GET /api/relay/chat-events/:sessionId?after=N 返回 event_type='agent.delta' 的行，按 event_id 升序"
    - "GET /api/server/chat-sessions/:sessionId/messages 响应顶层含 lastEventId 字段"
    - "新接口路由已加入 verifyLoginWhitelist"
  artifacts:
    - path: "apps/server/app/service/runtimeSyncRepository.ts"
      provides: "upsertChatRuntimeEvent 方法"
      contains: "upsertChatRuntimeEvent"
    - path: "apps/server/app/controller/chat-events.ts"
      provides: "GET chat-events 控制器"
      contains: "chatEventsRepository"
    - path: "apps/server/app/service/chatEventsRepository.ts"
      provides: "listDeltaEventsAfter 方法"
      contains: "listDeltaEventsAfter"
    - path: "apps/server/app/router.ts"
      provides: "路由注册"
      contains: "/api/relay/chat-events/"
  key_links:
    - from: "apps/server/app/controller/runtime-sync.ts"
      to: "apps/server/app/service/runtimeSyncRepository.ts"
      via: "transport === 'chat' 分支"
      pattern: "upsertChatRuntimeEvent"
    - from: "apps/server/app/controller/chat-events.ts"
      to: "apps/server/app/service/chatEventsRepository.ts"
      via: "ctx.service.chatEventsRepository.listDeltaEventsAfter"
      pattern: "listDeltaEventsAfter"
    - from: "apps/server/app/router.ts"
      to: "apps/server/app/controller/chat-events.ts"
      via: "requireRuntimeSyncSecret"
      pattern: "chatEvents"
---

<objective>
Server 侧完整写入和读取路径：
1. runtimeSyncRepository 新增 `upsertChatRuntimeEvent`：事务内写 `gateway_runtime_chats_events` + 更新 `gateway_chat_messages.raw_json`（仅 user.message / agent.result）
2. runtime-sync controller 按 `scope.transport === 'chat'` 分发写路径
3. 新建 chatEventsRepository + chat-events controller，实现 GET /api/relay/chat-events/:sessionId?after=N
4. messages API 响应新增 lastEventId 顶层字段
5. 新路由加入 verifyLoginWhitelist

Purpose: 这是 Phase 16 的核心写库路径和 catch-up 读取路径，Relay P04 必须能调用这两个接口。

Output: 8 个文件修改
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-chat-runtime-raw-events/16-CONTEXT.md
@.planning/phases/16-chat-runtime-raw-events/16-PATTERNS.md
</context>

<interfaces>
<!-- 现有关键接口，executor 直接使用 -->

From apps/server/app/service/runtimeSyncRepository.ts:
```typescript
// 现有内部函数（可复用，不需 export）
function maskPayload(payload: unknown): string  // 脱敏
function truncatePayload(value: string): string  // 截断到 MAX_PAYLOAD_BYTES=65536

// 现有私有方法（同一 class，可直接调用）
private async sessionWithinScope(sessionId, scope, db): Promise<boolean>
private async sessionScopeConflict(sessionId, scope, db): Promise<boolean>
private async sessionDeleted(sessionId, scope, userId?, db): Promise<boolean>

// 现有事务模式
await this.ctx.service.db.transaction(async connection => {
  const sessionRows = await connection.query(
    `SELECT user_id FROM gateway_sessions WHERE id = ? AND gateway_id = ? AND account_id = ? LIMIT 1`,
    [sessionId, scope.gatewayId, scope.accountId]
  );
  // ...
});

// 现有 RuntimeSyncScope 类型（需扩展）
type RuntimeSyncScope = { accountId: string; gatewayId: string; }
// Phase 16 扩展为：
type RuntimeSyncScope = { accountId: string; gatewayId: string; transport?: string; }
```

From apps/server/app/controller/runtime-sync.ts:
```typescript
// 现有 event() 方法（待修改）
public async event(): Promise<void> {
  const { ctx } = this;
  const body = ctx.request.body as Record<string, unknown>;
  const scope = requireScope(ctx, body.scope);  // 仍使用此函数，transport 字段单独提取
  const event = requireEvent(ctx, body.event);
  const eventType = String(event.type ?? '');
  const sessionId = String(event.sessionId ?? '');
  const eventId = Number(event.id ?? 0);
  // Phase 16: 加 transport 分支
}
```

From apps/server/app/service/chatRepository.ts:
```typescript
// listMessages 现有返回结构（待扩展 lastEventId）
public async listMessages(sessionId, accountId, userId): Promise<ChatMessageRecord[]>
// SQL: SELECT id, session_id, role, content, usage_json, created_at FROM gateway_chat_messages WHERE session_id = ? ORDER BY created_at ASC, id ASC
// Phase 16 需要额外 SELECT raw_json 来提取 lastDeltaEventId（仅最后一条 assistant 行）
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runtimeSyncRepository 新增 upsertChatRuntimeEvent + RuntimeSyncScope 扩展</name>
  <files>apps/server/app/service/runtimeSyncRepository.ts</files>
  <read_first>
    - apps/server/app/service/runtimeSyncRepository.ts — 完整读取（特别是 lines 35-38 RuntimeSyncScope，lines 149-189 upsertRuntimeEvent，lines 232-270 insertDerivedChatMessage）
  </read_first>
  <behavior>
    - RuntimeSyncScope 增加 `transport?: string` 字段
    - 新方法 `upsertChatRuntimeEvent(sessionId, eventId, eventType, event, scope, createdAt?)` 接收完整 event 对象（含 id/type/sessionId/ts/payload）
    - 方法开头：若 !mysqlModeEnabled() 直接 return
    - 事务内步骤：1) 查 session 验证 scope；2) 检查 deleted；3) 检查 scope conflict；4) maskPayload(event) + truncatePayload 构建 rawJson；5) INSERT INTO gateway_runtime_chats_events ON DUPLICATE KEY UPDATE；6) 若 eventType 为 'user.message' 或 'agent.result' 则额外执行 UPDATE gateway_chat_messages SET raw_json = ? WHERE session_id = ? AND source_event_id = ?
    - 不修改 upsertRuntimeEvent，两路径完全解耦（D-09）
    - 注意 step 6 只 UPDATE（不 INSERT），因为 insertDerivedChatMessage 会在原路径中先 INSERT 行；raw_json 更新是 catch-up 路径，不是行创建
  </behavior>
  <action>
在 apps/server/app/service/runtimeSyncRepository.ts 中：

**1. 扩展 RuntimeSyncScope 类型**（约 line 35-38）：
```typescript
type RuntimeSyncScope = {
  accountId: string;
  gatewayId: string;
  transport?: string;  // Phase 16: 'chat' | undefined
};
```

**2. 在 class RuntimeSyncRepositoryService 中新增 public 方法**（在 updateSessionLastActiveAt 之后添加）：
```typescript
public async upsertChatRuntimeEvent(
  sessionId: string,
  eventId: number,
  eventType: string,
  event: unknown,
  scope: RuntimeSyncScope,
  createdAt?: unknown
): Promise<void> {
  if (!this.mysqlModeEnabled()) {
    return;
  }
  await this.ctx.service.db.transaction(async connection => {
    const sessionRows = await connection.query(
      `SELECT user_id FROM gateway_sessions
       WHERE id = ? AND gateway_id = ? AND account_id = ? LIMIT 1`,
      [sessionId, scope.gatewayId, scope.accountId]
    );
    const userId = Array.isArray(sessionRows) && sessionRows.length > 0
      ? String((sessionRows[0] as { user_id?: unknown }).user_id ?? '')
      : undefined;
    if (await this.sessionDeleted(sessionId, scope, userId, connection)) {
      return;
    }
    if (await this.sessionScopeConflict(sessionId, scope, connection)) {
      console.warn(`[server] upsertChatRuntimeEvent scope mismatch: ${sessionId}`);
      return;
    }
    const rawJson = truncatePayload(maskPayload(event));
    await connection.query(
      `INSERT INTO gateway_runtime_chats_events (session_id, event_id, event_type, raw_json, created_at)
       VALUES (?, ?, ?, ?, ?)
       ON DUPLICATE KEY UPDATE
         event_type = VALUES(event_type),
         raw_json = VALUES(raw_json),
         updated_at = CURRENT_TIMESTAMP`,
      [sessionId, eventId, eventType, rawJson, this.toDate(createdAt)]
    );
    // 对 user.message / agent.result 同步更新 gateway_chat_messages.raw_json
    if (eventType === 'user.message' || eventType === 'agent.result') {
      await connection.query(
        `UPDATE gateway_chat_messages SET raw_json = ? WHERE session_id = ? AND source_event_id = ?`,
        [rawJson, sessionId, eventId]
      );
    }
  });
}
```

注意：使用 `this.toDate(createdAt)` 复用已有私有方法；maskPayload 和 truncatePayload 是文件内部函数，直接调用无需 import。
  </action>
  <verify>
    <automated>grep -c "upsertChatRuntimeEvent" /Users/dream/code/tether/apps/server/app/service/runtimeSyncRepository.ts</automated>
  </verify>
  <acceptance_criteria>
    - runtimeSyncRepository.ts 含 "upsertChatRuntimeEvent" 公开方法
    - RuntimeSyncScope 类型含 "transport?: string"
    - 方法内含 "gateway_runtime_chats_events" INSERT 语句
    - 方法内含 "UPDATE gateway_chat_messages SET raw_json"
    - 方法内含 "ON DUPLICATE KEY UPDATE"（幂等保证）
    - 不修改 upsertRuntimeEvent 方法（grep 确认该方法行数/内容不变）
    - pnpm --filter @tether/server typecheck 通过
  </acceptance_criteria>
  <done>upsertChatRuntimeEvent 方法存在，事务内双表写入，类型扩展，typecheck 通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runtime-sync controller 分支 + chatEventsRepository + chat-events controller + 路由 + messages API lastEventId</name>
  <files>
    apps/server/app/controller/runtime-sync.ts,
    apps/server/app/service/chatEventsRepository.ts,
    apps/server/app/controller/chat-events.ts,
    apps/server/app/router.ts,
    apps/server/config/config.default.ts,
    apps/server/app/controller/chat.ts,
    apps/server/app/service/chatRepository.ts
  </files>
  <read_first>
    - apps/server/app/controller/runtime-sync.ts — 完整读取，特别是 event() 方法（lines 63-80）
    - apps/server/app/service/chatRepository.ts — listMessages 方法（lines 128-153）和 messageFromRow（找完整方法）
    - apps/server/app/controller/chat.ts — messages() 方法（完整读取，约前 80 行）
    - apps/server/app/router.ts — 完整读取（了解现有路由结构和 requireRuntimeSyncSecret 位置）
    - apps/server/config/config.default.ts — 找到 verifyLoginWhitelist 数组位置
  </read_first>
  <behavior>
    - runtime-sync controller event() 方法：从 body.scope 提取 transport 字段；若 transport === 'chat' 调用 upsertChatRuntimeEvent(sessionId, eventId, eventType, event, scope, event.ts)；否则走原来的 upsertRuntimeEvent
    - 新 chatEventsRepository Service：listDeltaEventsAfter(sessionId, after) 从 gateway_runtime_chats_events 读 event_type='agent.delta' AND event_id > after，ORDER BY event_id ASC，返回 {eventId, rawJson}[]
    - 新 chat-events controller：GET list()，取 ctx.params.sessionId + ctx.query.after，调用 chatEventsRepository.listDeltaEventsAfter，ctx.success({events})
    - router.ts：新增 GET /api/relay/chat-events/:sessionId 路由（requireRuntimeSyncSecret）
    - verifyLoginWhitelist：新增 '/api/relay/chat-events/:sessionId'
    - chatRepository.listMessages：增加查最后一条 assistant 行的 raw_json，提取 payload.lastDeltaEventId
    - chat controller messages()：响应包含 { messages, lastEventId } 而非仅 { messages }
  </behavior>
  <action>
**A. 修改 runtime-sync controller event() 方法**

在 apps/server/app/controller/runtime-sync.ts 的 event() 方法中，将现有调用修改为：
```typescript
public async event(): Promise<void> {
  const { ctx } = this;
  const body = ctx.request.body as Record<string, unknown>;
  const scope = requireScope(ctx, body.scope);
  const event = requireEvent(ctx, body.event);
  const eventType = String(event.type ?? '');
  const sessionId = String(event.sessionId ?? '');
  const eventId = Number(event.id ?? 0);
  // Phase 16: 按 transport 分发写路径（D-11）
  const scopeRaw = body.scope as Record<string, unknown>;
  const transport = typeof scopeRaw?.transport === 'string' ? scopeRaw.transport : undefined;
  if (transport === 'chat') {
    await ctx.service.runtimeSyncRepository.upsertChatRuntimeEvent(
      sessionId, eventId, eventType, event, scope, event.ts
    );
  } else {
    await ctx.service.runtimeSyncRepository.upsertRuntimeEvent(
      sessionId, eventId, eventType, event.payload, scope, event.ts
    );
  }
  ctx.success({ ok: true });
}
```

**B. 新建 apps/server/app/service/chatEventsRepository.ts**

```typescript
import { Service } from 'egg';

export type ChatDeltaEventRow = {
  eventId: number;
  rawJson: string;
};

export default class ChatEventsRepositoryService extends Service {
  private mysqlModeEnabled() {
    return this.ctx.service.db.mysqlModeEnabled();
  }

  public async listDeltaEventsAfter(sessionId: string, after: number): Promise<ChatDeltaEventRow[]> {
    if (!this.mysqlModeEnabled()) {
      return [];
    }
    const rows = await this.ctx.service.db.query(
      `SELECT event_id, raw_json
       FROM gateway_runtime_chats_events
       WHERE session_id = ? AND event_type = 'agent.delta' AND event_id > ?
       ORDER BY event_id ASC`,
      [sessionId, after]
    );
    return (rows as Record<string, unknown>[]).map((row) => ({
      eventId: Number(row.event_id ?? 0),
      rawJson: String(row.raw_json ?? '{}')
    }));
  }
}
```

**C. 新建 apps/server/app/controller/chat-events.ts**

```typescript
import { Controller } from 'egg';

export default class ChatEventsController extends Controller {
  public async list(): Promise<void> {
    const { ctx } = this;
    const sessionId = String(ctx.params['sessionId'] ?? '');
    if (!sessionId) {
      ctx.throw(400, 'Missing sessionId');
      return;
    }
    const after = Number(ctx.query['after'] ?? 0);
    const events = await ctx.service.chatEventsRepository.listDeltaEventsAfter(sessionId, after);
    ctx.success({ events });
  }
}
```

**D. 修改 apps/server/app/router.ts**

在 `/api/relay/gateway-sessions/:sessionId/metadata` 路由的上方或下方添加：
```typescript
router.get('/api/relay/chat-events/:sessionId', requireRuntimeSyncSecret, controller.chatEvents.list);
```

**E. 修改 apps/server/config/config.default.ts 的 verifyLoginWhitelist**

找到 verifyLoginWhitelist 数组，添加：
```
'/api/relay/chat-events/:sessionId'
```

**F. 修改 chatRepository.listMessages，新增 lastEventId 计算**

在 listMessages 方法中，现有查询 gateway_chat_messages 后，额外执行一条查询最后一条 assistant 行的 raw_json：
```typescript
// 提取 lastEventId：取最后一条 assistant 行的 raw_json.payload.lastDeltaEventId
let lastEventId = 0;
const lastAssistantRows = await this.ctx.service.db.query(
  `SELECT raw_json FROM gateway_chat_messages
   WHERE session_id = ? AND role = 'assistant'
   ORDER BY created_at DESC, id DESC LIMIT 1`,
  [sessionId]
);
if (Array.isArray(lastAssistantRows) && lastAssistantRows.length > 0) {
  const rawJsonStr = String((lastAssistantRows[0] as Record<string, unknown>).raw_json ?? '');
  if (rawJsonStr) {
    try {
      const parsed = JSON.parse(rawJsonStr) as unknown;
      if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
        const payload = (parsed as Record<string, unknown>).payload;
        if (payload && typeof payload === 'object' && !Array.isArray(payload)) {
          const lid = (payload as Record<string, unknown>).lastDeltaEventId;
          if (typeof lid === 'number' && Number.isFinite(lid)) {
            lastEventId = lid;
          }
        }
      }
    } catch {
      // 忽略 JSON 解析错误，lastEventId 保持 0
    }
  }
}
return { messages: rows.map(row => this.messageFromRow(row)), lastEventId };
```

修改 listMessages 返回类型为 `Promise<{ messages: ChatMessageRecord[]; lastEventId: number }>` 并更新类型签名。

**G. 修改 chat controller messages() 方法**

读取 chatRepository.listMessages 的新返回值后：
```typescript
const { messages, lastEventId } = await ctx.service.chatRepository.listMessages(sessionId, accountId, userId);
ctx.success({ messages, lastEventId });
```
  </action>
  <verify>
    <automated>grep -c "upsertChatRuntimeEvent\|chatEventsRepository\|chat-events\|lastEventId" /Users/dream/code/tether/apps/server/app/router.ts</automated>
  </verify>
  <acceptance_criteria>
    - runtime-sync.ts event() 含 "transport === 'chat'" 分支
    - chatEventsRepository.ts 存在且含 "listDeltaEventsAfter"
    - chat-events.ts 存在且含 "chatEventsRepository"
    - router.ts 含 "'/api/relay/chat-events/:sessionId'" 路由
    - config.default.ts 中 verifyLoginWhitelist 含 "'/api/relay/chat-events/:sessionId'"
    - chatRepository.ts listMessages 返回含 lastEventId
    - chat controller messages() 响应含 "lastEventId"
    - pnpm --filter @tether/server typecheck 通过
    - pnpm --filter @tether/server test 通过（现有测试不能因此 break）
  </acceptance_criteria>
  <done>Server 侧完整写路径和读路径就绪；messages API 含 lastEventId；全量测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relay → Server /api/relay/chat-events | 内部 HTTP，仅 runtimeSyncSecret 授权 |
| Client → Server /api/server/chat-sessions/:id/messages | 用户 JWT 授权 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-A | Elevation of Privilege | GET /api/relay/chat-events/:sessionId | mitigate | requireRuntimeSyncSecret 中间件鉴权；路由加入 verifyLoginWhitelist（不走用户 JWT，但有独立 secret 验证） |
| T-16-03-B | Information Disclosure | raw_json 字段暴露给客户端 | mitigate | listMessages 不返回 raw_json 字段到前端；maskPayload 在写入时已脱敏 |
| T-16-03-C | Tampering | scope transport 字段客户端伪造 | accept | transport 字段由 Relay 内部填充，不接受外部客户端直接调用此端点（requireRuntimeSyncSecret 保护） |
| T-16-03-D | Spoofing | 跨账号 session 读取 | mitigate | upsertChatRuntimeEvent 和现有 upsertRuntimeEvent 同样检查 sessionScopeConflict，拒绝跨账号写入 |
</threat_model>

<verification>
pnpm --filter @tether/server typecheck
pnpm --filter @tether/server test
</verification>

<success_criteria>
- 8 个文件按计划修改/新建
- runtime-sync controller 按 transport 分发写路径
- chat-events 读接口可访问，受 runtimeSyncSecret 保护
- messages API 返回 lastEventId
- Server typecheck + test 通过
</success_criteria>

<output>
完成后创建 `.planning/phases/16-chat-runtime-raw-events/16-03-SUMMARY.md`
</output>
