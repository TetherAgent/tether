---
phase: "16-chat-runtime-raw-events"
plan: "04"
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - apps/relay/src/relay.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Relay 收到 agent.delta 后，在发给 Client 之前（break 之前）void syncToServer 写入 Server"
    - "发出的 agent.delta 帧携带 eventId 字段（来自 frame.event.id）"
    - "client.subscribe 处理 chat session 时，若 session.status === 'running' 则拉取 catch-up delta 并发 gateway.chat-catchup（携带 lastEventId）"
    - "catch-up 拉取使用 runtimeSyncSecret header，调用 /api/relay/chat-events/:sessionId?after=N"
    - "WHITELIST 路径中属于 chat session 的事件（chatSessionOwners.has 命中）的 syncToServer 调用携带 transport:'chat'"
  artifacts:
    - path: "apps/relay/src/relay.ts"
      provides: "agent.delta syncToServer 插入 + client.subscribe catch-up 逻辑 + WHITELIST chat transport 注入"
      contains: "transport: 'chat'"
  key_links:
    - from: "apps/relay/src/relay.ts agent.delta handler"
      to: "Server /api/relay/runtime-sync/gateway/event"
      via: "void syncToServer with scope.transport='chat'"
      pattern: "syncToServer.*runtime-sync.*transport.*chat"
    - from: "apps/relay/src/relay.ts WHITELIST syncToServer"
      to: "Server /api/relay/runtime-sync/gateway/event"
      via: "chatSessionOwners.has check injects transport:'chat'"
      pattern: "chatSessionOwners\\.has.*transport.*chat"
    - from: "apps/relay/src/relay.ts client.subscribe chat handler"
      to: "Server /api/relay/chat-events/:sessionId?after=N"
      via: "fetch GET with runtimeSyncSecret"
      pattern: "chat-events"
---

<objective>
修改 Relay relay.ts 三处逻辑：
1. agent.delta handler：在 break 之前插入 void syncToServer，携带完整 event 和 transport: 'chat'
2. WHITELIST syncToServer 路径：若事件属于 chat session（chatSessionOwners.has(frame.event.sessionId)），则在 scope 中补充 transport: 'chat'，确保 agent.result/user.message/agent.tool/session.error 等 chat event 写入新表而非旧表（D-01/D-02）
3. client.subscribe chat session handler：若 session.status === 'running' 则调 /api/relay/chat-events 拉缺口 delta，拼 blob，发 gateway.chat-catchup（含 lastEventId）

Purpose: 完成 chat event 的实时入库通道 + 断线 catch-up 通道。D-01/D-02 要求所有 chat events 只写新表，WHITELIST 路径必须同样注入 transport:'chat'。

Output: apps/relay/src/relay.ts
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-chat-runtime-raw-events/16-CONTEXT.md
@.planning/phases/16-chat-runtime-raw-events/16-PATTERNS.md
</context>

<interfaces>
<!-- 关键代码片段，从 relay.ts 提取 -->

现有 agent.delta handler（约 lines 386-397），Phase 16 在 break 前插入：
```typescript
if (frame.event.type === 'agent.delta') {
  const clientId = chatSessionOwners.get(frame.event.sessionId)
    ?? (typeof frame.event.payload.clientId === 'string' ? frame.event.payload.clientId : undefined);
  if (clientId) {
    sendToClient(clientId, {
      type: 'agent.delta',
      sessionId: frame.event.sessionId,
      text: String(frame.event.payload.text ?? ''),
      eventId: typeof frame.event.id === 'number' ? frame.event.id : undefined  // Phase 16: D-17
    });
  }
  // Phase 16 插入：void syncToServer + break
  break;
}
```

现有 gatewayScope（在 case 'gateway.event': 附近）：Relay 在 handleGatewayFrame 里维护了 gatewayScope，包含 accountId/gatewayId 等字段，同 RUNTIME_EVENT_WHITELIST 路径使用的 scope。

RUNTIME_EVENT_WHITELIST syncToServer 调用（约 lines 498-504）——Phase 16 在此处注入 transport:'chat'：
```typescript
// 现有 WHITELIST 路径（伪代码）
if (RUNTIME_EVENT_WHITELIST.has(frame.event.type)) {
  void syncToServer('/api/relay/runtime-sync/gateway/event', {
    gatewayId: frame.gatewayId,
    event: frame.event,
    scope: gatewayScope   // <-- Phase 16: 若 chatSessionOwners.has(frame.event.sessionId) 则改为 { ...gatewayScope, transport: 'chat' }
  });
}
```

fetchSessionMetadata GET 模式（lines 105-142）— catch-up 接口调用复用此模式（同样 GET + runtimeSyncSecret）。

现有 client.subscribe chat 分支（lines 764-767），Phase 16 在 break 前插入 catch-up：
```typescript
if (session.transport === 'chat') {
  chatSessionOwners.set(frame.sessionId, clientId);
  // Phase 16: catch-up 逻辑
  break;
}
```

sendToClient 签名：`sendToClient(clientId: string, frame: RelayServerToClientFrame): void`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agent.delta handler 插入 syncToServer + eventId 注入 + WHITELIST chat transport 修正</name>
  <files>apps/relay/src/relay.ts</files>
  <read_first>
    - apps/relay/src/relay.ts lines 386-510 — 完整读取 agent.delta handler 和 RUNTIME_EVENT_WHITELIST syncToServer 调用，理解 gatewayScope 变量的作用域和来源
    - 确认 gatewayScope 在 case 'gateway.event' 的作用域内是否可用于 delta handler（delta handler 在同一个 if 链中）
    - 找到 RUNTIME_EVENT_WHITELIST syncToServer 调用的确切行号（约 lines 498-504）
  </read_first>
  <behavior>
    - agent.delta 的 sendToClient 调用中，新增 eventId 字段：若 frame.event.id 是 number 则传递，否则不传（可选字段，符合 Protocol 类型 eventId?: number）
    - 在 sendToClient 之后、break 之前，插入 void syncToServer 调用（D-06）
    - syncToServer 调用格式：endpoint='/api/relay/runtime-sync/gateway/event'，body={gatewayId: frame.gatewayId, event: frame.event, scope: {...gatewayScope, transport: 'chat'}}
    - D-01/D-02 修正：在 RUNTIME_EVENT_WHITELIST syncToServer 调用处，检查 chatSessionOwners.has(frame.event.sessionId)；若为 true，scope 中补充 transport: 'chat'；否则保持原有 scope 不变。这确保白名单中的 chat events（agent.result/user.message/agent.tool/session.error）写入新表而非旧表
    - syncToServer 调用用 void（不 await），失败只 console.warn（继承自 syncToServer 内部实现）
  </behavior>
  <action>
**修改一：agent.delta handler（约 lines 386-397）**

找到：
```typescript
if (frame.event.type === 'agent.delta') {
  const clientId = chatSessionOwners.get(frame.event.sessionId)
    ?? (typeof frame.event.payload.clientId === 'string' ? frame.event.payload.clientId : undefined);
  if (clientId) {
    sendToClient(clientId, {
      type: 'agent.delta',
      sessionId: frame.event.sessionId,
      text: String(frame.event.payload.text ?? '')
    });
  }
  break;
}
```

修改为：
```typescript
if (frame.event.type === 'agent.delta') {
  const clientId = chatSessionOwners.get(frame.event.sessionId)
    ?? (typeof frame.event.payload.clientId === 'string' ? frame.event.payload.clientId : undefined);
  if (clientId) {
    sendToClient(clientId, {
      type: 'agent.delta',
      sessionId: frame.event.sessionId,
      text: String(frame.event.payload.text ?? ''),
      ...(typeof frame.event.id === 'number' ? { eventId: frame.event.id } : {})
    });
  }
  // Phase 16 (D-06/D-07/D-08): 同步 delta 到 Server，transport='chat' 触发新写路径
  void syncToServer('/api/relay/runtime-sync/gateway/event', {
    gatewayId: frame.gatewayId,
    event: frame.event,
    scope: {
      ...gatewayScope,
      transport: 'chat'
    }
  });
  break;
}
```

注意：gatewayScope 是在 case 'gateway.event': 分支内定义的局部变量，需确认其作用域可覆盖 delta handler（delta handler 是 case 'gateway.event' 内的 if 子句）。若 gatewayScope 在 delta handler 外层定义则直接使用；若不可用则用 gateway 的 scope 字段直接构建（`{ accountId: gateway?.scope?.accountId ?? '', gatewayId: frame.gatewayId, transport: 'chat' }`）。

读取文件后确认 gatewayScope 的实际作用域，再决定引用方式。

---

**修改二：RUNTIME_EVENT_WHITELIST syncToServer 调用（约 lines 498-504）**

找到 WHITELIST 路径中的 syncToServer 调用（形如）：
```typescript
if (RUNTIME_EVENT_WHITELIST.has(frame.event.type)) {
  void syncToServer('/api/relay/runtime-sync/gateway/event', {
    gatewayId: frame.gatewayId,
    event: frame.event,
    scope: gatewayScope
  });
}
```

修改 scope 构建，注入 chat transport（D-01/D-02：chat events 只写新表）：
```typescript
if (RUNTIME_EVENT_WHITELIST.has(frame.event.type)) {
  // Phase 16 (D-01/D-02): 若事件属于 chat session，注入 transport:'chat' 以触发新写路径
  const whitelistScope = chatSessionOwners.has(frame.event.sessionId)
    ? { ...gatewayScope, transport: 'chat' as const }
    : gatewayScope;
  void syncToServer('/api/relay/runtime-sync/gateway/event', {
    gatewayId: frame.gatewayId,
    event: frame.event,
    scope: whitelistScope
  });
}
```

注意：读取实际 WHITELIST 调用代码后，按真实结构调整。关键是：`chatSessionOwners.has(frame.event.sessionId)` 为 true 时，scope 中包含 `transport: 'chat'`。
  </action>
  <verify>
    <automated>grep -v '^//' apps/relay/src/relay.ts | grep -c "transport: 'chat'"</automated>
  </verify>
  <acceptance_criteria>
    - relay.ts 中 agent.delta handler 含 "transport: 'chat'"（syncToServer scope 中）
    - relay.ts 中 agent.delta 的 sendToClient 调用含 "eventId"
    - relay.ts 中 void syncToServer 调用在 break 之前，位于 agent.delta handler 内
    - relay.ts 中 WHITELIST syncToServer 调用处含 chatSessionOwners.has 检查（D-01/D-02 修正）
    - grep -v '^//' apps/relay/src/relay.ts | grep -c "transport: 'chat'" 返回 >= 2（delta handler 一处 + WHITELIST 一处）
    - pnpm --filter @tether/relay typecheck 通过
  </acceptance_criteria>
  <done>agent.delta handler 在 break 前调用 syncToServer，eventId 透传给客户端；WHITELIST 路径中 chat session 事件携带 transport:'chat'，满足 D-01/D-02</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: client.subscribe chat session catch-up 逻辑</name>
  <files>apps/relay/src/relay.ts</files>
  <read_first>
    - apps/relay/src/relay.ts lines 719-778 — 完整读取 client.subscribe handler，特别是 chat 分支（lines 764-767）
    - apps/relay/src/relay.ts lines 105-142 — fetchSessionMetadata GET 模式（catch-up 接口调用复用）
    - 确认 frame.after 字段在 client.subscribe 的类型中是否可用（Protocol 中已有 after?: number）
  </read_first>
  <behavior>
    - catch-up 只在 session.transport === 'chat' && session.status === 'running' 时触发
    - after 值取自 frame.after（可能为 undefined，此时用 0）
    - 调用 GET ${options.serverSyncUrl}/api/relay/chat-events/${encodeURIComponent(sessionId)}?after=${after}，header: x-tether-runtime-sync-secret
    - 响应格式: { data: { events: Array<{eventId: number, rawJson: string}> } }
    - 若 events 为空则不发 catch-up 帧（直接 break）
    - 若有 events，拼接 blob = events.map(e => { const p = JSON.parse(e.rawJson).payload; return typeof p?.text === 'string' ? p.text : '' }).join('')
    - lastEventId = events[events.length - 1].eventId
    - 发 sendToClient(clientId, { type: 'gateway.chat-catchup', sessionId: frame.sessionId, text: blob, lastEventId })
    - catch-up 调用用 await（需要等到数据才能发 catchup 帧），但整个 catch-up 逻辑包在 try/catch 中，失败时只 console.warn 不阻断 subscribe
    - D-15: catch-up 只补 delta，不补其他类型（由 Server 端的 WHERE event_type='agent.delta' 保证）
  </behavior>
  <action>
在 apps/relay/src/relay.ts 中找到 client.subscribe chat 分支（约 lines 764-767）：

```typescript
if (session.transport === 'chat') {
  chatSessionOwners.set(frame.sessionId, clientId);
  break;
}
```

修改为：
```typescript
if (session.transport === 'chat') {
  chatSessionOwners.set(frame.sessionId, clientId);
  // Phase 16 (D-13): catch-up 逻辑，仅在 session running 时触发
  if (session.status === 'running' && options.serverSyncUrl && options.runtimeSyncSecret) {
    const after = typeof frame.after === 'number' ? frame.after : 0;
    try {
      const catchupResponse = await fetch(
        `${options.serverSyncUrl}/api/relay/chat-events/${encodeURIComponent(frame.sessionId)}?after=${after}`,
        {
          method: 'GET',
          headers: { 'x-tether-runtime-sync-secret': options.runtimeSyncSecret },
          signal: AbortSignal.timeout(3000)
        }
      );
      if (catchupResponse.ok) {
        const catchupJson = await catchupResponse.json() as { data?: { events?: unknown } };
        const events = Array.isArray(catchupJson.data?.events) ? catchupJson.data.events as Array<{ eventId: number; rawJson: string }> : [];
        if (events.length > 0) {
          const blob = events.map((e) => {
            try {
              const parsed = JSON.parse(e.rawJson) as unknown;
              if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
                const payload = (parsed as Record<string, unknown>).payload;
                if (payload && typeof payload === 'object' && !Array.isArray(payload)) {
                  const text = (payload as Record<string, unknown>).text;
                  return typeof text === 'string' ? text : '';
                }
              }
            } catch {
              // ignore
            }
            return '';
          }).join('');
          const lastEventId = events[events.length - 1].eventId;
          sendToClient(clientId, {
            type: 'gateway.chat-catchup',
            sessionId: frame.sessionId,
            text: blob,
            lastEventId
          });
        }
      } else {
        console.warn(`[relay] chat catch-up failed: HTTP ${catchupResponse.status} for ${frame.sessionId}`);
      }
    } catch (error) {
      console.warn(`[relay] chat catch-up error for ${frame.sessionId}:`, String(error));
    }
  }
  break;
}
```
  </action>
  <verify>
    <automated>grep -c "chat-events" /Users/dream/code/tether/apps/relay/src/relay.ts</automated>
  </verify>
  <acceptance_criteria>
    - relay.ts 中 client.subscribe chat 分支含 "chat-events" URL
    - 含 "session.status === 'running'" 条件判断
    - 含 "lastEventId" 发给客户端
    - 含 "gateway.chat-catchup" sendToClient 调用，且 frame 带 lastEventId 字段
    - catch-up 逻辑包在 try/catch 中
    - pnpm --filter @tether/relay typecheck 通过
  </acceptance_criteria>
  <done>client.subscribe chat session 时若 running 则拉取并发送 catch-up blob + lastEventId</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relay → Server (syncToServer) | 仅在 Relay 进程内调用，凭 runtimeSyncSecret 鉴权 |
| Relay → Client (sendToClient) | 客户端已通过 token/ticket 认证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-A | Denial of Service | agent.delta syncToServer void | accept | void 调用不阻塞转发，syncToServer 有 3000ms timeout 和内部 catch，失败仅 warn |
| T-16-04-B | Information Disclosure | catch-up blob 发给客户端 | mitigate | Server 端 maskPayload 在写入时脱敏；Relay 直接转发 raw_json.payload.text（非 raw_json 本身），且客户端已通过 scope 验证（session 归属已在 subscribe 前检查） |
| T-16-04-C | Replay | catch-up blob 重复发送 | mitigate | Client 侧（P05）用 lastDeltaEventIdRef 去重（D-17）；即使 Relay 多次发 catch-up，Client 会忽略 eventId <= 当前游标的帧 |
</threat_model>

<verification>
pnpm --filter @tether/relay typecheck
</verification>

<success_criteria>
- relay.ts 中 agent.delta handler 在 break 前调用 syncToServer，携带 transport:'chat'
- relay.ts 中 WHITELIST syncToServer 路径对 chat session 事件注入 transport:'chat'（D-01/D-02）
- relay.ts 中 client.subscribe chat 分支实现 catch-up 逻辑
- Relay typecheck 通过
</success_criteria>

<output>
完成后创建 `.planning/phases/16-chat-runtime-raw-events/16-04-SUMMARY.md`
</output>
