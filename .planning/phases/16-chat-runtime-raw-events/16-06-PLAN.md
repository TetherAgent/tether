---
phase: "16-chat-runtime-raw-events"
plan: "06"
type: execute
wave: 3
depends_on: ["16-03", "16-04"]
files_modified:
  - apps/server/test/runtime-sync.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "upsertChatRuntimeEvent MySQL 未启用时静默返回"
    - "upsertChatRuntimeEvent 在 test 环境（mysqlModeEnabled=false）不抛错"
    - "/api/relay/chat-events/:sessionId 已加入 verifyLoginWhitelist"
    - "runtime-sync controller 的 transport='chat' 分支调用 upsertChatRuntimeEvent"
    - "现有测试全量通过"
  artifacts:
    - path: "apps/server/test/runtime-sync.test.ts"
      provides: "Phase 16 新增测试用例"
      contains: "upsertChatRuntimeEvent"
  key_links:
    - from: "apps/server/test/runtime-sync.test.ts"
      to: "apps/server/app/service/runtimeSyncRepository.ts"
      via: "ctx.service.runtimeSyncRepository.upsertChatRuntimeEvent"
      pattern: "upsertChatRuntimeEvent"
---

<objective>
在现有 runtime-sync.test.ts 中追加 Phase 16 测试用例：
1. upsertChatRuntimeEvent MySQL 未启用时静默返回（测试模式守卫）
2. /api/relay/chat-events/:sessionId 已加入 verifyLoginWhitelist
3. upsertChatRuntimeEvent 在 transport='chat' 时被 controller 调用（mock 数据库事务）
4. chatEventsRepository.listDeltaEventsAfter MySQL 未启用时返回空数组

Purpose: 保证新写路径的基本行为可验证，且 whitelist 配置不遗漏。

Output: apps/server/test/runtime-sync.test.ts
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-chat-runtime-raw-events/16-CONTEXT.md
@.planning/phases/16-chat-runtime-raw-events/16-PATTERNS.md
</context>

<interfaces>
<!-- 现有测试模式，从 runtime-sync.test.ts 提取 -->

```typescript
// 现有 mysqlModeEnabled mock 模式（用于 upsertGatewaySession 测试）
const db = ctx.service.db as unknown as {
  mysqlModeEnabled: () => boolean
  transaction: <T>(run: (connection: { query: (...) => Promise<unknown> }) => Promise<T>) => Promise<T>
}
db.mysqlModeEnabled = () => true
db.transaction = async run => {
  const connection = {
    query: async (sql: string, values?: any[]) => {
      queries.push({ sql, values })
      if (/CONDITION/.test(sql)) { return []; }
      return { affectedRows: 1 }
    }
  }
  return await run(connection)
}

// 现有 service 访问方式（test 环境 mysqlModeEnabled = false）
ctx.service.runtimeSyncRepository.upsertGatewaySession(...)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 追加 Phase 16 单测到 runtime-sync.test.ts</name>
  <files>apps/server/test/runtime-sync.test.ts</files>
  <read_first>
    - apps/server/test/runtime-sync.test.ts — 完整读取，了解现有测试结构、beforeEach ctx 设置、service 访问方式
    - apps/server/app/service/runtimeSyncRepository.ts — 确认 upsertChatRuntimeEvent 方法签名（已在 P03 添加）
    - apps/server/app/service/chatEventsRepository.ts — 确认 listDeltaEventsAfter 方法签名（已在 P03 新建）
    - apps/server/config/config.default.ts — 确认 verifyLoginWhitelist 中新增的路由
  </read_first>
  <behavior>
    - 测试 1：upsertChatRuntimeEvent MySQL 未启用时静默返回（test 环境默认 mysqlModeEnabled=false）
    - 测试 2：chatEventsRepository.listDeltaEventsAfter MySQL 未启用时返回空数组
    - 测试 3：/api/relay/chat-events/:sessionId 已加入 verifyLoginWhitelist
    - 测试 4：verifyLogin 白名单支持 chat-events 路由模板（类似现有 "verifyLogin 白名单支持 :param 路由模板匹配真实路径" 测试，验证 /api/relay/chat-events/some-session-id 能匹配白名单）
    - 测试 5（可选，若 mock 难度低）：upsertChatRuntimeEvent 在 transport='chat' 分支被正确调用（mock mysqlModeEnabled=true + mock transaction，验证 SQL 包含 gateway_runtime_chats_events）
    - 所有新测试追加在文件末尾，不修改现有测试
  </behavior>
  <action>
在 apps/server/test/runtime-sync.test.ts 文件末尾追加以下测试用例（在最后一个 `it(...)` 后，`})` 关闭 describe 前）：

```typescript
  it('runtimeSyncRepository.upsertChatRuntimeEvent — MySQL 未启用时静默返回', async () => {
    const scope = { accountId: 'acct_1', gatewayId: 'gw_1' }
    const event = { id: 1, type: 'agent.delta', sessionId: 'tth_chat_01', ts: Date.now(), payload: { text: 'hello' } }
    await assert.doesNotReject(async () => {
      await (
        ctx.service as unknown as {
          runtimeSyncRepository?: {
            upsertChatRuntimeEvent?: (
              sessionId: string,
              eventId: number,
              eventType: string,
              event: unknown,
              scope: unknown,
              createdAt?: unknown
            ) => Promise<void>
          }
        }
      ).runtimeSyncRepository?.upsertChatRuntimeEvent?.(
        'tth_chat_01',
        1,
        'agent.delta',
        event,
        scope,
        Date.now()
      )
    })
  })

  it('chatEventsRepository.listDeltaEventsAfter — MySQL 未启用时返回空数组', async () => {
    const result = await (
      ctx.service as unknown as {
        chatEventsRepository?: {
          listDeltaEventsAfter?: (sessionId: string, after: number) => Promise<unknown[]>
        }
      }
    ).chatEventsRepository?.listDeltaEventsAfter?.('tth_chat_01', 0)
    assert.deepEqual(result ?? [], [])
  })

  it('runtime sync chat-events 接口必须进入登录白名单', async () => {
    const whitelist = app.config.verifyLoginWhitelist
    assert(whitelist.includes('/api/relay/chat-events/:sessionId'))
  })

  it('verifyLogin 白名单支持 chat-events :sessionId 路由模板匹配真实路径', async () => {
    const middleware = verifyLogin()
    const requestCtx = app.mockContext({
      url: '/api/relay/chat-events/tth_chat_session_abc'
    }) as Context & { service: Context['service'] }
    let passed = false
    requestCtx.get = (name: string) => name.toLowerCase() === 'authorization' ? '' : ''
    requestCtx.service.auth.verifyToken = async () => {
      throw new Error('verifyToken should not be called for whitelist route')
    }

    await middleware(requestCtx, async () => {
      passed = true
    })

    assert.equal(passed, true)
  })

  it('upsertChatRuntimeEvent transport=chat — 写入 gateway_runtime_chats_events', async () => {
    const queries: Array<{ sql: string; values?: any[] }> = []
    const db = ctx.service.db as unknown as {
      mysqlModeEnabled: () => boolean
      transaction: <T>(run: (connection: { query: (sql: string, values?: any[]) => Promise<unknown> }) => Promise<T>) => Promise<T>
    }
    db.mysqlModeEnabled = () => true
    db.transaction = async run => {
      const connection = {
        query: async (sql: string, values?: any[]) => {
          queries.push({ sql, values })
          // gateway_sessions 查询返回模拟行
          if (/SELECT user_id FROM gateway_sessions/.test(sql)) {
            return [{ user_id: 'user_1' }]
          }
          // deleted 查询返回空
          if (/SELECT session_id FROM gateway_deleted_sessions/.test(sql)) {
            return []
          }
          // scope conflict 查询返回空（无冲突）
          if (/SELECT id FROM gateway_sessions.*gateway_id <> /.test(sql)) {
            return []
          }
          return { affectedRows: 1 }
        }
      }
      return await run(connection)
    }

    const event = { id: 5, type: 'agent.delta', sessionId: 'tth_delta_session', ts: Date.now(), payload: { text: 'world' } }
    await ctx.service.runtimeSyncRepository.upsertChatRuntimeEvent(
      'tth_delta_session',
      5,
      'agent.delta',
      event,
      { accountId: 'acct_1', gatewayId: 'gw_1' },
      Date.now()
    )

    const chatEventsInsert = queries.find(q => /INSERT INTO gateway_runtime_chats_events/.test(q.sql))
    assert(chatEventsInsert, 'should have INSERT INTO gateway_runtime_chats_events')
    // agent.delta 不更新 gateway_chat_messages.raw_json
    const rawJsonUpdate = queries.find(q => /UPDATE gateway_chat_messages SET raw_json/.test(q.sql))
    assert(!rawJsonUpdate, 'agent.delta should NOT update gateway_chat_messages.raw_json')
  })
```

注意：新测试用例追加在文件现有最后一个 `it(...)` 之后，`})` describe 关闭大括号之前。
确保 verifyLogin import 在文件顶部已存在（现有文件已 import）。
  </action>
  <verify>
    <automated>pnpm --filter @tether/server test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - runtime-sync.test.ts 含 "upsertChatRuntimeEvent" 测试用例（至少 2 个）
    - runtime-sync.test.ts 含 "chat-events" whitelist 测试用例
    - pnpm --filter @tether/server test 全量通过（含新增测试）
    - 无 "passing 0" 或 test failure
  </acceptance_criteria>
  <done>Phase 16 新增测试全部通过，现有测试无回归</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Service | 单测在 test 环境（mysqlModeEnabled=false）下验证行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-A | Denial of Service | 测试 mock db.transaction | accept | 仅用于测试环境，不影响生产路径 |
</threat_model>

<verification>
pnpm --filter @tether/server test
pnpm --filter @tether/server typecheck
</verification>

<success_criteria>
- runtime-sync.test.ts 追加 5 个新测试用例
- 全量 server test 通过
- whitelist 测试确认新路由已注册
</success_criteria>

<output>
完成后创建 `.planning/phases/16-chat-runtime-raw-events/16-06-SUMMARY.md`
</output>
