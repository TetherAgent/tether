---
phase: "16-chat-runtime-raw-events"
plan: "05"
type: execute
wave: 3
depends_on: ["16-03", "16-04"]
files_modified:
  - apps/web/src/components/chats/chat-data.ts
  - apps/web/src/components/chats/chat-panel.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "chat-panel.tsx 含 lastDeltaEventIdRef（初始值 0）"
    - "client.subscribe 发出时携带 after: lastDeltaEventIdRef.current"
    - "收到 agent.delta 且 eventId > 0 时，若 eventId <= lastDeltaEventIdRef.current 则丢弃（去重）；否则更新游标"
    - "收到 gateway.chat-catchup 时，若 lastEventId 大于当前游标则更新"
    - "fetchChatMessages 返回含 lastEventId 字段，初始化游标"
  artifacts:
    - path: "apps/web/src/components/chats/chat-panel.tsx"
      provides: "lastDeltaEventIdRef + 三个写入点 + subscribe after + dedup"
      contains: "lastDeltaEventIdRef"
    - path: "apps/web/src/components/chats/chat-data.ts"
      provides: "fetchChatMessages 返回类型含 lastEventId"
      contains: "lastEventId"
  key_links:
    - from: "chat-panel.tsx loadActiveSessionHistory"
      to: "chat-data.ts fetchChatMessages"
      via: "lastEventId 初始化 lastDeltaEventIdRef"
      pattern: "lastDeltaEventIdRef.current.*lastEventId"
    - from: "chat-panel.tsx handleRelayFrame"
      to: "lastDeltaEventIdRef"
      via: "agent.delta 去重 + catch-up 更新"
      pattern: "lastDeltaEventIdRef.current"
---

<objective>
Web 客户端 catch-up 支持：
1. chat-data.ts fetchChatMessages 返回类型新增 lastEventId 字段
2. chat-panel.tsx 新增 lastDeltaEventIdRef，三个写入点，subscribe 带 after，agent.delta 去重

Purpose: Client 端配合 Relay catch-up（P04）完成断线重连后的 delta 补全，避免重复渲染。

Output: chat-data.ts 和 chat-panel.tsx
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-chat-runtime-raw-events/16-CONTEXT.md
@.planning/phases/16-chat-runtime-raw-events/16-PATTERNS.md
</context>

<interfaces>
<!-- 关键现有代码片段 -->

From apps/web/src/components/chats/chat-data.ts:
```typescript
// 现有 fetchChatMessages 函数（待修改）
export async function fetchChatMessages(sessionId: string, token?: string): Promise<ChatHistoryMessage[]> {
  const http = createHttpClient();
  const data = await http.get<{ messages: ChatHistoryMessage[] }>(
    `/api/server/chat-sessions/${sessionId}/messages`,
    undefined,
    { token }
  );
  return data.messages ?? [];
}
```

From apps/web/src/components/chats/chat-panel.tsx:
```typescript
// 现有游标 refs 模式（lines 390-414）
const subscribedSessionIdRef = React.useRef<string | null>(null);
const currentAgentIdRef = React.useRef<string | null>(null);
// ... 其他 useRef

// 现有 subscribe 发帧（约 line 1156）
sendFrame({ type: 'client.subscribe', sessionId: currentSessionId, mode: 'control' });

// 现有 fetchChatMessages 调用（约 line 511）
const historyMessages = await fetchChatMessages(sessionId, token);
// 之后 setMessages(items)

// 现有 gateway.chat-catchup handler（约 lines 798-816）
if (frame.type === 'gateway.chat-catchup' && typeof frame.text === 'string') {
  // setMessages...
}

// 现有 agent.delta handler（约 lines 818-857）
if (frame.type === 'agent.delta' && typeof frame.text === 'string') {
  if (frame.sessionId !== currentSessionIdRef.current) { return; }
  // setMessages 累积 text...
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chat-data.ts 扩展 fetchChatMessages 返回结构</name>
  <files>apps/web/src/components/chats/chat-data.ts</files>
  <read_first>
    - apps/web/src/components/chats/chat-data.ts — 完整读取（共 64 行），了解现有 fetchChatMessages 返回类型和 http.get 泛型参数
  </read_first>
  <behavior>
    - 新增类型 ChatMessagesResponse：{ messages: ChatHistoryMessage[]; lastEventId?: number }
    - fetchChatMessages 返回改为 Promise<ChatMessagesResponse> 而非 Promise<ChatHistoryMessage[]>
    - 响应解析：messages = data.messages ?? []，lastEventId = typeof data.lastEventId === 'number' ? data.lastEventId : 0
    - 返回 { messages, lastEventId }
    - 原先返回 ChatHistoryMessage[] 的调用方（chat-panel.tsx loadActiveSessionHistory）需同步更新（见 Task 2）
  </behavior>
  <action>
在 apps/web/src/components/chats/chat-data.ts 中：

**1. 新增导出类型：**
```typescript
export type ChatMessagesResponse = {
  messages: ChatHistoryMessage[];
  lastEventId: number;
};
```

**2. 修改 fetchChatMessages 签名和返回类型：**
```typescript
export async function fetchChatMessages(sessionId: string, token?: string): Promise<ChatMessagesResponse> {
  const http = createHttpClient();
  const data = await http.get<{ messages: ChatHistoryMessage[]; lastEventId?: number }>(
    `/api/server/chat-sessions/${sessionId}/messages`,
    undefined,
    { token }
  );
  return {
    messages: data.messages ?? [],
    lastEventId: typeof data.lastEventId === 'number' ? data.lastEventId : 0
  };
}
```
  </action>
  <verify>
    <automated>grep -c "ChatMessagesResponse\|lastEventId" /Users/dream/code/tether/apps/web/src/components/chats/chat-data.ts</automated>
  </verify>
  <acceptance_criteria>
    - chat-data.ts 导出 ChatMessagesResponse 类型
    - fetchChatMessages 返回类型为 Promise<ChatMessagesResponse>
    - 返回值含 lastEventId（number，默认 0）
    - pnpm --filter @tether/web typecheck 通过（需同步 chat-panel.tsx 调用方）
  </acceptance_criteria>
  <done>fetchChatMessages 返回结构含 lastEventId；类型安全</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat-panel.tsx lastDeltaEventIdRef + 三写入点 + subscribe after + delta dedup</name>
  <files>apps/web/src/components/chats/chat-panel.tsx</files>
  <read_first>
    - apps/web/src/components/chats/chat-panel.tsx lines 385-415 — useRef 声明区（找到合适插入位置）
    - apps/web/src/components/chats/chat-panel.tsx lines 509-535 — loadActiveSessionHistory（fetchChatMessages 调用）
    - apps/web/src/components/chats/chat-panel.tsx lines 795-860 — gateway.chat-catchup 和 agent.delta handler
    - apps/web/src/components/chats/chat-panel.tsx lines 1148-1158 — subscribe 发帧
    - apps/web/src/components/chats/chat-panel.tsx lines 20-30 — import 区（确认 ChatMessagesResponse 是否需要 import）
  </read_first>
  <behavior>
    - lastDeltaEventIdRef 初始值 0，随 session 切换重置为 0
    - 写入点 1（实时 delta）：agent.delta handler 中，sessionId 检查通过后、setMessages 之前，若 frame.eventId 是 number 且 > 0，则：若 eventId <= lastDeltaEventIdRef.current 则 return（去重丢弃）；否则 lastDeltaEventIdRef.current = frame.eventId
    - 写入点 2（catch-up）：gateway.chat-catchup handler 中，setMessages 之后，若 frame.lastEventId 是 number 且 > lastDeltaEventIdRef.current 则更新
    - 写入点 3（历史加载）：loadActiveSessionHistory 中，fetchChatMessages 返回后，用 lastEventId 初始化 lastDeltaEventIdRef.current
    - session 切换时重置：在 loadActiveSessionHistory 开始时（或 setMessages 之前）将 lastDeltaEventIdRef.current = 0，然后在收到 lastEventId 后更新
    - subscribe 发帧：from `sendFrame({ type: 'client.subscribe', sessionId: currentSessionId, mode: 'control' })` to `sendFrame({ type: 'client.subscribe', sessionId: currentSessionId, mode: 'control', after: lastDeltaEventIdRef.current })`
    - fetchChatMessages 返回类型变更后，调用点从 `const historyMessages = await fetchChatMessages(...)` 改为 `const { messages: historyMessages, lastEventId } = await fetchChatMessages(...)`
    - import 中若有 ChatHistoryMessage[] 相关类型，需从 chat-data.ts 同时 import ChatMessagesResponse 或直接用解构
  </behavior>
  <action>
在 apps/web/src/components/chats/chat-panel.tsx 中进行以下修改：

**A. 在 useRef 声明区添加 lastDeltaEventIdRef**（在 subscribedSessionIdRef 之后）：
```typescript
const lastDeltaEventIdRef = React.useRef<number>(0);
```

**B. 修改 loadActiveSessionHistory（约 lines 509-535）**

找到：
```typescript
const historyMessages = await fetchChatMessages(sessionId, token);
const items = historyMessagesToItems(historyMessages, activeSessionProviderRef.current ?? 'agent');
```
改为：
```typescript
const { messages: historyMessages, lastEventId: historyLastEventId } = await fetchChatMessages(sessionId, token);
// Phase 16 (D-14 写入点 3): 初始化 catch-up 游标
lastDeltaEventIdRef.current = historyLastEventId;
const items = historyMessagesToItems(historyMessages, activeSessionProviderRef.current ?? 'agent');
```

**C. 修改 agent.delta handler（约 lines 818-857）**

在 `if (frame.sessionId !== currentSessionIdRef.current) { return; }` 之后、setConnectionError 之前插入去重逻辑：
```typescript
// Phase 16 (D-14 写入点 1 + D-17 去重)
if (typeof (frame as { eventId?: unknown }).eventId === 'number') {
  const eventId = (frame as { eventId: number }).eventId;
  if (eventId > 0) {
    if (eventId <= lastDeltaEventIdRef.current) {
      return; // 已收到或 catch-up 已覆盖，丢弃
    }
    lastDeltaEventIdRef.current = eventId;
  }
}
```

注意：`frame` 来自 RelayFrame 类型，agent.delta 帧现在含可选 eventId?: number（Protocol P02 已更新），但类型系统可能需要类型断言。

**D. 修改 gateway.chat-catchup handler（约 lines 798-816）**

在 `setMessages(...)` 之后（handler 末尾，return 之前）添加：
```typescript
// Phase 16 (D-14 写入点 2)
const catchupLastEventId = typeof (frame as { lastEventId?: unknown }).lastEventId === 'number'
  ? (frame as { lastEventId: number }).lastEventId
  : 0;
if (catchupLastEventId > lastDeltaEventIdRef.current) {
  lastDeltaEventIdRef.current = catchupLastEventId;
}
```

**E. 修改 subscribe 发帧（约 line 1156）**

找到：
```typescript
sendFrame({ type: 'client.subscribe', sessionId: currentSessionId, mode: 'control' });
```
改为：
```typescript
sendFrame({ type: 'client.subscribe', sessionId: currentSessionId, mode: 'control', after: lastDeltaEventIdRef.current });
```

**F. import 区同步**（约 line 23）

若 chat-data.ts 新导出了 ChatMessagesResponse 类型，且代码中需要引用，在 import 行添加该类型（若仅通过解构使用则不需要显式 import 类型）。
  </action>
  <verify>
    <automated>grep -c "lastDeltaEventIdRef" /Users/dream/code/tether/apps/web/src/components/chats/chat-panel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - chat-panel.tsx 含 "lastDeltaEventIdRef" useRef 声明
    - chat-panel.tsx loadActiveSessionHistory 中含 "historyLastEventId" 和 "lastDeltaEventIdRef.current = historyLastEventId"
    - chat-panel.tsx agent.delta handler 含 "lastDeltaEventIdRef.current" 去重逻辑
    - chat-panel.tsx gateway.chat-catchup handler 含 "lastDeltaEventIdRef.current" 更新
    - chat-panel.tsx subscribe 发帧含 "after: lastDeltaEventIdRef.current"
    - pnpm --filter @tether/web typecheck 通过
  </acceptance_criteria>
  <done>三个写入点就位，subscribe 带 after，delta/catchup 去重；Web typecheck 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Relay WS | client.subscribe after 字段由客户端填入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-A | Tampering | client.subscribe.after 字段 | accept | after 字段仅用于拉取 delta 补全，不影响写入权限；Relay catch-up 接口有 runtimeSyncSecret 保护，不受 Client 控制 |
| T-16-05-B | Information Disclosure | catch-up blob 展示 | accept | blob 来自 gateway_runtime_chats_events.raw_json.payload.text（已脱敏写入），仅返回 text 字段非完整 raw_json |
</threat_model>

<verification>
pnpm --filter @tether/web typecheck
</verification>

<success_criteria>
- chat-data.ts fetchChatMessages 返回含 lastEventId
- chat-panel.tsx 完整实现 lastDeltaEventIdRef 的三写入点和去重
- Web typecheck 通过
</success_criteria>

<output>
完成后创建 `.planning/phases/16-chat-runtime-raw-events/16-05-SUMMARY.md`
</output>
