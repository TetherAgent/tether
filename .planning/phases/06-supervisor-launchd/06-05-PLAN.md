---
phase: 6
plan: 05
title: Supervisor Documentation and End-to-End Verification
type: implementation
wave: 4
depends_on: [02, 03, 04]
files_modified:
  - docs/current/gateway-supervisor.md
  - docs/current/relay-mvp.md
  - docs/README.md
  - AI_CONTEXT.md
  - PROJECT.md
autonomous: false
requirements: [GW-01, GW-02]
---

<objective>
Document the new daily Gateway workflow, update long-term project context, and verify that foreground, background, CLI forwarding, and Relay-compatible persistent Gateway behavior work on the user's Mac.
</objective>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| User exposes unauthenticated session creation publicly | high | Docs must state API session creation is default-off, whitelist-only, and not a replacement for Phase 4 auth. |
| User installs launchd with a cwd-dependent command | medium | Docs must show status/install behavior and explain absolute entry expectation. |
| Phase 4/5 scope confusion persists | low | Update docs to state auth and retention remain later phases. |
</threat_model>

<tasks>
<task id="06-05-T01" type="execute">
  <title>Write current Gateway supervisor documentation</title>
  <read_first>
    <file>docs/current/relay-mvp.md</file>
    <file>docs/README.md</file>
    <file>.planning/phases/06-supervisor-launchd/06-CONTEXT.md</file>
  </read_first>
  <files>
    <file>docs/current/gateway-supervisor.md</file>
    <file>docs/README.md</file>
  </files>
  <action>
    Create `docs/current/gateway-supervisor.md` in Chinese.
    Include sections with these exact headings:
    `# Gateway Supervisor 使用说明`,
    `## 日常命令`,
    `## 配置文件`,
    `## 后台运行`,
    `## Session 创建安全边界`,
    `## 本地验证`,
    and `## 故障排查`.
    Document commands:
    `tether gateway`,
    `tether gateway config`,
    `tether gateway install`,
    `tether gateway start`,
    `tether gateway stop`,
    `tether gateway restart`,
    `tether gateway status`,
    `tether gateway uninstall`,
    `tether codex`,
    and `tether codex --inline`.
    State that `~/.tether/config.json` is JSON, `allowApiSessionCreate` defaults to false, and session creation remains provider-whitelist-only.
    Add `gateway-supervisor.md` to `docs/README.md` current docs table.
  </action>
  <acceptance_criteria>
    <criterion>`docs/current/gateway-supervisor.md` contains `# Gateway Supervisor 使用说明`.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `allowApiSessionCreate`.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `tether gateway start`.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `tether codex --inline`.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `不能接受任意 command/args/env`.</criterion>
    <criterion>`docs/README.md` contains `gateway-supervisor.md`.</criterion>
  </acceptance_criteria>
</task>

<task id="06-05-T02" type="execute">
  <title>Update project context for persistent Gateway reality</title>
  <read_first>
    <file>AI_CONTEXT.md</file>
    <file>PROJECT.md</file>
    <file>docs/current/gateway-supervisor.md</file>
    <file>.planning/PROJECT.md</file>
  </read_first>
  <files>
    <file>AI_CONTEXT.md</file>
    <file>PROJECT.md</file>
    <file>docs/current/relay-mvp.md</file>
  </files>
  <action>
    Update `AI_CONTEXT.md` current implementation limits so it no longer says PTY sessions are held by short-lived CLI processes after Phase 6 is implemented. Replace that with the new persistent Gateway behavior and note `--inline` as the debug fallback.
    Update `PROJECT.md` command list to include `pnpm tether gateway config`, `pnpm tether gateway install`, `pnpm tether gateway start`, `pnpm tether gateway stop`, `pnpm tether gateway restart`, `pnpm tether gateway status`, and `pnpm tether gateway uninstall`.
    Update `docs/current/relay-mvp.md` Gateway startup examples so Relay can be managed by persistent `tether gateway` config/start instead of only `pnpm tether run codex --no-attach` with relay env vars.
  </action>
  <acceptance_criteria>
    <criterion>`AI_CONTEXT.md` contains `tether codex --inline`.</criterion>
    <criterion>`AI_CONTEXT.md` contains `常驻 Gateway`.</criterion>
    <criterion>`PROJECT.md` contains `pnpm tether gateway status`.</criterion>
    <criterion>`PROJECT.md` contains `pnpm tether gateway start`.</criterion>
    <criterion>`docs/current/relay-mvp.md` contains `tether gateway config`.</criterion>
  </acceptance_criteria>
</task>

<task id="06-05-T03" type="manual">
  <title>Run local foreground and forwarding smoke test</title>
  <read_first>
    <file>docs/current/gateway-supervisor.md</file>
    <file>apps/cli/src/main.ts</file>
    <file>apps/gateway/src/daemon.ts</file>
  </read_first>
  <files>
    <file>docs/current/gateway-supervisor.md</file>
  </files>
  <action>
    On macOS, run:
    `pnpm tether gateway config --host 127.0.0.1 --port 4789 --allow-api-session-create`.
    In terminal 1, run `pnpm tether gateway`.
    In terminal 2, run `pnpm tether run codex --no-attach` or a provider available on the machine.
    Run `pnpm tether gateway status`.
    Verify the session appears in `pnpm tether ls` and Web/Relay session list if Relay is configured.
    Then run `pnpm tether codex --inline --no-attach` to verify inline fallback/debug path still works.
    Append the exact commands run and pass/fail result under `## Verification` in `docs/current/gateway-supervisor.md`.
  </action>
  <acceptance_criteria>
    <criterion>`docs/current/gateway-supervisor.md` contains `## Verification`.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `pnpm tether gateway status`.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `pnpm tether codex --inline` or `pnpm tether run codex --inline`.</criterion>
    <criterion>The manual smoke result is recorded as pass or with explicit residual risk.</criterion>
  </acceptance_criteria>
</task>

<task id="06-05-T04" type="manual">
  <title>Run launchd background lifecycle smoke test</title>
  <read_first>
    <file>apps/cli/src/launchd.ts</file>
    <file>docs/current/gateway-supervisor.md</file>
  </read_first>
  <files>
    <file>docs/current/gateway-supervisor.md</file>
  </files>
  <action>
    On macOS, run:
    `pnpm tether gateway install`,
    `pnpm tether gateway start`,
    `pnpm tether gateway status`,
    `pnpm tether gateway stop`,
    and `pnpm tether gateway uninstall`.
    Verify `~/Library/LaunchAgents/sh.tether.gateway.plist` exists after install/start and is removed after uninstall.
    Verify status output is Chinese and reports LaunchAgent installed/running state.
    Append results under `## Verification` in `docs/current/gateway-supervisor.md`.
  </action>
  <acceptance_criteria>
    <criterion>`docs/current/gateway-supervisor.md` contains `sh.tether.gateway.plist`.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `pnpm tether gateway install`.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `pnpm tether gateway uninstall`.</criterion>
    <criterion>The launchd smoke result is recorded as pass or with explicit residual risk.</criterion>
  </acceptance_criteria>
</task>

<task id="06-05-T05" type="execute">
  <title>Run final automated verification</title>
  <read_first>
    <file>package.json</file>
    <file>apps/gateway/package.json</file>
    <file>apps/cli/package.json</file>
    <file>docs/current/gateway-supervisor.md</file>
  </read_first>
  <files>
    <file>docs/current/gateway-supervisor.md</file>
  </files>
  <action>
    Run `pnpm typecheck` and `pnpm test`.
    Append the command results under `## Verification` in `docs/current/gateway-supervisor.md`.
    If any manual verification cannot run in the current environment, record the exact command that was not run, why it was not run, and the residual risk.
  </action>
  <acceptance_criteria>
    <criterion>`pnpm typecheck` exits 0.</criterion>
    <criterion>`pnpm test` exits 0.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `pnpm typecheck`.</criterion>
    <criterion>`docs/current/gateway-supervisor.md` contains `pnpm test`.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `pnpm typecheck`.
- Run `pnpm test`.
- Manually verify foreground Gateway, CLI forwarding, inline fallback, launchd start/stop/status/uninstall.
</verification>

<must_haves>
## Must Haves

### Truths
- GW-01 requires persistent Gateway-owned sessions to be demonstrated.
- GW-02 requires launchd install/start/stop/status/uninstall to be demonstrated.
- D-25 requires Chinese CLI output; docs must show expected Chinese status.
- D-29, D-30, D-31, and D-32 require docs to keep Phase 4 auth, Phase 5 retention, tmux cleanup, and npm registry publication out of this phase.
- Phase 4 auth and Phase 5 retention remain deferred.

### Checks
- `docs/current/gateway-supervisor.md` exists and documents the daily flow.
- Long-term docs no longer describe short-lived CLI Gateway as the normal path after Phase 6.
- Automated and manual verification results are recorded.
</must_haves>

<success_criteria>
- User has a current Chinese runbook for Gateway supervisor.
- Foreground, background, forwarding, and inline fallback paths are verified or residual risks are explicitly recorded.
- Full typecheck and test suite pass.
</success_criteria>
