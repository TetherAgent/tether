---
phase: 13-mobile-web-chat
plan: "03"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - apps/relay/src/relay.ts
  - apps/server/app/controller/chat.ts
  - apps/server/app/service/chatRepository.ts
  - apps/server/app/router.ts
autonomous: true
requirements:
  - D-21
  - D-42
  - D-43

must_haves:
  truths:
    - "Relay 把 client.chat 帧正确转发给 Gateway（无论 sessionId 为 null 还是 string）"
    - "Relay RUNTIME_EVENT_WHITELIST 包含 agent.result 和 agent.tool，触发 syncToServer"
    - "Relay 把 gateway.session-created / agent.delta / agent.result / agent.tool / gateway.chat-catchup 转发给目标 Web client"
    - "GET /api/server/chat-sessions 返回当前用户的 chat transport 会话列表"
    - "GET /api/server/chat-sessions/:sessionId/messages 返回该会话的消息历史（按 created_at ASC）"
  artifacts:
    - path: apps/relay/src/relay.ts
      provides: "扩展后的 RUNTIME_EVENT_WHITELIST（含 agent.result/agent.tool）、client.chat 无鉴权转发"
      contains: "agent.result"
    - path: apps/server/app/controller/chat.ts
      provides: "ChatController.sessions() 和 ChatController.messages()"
      exports: ["ChatController"]
    - path: apps/server/app/service/chatRepository.ts
      provides: "listChatSessions() 和 listMessages() 数据访问方法"
      exports: ["ChatRepositoryService"]
    - path: apps/server/app/router.ts
      provides: "GET /api/server/chat-sessions 和 GET /api/server/chat-sessions/:sessionId/messages 路由"
      contains: "chat-sessions"
  key_links:
    - from: apps/relay/src/relay.ts
      to: apps/server/app/controller/runtime-sync.ts
      via: "syncToServer('/api/relay/runtime-sync/gateway/event') on agent.result 命中白名单"
      pattern: "RUNTIME_EVENT_WHITELIST.*agent\\.result"
    - from: apps/server/app/controller/chat.ts
      to: apps/server/app/service/chatRepository.ts
      via: "ctx.service.chatRepository.listChatSessions() / listMessages()"
      pattern: "chatRepository"
---

<objective>
实现 Relay 侧 chat 帧转发逻辑（白名单扩展 + client.chat 无校验转发），以及 Server 侧 chat-sessions HTTP API（两个端点）。

Purpose: Relay 是 chat 链路的消息总线，server 的 HTTP API 是历史加载的唯一来源（D-43）。
Output: relay.ts（WHITELIST 扩展 + handleClientFrame chat 分支）、chat.ts controller、chatRepository.ts service、router.ts（新增 2 条路由）。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-mobile-web-chat/13-CONTEXT.md
@.planning/phases/13-mobile-web-chat/13-RESEARCH.md
@.planning/phases/13-mobile-web-chat/13-PATTERNS.md

<interfaces>
<!-- 从 apps/relay/src/relay.ts 提取的关键结构 -->
当前 RUNTIME_EVENT_WHITELIST（需扩展）：
```typescript
const RUNTIME_EVENT_WHITELIST = new Set([
  'terminal.output', 'terminal.input', 'session.error', 'session.exited', 'agent.status'
]);
```

syncToServer 签名（relay.ts 内部函数）：
```typescript
async function syncToServer(endpoint: string, body: unknown): Promise<void>
// 失败只 console.warn，不阻塞转发
```

handleClientFrame 中 client.chat 的问题（见 RESEARCH.md Pitfall 1）：
- 当前 client.chat 要求 session 已在 latestSessions 中且已 subscribed
- chat 链路新行为：client.chat 直接转发给 Gateway，不检查 session 存在性

<!-- 阅读 relay.ts 中的 handleClientFrame 函数，理解：
     - clientCanSeeSession() 的调用位置
     - sendToGateway() 的签名和用法
     - sendToSubscribers() 的签名和用法
     - handleGatewayFrame 中 gateway.event 的处理（syncToServer 调用点）
-->

<!-- 从 apps/server/app/controller/session.ts 提取的 controller 模式 -->
SessionController 模式：
```typescript
import { Controller } from 'egg';
function authScope(ctx) { return { accountId, workspaceId, userId } }
export default class SessionController extends Controller {
  public async list(): Promise<void> {
    const { accountId, workspaceId, userId } = authScope(this.ctx);
    const sessions = await this.ctx.service.sessionRepository.listSessions(...);
    this.ctx.success({ sessions });
  }
}
```

<!-- 从 apps/server/app/service/sessionRepository.ts 提取的查询模式 -->
MySQL 查询模式：
```typescript
const rows = await this.ctx.service.db.query(
  `SELECT * FROM table WHERE account_id = ? AND workspace_id = ? ORDER BY last_active_at DESC LIMIT 50`,
  [accountId, workspaceId]
);
return (rows as Record<string, unknown>[]).map(this.xxxFromRow.bind(this));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 relay.ts — 白名单扩展 + client.chat 无校验转发 + chat 帧路由</name>
  <read_first>
    - apps/relay/src/relay.ts（完整文件前 150 行：WHITELIST、syncToServer、handleClientFrame、handleGatewayFrame）
    - apps/relay/src/relay.ts（handleClientFrame 函数中 client.chat 处理逻辑，找到 Pitfall 1 描述的校验代码）
    - packages/protocol/src/index.ts（Plan 01 产出，确认新帧类型定义）
    - .planning/phases/13-mobile-web-chat/13-RESEARCH.md（Pitfall 1、2 详细说明、Open Questions Q-1、Q-2 解法建议）
  </read_first>
  <files>apps/relay/src/relay.ts</files>
  <action>
在 relay.ts 中做以下精确修改（4 处独立改动，互不干扰）：

**改动 1：扩展 RUNTIME_EVENT_WHITELIST**（per D-21，Pitfall 2）：
```typescript
// 在现有 RUNTIME_EVENT_WHITELIST 定义处，追加 2 个 event 类型：
const RUNTIME_EVENT_WHITELIST = new Set([
  'terminal.output',
  'terminal.input',
  'session.error',
  'session.exited',
  'agent.status',
  'agent.result',   // 新增：chat 完整回复，触发写 gateway_chat_messages
  'agent.tool'      // 新增：chat 工具调用，触发写 gateway_runtime_events
]);
```

**改动 2：修改 handleClientFrame 中的 client.chat 分支**（per Pitfall 1 解法）：
找到现有 `case 'client.chat':` 代码块（当前检查 session 存在性和 subscribe 状态），替换为直接转发逻辑：
```typescript
case 'client.chat': {
  // chat 帧直接转发给 Gateway，不检查 session 存在性（D-32 隐式创建）
  // 找到 client 绑定的 gateway
  const clientState = clients.get(clientId);
  if (!clientState?.gatewayId) {
    sendToSocket(socket, { type: 'error', code: 'no_gateway', message: 'No gateway bound to this client' });
    return;
  }
  const gatewayState = gateways.get(clientState.gatewayId);
  if (!gatewayState) {
    sendToSocket(socket, { type: 'error', code: 'gateway_offline', message: 'Gateway is offline' });
    return;
  }
  // 透传 client.chat 帧给 Gateway，附加 clientId
  sendToSocket(gatewayState.socket, {
    type: 'client.chat',
    clientId,
    sessionId: frame.sessionId,
    ...(frame.sessionId === null ? { provider: (frame as { provider: string }).provider, model: (frame as { model: string }).model, cwd: (frame as { cwd: string }).cwd } : {}),
    message: frame.message
  } as RelayServerToGatewayFrame);
  return;
}
```

注意：由于 Protocol Plan 01 中 client.chat 是 union 类型（sessionId: null | string），TypeScript 需要 type narrowing。仔细看 frame 的类型定义确认正确的写法。

**改动 3：在 handleClientFrame 新增 client.list-providers 和 client.switch-model 分支**：
```typescript
case 'client.list-providers': {
  const clientState = clients.get(clientId);
  if (!clientState?.gatewayId) {
    sendToSocket(socket, { type: 'error', code: 'no_gateway', message: 'No gateway bound' });
    return;
  }
  const gatewayState = gateways.get(clientState.gatewayId);
  if (!gatewayState) return;
  sendToSocket(gatewayState.socket, { type: 'client.list-providers', clientId } as RelayServerToGatewayFrame);
  return;
}

case 'client.switch-model': {
  const clientState = clients.get(clientId);
  if (!clientState?.gatewayId) return;
  const gatewayState = gateways.get(clientState.gatewayId);
  if (!gatewayState) return;
  sendToSocket(gatewayState.socket, {
    type: 'client.switch-model', clientId, sessionId: frame.sessionId, provider: frame.provider, model: frame.model
  } as RelayServerToGatewayFrame);
  return;
}
```

**改动 4：在 handleGatewayFrame 中处理 Gateway chat 回包**（gateway.session-created / gateway.chat-catchup），路由给 clientId 指定的客户端：

找到 handleGatewayFrame switch，在现有 case 之后追加：
```typescript
case 'gateway.session-created': {
  // 直接路由给指定 client
  const targetClient = clients.get(frame.clientId);
  if (targetClient) {
    sendToSocket(targetClient.socket, {
      type: 'gateway.session-created',
      sessionId: frame.sessionId
    } as RelayServerToClientFrame);
  }
  return;
}

case 'gateway.chat-catchup': {
  const targetClient = clients.get(frame.clientId);
  if (targetClient) {
    sendToSocket(targetClient.socket, {
      type: 'gateway.chat-catchup',
      sessionId: frame.sessionId,
      text: frame.text
    } as RelayServerToClientFrame);
  }
  return;
}
```

**改动 5：在 `gateway.event` handler 中新增 chat 事件的 clientId 直接路由（必须明确实现）**：

chat session 不经过 subscribe 机制，`sendEventToSubscribers` 无法推送 chat 事件给 chat 客户端。**必须**在 `handleGatewayFrame` 的 `gateway.event` case 中，在调用 `sendEventToSubscribers` 之前插入以下逻辑：

```typescript
const CHAT_EVENT_TYPES = new Set(['agent.delta', 'agent.result', 'agent.tool', 'gateway.chat-catchup']);

if (CHAT_EVENT_TYPES.has(frame.event.type)) {
  const targetClientId = (frame.event.payload as { clientId?: string }).clientId;
  if (targetClientId) {
    const targetClient = clients.get(targetClientId);
    if (targetClient) {
      sendToSocket(targetClient.socket, {
        type: frame.event.type,
        sessionId: frame.event.sessionId,
        ...(frame.event.payload as Record<string, unknown>),
      } as RelayServerToClientFrame);
    }
    return; // chat 事件直接 return，不走 sendEventToSubscribers
  }
}
// 非 chat 事件继续走现有的 sendEventToSubscribers 逻辑
```

`frame.event.payload.clientId` 由 Gateway 在 ChatSessionRunner 的 onDelta/onResult/onTool 回调中注入（见 Plan 02）。
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether && pnpm --filter @tether/relay typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>
    - relay.ts RUNTIME_EVENT_WHITELIST 包含 agent.result 和 agent.tool
    - client.chat case 直接转发给 Gateway，不做 session 存在性校验
    - gateway.session-created 和 gateway.chat-catchup 路由给指定 clientId 的客户端
    - gateway.event handler 中，agent.delta/agent.result/agent.tool/gateway.chat-catchup 通过 payload.clientId 直接路由给 chat client（不走 sendEventToSubscribers）
    - pnpm --filter @tether/relay typecheck 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: Server — chat controller + repository + router（两个 HTTP 端点）</name>
  <read_first>
    - apps/server/app/controller/session.ts（完整 controller 模式参考）
    - apps/server/app/service/sessionRepository.ts（查询模式：db.query、row 转换方法）
    - apps/server/app/router.ts（现有路由结构，requireNormalAccess 变量）
    - apps/server/sql/002_gateway_runtime_sync.sql（gateway_sessions 表字段确认：transport、agent_session_id 等列名）
    - apps/server/sql/004_chat_messages.sql（Plan 01 产出，gateway_chat_messages 字段确认）
    - .planning/phases/13-mobile-web-chat/13-CONTEXT.md（D-42 API 规范、D-43 HTTP only）
    - .planning/phases/13-mobile-web-chat/13-PATTERNS.md（Server controller/service/router 模式）
  </read_first>
  <files>
    apps/server/app/controller/chat.ts,
    apps/server/app/service/chatRepository.ts,
    apps/server/app/router.ts
  </files>
  <action>
按 apps/server/CLAUDE.md 分层规范新建 controller + service，在 router.ts 新增 2 条路由。

**新建 apps/server/app/controller/chat.ts**：
```typescript
import { Controller } from 'egg';

type AuthScope = {
  accountId?: string;
  workspaceId?: string;
  userId?: string;
};

function authScope(ctx: Controller['ctx']): { accountId: string; workspaceId: string; userId: string } {
  const auth = ctx.state.auth as AuthScope | undefined;
  return {
    accountId: auth?.accountId ?? '',
    workspaceId: auth?.workspaceId ?? '',
    userId: auth?.userId ?? ''
  };
}

export default class ChatController extends Controller {
  public async sessions(): Promise<void> {
    const { ctx } = this;
    const { accountId, workspaceId } = authScope(ctx);
    const sessions = await ctx.service.chatRepository.listChatSessions(accountId, workspaceId);
    ctx.success({ sessions });
  }

  public async messages(): Promise<void> {
    const { ctx } = this;
    const { accountId, workspaceId } = authScope(ctx);
    const { sessionId } = ctx.params as { sessionId: string };
    if (!sessionId) {
      ctx.throw(400, 'sessionId is required');
      return;
    }
    const messages = await ctx.service.chatRepository.listMessages(sessionId, accountId, workspaceId);
    ctx.success({ messages });
  }
}
```

**新建 apps/server/app/service/chatRepository.ts**：
```typescript
import { Service } from 'egg';

export type ChatSessionRecord = {
  id: string;
  provider: string;
  model?: string;
  projectPath: string;
  agentSessionId?: string;
  status: string;
  transport: string;
  lastActiveAt: number;
  createdAt: number;
};

export type ChatMessageRecord = {
  id: number;
  sessionId: string;
  role: 'user' | 'assistant';
  content: string;
  usageJson?: { input_tokens: number; output_tokens: number; cost_usd?: number };
  createdAt: string;
};

export default class ChatRepositoryService extends Service {
  public async listChatSessions(accountId: string, workspaceId: string): Promise<ChatSessionRecord[]> {
    const rows = await this.ctx.service.db.query(
      `SELECT id, provider, project_path, agent_session_id, status, transport, last_active_at, created_at
       FROM gateway_sessions
       WHERE account_id = ? AND workspace_id = ? AND transport = 'chat'
       ORDER BY last_active_at DESC
       LIMIT 50`,
      [accountId, workspaceId]
    );
    return (rows as Record<string, unknown>[]).map(this.sessionFromRow.bind(this));
  }

  public async listMessages(sessionId: string, accountId: string, workspaceId: string): Promise<ChatMessageRecord[]> {
    // 先校验 sessionId 属于当前 account（防止跨账户访问）
    const sessionRows = await this.ctx.service.db.query(
      `SELECT id FROM gateway_sessions WHERE id = ? AND account_id = ? AND workspace_id = ?`,
      [sessionId, accountId, workspaceId]
    );
    if (!Array.isArray(sessionRows) || sessionRows.length === 0) {
      this.ctx.throw(403, 'Session not found or access denied');
      return [];
    }

    const rows = await this.ctx.service.db.query(
      `SELECT id, session_id, role, content, usage_json, created_at
       FROM gateway_chat_messages
       WHERE session_id = ?
       ORDER BY created_at ASC`,
      [sessionId]
    );
    return (rows as Record<string, unknown>[]).map(this.messageFromRow.bind(this));
  }

  private sessionFromRow(row: Record<string, unknown>): ChatSessionRecord {
    return {
      id: String(row['id'] ?? ''),
      provider: String(row['provider'] ?? ''),
      projectPath: String(row['project_path'] ?? ''),
      agentSessionId: row['agent_session_id'] != null ? String(row['agent_session_id']) : undefined,
      status: String(row['status'] ?? ''),
      transport: String(row['transport'] ?? ''),
      lastActiveAt: Number(row['last_active_at'] ?? 0),
      createdAt: Number(row['created_at'] ?? 0)
    };
  }

  private messageFromRow(row: Record<string, unknown>): ChatMessageRecord {
    let usageJson: ChatMessageRecord['usageJson'];
    if (typeof row['usage_json'] === 'string' && row['usage_json']) {
      try { usageJson = JSON.parse(row['usage_json']) as ChatMessageRecord['usageJson']; } catch { /* ignore */ }
    }
    return {
      id: Number(row['id'] ?? 0),
      sessionId: String(row['session_id'] ?? ''),
      role: (row['role'] === 'user' ? 'user' : 'assistant'),
      content: String(row['content'] ?? ''),
      usageJson,
      createdAt: String(row['created_at'] ?? '')
    };
  }
}
```

**修改 apps/server/app/router.ts**：
在 `router.get('/api/server/sessions/:id/events', ...)` 之后追加 2 行：
```typescript
// Chat Sessions API（D-42）
router.get('/api/server/chat-sessions', requireNormalAccess, controller.chat.sessions);
router.get('/api/server/chat-sessions/:sessionId/messages', requireNormalAccess, controller.chat.messages);
```

注意：Egg 框架通过目录约定自动加载 controller，controller 文件名 `chat.ts` 对应 `controller.chat`。确认 chatRepository.ts 在 service 目录下能被自动加载（`ctx.service.chatRepository`）。
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether && pnpm --filter @tether/server typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>
    - apps/server/app/controller/chat.ts 存在，导出 ChatController（sessions 和 messages 方法）
    - apps/server/app/service/chatRepository.ts 存在，导出 ChatRepositoryService（listChatSessions 和 listMessages）
    - apps/server/app/router.ts 包含 /api/server/chat-sessions 和 /api/server/chat-sessions/:sessionId/messages
    - pnpm --filter @tether/server typecheck 通过，无新增错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web client → Relay client.chat | 用户输入到消息路由的入口 |
| Relay → Server syncToServer | gateway.event 白名单过滤后同步写库 |
| HTTP GET /api/server/chat-sessions/* | 已认证的普通用户访问自己的会话数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Tampering | relay.ts client.chat 转发 | mitigate | Relay 已有 hasForbiddenKey 检查（relay.ts）在所有 clientFrame 上执行，client.chat message 字段经过此过滤；cwd 字段不执行路径，只作为 spawn 的工作目录参数 |
| T-13-03-02 | Information Disclosure | GET /api/server/chat-sessions/:id/messages | mitigate | listMessages() 中先 JOIN gateway_sessions WHERE account_id=? 验证会话所有权，返回 403 if not found |
| T-13-03-03 | Tampering | syncToServer agent.result/agent.tool | accept | syncToServer 是 void 调用（不阻塞），失败只 warn；数据来自 Gateway 内部 subprocess 解析结果，非用户直接输入 |
| T-13-03-04 | Elevation | 跨账户会话访问 chat-sessions | mitigate | chatRepository.listMessages() 在 SQL 层用 account_id + workspace_id 联合过滤，不依赖应用层 sessionId 可信性 |
</threat_model>

<verification>
```bash
# 1. Relay 编译
cd /Users/dream/code/tether && pnpm --filter @tether/relay typecheck

# 2. Server 编译
pnpm --filter @tether/server typecheck

# 3. 关键内容验证
grep -c "agent.result" apps/relay/src/relay.ts
grep -c "agent.tool" apps/relay/src/relay.ts
grep -c "gateway.session-created" apps/relay/src/relay.ts
grep -c "chat-sessions" apps/server/app/router.ts
grep -c "listChatSessions" apps/server/app/service/chatRepository.ts
```
</verification>

<success_criteria>
1. `pnpm --filter @tether/relay typecheck` 通过
2. `pnpm --filter @tether/server typecheck` 通过
3. relay.ts RUNTIME_EVENT_WHITELIST 包含 agent.result 和 agent.tool
4. relay.ts handleClientFrame client.chat 分支不再调用 clientCanAccessSession 或检查 subscribe 状态
5. router.ts 包含 `/api/server/chat-sessions` 和 `/api/server/chat-sessions/:sessionId/messages` 两条路由
6. chatRepository.ts listMessages() SQL 包含 JOIN gateway_sessions AND account_id = ? 访问控制
7. relay.ts gateway.event handler 包含 CHAT_EVENT_TYPES 检查，chat 事件通过 payload.clientId 直接路由，不走 sendEventToSubscribers
</success_criteria>

<output>
完成后创建 `.planning/phases/13-mobile-web-chat/13-03-SUMMARY.md`
</output>
