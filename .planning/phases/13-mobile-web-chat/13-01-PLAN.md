---
phase: 13-mobile-web-chat
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/protocol/src/index.ts
  - apps/gateway/src/store.ts
  - apps/server/sql/004_chat_messages.sql
autonomous: true
requirements:
  - D-01
  - D-40
  - D-41

must_haves:
  truths:
    - "protocol 包含所有新 chat 帧类型，TypeScript 编译通过"
    - "Gateway SQLite 在启动时自动创建 session_chats_events 表和索引"
    - "Server DB migration 004 定义 gateway_chat_messages 表"
  artifacts:
    - path: packages/protocol/src/index.ts
      provides: "chat 链路所有帧类型（client.chat、agent.delta、agent.result、agent.tool、gateway.session-created、gateway.chat-catchup、client.list-providers、client.switch-model、gateway.providers）"
      contains: "agent.delta"
    - path: apps/gateway/src/store.ts
      provides: "session_chats_events 表 DDL、appendChatEvent()、listChatEvents()、updateSessionAgentId() 方法"
      contains: "session_chats_events"
    - path: apps/server/sql/004_chat_messages.sql
      provides: "gateway_chat_messages MySQL 表定义"
      contains: "gateway_chat_messages"
  key_links:
    - from: RelayClientToServerFrame
      to: client.chat
      via: "sessionId: null | string discriminated union"
      pattern: "sessionId.*null"
    - from: RelayServerToClientFrame
      to: agent.delta
      via: "discriminated union type"
      pattern: "agent\\.delta"
---

<objective>
建立 Phase 13 所有新 chat 帧的协议契约，扩展 Gateway SQLite 存储，新建 Server DB 迁移文件。

Purpose: 后续所有计划（Gateway ChatSessionRunner、Relay 路由、Web UI）都依赖这里定义的类型和表结构。先建立合同，再实现。
Output: 扩展后的 protocol/index.ts（新增 9 种帧变体）、store.ts（新增表+方法）、004_chat_messages.sql（MySQL 建表）。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-mobile-web-chat/13-CONTEXT.md
@.planning/phases/13-mobile-web-chat/13-RESEARCH.md
@.planning/phases/13-mobile-web-chat/13-PATTERNS.md

<interfaces>
<!-- 从 packages/protocol/src/index.ts 提取的当前帧类型 -->
<!-- 执行前必须阅读 protocol/index.ts 了解现有结构 -->

当前 RelayClientToServerFrame（需扩展）：
```typescript
export type RelayClientToServerFrame =
  | { type: 'client.auth'; token?: string; ticket?: string; scope?: RelayAuthScope; secret?: string }
  | { type: 'client.list' }
  | { type: 'client.subscribe'; sessionId: string; ... }
  | { type: 'client.input'; sessionId: string; data: string }
  | { type: 'client.resize'; sessionId: string; cols: number; rows: number }
  | { type: 'client.stop'; sessionId: string }
  | { type: 'client.detach'; sessionId: string }
  | { type: 'client.chat'; sessionId: string; message: string };  // 旧结构，需替换
```

当前 RelayServerToClientFrame（需扩展）：
```typescript
export type RelayServerToClientFrame =
  | { type: 'client.auth.ok'; clientId: string }
  | { type: 'client.auth.failed'; code: string; message: string }
  | { type: 'sessions'; sessions: RelaySession[] }
  | { type: 'hello'; clientId: string; gatewayId?: string }
  | { type: 'event'; event: RelayTerminalEvent }
  | { type: 'replay.output'; sessionId: string; data: string; latestEventId: number }
  | { type: 'replay.done'; sessionId: string; latestEventId: number }
  | { type: 'error'; sessionId?: string; code: string; message: string };
```

当前 Gateway store.ts 中 sessions 表列（有 agent_session_id 列需确认）：
- 在 store.ts 中找 migrate() 方法确认 agent_session_id 是否已存在
- appendEvent()：INSERT INTO session_events，是 chat 表新方法的模板
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 protocol/index.ts 新增所有 chat 帧类型</name>
  <read_first>
    - packages/protocol/src/index.ts（完整文件，理解现有 discriminated union 结构）
    - .planning/phases/13-mobile-web-chat/13-CONTEXT.md（D-01、D-02b 帧格式规范）
    - .planning/phases/13-mobile-web-chat/13-PATTERNS.md（Pattern: 新帧类型定义方式）
  </read_first>
  <files>packages/protocol/src/index.ts</files>
  <action>
按 discriminated union 模式扩展 4 个帧类型，保留所有现有帧不变：

**1. RelayClientToServerFrame — 替换旧 client.chat，新增 3 个帧**：

将现有 `{ type: 'client.chat'; sessionId: string; message: string }` 替换为两种变体（per D-01）：
```typescript
| { type: 'client.chat'; sessionId: null; provider: string; model: string; cwd: string; message: string }
| { type: 'client.chat'; sessionId: string; message: string }
| { type: 'client.list-providers' }
| { type: 'client.switch-model'; sessionId: string; provider: string; model: string }
```

**2. RelayServerToGatewayFrame — 新增 3 个帧**（Relay 转发给 Gateway 的帧）：
```typescript
| { type: 'client.chat'; clientId: string; sessionId: string | null; provider?: string; model?: string; cwd?: string; message: string }
| { type: 'client.list-providers'; clientId: string }
| { type: 'client.switch-model'; clientId: string; sessionId: string; provider: string; model: string }
```
注意：RelayServerToGatewayFrame 中已有 client.chat 变体（旧版），需要替换。

**3. RelayGatewayToServerFrame — 新增 2 个帧**（Gateway 发给 Relay）：
```typescript
| { type: 'gateway.session-created'; gatewayId: string; clientId: string; sessionId: string }
| { type: 'gateway.chat-catchup'; gatewayId: string; clientId: string; sessionId: string; text: string }
```

**4. RelayServerToClientFrame — 新增 5 个帧**（Relay 发给 Web 客户端）：
```typescript
| { type: 'gateway.session-created'; sessionId: string }
| { type: 'agent.delta'; sessionId: string; text: string }
| { type: 'agent.result'; sessionId: string; text: string; usage: { input_tokens: number; output_tokens: number; cost_usd?: number }; stop_reason?: string }
| { type: 'agent.tool'; sessionId: string; name: string; input: Record<string, unknown>; result?: string; isError?: boolean }
| { type: 'gateway.chat-catchup'; sessionId: string; text: string }
| { type: 'gateway.providers'; providers: Array<{ provider: string; models: string[] }> }
```

同时更新 RelaySession transport 字段为：`transport: 'pty-event-stream' | 'tmux' | 'chat'`（D-40 要求 chat transport）。

确保文件没有任何其他改动（不改注释、不改格式、不改现有帧）。
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether && pnpm --filter @tether/protocol typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - RelayClientToServerFrame 包含 client.chat（两种变体）、client.list-providers、client.switch-model
    - RelayServerToClientFrame 包含 gateway.session-created、agent.delta、agent.result、agent.tool、gateway.chat-catchup、gateway.providers
    - RelayGatewayToServerFrame 包含 gateway.session-created、gateway.chat-catchup
    - RelayServerToGatewayFrame 包含更新后的 client.chat、client.list-providers、client.switch-model
    - pnpm typecheck 通过，无新增 TypeScript 错误
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩展 Gateway store.ts — session_chats_events 表 + chat 存储方法</name>
  <read_first>
    - apps/gateway/src/store.ts（完整文件：constructor DDL、migrate()、appendEvent()、listEvents() 模式）
    - .planning/phases/13-mobile-web-chat/13-CONTEXT.md（D-40 DDL 规范、D-33 只存完整事件不存 delta）
    - .planning/phases/13-mobile-web-chat/13-PATTERNS.md（store.ts 模式说明）
  </read_first>
  <files>apps/gateway/src/store.ts</files>
  <action>
在 store.ts 中做以下外科手术式修改（不改任何现有方法）：

**1. 新增类型定义**（在现有 SessionEventType 定义之后）：
```typescript
export type ChatEventType = 'user.message' | 'agent.result' | 'agent.tool' | 'session.error';

export type ChatEvent<TPayload extends Record<string, unknown> = Record<string, unknown>> = {
  id: number;
  sessionId: string;
  type: ChatEventType;
  ts: number;
  payload: TPayload;
};

type ChatEventRow = {
  id: number;
  session_id: string;
  type: ChatEventType;
  ts: number;
  payload_json: string;
};
```

**2. 在 constructor 的 `this.db.exec(...)` 块内**，在 `CREATE INDEX IF NOT EXISTS idx_session_events_cursor` 之后追加（per D-40）：
```sql
CREATE TABLE IF NOT EXISTS session_chats_events (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  session_id   TEXT NOT NULL,
  type         TEXT NOT NULL,
  ts           INTEGER NOT NULL,
  payload_json TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_session_chats_events_cursor
  ON session_chats_events(session_id, id);
```

**3. 新增 SessionTransport 类型扩展**（在现有 SessionTransport 定义处）：
```typescript
export type SessionTransport = 'tmux' | 'pty-event-stream' | 'chat';
```

**4. 在 Store class 中新增 3 个方法**（在 listEvents 方法之后追加）：
```typescript
appendChatEvent(sessionId: string, type: ChatEventType, payload: Record<string, unknown>, ts = Date.now()): void {
  this.db
    .prepare('INSERT INTO session_chats_events (session_id, type, ts, payload_json) VALUES (?, ?, ?, ?)')
    .run(sessionId, type, ts, JSON.stringify(payload));
}

listChatEvents(sessionId: string): ChatEvent[] {
  const rows = this.db
    .prepare('SELECT * FROM session_chats_events WHERE session_id = ? ORDER BY id ASC')
    .all(sessionId) as ChatEventRow[];
  return rows.map((row): ChatEvent => ({
    id: row.id,
    sessionId: row.session_id,
    type: row.type,
    ts: row.ts,
    payload: JSON.parse(row.payload_json) as Record<string, unknown>
  }));
}

updateSessionAgentId(sessionId: string, agentSessionId: string): void {
  this.db
    .prepare('UPDATE sessions SET agent_session_id = ?, updated_at = ? WHERE id = ?')
    .run(agentSessionId, Date.now(), sessionId);
}
```

注意：先在 migrate() 中确认 `agent_session_id` 列是否已存在于 sessions 表。如果 migrate() 中已有检测，则 updateSessionAgentId 方法可直接使用；如不存在，在 migrate() 中添加列检测：
```typescript
if (!columns.has('agent_session_id')) {
  this.db.exec('ALTER TABLE sessions ADD COLUMN agent_session_id TEXT');
}
```
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether && pnpm --filter @tether/gateway typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - store.ts constructor exec 块中包含 session_chats_events CREATE TABLE 和对应索引
    - ChatEventType、ChatEvent 类型已导出
    - appendChatEvent()、listChatEvents()、updateSessionAgentId() 方法已存在于 Store 类
    - sessions 表有 agent_session_id 列（通过 migrate() 或已有 DDL）
    - pnpm --filter @tether/gateway typecheck 通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 新建 Server DB migration 004_chat_messages.sql</name>
  <read_first>
    - apps/server/sql/002_gateway_runtime_sync.sql（MySQL CREATE TABLE 格式参考）
    - apps/server/sql/003_drop_chat_messages.sql（确认旧表已 DROP，004 是重建）
    - .planning/phases/13-mobile-web-chat/13-CONTEXT.md（D-41 新表结构规范）
  </read_first>
  <files>apps/server/sql/004_chat_messages.sql</files>
  <action>
新建文件 `apps/server/sql/004_chat_messages.sql`，内容如下：

```sql
-- migration 004: recreate gateway_chat_messages table (new structure)
-- migration 003 dropped the old table (with turn_index, tools_json)
-- this migration creates the new structure (with role, content, usage_json)

CREATE TABLE IF NOT EXISTS gateway_chat_messages (
  id          BIGINT NOT NULL AUTO_INCREMENT,
  session_id  VARCHAR(128) NOT NULL,
  role        VARCHAR(16)  NOT NULL,
  content     MEDIUMTEXT   NOT NULL,
  usage_json  TEXT         DEFAULT NULL,
  created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  KEY idx_chat_messages_session_id (session_id)
);
```

注意：`role` 值为 `'user'` 或 `'assistant'`。`usage_json` 仅 assistant 行填写，格式 `{ input_tokens, output_tokens, cost_usd }`。

然后确认 `apps/server/app/service/db.ts` 的 `ensureSchema()` 方法是否会自动加载 sql/ 目录下的所有 .sql 文件按序执行。如果不是自动加载，需要在 db.ts 的 `ensureSchema()` 或类似初始化方法中添加对 004_chat_messages.sql 的引用（按现有 001、002、003 的模式追加）。
  </action>
  <verify>
    <automated>grep -c "gateway_chat_messages" /Users/dream/code/tether/apps/server/sql/004_chat_messages.sql</automated>
  </verify>
  <done>
    - apps/server/sql/004_chat_messages.sql 存在
    - 文件包含 CREATE TABLE IF NOT EXISTS gateway_chat_messages
    - 包含字段：id BIGINT AUTO_INCREMENT、session_id VARCHAR(128)、role VARCHAR(16)、content MEDIUMTEXT、usage_json TEXT、created_at DATETIME
    - 包含 KEY idx_chat_messages_session_id(session_id)
    - db.ts 能够加载该迁移文件（按现有迁移模式确认）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protocol types → compiler | TypeScript 类型定义本身无运行时攻击面；边界是类型正确性 |
| SQLite DDL → Gateway process | CREATE TABLE 在进程启动时执行，DDL 本身无用户输入 |
| SQL migration file → MySQL | migration 由 Server 启动时执行，文件内容来自代码库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | protocol/index.ts client.chat 类型 | mitigate | client.chat sessionId:null 变体不含 forbidden keys（command/args/env 等）；Relay 已有 hasForbiddenKey 检查在所有客户端帧上执行 |
| T-13-01-02 | Tampering | store.ts appendChatEvent | accept | payload 只来自 Gateway 内部解析的 stream-json，不来自用户输入；SQLite prepared statement 防 SQL 注入 |
| T-13-01-03 | Information Disclosure | 004_chat_messages.sql usage_json 字段 | accept | cost 和 token 数量属于非敏感聚合数据；无 PII；role 和 content 由 Server access control 保护 |
</threat_model>

<verification>
```bash
# 1. Protocol 编译
cd /Users/dream/code/tether && pnpm --filter @tether/protocol typecheck

# 2. Gateway 编译（含 store.ts 新增内容）
pnpm --filter @tether/gateway typecheck

# 3. Migration 文件存在
ls apps/server/sql/004_chat_messages.sql

# 4. 关键类型存在于 protocol
grep -c "agent\.delta" packages/protocol/src/index.ts
grep -c "gateway\.session-created" packages/protocol/src/index.ts
grep -c "session_chats_events" apps/gateway/src/store.ts
grep -c "appendChatEvent" apps/gateway/src/store.ts
```
</verification>

<success_criteria>
1. `pnpm --filter @tether/protocol typecheck` 通过，无新增错误
2. `pnpm --filter @tether/gateway typecheck` 通过，无新增错误
3. `packages/protocol/src/index.ts` 包含：agent.delta、agent.result、agent.tool、gateway.session-created、gateway.chat-catchup、gateway.providers、client.list-providers、client.switch-model（所有新帧）
4. `apps/gateway/src/store.ts` 包含：session_chats_events 建表 DDL、appendChatEvent()、listChatEvents()、updateSessionAgentId()
5. `apps/server/sql/004_chat_messages.sql` 存在且包含 gateway_chat_messages CREATE TABLE
</success_criteria>

<output>
完成后创建 `.planning/phases/13-mobile-web-chat/13-01-SUMMARY.md`
</output>
