---
phase: 5
plan: 07
title: Cross-Package Verification, E2E Auth Checks, and Phase Fact Sync
type: implementation
wave: 4
depends_on: [03, 04, 05, 06]
files_modified:
  - apps/server/test/auth.test.ts
  - apps/server/test/gateway.test.ts
  - apps/server/test/notification.test.ts
  - apps/server/test/audit.test.ts
  - apps/gateway/src/daemon.test.ts
  - apps/gateway/src/relay-client.test.ts
  - apps/relay/src/relay.test.ts
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/05-web-first-account-setup-server-auth-runtime/05-VALIDATION.md
autonomous: false
requirements: [SERVER-02, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-07, RELAY-AUTH-01, RELAY-AUTH-02, AUDIT-01, AUDIT-02]
---

<objective>
Close Phase 5 with a single verification wave that exercises Server, Gateway, Relay, and Web-facing auth behaviors together, and bring planning facts back into sync so the repo has one authoritative story for Phase 5.
</objective>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Each package passes in isolation but revoke/isolation behavior still fails end to end | high | Add cross-package verification that covers refresh, revoke, Gateway write denial, Relay attach denial, and account isolation in one pass. |
| Audit and masking regress after auth payloads become richer | high | Extend tests to verify raw tokens and relay secrets never appear in stored or streamed events. |
| Planning docs keep stale `/setup?token=...` statements and future work reopens closed decisions | medium | Sync `.planning/ROADMAP.md`, `.planning/STATE.md`, and `05-VALIDATION.md` to the `/register`-first Phase 5 story. |
</threat_model>

<tasks>
<task id="05-07-T01" type="execute">
  <title>Add cross-package revoke and isolation verification</title>
  <read_first>
    <file>apps/server/test/auth.test.ts</file>
    <file>apps/gateway/src/daemon.test.ts</file>
    <file>apps/relay/src/relay.test.ts</file>
    <file>.planning/phases/05-web-first-account-setup-server-auth-runtime/05-VALIDATION.md</file>
  </read_first>
  <files>
    <file>apps/server/test/auth.test.ts</file>
    <file>apps/server/test/gateway.test.ts</file>
    <file>apps/gateway/src/daemon.test.ts</file>
    <file>apps/relay/src/relay.test.ts</file>
  </files>
  <action>
    Add or extend tests so the same revoked or wrong-scope token fails in all relevant layers:
    Server refresh/logout,
    Gateway `/api/ws-ticket`,
    Gateway session write routes,
    Relay `/gateway` auth,
    and Relay `/client` attach.
    Include at least one cross-account case and one same-account wrong-session case.
  </action>
  <acceptance_criteria>
    <criterion>`apps/server/test/auth.test.ts` contains revoke coverage.</criterion>
    <criterion>`apps/gateway/src/daemon.test.ts` contains wrong-session or cross-account coverage.</criterion>
    <criterion>`apps/relay/src/relay.test.ts` contains cross-account rejection coverage.</criterion>
  </acceptance_criteria>
</task>

<task id="05-07-T02" type="execute">
  <title>Verify audit and masking behavior after auth changes</title>
  <read_first>
    <file>apps/server/test/audit.test.ts</file>
    <file>apps/gateway/src/mask.ts</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <files>
    <file>apps/server/test/audit.test.ts</file>
    <file>apps/server/test/notification.test.ts</file>
    <file>apps/gateway/src/relay-client.test.ts</file>
  </files>
  <action>
    Extend audit and event-stream verification so auth success/failure, gateway bind, revoke, and notification flows prove that identity metadata is present while raw tokens, refresh tokens, relay secrets, and maskable secrets are absent.
    Include one same-user multi-device notification test that confirms metadata refresh is delivered without terminal payload bytes.
  </action>
  <acceptance_criteria>
    <criterion>`apps/server/test/audit.test.ts` contains token masking assertions.</criterion>
    <criterion>`apps/server/test/notification.test.ts` contains metadata-only notification coverage.</criterion>
    <criterion>`apps/gateway/src/relay-client.test.ts` contains auth failure or relogin-path coverage.</criterion>
  </acceptance_criteria>
</task>

<task id="05-07-T03" type="execute">
  <title>Run final suite and sync Phase 5 planning facts</title>
  <read_first>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/05-web-first-account-setup-server-auth-runtime/05-VALIDATION.md</file>
    <file>.planning/phases/05-web-first-account-setup-server-auth-runtime/05-CONTEXT.md</file>
  </read_first>
  <files>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/05-web-first-account-setup-server-auth-runtime/05-VALIDATION.md</file>
  </files>
  <action>
    Run `pnpm -r test` and `pnpm typecheck`.
    Record any residual risk in the Phase 5 planning artifacts if a dependency such as MySQL or Redis is unavailable in the environment.
    Sync planning facts so Phase 5 consistently describes `/register`, `/login`, `/admin/register`, and `/admin/login` as the required Web paths and keeps `/setup?token=...` out of the required runtime path.
  </action>
  <acceptance_criteria>
    <criterion>`pnpm -r test` exits 0 or the exact blocker is recorded.</criterion>
    <criterion>`pnpm typecheck` exits 0 or the exact blocker is recorded.</criterion>
    <criterion>`.planning/ROADMAP.md` contains `05-07-PLAN.md`.</criterion>
    <criterion>`.planning/STATE.md` contains `/register`.</criterion>
    <criterion>`05-VALIDATION.md` contains `05-07-T03`.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `pnpm -r test`.
- Run `pnpm typecheck`.
- Manually verify the Web auth routes and a same-user multi-device metadata refresh flow if environment dependencies are available.
</verification>

<must_haves>
## Must Haves

### Truths
- Phase 5 is not done until revoke/isolation/audit behavior is proven across Server, Gateway, and Relay together.
- Planning docs must describe the same product path the code is about to implement.

### Checks
- Full suite results are known.
- Revoke, isolation, notification, and masking behaviors are covered.
- `.planning` no longer tells two different stories about `/setup` versus `/register`.
</must_haves>

<success_criteria>
- Phase 5 has a final verification plan that catches cross-package auth failures and leaves the planning docs internally consistent.
</success_criteria>
