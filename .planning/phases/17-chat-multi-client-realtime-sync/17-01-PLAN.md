---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/relay/src/relay.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-06
  - D-07

must_haves:
  truths:
    - "同账号的多个 client 订阅同一 chat session 后都能收到 agent.delta / agent.result"
    - "跨账号 client 订阅不同账号的 chat session 时收不到对方的 chat 事件"
    - "client 断线、unsubscribe、detach 后从 chatSessionSubscribers 的 Set 中被移除"
    - "空 Set 会被从 chatSessionSubscribers 中删除，不产生内存泄漏"
    - "client.permission_response 转发前经过 clientCanAccessSession 账号校验，跨账号 client 无法向他人 Gateway 发送 permission_response"
  artifacts:
    - path: "apps/relay/src/relay.ts"
      provides: "chatSessionSubscribers Map + sendChatEventToSubscribers 函数 + permission_response 访问控制"
      contains: "chatSessionSubscribers"
  key_links:
    - from: "relay.ts client.subscribe handler"
      to: "chatSessionSubscribers Set.add"
      via: "session.transport === 'chat' 分支"
      pattern: "chatSessionSubscribers"
    - from: "relay.ts gateway.event handler"
      to: "sendChatEventToSubscribers"
      via: "agent.delta / agent.result / agent.tool / agent.permission_request / session.error"
      pattern: "sendChatEventToSubscribers"
    - from: "sendChatEventToSubscribers"
      to: "clientCanAccessSession"
      via: "per-subscriber 账号校验"
      pattern: "clientCanAccessSession"
    - from: "relay.ts client.permission_response handler"
      to: "clientCanAccessSession"
      via: "写路径账号校验（HIGH-3 修复）"
      pattern: "clientCanAccessSession.*permission_response"
---

<objective>
将 relay.ts 中的 1:1 chat routing（chatSessionOwners）替换为 1:N 广播（chatSessionSubscribers），实现多端同时订阅同一 chat session 并实时收到所有 chat 事件。同时修复 client.permission_response 缺少 Relay 侧账号校验的安全漏洞（HIGH-3）。

Purpose: 让同一账号的多个 Web/App 客户端同时连接时，每一端都能收到 agent.delta/result 等 chat 事件，不再是先订阅者独占。同时确保 permission_response 写路径有账号和订阅校验，防止跨账号攻击。
Output: 修改后的 relay.ts，含 chatSessionSubscribers Map + sendChatEventToSubscribers 辅助函数 + 12 处改动点完整替换 + permission_response 访问控制。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/PROJECT.md
@/Users/dream/code/tether/.planning/ROADMAP.md
@/Users/dream/code/tether/.planning/STATE.md
@/Users/dream/code/tether/.planning/phases/17-chat-multi-client-realtime-sync/17-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/17-chat-multi-client-realtime-sync/17-RESEARCH.md
@/Users/dream/code/tether/.planning/phases/17-chat-multi-client-realtime-sync/17-PATTERNS.md

<interfaces>
<!-- 关键函数签名，来自 apps/relay/src/relay.ts -->

```typescript
// L1159-1178: 账号校验函数（广播循环内每个 subscriber 独立调用）
function clientCanAccessSession(
  clientScope: RelayAuthScope | undefined,
  authMethod: RelayAuthMethod,
  sessionId: string,
  requiredTicketMode?: RelayClientMode
): boolean

// L1189-1194: 单播工具（广播循环内仍调用它）
function sendToClient(clientId: string, frame: RelayServerToClientFrame): void {
  const client = clients.get(clientId);
  if (client) sendToSocket<RelayServerToClientFrame>(client.socket, frame);
}

// L193: 待替换的声明（当前）
const chatSessionOwners = new Map<string, string>(); // sessionId → clientId

// L975-983: client.permission_response 当前 handler（缺少账号校验 — HIGH-3 修复目标）
case 'client.permission_response':
  forwardToSessionGateway({
    type: 'client.permission_response',
    clientId,
    sessionId: frame.sessionId,
    requestId: frame.requestId,
    decision: frame.decision
  });
  break;

// handleClientFrame 签名（permission_response 所在作用域）
async function handleClientFrame(
  clientId: string,
  clientScope: RelayAuthScope | undefined,
  subscriptions: Map<string, RelayClientMode>,
  frame: RelayClientToServerFrame
): Promise<void>

// L1110-1117: 按账号广播的参照模式（broadcastGatewayUnavailableForScope）
function broadcastGatewayUnavailableForScope(gatewayId: string, gatewayScope: RelayAuthScope): void {
  for (const client of clients.values()) {
    if (!clientCanUseGateway(client.scope, gatewayScope)) continue;
    if (client.gatewayId && client.gatewayId !== gatewayId) continue;
    sendToSocket<RelayServerToClientFrame>(client.socket, { ... });
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 替换 chatSessionOwners 为 chatSessionSubscribers + 新增 sendChatEventToSubscribers</name>
  <read_first>
    - apps/relay/src/relay.ts（完整文件，重点：L193、L403-495、L524-532、L718-724、L779-828、L984-1010）
    - apps/relay/src/relay.ts L1110-1117（broadcastGatewayUnavailableForScope 广播模式参照）
    - apps/relay/src/relay.ts L1151-1178（sendEventToSubscribers + clientCanAccessSession 参照）
    - apps/relay/src/relay.ts L1189-1194（sendToClient 函数）
    - CLAUDE.md（R3 广播必须过滤账号）
  </read_first>
  <files>apps/relay/src/relay.ts</files>
  <action>
按以下顺序精确修改 relay.ts。每处改动必须单独完成，不得扩展到相邻代码。

**改动 1：L193 — Map 声明替换（D-01）**

将：
```typescript
const chatSessionOwners = new Map<string, string>(); // sessionId → clientId
```
改为：
```typescript
const chatSessionSubscribers = new Map<string, Set<string>>(); // sessionId → Set<clientId>
```

**改动 2：紧接 chatSessionSubscribers 声明后新增 sendChatEventToSubscribers 辅助函数**

在 L193 的 Map 声明之后（即 `const server = createServer(...)` 之前）插入：
```typescript
function sendChatEventToSubscribers(sessionId: string, frame: RelayServerToClientFrame): void {
  const subscribers = chatSessionSubscribers.get(sessionId);
  if (!subscribers) return;
  for (const subscriberId of subscribers) {
    const client = clients.get(subscriberId);
    if (!client) continue;
    if (!clientCanAccessSession(client.scope, client.authMethod, sessionId)) continue;
    sendToClient(subscriberId, frame);
  }
}
```
注意：使用 `function` 声明（与 relay.ts 现有 helper 如 sendEventToSubscribers、sendToClient 风格一致）。clients、clientCanAccessSession、sendToClient 在闭包中可访问。

**改动 3：L403-413 — agent.delta 单播改为广播**

将原来从 `chatSessionOwners.get` 读 clientId 后点发的块（约 12 行）替换为：
```typescript
if (frame.event.type === 'agent.delta') {
  sendChatEventToSubscribers(frame.event.sessionId, {
    type: 'agent.delta',
    sessionId: frame.event.sessionId,
    text: String(frame.event.payload.text ?? ''),
    ...(typeof frame.event.id === 'number' ? { eventId: frame.event.id } : {})
  });
  void syncToServer('/api/relay/runtime-sync/gateway/event', {
    gatewayId: frame.gatewayId,
    event: frame.event,
    scope: { ...gatewayScope, transport: 'chat' }
  });
  break;
}
```

**改动 4：L424-451 — agent.result 单播改为广播**

找到 `if (frame.event.type === 'agent.result')` 块，将其中的 clientId 读取 + sendToClient 调用替换为：
```typescript
sendChatEventToSubscribers(frame.event.sessionId, {
  type: 'agent.result',
  sessionId: frame.event.sessionId,
  text: String(frame.event.payload.text ?? ''),
  usage: (frame.event.payload.usage ?? { input_tokens: 0, output_tokens: 0 }) as {
    input_tokens: number;
    output_tokens: number;
    cost_usd?: number;
    cache_creation_input_tokens?: number;
    cache_read_input_tokens?: number;
  },
  ...(typeof frame.event.payload.stop_reason === 'string' ? { stop_reason: frame.event.payload.stop_reason } : {}),
  ...(typeof frame.event.payload.contextWindow === 'number' ? { contextWindow: frame.event.payload.contextWindow } : {}),
  ...(frame.event.payload.rateLimitInfo && typeof frame.event.payload.rateLimitInfo === 'object'
    ? { rateLimitInfo: frame.event.payload.rateLimitInfo as { resetsAt: number; rateLimitType: string; status: string } }
    : {}),
  ...(Array.isArray(frame.event.payload.nextSuggestions)
    ? { nextSuggestions: frame.event.payload.nextSuggestions.filter(isNextSuggestion) }
    : {})
});
```
（保留该 if 块的其他代码不变，例如后续的 syncToServer 调用）

**改动 5：L453-465 — agent.permission_request 单播改为广播**

找到 `else if (frame.event.type === 'agent.permission_request')` 块，将 clientId 读取 + sendToClient 调用替换为：
```typescript
sendChatEventToSubscribers(frame.event.sessionId, {
  type: 'agent.permission_request',
  sessionId: frame.event.sessionId,
  requestId: String(frame.event.payload.requestId ?? ''),
  toolName: String(frame.event.payload.toolName ?? ''),
  input: (frame.event.payload.input && typeof frame.event.payload.input === 'object'
    ? frame.event.payload.input
    : {}) as Record<string, unknown>
});
```

**改动 6：L466-479 — session.error 单播改为广播（含 else 分支清理，Pitfall1）**

找到 `else if (frame.event.type === 'session.error')` 块。当前有 if/else 结构，将整块替换为：
```typescript
} else if (frame.event.type === 'session.error') {
  sendChatEventToSubscribers(frame.event.sessionId, {
    type: 'error',
    sessionId: frame.event.sessionId,
    code: String(frame.event.payload.code ?? 'session_error'),
    message: String(frame.event.payload.message ?? 'session error')
  });
```
删除原有的 `else { sendEventToSubscribers(frame.event); }` 分支，不得保留。

**改动 7：L480-495 — agent.tool 单播改为广播**

找到 `else if (frame.event.type === 'agent.tool')` 块，将 clientId 读取 + sendToClient 调用替换为：
```typescript
sendChatEventToSubscribers(frame.event.sessionId, {
  type: 'agent.tool',
  sessionId: frame.event.sessionId,
  name: String(frame.event.payload.name ?? ''),
  input: frame.event.payload.input && typeof frame.event.payload.input === 'object'
    ? (frame.event.payload.input as Record<string, unknown>)
    : {},
  ...(typeof frame.event.payload.result === 'string' ? { result: frame.event.payload.result } : {}),
  ...(typeof frame.event.payload.isError === 'boolean' ? { isError: frame.event.payload.isError } : {})
});
```

**改动 8：L524-526 — syncToServer transport 判断**

将：
```typescript
const whitelistScope = chatSessionOwners.has(frame.event.sessionId)
```
改为：
```typescript
const whitelistScope = chatSessionSubscribers.has(frame.event.sessionId)
```
（仅改变量名，其余不动）

**改动 9：L718-724 — socket close 断线清理（Pitfall4，逻辑完全不同，必须重写）**

将：
```typescript
for (const [sessionId, ownerId] of chatSessionOwners) {
  if (ownerId === clientId) chatSessionOwners.delete(sessionId);
}
```
改为：
```typescript
for (const [sessionId, subs] of chatSessionSubscribers) {
  subs.delete(clientId);
  if (subs.size === 0) chatSessionSubscribers.delete(sessionId);
}
```

**改动 10：L793-794 — subscribe 时写入 Set（D-01/D-02）**

将：
```typescript
chatSessionOwners.set(frame.sessionId, clientId);
```
改为：
```typescript
const subs = chatSessionSubscribers.get(frame.sessionId) ?? new Set<string>();
subs.add(clientId);
chatSessionSubscribers.set(frame.sessionId, subs);
```
注意：catch-up 逻辑（Phase 16）不变，原样保留。

**改动 11：L984-988 — client.unsubscribe 清理（D-02）**

将：
```typescript
if (chatSessionOwners.get(frame.sessionId) === clientId) {
  chatSessionOwners.delete(frame.sessionId);
}
```
改为：
```typescript
const unsubSubs = chatSessionSubscribers.get(frame.sessionId);
if (unsubSubs) {
  unsubSubs.delete(clientId);
  if (unsubSubs.size === 0) chatSessionSubscribers.delete(frame.sessionId);
}
```

**改动 12：L1003-1004 — client.detach 清理（D-02，同 unsubscribe 模式）**

将：
```typescript
if (chatSessionOwners.get(frame.sessionId) === clientId) {
  chatSessionOwners.delete(frame.sessionId);
}
```
改为：
```typescript
const detachSubs = chatSessionSubscribers.get(frame.sessionId);
if (detachSubs) {
  detachSubs.delete(clientId);
  if (detachSubs.size === 0) chatSessionSubscribers.delete(frame.sessionId);
}
```

完成后，文件中不应有任何 `chatSessionOwners` 引用。
  </action>
  <verify>
    <automated>grep -c "chatSessionOwners" /Users/dream/code/tether/apps/relay/src/relay.ts</automated>
    <automated>grep -c "chatSessionSubscribers" /Users/dream/code/tether/apps/relay/src/relay.ts</automated>
    <automated>grep -c "sendChatEventToSubscribers" /Users/dream/code/tether/apps/relay/src/relay.ts</automated>
    <automated>pnpm --filter @tether/relay typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' apps/relay/src/relay.ts | grep -c "chatSessionOwners"` 输出 `0`（无残留）
    - `grep -c "chatSessionSubscribers" apps/relay/src/relay.ts` 输出 `>= 8`（声明 + 多处引用）
    - `grep -c "sendChatEventToSubscribers" apps/relay/src/relay.ts` 输出 `>= 6`（函数声明 + 5 处调用：delta/result/permission_request/session.error/tool）
    - relay.ts 中 `session.error` handler 不再含 `else { sendEventToSubscribers` 模式
    - `pnpm --filter @tether/relay typecheck` 退出码为 0
  </acceptance_criteria>
  <done>relay.ts 中 chatSessionOwners 已完全替换为 chatSessionSubscribers，所有 5 处 chat 事件发送改为 sendChatEventToSubscribers 广播，三处清理路径（close/unsubscribe/detach）改为 Set 操作，typecheck 通过</done>
</task>

<task type="auto">
  <name>Task 2: client.permission_response 添加 Relay 侧访问校验（HIGH-3）</name>
  <read_first>
    - apps/relay/src/relay.ts（重点：L975-983 client.permission_response case + L840-890 client.input/resize/stop 中 clientCanAccessSession + subscriptions 校验模式参照）
    - apps/relay/src/relay.ts L1159-1178（clientCanAccessSession 函数完整签名）
    - CLAUDE.md（R1 按账号查找 Gateway；R4 新增路由路径必须附带隔离测试）
  </read_first>
  <files>apps/relay/src/relay.ts</files>
  <action>
在 relay.ts 的 `handleClientFrame` 函数内，找到 `case 'client.permission_response':` 块（当前约 L975-983）。

**当前代码（缺少账号校验，HIGH-3 漏洞）：**
```typescript
case 'client.permission_response':
  forwardToSessionGateway({
    type: 'client.permission_response',
    clientId,
    sessionId: frame.sessionId,
    requestId: frame.requestId,
    decision: frame.decision
  });
  break;
```

**替换为（添加 clientCanAccessSession + chatSessionSubscribers 订阅双重校验）：**
```typescript
case 'client.permission_response':
  // HIGH-3 修复：写路径账号校验 — 防止跨账号 client 向他人 Gateway 发送 permission_response
  if (!clientCanAccessSession(clientScope, authMethod, frame.sessionId)) {
    sendToClient(clientId, {
      type: 'error',
      sessionId: frame.sessionId,
      code: 'forbidden',
      message: 'session is outside client scope'
    });
    break;
  }
  // 订阅校验：仅已订阅该 session 的 client 可响应（符合 D-07 广播语义）
  if (!chatSessionSubscribers.get(frame.sessionId)?.has(clientId)) {
    sendToClient(clientId, {
      type: 'error',
      sessionId: frame.sessionId,
      code: 'not_subscribed',
      message: 'client is not subscribed to this session'
    });
    break;
  }
  forwardToSessionGateway({
    type: 'client.permission_response',
    clientId,
    sessionId: frame.sessionId,
    requestId: frame.requestId,
    decision: frame.decision
  });
  break;
```

注意：
- `clientScope`、`authMethod`、`clientId` 在 `handleClientFrame` 作用域内均已可用（参数）
- `chatSessionSubscribers` 在外层闭包中可访问（Task 1 改动 1 已声明）
- 校验顺序：先账号校验，再订阅校验，与 client.input/resize/stop 的模式一致
- 仅修改这一个 case 块，不触碰邻近代码
  </action>
  <verify>
    <automated>grep -A 20 "case 'client.permission_response'" /Users/dream/code/tether/apps/relay/src/relay.ts | grep -c "clientCanAccessSession"</automated>
    <automated>grep -A 20 "case 'client.permission_response'" /Users/dream/code/tether/apps/relay/src/relay.ts | grep -c "chatSessionSubscribers"</automated>
    <automated>pnpm --filter @tether/relay typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `client.permission_response` case 块内包含 `clientCanAccessSession(clientScope, authMethod, frame.sessionId)` 调用
    - `client.permission_response` case 块内包含 `chatSessionSubscribers.get(frame.sessionId)?.has(clientId)` 校验
    - 校验失败时向 client 发送 `type: 'error'` 帧（`code: 'forbidden'` 或 `code: 'not_subscribed'`），而非静默丢弃
    - `forwardToSessionGateway` 调用仅在两项校验均通过后执行
    - `pnpm --filter @tether/relay typecheck` 退出码为 0
  </acceptance_criteria>
  <done>relay.ts client.permission_response handler 已添加 clientCanAccessSession 账号校验和 chatSessionSubscribers 订阅校验，跨账号 client 无法向他人 Gateway 转发 permission_response，typecheck 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client WebSocket → Relay handleClientFrame | 外部 client 发送 subscribe，Relay 将其加入 Set |
| chatSessionSubscribers Set → sendToClient | Relay 从 Set 读出 subscriberId 并发送帧，此处是信息边界 |
| client WebSocket → Relay client.permission_response | 外部 client 发送 permission_response，Relay 转发给 Gateway（写路径） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | sendChatEventToSubscribers | mitigate | 广播循环内每个 subscriber 独立调用 `clientCanAccessSession(client.scope, client.authMethod, sessionId)`，跳过不满足的 subscriber（per D-03，CLAUDE.md R3） |
| T-17-02 | Denial of Service | chatSessionSubscribers Map | mitigate | 断线/unsubscribe/detach 三路径均执行 Set.delete + 空 Set 删 key（per D-02），防止僵尸 subscriber 内存泄漏 |
| T-17-03 | Tampering | session.error else 分支 | mitigate | 删除旧 else 分支（Pitfall1），统一走 sendChatEventToSubscribers，防止双路径发送 |
| T-17-09 | Elevation of Privilege | client.permission_response 写路径 | mitigate | 添加 `clientCanAccessSession` 账号校验 + `chatSessionSubscribers.has` 订阅校验（HIGH-3 修复），防止跨账号 client 向他人 Gateway 注入 permission_response（CLAUDE.md R4） |
</threat_model>

<verification>
pnpm --filter @tether/relay typecheck
</verification>

<success_criteria>
- relay.ts 中零 chatSessionOwners 引用
- sendChatEventToSubscribers 函数存在且含 clientCanAccessSession 校验，使用 function 声明
- 5 处 chat 事件 handler（delta/result/permission_request/session.error/tool）均调用 sendChatEventToSubscribers
- client.subscribe（chat）写入 Set，client.unsubscribe/detach/close 三路清理均为 Set 操作
- client.permission_response 包含 clientCanAccessSession 账号校验和 chatSessionSubscribers 订阅校验
- typecheck 通过
</success_criteria>

<output>
完成后创建 `.planning/phases/17-chat-multi-client-realtime-sync/17-01-SUMMARY.md`
</output>
</output>
