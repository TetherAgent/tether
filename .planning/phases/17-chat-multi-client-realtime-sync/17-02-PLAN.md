---
phase: 17
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gateway/src/relay-client.ts
autonomous: true
requirements:
  - D-05
  - D-06
  - D-08
  - D-09
  - D-10
  - D-11

must_haves:
  truths:
    - "同一 session 第二个 client.chat 立即收到 gateway.error{code:'chat_in_progress'}"
    - "agent.result 发出后，相同 session 可以再次发起 client.chat"
    - "session.error 发出后锁释放，相同 session 可以再次发起 client.chat"
    - "chatClientBindings 已从代码库中完全移除"
  artifacts:
    - path: "apps/gateway/src/relay-client.ts"
      provides: "chatInFlight Set + in-flight 检查 + chatClientBindings 完全删除"
      contains: "chatInFlight"
  key_links:
    - from: "relay-client.ts client.chat case"
      to: "chatInFlight.has(sessionId)"
      via: "sessionId !== null 分支顶部检查（D-08/D-09）"
      pattern: "chatInFlight"
    - from: "chatRunnerOptions.onResult"
      to: "chatInFlight.delete(sessionId)"
      via: "D-11 锁释放"
      pattern: "chatInFlight.delete"
---

<objective>
在 relay-client.ts 中删除 chatClientBindings Map（13 处引用），新增 chatInFlight Set 实现 session 级并发锁，防止同一 chat session 并发发送。

Purpose: 保证 chat session 同一时间只有一个 runner 在执行，第二个发送请求立即返回 chat_in_progress 错误。
Output: 修改后的 relay-client.ts，chatClientBindings 完全移除，chatInFlight 锁新增并覆盖三个释放路径（onResult/onError/子进程退出）。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/PROJECT.md
@/Users/dream/code/tether/.planning/ROADMAP.md
@/Users/dream/code/tether/.planning/STATE.md
@/Users/dream/code/tether/.planning/phases/17-chat-multi-client-realtime-sync/17-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/17-chat-multi-client-realtime-sync/17-RESEARCH.md
@/Users/dream/code/tether/.planning/phases/17-chat-multi-client-realtime-sync/17-PATTERNS.md

<interfaces>
<!-- 关键函数签名，来自 apps/gateway/src/relay-client.ts -->

```typescript
// L741-743: sendError 函数（in-flight 拒绝时调用）
const sendError = (clientId: string, sessionId: string, code: string, message: string) => {
  send({ type: 'gateway.error', gatewayId: effectiveGatewayId, clientId, sessionId, code, message });
};

// L71-72: 待改动的声明区（当前）
const subscriptions = new Map<string, RelaySubscription>();
const chatClientBindings = new Map<string, string>();

// client.chat case 当前结构（L351-407）：
// case 'client.chat': {
//   if (frame.sessionId === null) { ... 新建路径 ... return; }
//   if (!frame.session) { ... missing_session_metadata ... return; }
//   const runner = runnerForProvider(frame.session.provider);
//   if (!runner) { ... provider_not_supported ... return; }
//   void runner.run({ ... });
//   return;
// }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 chatClientBindings（13 处）+ 新增 chatInFlight Set</name>
  <read_first>
    - apps/gateway/src/relay-client.ts（完整文件，重点：L71-72、L80-92、L94-148、L351-407、L505-527、L751-767）
    - apps/gateway/src/relay-client.ts L741-743（sendError 函数签名）
  </read_first>
  <files>apps/gateway/src/relay-client.ts</files>
  <action>
先用 `grep -n "chatClientBindings"` 列出所有引用行，再逐一处理。以下是完整改动清单：

**改动 1：L72 — 声明区**

将：
```typescript
const chatClientBindings = new Map<string, string>();
```
改为：
```typescript
const chatInFlight = new Set<string>();
```

（subscriptions 声明和其他邻近代码不变）

**改动 2：L81 — onSessionCreated 回调**

找到 `onSessionCreated: (clientId, sessionId) => {` 内部，删除：
```typescript
chatClientBindings.set(sessionId, clientId);
```
仅删除该行，保留 `send(...)` 和 `void sendSessions()` 两行。

**改动 3：L86 — onChatSessionCreated 回调**

找到 `onChatSessionCreated: (clientId, metadata) => {` 内部，删除：
```typescript
chatClientBindings.set(metadata.id, clientId);
```
仅删除该行，保留 `send(...)` 调用。

**改动 4：L96 — onUserMessage（chatClientBindings.get 替换为直接 clientId）**

将：
```typescript
clientId: chatClientBindings.get(sessionId) ?? clientId,
```
改为：
```typescript
clientId,
```

**改动 5：L101 — onDelta**

同上，将：
```typescript
clientId: chatClientBindings.get(sessionId) ?? clientId,
```
改为：
```typescript
clientId,
```

**改动 6：L105 — onResult（含 chatInFlight.delete 锁释放，D-11）**

找到 `onResult: ({ clientId, sessionId, event, text, usage, ... }) => {` 函数体最顶部，添加：
```typescript
chatInFlight.delete(sessionId);  // D-11: 释放 in-flight 锁
```
然后将 `sendChatEvent` 调用中的：
```typescript
clientId: chatClientBindings.get(sessionId) ?? clientId,
```
改为：
```typescript
clientId,
```

**改动 7：L117 — onPermissionRequest**

将：
```typescript
clientId: chatClientBindings.get(sessionId) ?? clientId,
```
改为：
```typescript
clientId,
```

**改动 8：L125 — onTool**

将：
```typescript
clientId: chatClientBindings.get(sessionId) ?? clientId,
```
改为：
```typescript
clientId,
```

**改动 9：L132-148 — onError（含 chatInFlight.delete 锁释放，D-11）**

找到 `onError: ({ clientId, sessionId, code, message, event }) => {` 函数体最顶部，添加：
```typescript
chatInFlight.delete(sessionId);  // D-11: 释放 in-flight 锁
```
然后将函数体内所有：
```typescript
clientId: chatClientBindings.get(sessionId) ?? clientId,
```
改为：
```typescript
clientId,
```
（onError 内有 2 处：sendChatEvent 调用和 send gateway.error 调用各一处）

**改动 10：L351 — client.chat case — 新增 in-flight 检查（D-08/D-09/D-10）**

在 `case 'client.chat': {` 内部，找到 `if (frame.sessionId === null) { ... return; }` 块结束处（即 `return;` 后），在已有 `if (!frame.session) { ... }` 之前插入：
```typescript
// D-08/D-09: in-flight 锁检查（仅对已有 session）
if (chatInFlight.has(frame.sessionId)) {
  sendError(frame.clientId, frame.sessionId, 'chat_in_progress', '当前会话正在回复中');
  return;
}
```
然后在 `void runner.run({ ... });` 之前插入：
```typescript
chatInFlight.add(frame.sessionId);  // D-10: 加锁，在启动 runner 之前
```

完整的 sessionId !== null 分支结构变为：
```
if (!frame.session) { ... return; }
const runner = runnerForProvider(frame.session.provider);
if (!runner) { ... return; }
// D-08/D-09
if (chatInFlight.has(frame.sessionId)) {
  sendError(frame.clientId, frame.sessionId, 'chat_in_progress', '当前会话正在回复中');
  return;
}
chatInFlight.add(frame.sessionId);  // D-10
void runner.run({ ... });
return;
```

注意：in-flight 检查放在 `frame.session` 和 `runner` 校验之后（避免无效 session 也占锁）。

**改动 11：L515 — subscribeClient 中 chatClientBindings.set 删除**

找到 `subscribeClient` 函数中：
```typescript
if (session.transport === 'chat') {
  chatClientBindings.set(sessionId, clientId);
```
删除 `chatClientBindings.set(sessionId, clientId);` 这一行，保留后续代码不变。

**改动 12：L756-758 — removeSubscription 中 chatClientBindings 清理删除**

找到 `removeSubscription` 函数，删除：
```typescript
if (chatClientBindings.get(sessionId) === clientId) {
  chatClientBindings.delete(sessionId);
}
```
（保留 subscriptions.delete(key) 等其他代码）

**改动 13：L766 — clearSubscriptions 中 chatClientBindings.clear() 删除**

找到 `clearSubscriptions` 函数，删除：
```typescript
chatClientBindings.clear();
```
（保留 subscriptions.clear() 等其他代码）

完成后，文件中不应有任何 `chatClientBindings` 引用。
  </action>
  <verify>
    <automated>grep -c "chatClientBindings" /Users/dream/code/tether/apps/gateway/src/relay-client.ts</automated>
    <automated>grep -c "chatInFlight" /Users/dream/code/tether/apps/gateway/src/relay-client.ts</automated>
    <automated>pnpm --filter @tether/gateway typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chatClientBindings" apps/gateway/src/relay-client.ts` 输出 `0`（无残留）
    - `grep -c "chatInFlight" apps/gateway/src/relay-client.ts` 输出 `>= 5`（声明 + has + add + 至少 2 处 delete）
    - relay-client.ts 的 client.chat case 中包含 `chatInFlight.has(frame.sessionId)` 检查
    - relay-client.ts 的 onResult 回调函数体含 `chatInFlight.delete(sessionId)` 调用
    - relay-client.ts 的 onError 回调函数体含 `chatInFlight.delete(sessionId)` 调用
    - relay-client.ts 的 clearSubscriptions 函数不含 `chatClientBindings` 引用
    - `pnpm --filter @tether/gateway typecheck` 退出码为 0
  </acceptance_criteria>
  <done>relay-client.ts 中 chatClientBindings 完全移除，chatInFlight Set 已新增，in-flight 检查在 client.chat sessionId !== null 分支的正确位置，onResult 和 onError 均调用 chatInFlight.delete，typecheck 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relay → Gateway client.chat | Relay 转发 client.chat 帧给 Gateway，Gateway 需防止并发 runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | relay-client.ts chatInFlight | mitigate | chatInFlight.add 在 runner.run 之前执行（D-10），防止检查通过后到 add 前的竞态；Node.js 单线程保证无并发 |
| T-17-05 | Denial of Service | chatInFlight lock | accept | 第一版不加超时兜底（明确 deferred）；子进程异常退出由 onError 回调兜底释放锁，正常情况不会永久锁定 |
| T-17-06 | Information Disclosure | chatClientBindings 移除 | mitigate | 移除 `chatClientBindings.get(sessionId) ?? clientId` 路由兜底（D-06），Relay 侧路由完全依赖 chatSessionSubscribers，不再通过 payload.clientId 决策 |
</threat_model>

<verification>
pnpm --filter @tether/gateway typecheck
</verification>

<success_criteria>
- relay-client.ts 中零 chatClientBindings 引用
- chatInFlight Set 声明存在，in-flight 检查在 client.chat case 的 sessionId !== null 分支内
- onResult 和 onError 回调均在函数体顶部释放 chatInFlight 锁
- typecheck 通过
</success_criteria>

<output>
完成后创建 `.planning/phases/17-chat-multi-client-realtime-sync/17-02-SUMMARY.md`
</output>
