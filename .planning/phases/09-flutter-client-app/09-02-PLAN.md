---
phase: 09-flutter-client-app
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - native/flutter/lib/services/auth_service.dart
  - native/flutter/lib/services/relay_client.dart
  - native/flutter/test/auth_service_test.dart
  - native/flutter/test/relay_client_test.dart
autonomous: true
requirements: [D-07, D-08, D-09, D-10, D-11, D-22, D-23, D-24, D-25, SC-2, SC-3]

must_haves:
  truths:
    - "AuthService.login 调用 POST /login 并将 token 写入 flutter_secure_storage，不写入 SharedPreferences"
    - "AuthService.checkStoredToken 在 access token 过期时尝试 silent refresh；refresh 失败时清除所有 token"
    - "RelayClient 在 WebSocket 关闭后按 [1,2,4,8,16,30] 秒序列退避重连"
    - "RelayClient.subscribe 将 latestEventId 存于服务层（非 Widget 状态），每次 Event 帧更新，断线重连时作为 after 参数使用"
    - "auth_service_test.dart 和 relay_client_test.dart 全部通过"
  artifacts:
    - path: "native/flutter/lib/services/auth_service.dart"
      provides: "AuthService 类：login/register/checkStoredToken/logout；FlutterSecureStorage 存 tether:access_token + tether:refresh_token；dio QueuedInterceptor 处理 401 自动刷新"
      min_lines: 80
    - path: "native/flutter/lib/services/relay_client.dart"
      provides: "RelayClient ChangeNotifier：WebSocket 状态机（auth→list→subscribe→event→replay.done）；指数退避重连；latestEventIds Map；activeSessions/historySessions/gatewayUnavailable 可观察状态"
      min_lines: 120
    - path: "native/flutter/test/auth_service_test.dart"
      provides: "AuthService token 存储和 silent refresh 逻辑单元测试"
    - path: "native/flutter/test/relay_client_test.dart"
      provides: "RelayClient 退避序列和 latestEventId 游标单元测试"
  key_links:
    - from: "native/flutter/lib/services/auth_service.dart"
      to: "flutter_secure_storage"
      via: "FlutterSecureStorage() 实例"
      pattern: "FlutterSecureStorage"
    - from: "native/flutter/lib/services/relay_client.dart"
      to: "native/flutter/lib/models/protocol.dart"
      via: "import '../models/protocol.dart'"
      pattern: "RelayServerToClientFrame.fromJson"
    - from: "native/flutter/lib/services/auth_service.dart"
      to: "dio QueuedInterceptor"
      via: "TokenRefreshInterceptor extends QueuedInterceptor"
      pattern: "QueuedInterceptor"
---

<objective>
实现 AuthService 和 RelayClient 两个核心服务层。AuthService 封装所有鉴权逻辑（login/register/token refresh/secure storage）。RelayClient 实现完整的 Relay WebSocket 状态机，镜像 apps/web/src/main.tsx 的 Relay useEffect（lines 452-556）。

Purpose: 屏幕层（Wave 3）依赖这两个服务——SessionListScreen 使用 RelayClient 获取 session 列表，TerminalScreen/ReplayScreen 使用 RelayClient 订阅；AuthService 在 LoginScreen 和应用启动时调用。

Output: 可测试的服务层，令 flutter test test/auth_service_test.dart + relay_client_test.dart 全部通过；flutter analyze 保持干净。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-flutter-client-app/09-CONTEXT.md
@.planning/phases/09-flutter-client-app/09-RESEARCH.md
@.planning/phases/09-flutter-client-app/09-PATTERNS.md

<interfaces>
<!-- 从 Plan 01 创建的文件 -->
<!-- native/flutter/lib/models/protocol.dart 提供: -->
<!--   sealed class RelayClientToServerFrame -->
<!--   sealed class RelayServerToClientFrame -->
<!--   ClientAuth, ClientList, ClientSubscribe, ClientInput, ClientResize, ClientStop, ClientDetach -->
<!--   ClientAuthOk, ClientAuthFailed, Sessions, Hello, Event, ReplayDone, TetherError -->
<!--   RelaySession, RelayTerminalEvent, RelaySessionStatus, RelayClientMode -->
<!-- native/flutter/lib/theme.dart 提供: ThemeNotifier, LocaleNotifier -->

<!-- apps/web/src/main.tsx Relay 状态机关键行 (452-556 行) — 行为参考: -->
<!--   ws.send({ type: 'client.auth', token: normalAuth.accessToken }) on open -->
<!--   on 'client.auth.ok': sendList(); timer = setInterval(sendList, 3000) -->
<!--   on 'sessions': splitActiveSessions(frame.sessions) -->
<!--   on 'error' code='gateway_unavailable': setSessions([]); setRelayGatewayUnavailable(true) -->
<!--   on close: setStatus(t.statusRelayClosed) -->

<!-- apps/web/src/contexts/auth-context.tsx 鉴权模式: -->
<!--   loginNormal: POST /login { email, password } → { accessToken, refreshToken } -->
<!--   validateNormalSession: 验证 accessToken → 失败则 refreshNormal → 失败则 logoutNormal -->
<!--   token keys: NORMAL_STORAGE_KEY (flutter 等价: tether:access_token, tether:refresh_token) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthService — 登录/注册/token 存储/静默刷新</name>
  <read_first>
    - .planning/phases/09-flutter-client-app/09-RESEARCH.md（Pattern 4 flutter_secure_storage 模式；Pattern: Token Refresh Interceptor dio；Common Pitfalls: 不要用 SharedPreferences 存 token）
    - .planning/phases/09-flutter-client-app/09-PATTERNS.md（auth_service.dart 模式部分，包含 login flow + validateNormalSession 等价实现）
    - .planning/phases/09-flutter-client-app/09-CONTEXT.md（D-07 login/register 端点；D-08 Server URL 默认值；D-09 Relay URL 来源；D-10 flutter_secure_storage；D-11 silent refresh）
    - native/flutter/lib/models/protocol.dart（确认已存在，了解 RelayClientMode 等类型）
  </read_first>
  <files>
    native/flutter/lib/services/auth_service.dart,
    native/flutter/test/auth_service_test.dart
  </files>
  <behavior>
    - Test 1: login 成功时将 accessToken 写入 flutter_secure_storage key 'tether:access_token'
    - Test 2: login 成功时将 refreshToken 写入 flutter_secure_storage key 'tether:refresh_token'
    - Test 3: login 失败（401）时抛出 AuthException，不写入 storage
    - Test 4: checkStoredToken 在 storage 为空时返回 false
    - Test 5: checkStoredToken 在 access token 有效时返回 true（mock /validate 成功）
    - Test 6: checkStoredToken 在 access token 无效但 refresh 成功时返回 true 并更新 access token
    - Test 7: checkStoredToken 在 refresh 失败时调用 logout（清空所有 token）并返回false
    - Test 8: logout 清空 flutter_secure_storage 中两个 token key
  </behavior>
  <action>
**创建 native/flutter/lib/services/auth_service.dart**

包含以下内容：

```dart
// lib/services/auth_service.dart
// 鉴权服务 — 封装 login/register/token refresh/secure storage
// 镜像 apps/web/src/contexts/auth-context.tsx 的 loginNormal/validateNormalSession/refreshNormal
// 所有 token 使用 flutter_secure_storage，绝不使用 SharedPreferences
import 'package:dio/dio.dart';
import 'package:flutter/foundation.dart';
import 'package:flutter_secure_storage/flutter_secure_storage.dart';

// 默认 Server URL — per D-08，编译时常量，用户无法在 Phase 9 的 UI 更改
const String kDefaultServerUrl = 'https://tether.earntools.me';

class AuthException implements Exception {
  final String message;
  const AuthException(this.message);
  @override
  String toString() => 'AuthException: $message';
}

class AuthService extends ChangeNotifier {
  static const _storage = FlutterSecureStorage();
  static const _accessKey  = 'tether:access_token';
  static const _refreshKey = 'tether:refresh_token';
  static const _relayUrlKey = 'tether:relay_url';

  final Dio _dio;
  final String serverUrl;
  bool _isAuthenticated = false;
  bool get isAuthenticated => _isAuthenticated;

  AuthService({String? serverUrl})
      : serverUrl = serverUrl ?? kDefaultServerUrl,
        _dio = Dio(BaseOptions(
          baseUrl: serverUrl ?? kDefaultServerUrl,
          connectTimeout: const Duration(seconds: 10),
          receiveTimeout: const Duration(seconds: 10),
        )) {
    _dio.interceptors.add(TokenRefreshInterceptor(_storage, serverUrl ?? kDefaultServerUrl));
  }

  // 读取存储的 token（供 RelayClient 使用）
  Future<String?> readAccessToken() => _storage.read(key: _accessKey);
  Future<String?> readRefreshToken() => _storage.read(key: _refreshKey);
  Future<String?> readRelayUrl() => _storage.read(key: _relayUrlKey);

  // POST /login { email, password } — per D-07
  Future<void> login({required String email, required String password}) async {
    try {
      final res = await _dio.post('/login', data: {'email': email, 'password': password});
      final accessToken  = res.data['accessToken']  as String;
      final refreshToken = res.data['refreshToken'] as String;
      // D-09: Relay URL 来自 login 响应（如不存在则回退到 serverUrl + /relay 推算路径）
      final relayUrl = res.data['relayUrl'] as String?;
      await Future.wait([
        _storage.write(key: _accessKey, value: accessToken),
        _storage.write(key: _refreshKey, value: refreshToken),
        if (relayUrl != null) _storage.write(key: _relayUrlKey, value: relayUrl),
      ]);
      _isAuthenticated = true;
      notifyListeners();
    } on DioException catch (e) {
      final msg = e.response?.data?['message'] as String? ?? 'login failed';
      throw AuthException(msg);
    }
  }

  // POST /register { email, password, displayName? } — per D-07
  Future<void> register({required String email, required String password, String? displayName}) async {
    try {
      final body = {'email': email, 'password': password, if (displayName != null) 'displayName': displayName};
      final res = await _dio.post('/register', data: body);
      final accessToken  = res.data['accessToken']  as String;
      final refreshToken = res.data['refreshToken'] as String;
      await Future.wait([
        _storage.write(key: _accessKey, value: accessToken),
        _storage.write(key: _refreshKey, value: refreshToken),
      ]);
      _isAuthenticated = true;
      notifyListeners();
    } on DioException catch (e) {
      final msg = e.response?.data?['message'] as String? ?? 'registration failed';
      throw AuthException(msg);
    }
  }

  // D-11: 启动时检查 token — 尝试 silent refresh
  Future<bool> checkStoredToken() async {
    final access = await _storage.read(key: _accessKey);
    if (access == null) {
      _isAuthenticated = false;
      notifyListeners();
      return false;
    }
    try {
      // 验证 access token
      await _dio.get('/validate',
          options: Options(headers: {'Authorization': 'Bearer $access'}));
      _isAuthenticated = true;
      notifyListeners();
      return true;
    } catch (_) {
      // access token 失效，尝试 refresh
      return _tryRefresh();
    }
  }

  Future<bool> _tryRefresh() async {
    final refresh = await _storage.read(key: _refreshKey);
    if (refresh == null) {
      await logout();
      return false;
    }
    try {
      final res = await Dio(BaseOptions(baseUrl: serverUrl))
          .post('/refresh', data: {'refreshToken': refresh});
      final newAccess = res.data['accessToken'] as String;
      await _storage.write(key: _accessKey, value: newAccess);
      _isAuthenticated = true;
      notifyListeners();
      return true;
    } catch (_) {
      await logout();
      return false;
    }
  }

  Future<void> logout() async {
    await _storage.deleteAll();
    _isAuthenticated = false;
    notifyListeners();
  }
}

// dio QueuedInterceptor — 处理 HTTP 请求中的 401 自动 token 刷新
// 保证并发的 401 只发起一次刷新请求（QueuedInterceptor 保证顺序）
class TokenRefreshInterceptor extends QueuedInterceptor {
  final FlutterSecureStorage _store;
  final String _serverUrl;

  TokenRefreshInterceptor(this._store, this._serverUrl);

  @override
  Future<void> onError(DioException err, ErrorInterceptorHandler handler) async {
    if (err.response?.statusCode == 401) {
      final refresh = await _store.read(key: 'tether:refresh_token');
      if (refresh == null) {
        handler.next(err);
        return;
      }
      try {
        final res = await Dio(BaseOptions(baseUrl: _serverUrl))
            .post('/refresh', data: {'refreshToken': refresh});
        final newAccess = res.data['accessToken'] as String;
        await _store.write(key: 'tether:access_token', value: newAccess);
        // 用新 token 重试原请求
        handler.resolve(await Dio(BaseOptions(baseUrl: _serverUrl)).fetch(
          err.requestOptions
            ..headers['Authorization'] = 'Bearer $newAccess',
        ));
      } catch (_) {
        await _store.deleteAll();
        handler.next(err);
      }
    } else {
      handler.next(err);
    }
  }
}
```

**创建 native/flutter/test/auth_service_test.dart**

使用 Mockito 替代或直接 mock — 由于 pubspec 无 mockito，使用手写 stub 类测试 storage 逻辑：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_secure_storage/flutter_secure_storage.dart';
import 'package:tether/services/auth_service.dart';

// 注意：flutter_secure_storage 在 test 环境需要 mock
// 使用 FlutterSecureStorage.setMockInitialValues() 注入
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  setUp(() {
    FlutterSecureStorage.setMockInitialValues({});
  });

  group('AuthService storage keys', () {
    test('logout clears both token keys', () async {
      FlutterSecureStorage.setMockInitialValues({
        'tether:access_token': 'old_access',
        'tether:refresh_token': 'old_refresh',
      });
      const storage = FlutterSecureStorage();
      final svc = AuthService(serverUrl: 'http://localhost:3001');
      await svc.logout();
      expect(await storage.read(key: 'tether:access_token'), isNull);
      expect(await storage.read(key: 'tether:refresh_token'), isNull);
      expect(svc.isAuthenticated, false);
    });

    test('checkStoredToken returns false when storage empty', () async {
      FlutterSecureStorage.setMockInitialValues({});
      final svc = AuthService(serverUrl: 'http://localhost:3001');
      final result = await svc.checkStoredToken();
      expect(result, false);
    });
  });

  group('AuthService token key constants', () {
    test('access token key is tether:access_token', () {
      // 验证常量值（防止拼写错误）
      const storage = FlutterSecureStorage();
      expect(() async {
        await storage.write(key: 'tether:access_token', value: 'tok');
        final v = await storage.read(key: 'tether:access_token');
        expect(v, 'tok');
      }, returnsNormally);
    });
  });
}
```

**注意关于测试覆盖：** flutter_secure_storage 在单元测试中通过 `setMockInitialValues` 提供 in-memory 实现。对 login/register 的网络请求测试需要 mock HTTP server，在 Phase 9 范围内用集成测试覆盖——此处单元测试专注于 storage 键和 logout 逻辑的正确性。
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether/native/flutter && flutter test test/auth_service_test.dart --no-pub 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/auth_service_test.dart --no-pub` 所有测试通过
    - `grep -c "FlutterSecureStorage" native/flutter/lib/services/auth_service.dart` 返回至少 2
    - `grep -c "tether:access_token" native/flutter/lib/services/auth_service.dart` 返回至少 1
    - `grep -c "tether:refresh_token" native/flutter/lib/services/auth_service.dart` 返回至少 1
    - `grep -c "SharedPreferences" native/flutter/lib/services/auth_service.dart` 返回 0（token 绝对不能用 SharedPreferences）
    - `grep -c "QueuedInterceptor" native/flutter/lib/services/auth_service.dart` 返回 1
    - `grep -c "kDefaultServerUrl" native/flutter/lib/services/auth_service.dart` 返回至少 1
    - `flutter analyze --no-pub 2>&1 | grep -c "error"` 返回 0
  </acceptance_criteria>
  <done>AuthService 单元测试通过；token 只写 flutter_secure_storage；QueuedInterceptor 处理 401 刷新；flutter analyze 无错误</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RelayClient — WebSocket 状态机 + 指数退避 + latestEventId 游标</name>
  <read_first>
    - apps/web/src/main.tsx（lines 452-556 — Relay useEffect 状态机精确行为参考）
    - .planning/phases/09-flutter-client-app/09-RESEARCH.md（Pattern 1 RelayService Dart 模式；指数退避列表 [1,2,4,8,16,30]；Pitfall 4 latestEventId 存储位置）
    - .planning/phases/09-flutter-client-app/09-PATTERNS.md（relay_client.dart 目标模式，含 splitActiveSessions、disconnect displayMessage 映射）
    - native/flutter/lib/models/protocol.dart（所有帧类型的精确 Dart 类名）
    - native/flutter/lib/services/auth_service.dart（readAccessToken 接口）
  </read_first>
  <files>
    native/flutter/lib/services/relay_client.dart,
    native/flutter/test/relay_client_test.dart
  </files>
  <behavior>
    - Test 1: _computeBackoff(0) 返回 1 秒
    - Test 2: _computeBackoff(1) 返回 2 秒
    - Test 3: _computeBackoff(4) 返回 16 秒
    - Test 4: _computeBackoff(100) 返回 30 秒（上限）
    - Test 5: splitSessions 对 status='running' 过滤出 active，其余的最多 8 个为 history
    - Test 6: splitSessions 当所有 session 都是 running 时 history 为空
    - Test 7: splitSessions 当 history 超过 8 个时截断为 8 个
    - Test 8: updateLatestEventId 在 sessionId 首次出现时设置值
    - Test 9: updateLatestEventId 在后续调用时更新到更大的值
    - Test 10: getLatestEventId 对未知 sessionId 返回 null
  </behavior>
  <action>
**创建 native/flutter/lib/services/relay_client.dart**

严格镜像 apps/web/src/main.tsx Relay 状态机（lines 452-556）：

```dart
// lib/services/relay_client.dart
// Relay WebSocket 客户端 — 镜像 apps/web/src/main.tsx lines 452-556 的 useEffect
// 状态机: 连接 → client.auth → client.auth.ok → client.list(每3s) → client.subscribe → event → replay.done
// 退避: [1, 2, 4, 8, 16, 30] 秒
// latestEventId: 存于服务层（非 widget），防止屏幕重建时丢失游标
import 'dart:async';
import 'dart:convert';
import 'dart:io';
import 'package:flutter/foundation.dart';
import '../models/protocol.dart';

// 镜像 apps/web/src/main.tsx splitActiveSessions (lines 134-141)
({List<RelaySession> active, List<RelaySession> history}) splitSessions(
    List<RelaySession> all) {
  final active = all.where((s) => s.status == RelaySessionStatus.running).toList();
  final activeIds = {for (final s in active) s.id};
  final history = all.where((s) => !activeIds.contains(s.id)).take(8).toList();
  return (active: active, history: history);
}

// 镜像 apps/web/src/main.tsx displayMessage (lines 235-250)
String _displayMessage(String message) {
  return switch (message) {
    'authentication failed'                    => 'authentication failed',
    'gateway is not connected'                 => 'gateway_not_connected',
    'client is not subscribed to this session' => 'not_subscribed',
    'observer clients cannot send input'       => 'observe_cannot_send',
    'observer clients cannot resize'           => 'observe_cannot_resize',
    _ => message,
  };
}

// 指数退避序列 (秒) — 镜像 RESEARCH.md Pattern 1
const _backoffSeconds = [1, 2, 4, 8, 16, 30];

int _computeBackoff(int index) =>
    _backoffSeconds[index.clamp(0, _backoffSeconds.length - 1)];

class RelayClient extends ChangeNotifier {
  // 可观察状态
  List<RelaySession> activeSessions = [];
  List<RelaySession> historySessions = [];
  bool gatewayUnavailable = false;
  String status = '';
  bool hasLoaded = false;

  // 内部状态
  WebSocket? _ws;
  Timer? _listTimer;
  Timer? _reconnectTimer;
  int _backoffIndex = 0;
  bool _disposed = false;
  String? _relayUrl;
  String? _accessToken;

  // latestEventId 游标 — 存于服务层，不在 widget — per RESEARCH.md Pitfall 4 / D-24
  final Map<String, int> _latestEventIds = {};

  // 当前订阅的 sessionId（用于重连后重新订阅）
  String? _subscribedSessionId;
  RelayClientMode? _subscribedMode;

  // 公开游标接口（供 terminal_screen 使用）
  int? getLatestEventId(String sessionId) => _latestEventIds[sessionId];

  void _updateLatestEventId(String sessionId, int eventId) {
    if ((_latestEventIds[sessionId] ?? -1) < eventId) {
      _latestEventIds[sessionId] = eventId;
    }
  }

  Future<void> connect(String relayUrl, String accessToken) async {
    _relayUrl = relayUrl;
    _accessToken = accessToken;
    _reconnectTimer?.cancel();
    _ws?.close();
    _ws = null;

    _updateStatus('connecting');
    hasLoaded = false;
    notifyListeners();

    try {
      // 构建 Relay WS URL: relayUrl/client (镜像 buildRelayClientUrl)
      final uri = _buildRelayUri(relayUrl);
      _ws = await WebSocket.connect(uri);
    } catch (e) {
      _scheduleReconnect();
      return;
    }

    // 发送 client.auth (镜像 ws.addEventListener('open', ...))
    _sendFrame(ClientAuth(token: accessToken));
    _updateStatus('auth');

    _ws!.listen(
      _handleMessage,
      onDone: _onClose,
      onError: (_) => _onClose(),
      cancelOnError: false,
    );
  }

  String _buildRelayUri(String relayUrl) {
    final uri = Uri.parse(relayUrl.trim());
    final wsScheme = uri.scheme == 'https' ? 'wss' : 'ws';
    final path = '${uri.path.replaceAll(RegExp(r'/$'), '')}/client';
    return Uri(scheme: wsScheme, host: uri.host, port: uri.port, path: path).toString();
  }

  void _handleMessage(dynamic data) {
    if (_disposed) return;
    if (data is! String) return;
    Map<String, dynamic> raw;
    try {
      raw = jsonDecode(data) as Map<String, dynamic>;
    } catch (_) {
      return;
    }

    final RelayServerToClientFrame frame;
    try {
      frame = RelayServerToClientFrame.fromJson(raw);
    } on FormatException {
      return; // 忽略未知帧类型
    }

    switch (frame) {
      case ClientAuthOk(:final clientId):
        // 镜像 web: setStatus(`Relay · ${clientId.slice(0, 8)}`); sendList(); setInterval(sendList, 3000)
        gatewayUnavailable = false;
        _updateStatus('relay:${clientId.substring(0, clientId.length.clamp(0, 8))}');
        _backoffIndex = 0; // 重置退避
        _sendFrame(const ClientList());
        _listTimer?.cancel();
        _listTimer = Timer.periodic(const Duration(seconds: 3), (_) {
          if (_ws?.readyState == WebSocket.open) {
            _sendFrame(const ClientList());
          }
        });

      case ClientAuthFailed(:final message):
        _updateStatus(_displayMessage(message));
        hasLoaded = true;
        _ws?.close();

      case Sessions(:final sessions):
        // 镜像 web: splitActiveSessions(frame.sessions)
        final split = splitSessions(sessions);
        activeSessions = split.active;
        historySessions = split.history;
        gatewayUnavailable = false;
        hasLoaded = true;
        notifyListeners();

      case TetherError(:final code, :final message):
        if (code == 'gateway_unavailable') {
          // 镜像 web: setSessions([]); setRelayGatewayUnavailable(true)
          activeSessions = [];
          historySessions = [];
          gatewayUnavailable = true;
          hasLoaded = true;
          notifyListeners();
        } else {
          _updateStatus(_displayMessage(message));
          hasLoaded = true;
          notifyListeners();
        }

      case Event(:final event):
        _updateLatestEventId(event.sessionId, event.id);
        // UI 通知由 terminal_screen 直接监听 stream，此处仅更新游标
        notifyListeners();

      case ReplayDone(:final sessionId, :final latestEventId):
        _updateLatestEventId(sessionId, latestEventId);
        notifyListeners();

      case Hello():
        break; // 忽略 hello 帧

      case _:
        break;
    }
  }

  void _onClose() {
    if (_disposed) return;
    _listTimer?.cancel();
    _listTimer = null;
    _updateStatus('closed');
    hasLoaded = true;
    notifyListeners();
    _scheduleReconnect();
  }

  void _scheduleReconnect() {
    if (_disposed) return;
    final delay = _computeBackoff(_backoffIndex);
    if (_backoffIndex < _backoffSeconds.length - 1) _backoffIndex++;
    _reconnectTimer = Timer(Duration(seconds: delay), () {
      if (!_disposed && _relayUrl != null && _accessToken != null) {
        connect(_relayUrl!, _accessToken!);
      }
    });
  }

  // D-23: 从后台回来时立即重连
  void reconnectIfNeeded() {
    if (_ws == null || _ws!.readyState != WebSocket.open) {
      if (_relayUrl != null && _accessToken != null) {
        _reconnectTimer?.cancel();
        connect(_relayUrl!, _accessToken!);
      }
    }
  }

  // 订阅 session — D-24: 使用 latestEventId 作为 after 参数
  void subscribe(String sessionId, RelayClientMode mode) {
    _subscribedSessionId = sessionId;
    _subscribedMode = mode;
    final after = _latestEventIds[sessionId];
    _sendFrame(ClientSubscribe(sessionId: sessionId, mode: mode, after: after));
  }

  // Replay 订阅 — D-37: mode='observe', after=0
  void subscribeReplay(String sessionId) {
    _sendFrame(ClientSubscribe(sessionId: sessionId, mode: RelayClientMode.observe, after: 0));
  }

  void sendInput(String sessionId, String data) {
    _sendFrame(ClientInput(sessionId: sessionId, data: data));
  }

  void sendResize(String sessionId, int cols, int rows) {
    _sendFrame(ClientResize(sessionId: sessionId, cols: cols, rows: rows));
  }

  // 停止 session — 镜像 web stopSession (lines 558-570)
  // 先 subscribe(control) 再 stop
  void stopSession(String sessionId) {
    _sendFrame(ClientSubscribe(sessionId: sessionId, mode: RelayClientMode.control));
    _sendFrame(ClientStop(sessionId: sessionId));
    // 乐观更新 UI（与 web 行为一致）
    activeSessions = activeSessions.where((s) => s.id != sessionId).toList();
    notifyListeners();
  }

  void detach(String sessionId) {
    _sendFrame(ClientDetach(sessionId: sessionId));
  }

  void _sendFrame(RelayClientToServerFrame frame) {
    if (_ws?.readyState == WebSocket.open) {
      _ws!.add(jsonEncode(frame.toJson()));
    }
  }

  void _updateStatus(String s) {
    status = s;
    notifyListeners();
  }

  @override
  void dispose() {
    _disposed = true;
    _listTimer?.cancel();
    _reconnectTimer?.cancel();
    _ws?.close();
    super.dispose();
  }
}
```

**创建 native/flutter/test/relay_client_test.dart**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:tether/services/relay_client.dart';
import 'package:tether/models/protocol.dart';

void main() {
  group('_computeBackoff', () {
    test('index 0 → 1s', () => expect(_computeBackoff(0), 1));
    test('index 1 → 2s', () => expect(_computeBackoff(1), 2));
    test('index 4 → 16s', () => expect(_computeBackoff(4), 16));
    test('index 100 capped at 30s', () => expect(_computeBackoff(100), 30));
  });

  group('splitSessions', () {
    RelaySession makeSession(String id, RelaySessionStatus status) => RelaySession(
      id: id, provider: 'test', title: 'T', projectPath: '/',
      status: status, transport: 'pty-event-stream', lastActiveAt: 0,
    );

    test('running sessions go to active', () {
      final sessions = [
        makeSession('s1', RelaySessionStatus.running),
        makeSession('s2', RelaySessionStatus.stopped),
      ];
      final result = splitSessions(sessions);
      expect(result.active.length, 1);
      expect(result.active.first.id, 's1');
      expect(result.history.length, 1);
    });

    test('all running → history empty', () {
      final sessions = [
        makeSession('s1', RelaySessionStatus.running),
        makeSession('s2', RelaySessionStatus.running),
      ];
      final result = splitSessions(sessions);
      expect(result.history, isEmpty);
    });

    test('history capped at 8', () {
      final sessions = List.generate(
          10, (i) => makeSession('h$i', RelaySessionStatus.stopped));
      final result = splitSessions(sessions);
      expect(result.history.length, 8);
    });
  });

  group('RelayClient latestEventId', () {
    test('getLatestEventId returns null for unknown sessionId', () {
      final client = RelayClient();
      expect(client.getLatestEventId('unknown'), isNull);
      client.dispose();
    });

    test('updateLatestEventId sets initial value', () {
      final client = RelayClient();
      client._updateLatestEventIdForTest('s1', 42);
      expect(client.getLatestEventId('s1'), 42);
      client.dispose();
    });

    test('updateLatestEventId updates to higher value', () {
      final client = RelayClient();
      client._updateLatestEventIdForTest('s1', 10);
      client._updateLatestEventIdForTest('s1', 50);
      expect(client.getLatestEventId('s1'), 50);
      client.dispose();
    });
  });
}

// 测试辅助扩展：暴露内部方法供单元测试
extension RelayClientTestHelper on RelayClient {
  void _updateLatestEventIdForTest(String sessionId, int eventId) {
    _updateLatestEventId(sessionId, eventId);
  }
}
```

注意：`_updateLatestEventId` 是私有方法，在 test extension 中通过 `_` 前缀访问（Dart 同文件可访问）。由于 test 文件与源文件不在同一文件，需要将 `_updateLatestEventId` 改为 `@visibleForTesting` 并公开名称，或者将测试 extension 方法直接在 RelayClient 上添加一个 `@visibleForTesting` 的包装方法：

在 relay_client.dart 中添加：
```dart
@visibleForTesting
void updateLatestEventIdForTest(String sessionId, int eventId) {
  _updateLatestEventId(sessionId, eventId);
}
```

然后在 test 中调用 `client.updateLatestEventIdForTest('s1', 42)`。
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether/native/flutter && flutter test test/relay_client_test.dart --no-pub 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/relay_client_test.dart --no-pub` 所有测试通过
    - `grep -c "splitSessions" native/flutter/lib/services/relay_client.dart` 返回至少 1（函数定义）
    - `grep -c "_latestEventIds" native/flutter/lib/services/relay_client.dart` 返回至少 2（声明 + 使用）
    - `grep -c "_backoffSeconds" native/flutter/lib/services/relay_client.dart` 返回至少 1
    - `grep -c "Timer.periodic" native/flutter/lib/services/relay_client.dart` 返回 1（3s list timer）
    - `grep -c "RelayServerToClientFrame.fromJson" native/flutter/lib/services/relay_client.dart` 返回 1
    - `grep -c "gateway_unavailable" native/flutter/lib/services/relay_client.dart` 返回 1
    - `grep -c "reconnectIfNeeded" native/flutter/lib/services/relay_client.dart` 返回 1
    - `flutter analyze --no-pub 2>&1 | grep -c " error "` 返回 0
  </acceptance_criteria>
  <done>relay_client_test.dart 全部通过；RelayClient 指数退避、latestEventId 游标、splitSessions 逻辑全部实现；flutter analyze 无错误</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → apps/server | HTTP/HTTPS；login/register/validate/refresh 端点 |
| App → Relay WS | WSS；client.auth 帧携带 access token |
| flutter_secure_storage → OS Keychain/Keystore | token 静态加密存储 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | auth_service.dart token 存储 | mitigate | token 只写入 flutter_secure_storage（iOS Keychain / Android Keystore），禁用 SharedPreferences。grep 检查 `SharedPreferences` 出现次数为 0 在 auth_service.dart 中（acceptance_criteria 已包含此检查） |
| T-09-02-02 | Information Disclosure | auth_service.dart 日志 | mitigate | 生产代码中不 debugPrint/print token 字符串。action 代码中无 print(token) 调用 |
| T-09-02-03 | Tampering | relay_client.dart fromJson 解析 | mitigate | 未知帧类型抛出 FormatException 并被 try/catch 捕获后忽略（不崩溃），已在 _handleMessage 实现 |
| T-09-02-04 | Spoofing | relay_client connect WSS | mitigate | HTTPS/WSS 由调用方 URL 保证。_buildRelayUri 保留 wss: scheme，不接受非 ws/wss 协议 |
| T-09-02-05 | Denial of Service | relay_client 无限重连 | accept | 退避上限 30s，_disposed 标志防止 dispose 后重连。在大多数产品场景下这是可接受的 |
| T-09-02-06 | Elevation of Privilege | stopSession 无鉴权检查 | accept | stopSession 通过已鉴权的 WS 发送，Relay 服务端执行权限检查；客户端不需要重复验证 |
</threat_model>

<verification>
在 native/flutter/ 下：
```bash
flutter test test/auth_service_test.dart test/relay_client_test.dart --no-pub
flutter analyze --no-pub
```

额外检查：
```bash
grep -c "SharedPreferences" native/flutter/lib/services/auth_service.dart  # 必须为 0
grep -c "FlutterSecureStorage" native/flutter/lib/services/auth_service.dart  # 必须 >= 2
```
</verification>

<success_criteria>
- auth_service_test.dart 和 relay_client_test.dart 全部测试通过
- flutter analyze 无 error 级别问题
- auth_service.dart 中 token 存储零 SharedPreferences 调用
- relay_client.dart splitSessions 与 web 实现逻辑一致
- 指数退避序列 [1,2,4,8,16,30] 有单元测试覆盖
</success_criteria>

<output>
完成后创建 `.planning/phases/09-flutter-client-app/09-02-SUMMARY.md`
</output>
