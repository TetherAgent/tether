---
phase: 09-flutter-client-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - native/flutter/pubspec.yaml
  - native/flutter/lib/main.dart
  - native/flutter/lib/app_shell.dart
  - native/flutter/lib/theme.dart
  - native/flutter/lib/models/protocol.dart
  - native/flutter/lib/l10n/app_zh.arb
  - native/flutter/lib/l10n/app_en.arb
  - native/flutter/l10n.yaml
  - native/flutter/test/protocol_test.dart
  - packages/protocol/scripts/gen-dart.sh
autonomous: true
requirements: [D-01, D-02, D-04a, D-04b, D-19, D-20, D-28, D-29, D-30, D-31, D-32, D-33, D-39, D-40]

must_haves:
  truths:
    - "flutter analyze 在 native/flutter/ 下退出 0，无 error"
    - "flutter test test/protocol_test.dart 退出 0，16 个测试全部通过（8 client→server + 8 server→client round-trip + unknown type 抛 FormatException）"
    - "flutter build apk --debug 从 native/flutter/ 退出 0（Android smoke test）"
    - "pnpm typecheck 在 repo 根目录不受影响"
    - "Dart 协议类型与 packages/protocol/src/index.ts 完全对齐：8 个 RelayClientToServerFrame 变体（含 client.chat）+ 8 个 RelayServerToClientFrame 变体（含 replay.output、replay.done）"
    - "main.dart 使用 BottomNavigationBar shell（2 个 Tab：Sessions、Settings），不是纯堆栈路由"
    - "RelaySession.agentSessionId 和 RelayAuthScope 在 protocol.dart 中存在"
  artifacts:
    - path: "native/flutter/pubspec.yaml"
      provides: "全部依赖：xterm ^4.0.0, flutter_secure_storage ^10.0.0, dio ^5.9.2, shared_preferences ^2.3.0, web_socket_client ^0.2.1, go_router ^14.0.0, flutter_localizations, intl ^0.19.0, provider ^6.1.2；dependency_overrides: flutter_secure_storage_ohos ^1.0.0；generate: true"
    - path: "native/flutter/lib/models/protocol.dart"
      provides: "Dart 3 sealed class：RelayClientToServerFrame（8 个变体含 ClientChat）、RelayServerToClientFrame（8 个变体含 ReplayOutput、ReplayDone）、RelaySession（含 agentSessionId?）、RelayAuthScope、RelayTerminalEvent、RelaySessionStatus、RelayAuthTokenClass、RelayClientMode；未知 type 抛 FormatException"
    - path: "native/flutter/lib/main.dart"
      provides: "TetherApp 入口：ThemeMode.system 默认值，MultiProvider（ThemeNotifier + LocaleNotifier + AuthService）；GoRouter 配置：/login → /register → AppShell（底部 Tab）→ /session/:id → /replay/:id"
    - path: "native/flutter/lib/app_shell.dart"
      provides: "AppShell StatefulWidget：BottomNavigationBar 2 个 Tab（Sessions 图标 session，Settings 图标 settings），IndexedStack 保持两个子页面状态"
    - path: "native/flutter/lib/theme.dart"
      provides: "9 个 const Color 标记（tetherBackground/Surface/Border/InputBorder/Accent/Foreground/Muted/Destructive）；TerminalTheme _tetherTerminalTheme（23 色）；ThemeNotifier；LocaleNotifier"
    - path: "native/flutter/lib/l10n/app_zh.arb"
      provides: "全部 zh 文案键（含 chatTab、terminalTab、chatSend、thinkingLabel、toolCallLabel、selectPrompt 等 chat-first 相关键）"
    - path: "native/flutter/test/protocol_test.dart"
      provides: "16 个协议 round-trip 测试：全部 8 个 client→server variant toJson + 全部 8 个 server→client variant fromJson + unknown type throws FormatException"
    - path: "packages/protocol/scripts/gen-dart.sh"
      provides: "Dart codegen 占位脚本（可执行，exit 0）"
  key_links:
    - from: "native/flutter/lib/main.dart"
      to: "native/flutter/lib/models/protocol.dart"
      via: "import 'models/protocol.dart'"
      pattern: "import.*models/protocol"
    - from: "native/flutter/lib/main.dart"
      to: "native/flutter/lib/app_shell.dart"
      via: "GoRoute path: '/shell' → AppShell"
      pattern: "AppShell"
    - from: "native/flutter/pubspec.yaml"
      to: "go_router: \\^14"
      via: "dependencies section"
      pattern: "go_router"
    - from: "native/flutter/pubspec.yaml"
      to: "flutter_secure_storage_ohos"
      via: "dependency_overrides"
      pattern: "flutter_secure_storage_ohos"
---

<objective>
为 Tether Flutter 客户端建立项目基础：更新 pubspec.yaml（含 go_router）、创建 8+8 Dart sealed class 协议类型（与 packages/protocol/src/index.ts 完全对齐）、实现 BottomNavigationBar 主 Shell 骨架（D-19/D-20）、以及完整的 i18n ARB 文件（含 chat-first 文案键）。

Purpose: 所有后续 Wave 都依赖这个基础——协议类型的正确性决定 RelayClient 行为，BottomNav Shell 决定导航模型，ARB 文件决定所有屏幕的文案。当前 packages/protocol/src/index.ts 已是 8+8 变体（含 client.chat / replay.output / replay.done），旧版 7+7 会导致运行时类型不兼容。

Output: 可通过 flutter analyze + flutter test + flutter build apk --debug 的骨架工程；Dart 协议类型与 TypeScript 源完全对齐（8+8）；BottomNav Shell 就绪；ARB 覆盖 chat-first 文案。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-flutter-client-app/09-CONTEXT.md
@.planning/phases/09-flutter-client-app/09-RESEARCH.md
@.planning/phases/09-flutter-client-app/09-PATTERNS.md

<interfaces>
<!-- 协议类型来源 — packages/protocol/src/index.ts（当前真实版本，8+8） -->

<!-- RelayClientToServerFrame — 8 个变体 -->
<!-- client.auth: token?, ticket?, scope?: RelayAuthScope, secret? -->
<!-- client.list -->
<!-- client.subscribe: sessionId, after?, tail?, mode, cols?, rows? -->
<!-- client.input: sessionId, data -->
<!-- client.resize: sessionId, cols, rows -->
<!-- client.stop: sessionId -->
<!-- client.detach: sessionId -->
<!-- client.chat: sessionId, message  ← 新增，Plan 01 必须包含 -->

<!-- RelayServerToClientFrame — 8 个变体 -->
<!-- client.auth.ok: clientId -->
<!-- client.auth.failed: code, message -->
<!-- sessions: sessions: RelaySession[] -->
<!-- hello: clientId, gatewayId? -->
<!-- event: event: RelayTerminalEvent -->
<!-- replay.output: sessionId, data, latestEventId  ← 新增 -->
<!-- replay.done: sessionId, latestEventId -->
<!-- error: sessionId?, code, message -->

<!-- RelaySession 字段 -->
<!-- id, provider, title, projectPath, accountId?, workspaceId?, gatewayId?, userId? -->
<!-- agentSessionId?  ← 新增，必须包含 -->
<!-- status: RelaySessionStatus, transport: 'pty-event-stream'|'tmux', lastActiveAt -->

<!-- RelayAuthScope 字段 -->
<!-- accountId, workspaceId, gatewayId?, sessionId?, userId?, adminUserId?, deviceId? -->
<!-- mode?: RelayClientMode, tokenClass: RelayAuthTokenClass, expiresAt, jti -->

<!-- RelayAuthTokenClass union（7 个变体） -->
<!-- 'normal_client_access' | 'normal_client_refresh' | 'management_access' -->
<!-- 'management_refresh' | 'gateway_access' | 'gateway_refresh' | 'ws_ticket' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pubspec.yaml + protocol.dart（8+8）+ 协议单元测试</name>
  <read_first>
    - native/flutter/pubspec.yaml（当前状态，了解现有依赖）
    - packages/protocol/src/index.ts（协议类型精确来源——8+8 变体，含 RelayAuthScope、agentSessionId、client.chat、replay.output）
    - .planning/phases/09-flutter-client-app/09-RESEARCH.md（Standard Stack 版本；Pattern 3 sealed class 模式；Pattern 4 OHOS secure storage override）
    - native/flutter/test/protocol_test.dart（当前测试文件，将完整替换）
  </read_first>
  <files>
    native/flutter/pubspec.yaml,
    native/flutter/lib/models/protocol.dart,
    native/flutter/test/protocol_test.dart
  </files>
  <behavior>
    - Test 1: ClientAuth.toJson() 含 token 时返回 {type:'client.auth', token:'tok'}
    - Test 2: ClientAuth.toJson() 含 ticket 时返回 {type:'client.auth', ticket:'tkt'}
    - Test 3: ClientList.toJson() 返回 {type:'client.list'}
    - Test 4: ClientSubscribe.toJson() 含 sessionId/mode/after 字段
    - Test 5: ClientChat.toJson() 返回 {type:'client.chat', sessionId:'s1', message:'hello'}
    - Test 6: ClientStop.toJson() 返回 {type:'client.stop', sessionId:'sid'}
    - Test 7: ClientDetach.toJson() 返回 {type:'client.detach', sessionId:'sid'}
    - Test 8: ClientResize.toJson() 返回 {type:'client.resize', sessionId:'s', cols:80, rows:24}
    - Test 9: fromJson client.auth.ok → ClientAuthOk(clientId:'abc')
    - Test 10: fromJson client.auth.failed → ClientAuthFailed(code, message)
    - Test 11: fromJson sessions → Sessions，包含正确解析的 RelaySession 列表（含 agentSessionId）
    - Test 12: fromJson replay.output → ReplayOutput(sessionId, data, latestEventId)
    - Test 13: fromJson replay.done → ReplayDone(sessionId, latestEventId)
    - Test 14: fromJson error → TetherError(code, message, sessionId?)
    - Test 15: fromJson hello → Hello(clientId, gatewayId?)
    - Test 16: fromJson 未知 type → 抛出 FormatException（不静默 fallback）
  </behavior>
  <action>
**第一步：更新 native/flutter/pubspec.yaml**

完整替换为（保留现有 name/description/version 字段值）：

```yaml
name: tether
description: "Tether Flutter client — remote agent session viewer and controller."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  xterm: ^4.0.0
  flutter_secure_storage: ^10.0.0
  dio: ^5.9.2
  shared_preferences: ^2.3.0
  web_socket_client: ^0.2.1
  go_router: ^14.0.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  provider: ^6.1.2
  flutter_markdown: ^0.7.6

dependency_overrides:
  flutter_secure_storage_ohos: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
```

注意：`go_router: ^14.0.0` 用于 BottomNav Shell 路由（D-19/D-20）；`flutter_markdown` 用于 ChatSessionSurface 的 markdown 渲染。

**第二步：创建 native/flutter/lib/models/protocol.dart**

严格镜像 packages/protocol/src/index.ts（8+8 变体，当前真实版本）：

```dart
// lib/models/protocol.dart
// Dart 3 sealed class 协议类型 — 镜像 packages/protocol/src/index.ts（8+8 变体）
// 手写版本；生成脚本见 packages/protocol/scripts/gen-dart.sh
// 重要：未知 type 必须抛 FormatException，不允许静默 fallback（Codex Review Issue 1）

enum RelaySessionStatus {
  running, stopped, completed, failed, lost;

  static RelaySessionStatus fromString(String s) => switch (s) {
    'running'   => RelaySessionStatus.running,
    'stopped'   => RelaySessionStatus.stopped,
    'completed' => RelaySessionStatus.completed,
    'failed'    => RelaySessionStatus.failed,
    'lost'      => RelaySessionStatus.lost,
    final v     => throw FormatException('Unknown RelaySessionStatus: $v'),
  };

  String toJson() => name;
}

enum RelayClientMode {
  control, observe;

  static RelayClientMode fromString(String s) => switch (s) {
    'control' => RelayClientMode.control,
    'observe' => RelayClientMode.observe,
    final v   => throw FormatException('Unknown RelayClientMode: $v'),
  };

  String toJson() => name;
}

// RelayAuthTokenClass — 7 个变体（镜像 TS union）
enum RelayAuthTokenClass {
  normalClientAccess,
  normalClientRefresh,
  managementAccess,
  managementRefresh,
  gatewayAccess,
  gatewayRefresh,
  wsTicket;

  static RelayAuthTokenClass fromString(String s) => switch (s) {
    'normal_client_access'   => RelayAuthTokenClass.normalClientAccess,
    'normal_client_refresh'  => RelayAuthTokenClass.normalClientRefresh,
    'management_access'      => RelayAuthTokenClass.managementAccess,
    'management_refresh'     => RelayAuthTokenClass.managementRefresh,
    'gateway_access'         => RelayAuthTokenClass.gatewayAccess,
    'gateway_refresh'        => RelayAuthTokenClass.gatewayRefresh,
    'ws_ticket'              => RelayAuthTokenClass.wsTicket,
    final v                  => throw FormatException('Unknown RelayAuthTokenClass: $v'),
  };

  String toJson() => switch (this) {
    RelayAuthTokenClass.normalClientAccess  => 'normal_client_access',
    RelayAuthTokenClass.normalClientRefresh => 'normal_client_refresh',
    RelayAuthTokenClass.managementAccess    => 'management_access',
    RelayAuthTokenClass.managementRefresh   => 'management_refresh',
    RelayAuthTokenClass.gatewayAccess       => 'gateway_access',
    RelayAuthTokenClass.gatewayRefresh      => 'gateway_refresh',
    RelayAuthTokenClass.wsTicket            => 'ws_ticket',
  };
}

// RelayAuthScope — 镜像 TS RelayAuthScope 类型
final class RelayAuthScope {
  final String accountId;
  final String workspaceId;
  final String? gatewayId;
  final String? sessionId;
  final String? userId;
  final String? adminUserId;
  final String? deviceId;
  final RelayClientMode? mode;
  final RelayAuthTokenClass tokenClass;
  final int expiresAt;
  final String jti;

  const RelayAuthScope({
    required this.accountId,
    required this.workspaceId,
    required this.tokenClass,
    required this.expiresAt,
    required this.jti,
    this.gatewayId,
    this.sessionId,
    this.userId,
    this.adminUserId,
    this.deviceId,
    this.mode,
  });

  factory RelayAuthScope.fromJson(Map<String, dynamic> json) => RelayAuthScope(
    accountId:   json['accountId'] as String,
    workspaceId: json['workspaceId'] as String,
    tokenClass:  RelayAuthTokenClass.fromString(json['tokenClass'] as String),
    expiresAt:   json['expiresAt'] as int,
    jti:         json['jti'] as String,
    gatewayId:   json['gatewayId'] as String?,
    sessionId:   json['sessionId'] as String?,
    userId:      json['userId'] as String?,
    adminUserId: json['adminUserId'] as String?,
    deviceId:    json['deviceId'] as String?,
    mode:        json['mode'] != null
        ? RelayClientMode.fromString(json['mode'] as String)
        : null,
  );

  Map<String, dynamic> toJson() => {
    'accountId':  accountId,
    'workspaceId': workspaceId,
    'tokenClass': tokenClass.toJson(),
    'expiresAt':  expiresAt,
    'jti':        jti,
    if (gatewayId != null)   'gatewayId':   gatewayId,
    if (sessionId != null)   'sessionId':   sessionId,
    if (userId != null)      'userId':      userId,
    if (adminUserId != null) 'adminUserId': adminUserId,
    if (deviceId != null)    'deviceId':    deviceId,
    if (mode != null)        'mode':        mode!.toJson(),
  };
}

// RelaySession — 含 agentSessionId?（Codex Review Issue 1）
final class RelaySession {
  final String id;
  final String provider;
  final String title;
  final String projectPath;
  final String? accountId;
  final String? workspaceId;
  final String? gatewayId;
  final String? userId;
  final String? agentSessionId; // 新增，镜像 TS RelaySession.agentSessionId?
  final RelaySessionStatus status;
  final String transport; // 'pty-event-stream' | 'tmux'
  final int lastActiveAt;

  const RelaySession({
    required this.id,
    required this.provider,
    required this.title,
    required this.projectPath,
    required this.status,
    required this.transport,
    required this.lastActiveAt,
    this.accountId,
    this.workspaceId,
    this.gatewayId,
    this.userId,
    this.agentSessionId,
  });

  factory RelaySession.fromJson(Map<String, dynamic> json) => RelaySession(
    id:             json['id'] as String,
    provider:       json['provider'] as String,
    title:          json['title'] as String,
    projectPath:    json['projectPath'] as String,
    status:         RelaySessionStatus.fromString(json['status'] as String),
    transport:      json['transport'] as String,
    lastActiveAt:   json['lastActiveAt'] as int,
    accountId:      json['accountId'] as String?,
    workspaceId:    json['workspaceId'] as String?,
    gatewayId:      json['gatewayId'] as String?,
    userId:         json['userId'] as String?,
    agentSessionId: json['agentSessionId'] as String?,
  );
}

final class RelayTerminalEvent {
  final int id;
  final String sessionId;
  final String type;
  final int ts;
  final Map<String, dynamic> payload;

  const RelayTerminalEvent({
    required this.id,
    required this.sessionId,
    required this.type,
    required this.ts,
    required this.payload,
  });

  factory RelayTerminalEvent.fromJson(Map<String, dynamic> json) => RelayTerminalEvent(
    id:        json['id'] as int,
    sessionId: json['sessionId'] as String,
    type:      json['type'] as String,
    ts:        json['ts'] as int,
    payload:   Map<String, dynamic>.from(json['payload'] as Map),
  );
}

// ---- RelayClientToServerFrame — 8 个变体 ----

sealed class RelayClientToServerFrame {
  const RelayClientToServerFrame();
  Map<String, dynamic> toJson();
}

final class ClientAuth extends RelayClientToServerFrame {
  final String? token;
  final String? ticket;
  final RelayAuthScope? scope;
  final String? secret;
  const ClientAuth({this.token, this.ticket, this.scope, this.secret});
  @override
  Map<String, dynamic> toJson() => {
    'type': 'client.auth',
    if (token != null)  'token':  token,
    if (ticket != null) 'ticket': ticket,
    if (scope != null)  'scope':  scope!.toJson(),
    if (secret != null) 'secret': secret,
  };
}

final class ClientList extends RelayClientToServerFrame {
  const ClientList();
  @override
  Map<String, dynamic> toJson() => {'type': 'client.list'};
}

final class ClientSubscribe extends RelayClientToServerFrame {
  final String sessionId;
  final int? after;
  final int? tail;
  final RelayClientMode mode;
  final int? cols;
  final int? rows;
  const ClientSubscribe({
    required this.sessionId,
    required this.mode,
    this.after,
    this.tail,
    this.cols,
    this.rows,
  });
  @override
  Map<String, dynamic> toJson() => {
    'type': 'client.subscribe',
    'sessionId': sessionId,
    'mode': mode.toJson(),
    if (after != null) 'after': after,
    if (tail != null)  'tail':  tail,
    if (cols != null)  'cols':  cols,
    if (rows != null)  'rows':  rows,
  };
}

final class ClientInput extends RelayClientToServerFrame {
  final String sessionId;
  final String data;
  const ClientInput({required this.sessionId, required this.data});
  @override
  Map<String, dynamic> toJson() => {
    'type': 'client.input',
    'sessionId': sessionId,
    'data': data,
  };
}

final class ClientResize extends RelayClientToServerFrame {
  final String sessionId;
  final int cols;
  final int rows;
  const ClientResize({required this.sessionId, required this.cols, required this.rows});
  @override
  Map<String, dynamic> toJson() => {
    'type': 'client.resize',
    'sessionId': sessionId,
    'cols': cols,
    'rows': rows,
  };
}

final class ClientStop extends RelayClientToServerFrame {
  final String sessionId;
  const ClientStop({required this.sessionId});
  @override
  Map<String, dynamic> toJson() => {'type': 'client.stop', 'sessionId': sessionId};
}

final class ClientDetach extends RelayClientToServerFrame {
  final String sessionId;
  const ClientDetach({required this.sessionId});
  @override
  Map<String, dynamic> toJson() => {'type': 'client.detach', 'sessionId': sessionId};
}

// client.chat — 新增变体（Codex Review Issue 1）
final class ClientChat extends RelayClientToServerFrame {
  final String sessionId;
  final String message;
  const ClientChat({required this.sessionId, required this.message});
  @override
  Map<String, dynamic> toJson() => {
    'type': 'client.chat',
    'sessionId': sessionId,
    'message': message,
  };
}

// ---- RelayServerToClientFrame — 8 个变体 ----

sealed class RelayServerToClientFrame {
  const RelayServerToClientFrame();

  factory RelayServerToClientFrame.fromJson(Map<String, dynamic> json) {
    final type = json['type'] as String?;
    return switch (type) {
      'client.auth.ok'     => ClientAuthOk(clientId: json['clientId'] as String),
      'client.auth.failed' => ClientAuthFailed(
          code: json['code'] as String,
          message: json['message'] as String),
      'sessions'           => Sessions(
          sessions: (json['sessions'] as List)
              .map((e) => RelaySession.fromJson(e as Map<String, dynamic>))
              .toList()),
      'hello'              => Hello(
          clientId:  json['clientId'] as String,
          gatewayId: json['gatewayId'] as String?),
      'event'              => Event(
          event: RelayTerminalEvent.fromJson(json['event'] as Map<String, dynamic>)),
      'replay.output'      => ReplayOutput(  // 新增（Codex Review Issue 1）
          sessionId:     json['sessionId'] as String,
          data:          json['data'] as String,
          latestEventId: json['latestEventId'] as int),
      'replay.done'        => ReplayDone(
          sessionId:     json['sessionId'] as String,
          latestEventId: json['latestEventId'] as int),
      'error'              => TetherError(
          code:      json['code'] as String,
          message:   json['message'] as String,
          sessionId: json['sessionId'] as String?),
      // 未知 type：抛出 FormatException，不静默 fallback（Codex Review Issue 1）
      final t              => throw FormatException('Unknown Relay frame type: $t'),
    };
  }
}

final class ClientAuthOk extends RelayServerToClientFrame {
  final String clientId;
  const ClientAuthOk({required this.clientId});
}

final class ClientAuthFailed extends RelayServerToClientFrame {
  final String code;
  final String message;
  const ClientAuthFailed({required this.code, required this.message});
}

final class Sessions extends RelayServerToClientFrame {
  final List<RelaySession> sessions;
  const Sessions({required this.sessions});
}

final class Hello extends RelayServerToClientFrame {
  final String clientId;
  final String? gatewayId;
  const Hello({required this.clientId, this.gatewayId});
}

final class Event extends RelayServerToClientFrame {
  final RelayTerminalEvent event;
  const Event({required this.event});
}

// replay.output — 新增（Codex Review Issue 1）
final class ReplayOutput extends RelayServerToClientFrame {
  final String sessionId;
  final String data;
  final int latestEventId;
  const ReplayOutput({
    required this.sessionId,
    required this.data,
    required this.latestEventId,
  });
}

final class ReplayDone extends RelayServerToClientFrame {
  final String sessionId;
  final int latestEventId;
  const ReplayDone({required this.sessionId, required this.latestEventId});
}

final class TetherError extends RelayServerToClientFrame {
  final String code;
  final String message;
  final String? sessionId;
  const TetherError({required this.code, required this.message, this.sessionId});
}
```

**第三步：创建 native/flutter/test/protocol_test.dart（16 个测试）**

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:tether/models/protocol.dart';

void main() {
  // ---- client→server（8 个变体 toJson）----
  group('RelayClientToServerFrame.toJson', () {
    test('Test 1: ClientAuth with token', () {
      final f = ClientAuth(token: 'tok');
      expect(f.toJson(), {'type': 'client.auth', 'token': 'tok'});
    });

    test('Test 2: ClientAuth with ticket', () {
      final f = ClientAuth(ticket: 'tkt');
      expect(f.toJson(), {'type': 'client.auth', 'ticket': 'tkt'});
    });

    test('Test 3: ClientList', () {
      expect(const ClientList().toJson(), {'type': 'client.list'});
    });

    test('Test 4: ClientSubscribe includes sessionId/mode/after', () {
      final f = ClientSubscribe(
        sessionId: 's1',
        mode: RelayClientMode.control,
        after: 42,
      );
      final j = f.toJson();
      expect(j['type'], 'client.subscribe');
      expect(j['sessionId'], 's1');
      expect(j['mode'], 'control');
      expect(j['after'], 42);
    });

    test('Test 5: ClientChat', () {
      final f = ClientChat(sessionId: 's1', message: 'hello');
      expect(f.toJson(), {
        'type': 'client.chat',
        'sessionId': 's1',
        'message': 'hello',
      });
    });

    test('Test 6: ClientStop', () {
      expect(ClientStop(sessionId: 'sid').toJson(), {
        'type': 'client.stop',
        'sessionId': 'sid',
      });
    });

    test('Test 7: ClientDetach', () {
      expect(ClientDetach(sessionId: 'sid').toJson(), {
        'type': 'client.detach',
        'sessionId': 'sid',
      });
    });

    test('Test 8: ClientResize', () {
      expect(ClientResize(sessionId: 's', cols: 80, rows: 24).toJson(), {
        'type': 'client.resize',
        'sessionId': 's',
        'cols': 80,
        'rows': 24,
      });
    });
  });

  // ---- server→client（8 个变体 fromJson）----
  group('RelayServerToClientFrame.fromJson', () {
    test('Test 9: client.auth.ok', () {
      final f = RelayServerToClientFrame.fromJson({
        'type': 'client.auth.ok',
        'clientId': 'abc',
      });
      expect(f, isA<ClientAuthOk>());
      expect((f as ClientAuthOk).clientId, 'abc');
    });

    test('Test 10: client.auth.failed', () {
      final f = RelayServerToClientFrame.fromJson({
        'type': 'client.auth.failed',
        'code': 'invalid_token',
        'message': 'bad token',
      });
      expect(f, isA<ClientAuthFailed>());
      expect((f as ClientAuthFailed).code, 'invalid_token');
    });

    test('Test 11: sessions with agentSessionId', () {
      final f = RelayServerToClientFrame.fromJson({
        'type': 'sessions',
        'sessions': [
          {
            'id': 's1',
            'provider': 'claude',
            'title': 'Test',
            'projectPath': '/tmp',
            'status': 'running',
            'transport': 'pty-event-stream',
            'lastActiveAt': 1000,
            'agentSessionId': 'agent-123',
          }
        ],
      });
      expect(f, isA<Sessions>());
      final session = (f as Sessions).sessions.first;
      expect(session.id, 's1');
      expect(session.agentSessionId, 'agent-123');
    });

    test('Test 12: replay.output', () {
      final f = RelayServerToClientFrame.fromJson({
        'type': 'replay.output',
        'sessionId': 's1',
        'data': 'base64data',
        'latestEventId': 99,
      });
      expect(f, isA<ReplayOutput>());
      final ro = f as ReplayOutput;
      expect(ro.sessionId, 's1');
      expect(ro.data, 'base64data');
      expect(ro.latestEventId, 99);
    });

    test('Test 13: replay.done', () {
      final f = RelayServerToClientFrame.fromJson({
        'type': 'replay.done',
        'sessionId': 's1',
        'latestEventId': 42,
      });
      expect(f, isA<ReplayDone>());
      expect((f as ReplayDone).latestEventId, 42);
    });

    test('Test 14: error with optional sessionId', () {
      final f = RelayServerToClientFrame.fromJson({
        'type': 'error',
        'code': 'gateway_unavailable',
        'message': 'no gateway',
        'sessionId': 's1',
      });
      expect(f, isA<TetherError>());
      expect((f as TetherError).code, 'gateway_unavailable');
    });

    test('Test 15: hello with optional gatewayId', () {
      final f = RelayServerToClientFrame.fromJson({
        'type': 'hello',
        'clientId': 'c1',
        'gatewayId': 'g1',
      });
      expect(f, isA<Hello>());
      expect((f as Hello).gatewayId, 'g1');
    });

    test('Test 16: unknown type throws FormatException', () {
      expect(
        () => RelayServerToClientFrame.fromJson({'type': 'unknown.frame.type'}),
        throwsA(isA<FormatException>()),
      );
    });
  });

  // RelaySessionStatus 未知值也抛出
  group('RelaySessionStatus', () {
    test('unknown status throws FormatException', () {
      expect(
        () => RelaySessionStatus.fromString('totally_unknown'),
        throwsA(isA<FormatException>()),
      );
    });
    test('running round-trip', () {
      expect(RelaySessionStatus.fromString('running'), RelaySessionStatus.running);
      expect(RelaySessionStatus.running.toJson(), 'running');
    });
  });
}
```
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether/native/flutter && flutter pub get && flutter test test/protocol_test.dart --no-pub 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/protocol_test.dart --no-pub` 输出所有 16+ 个测试通过
    - `grep -c "final class ClientChat" native/flutter/lib/models/protocol.dart` 返回 1
    - `grep -c "final class ReplayOutput" native/flutter/lib/models/protocol.dart` 返回 1
    - `grep -c "agentSessionId" native/flutter/lib/models/protocol.dart` 返回至少 2（声明 + 使用）
    - `grep -c "RelayAuthScope" native/flutter/lib/models/protocol.dart` 返回至少 3（class + field + fromJson）
    - `grep -c "FormatException" native/flutter/lib/models/protocol.dart` 返回至少 4（每个 fromString/switch 有 throw）
    - `grep -c "go_router" native/flutter/pubspec.yaml` 返回 1
    - `grep -c "flutter_secure_storage_ohos" native/flutter/pubspec.yaml` 返回 1
    - `grep -c "generate: true" native/flutter/pubspec.yaml` 返回 1
    - `grep -v '^#' native/flutter/pubspec.yaml | grep -c "flutter_markdown"` 返回 1
  </acceptance_criteria>
  <done>protocol_test.dart 16 个测试全部通过；protocol.dart 包含 8+8 变体（含 ClientChat、ReplayOutput、RelayAuthScope、agentSessionId）；所有 fromString 对未知值抛 FormatException；pubspec.yaml 含 go_router</done>
</task>

<task type="auto">
  <name>Task 2: theme.dart + main.dart（BottomNav Shell）+ ARB i18n + gen-dart.sh</name>
  <read_first>
    - native/flutter/lib/main.dart（当前内容，将完整替换）
    - .planning/phases/09-flutter-client-app/09-CONTEXT.md（D-19 Bottom Tab Bar；D-20 Settings Tab；D-39 locale key；D-40 theme key；D-04a/D-04b Chat-first；D-25 Relay only）
    - .planning/phases/09-flutter-client-app/09-PATTERNS.md（main.dart 模式；颜色常量声明；ARB 字符串清单）
    - native/flutter/lib/screens/（当前 stub 屏幕——保留 stub，仅更新路由骨架）
  </read_first>
  <files>
    native/flutter/lib/theme.dart,
    native/flutter/lib/main.dart,
    native/flutter/lib/app_shell.dart,
    native/flutter/lib/l10n/app_zh.arb,
    native/flutter/lib/l10n/app_en.arb,
    native/flutter/l10n.yaml,
    packages/protocol/scripts/gen-dart.sh
  </files>
  <action>
**第一步：创建 native/flutter/lib/theme.dart**

```dart
// lib/theme.dart — Tether 颜色系统和主题提供者
// 颜色源自 apps/web/src/styles.css，保持两端视觉统一
import 'package:flutter/material.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:xterm/xterm.dart';

// 9 个语义颜色标记
const Color tetherBackground  = Color(0xFF0c0e10);
const Color tetherSurface     = Color(0xFF171a1f);
const Color tetherBorder      = Color(0xFF2b3137);
const Color tetherInputBorder = Color(0xFF3a424b);
const Color tetherAccent      = Color(0xFF8fd0ff);
const Color tetherForeground  = Color(0xFFe8ecef);
const Color tetherMuted       = Color(0xFF9aa4af);
const Color tetherDestructive = Color(0xFFe05252);
const Color tetherAssistant   = Color(0xFF232b34); // 助手气泡背景
const Color tetherUser        = Color(0xFF1a3a5c); // 用户气泡背景

// Terminal 别名
const Color tetherTerminalBackground = tetherBackground;
const Color tetherTerminalForeground = tetherForeground;
const Color tetherTerminalCursor     = tetherAccent;

// 23 色 TerminalTheme（xterm pub.dev）
const TerminalTheme tetherTerminalTheme = TerminalTheme(
  cursor:                     tetherTerminalCursor,
  selection:                  Color(0x408fd0ff),
  foreground:                 tetherTerminalForeground,
  background:                 tetherTerminalBackground,
  black:                      Color(0xFF171a1f),
  red:                        Color(0xFFe05252),
  green:                      Color(0xFF4caf50),
  yellow:                     Color(0xFFffb74d),
  blue:                       Color(0xFF8fd0ff),
  magenta:                    Color(0xFFba68c8),
  cyan:                       Color(0xFF4dd0e1),
  white:                      Color(0xFFe8ecef),
  brightBlack:                Color(0xFF9aa4af),
  brightRed:                  Color(0xFFef5350),
  brightGreen:                Color(0xFF66bb6a),
  brightYellow:               Color(0xFFffa726),
  brightBlue:                 Color(0xFF90caf9),
  brightMagenta:              Color(0xFFce93d8),
  brightCyan:                 Color(0xFF80deea),
  brightWhite:                Color(0xFFffffff),
  searchHitBackground:        Color(0xFF8fd0ff),
  searchHitBackgroundCurrent: Color(0xFFffb74d),
  searchHitForeground:        tetherBackground,
);

ThemeData _buildDarkTheme() => ThemeData(
  brightness: Brightness.dark,
  scaffoldBackgroundColor: tetherBackground,
  colorScheme: const ColorScheme.dark(
    surface: tetherSurface,
    primary: tetherAccent,
    error: tetherDestructive,
    onSurface: tetherForeground,
  ),
  appBarTheme: const AppBarTheme(
    backgroundColor: tetherSurface,
    foregroundColor: tetherForeground,
    elevation: 0,
  ),
  bottomNavigationBarTheme: const BottomNavigationBarThemeData(
    backgroundColor: tetherSurface,
    selectedItemColor: tetherAccent,
    unselectedItemColor: tetherMuted,
    elevation: 0,
  ),
  cardTheme: const CardTheme(color: tetherSurface, margin: EdgeInsets.zero),
  elevatedButtonTheme: ElevatedButtonThemeData(
    style: ElevatedButton.styleFrom(
      backgroundColor: tetherAccent,
      foregroundColor: tetherBackground,
      minimumSize: const Size.fromHeight(48),
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)),
    ),
  ),
  inputDecorationTheme: const InputDecorationTheme(
    filled: true,
    fillColor: tetherSurface,
    border: OutlineInputBorder(borderSide: BorderSide(color: tetherInputBorder)),
    enabledBorder: OutlineInputBorder(borderSide: BorderSide(color: tetherInputBorder)),
    focusedBorder: OutlineInputBorder(borderSide: BorderSide(color: tetherAccent)),
  ),
  textTheme: const TextTheme(
    titleLarge: TextStyle(fontSize: 20, fontWeight: FontWeight.w600, color: tetherForeground),
    bodyMedium: TextStyle(fontSize: 15, color: tetherForeground),
    labelSmall: TextStyle(fontSize: 12, color: tetherMuted),
  ),
);

ThemeData _buildLightTheme() => ThemeData(
  brightness: Brightness.light,
  scaffoldBackgroundColor: const Color(0xFFf5f7fa),
  colorScheme: ColorScheme.light(
    surface: Colors.white,
    primary: tetherAccent,
    error: tetherDestructive,
  ),
  appBarTheme: const AppBarTheme(backgroundColor: Colors.white, elevation: 0),
  bottomNavigationBarTheme: const BottomNavigationBarThemeData(
    selectedItemColor: tetherAccent,
    unselectedItemColor: tetherMuted,
    elevation: 0,
  ),
  elevatedButtonTheme: ElevatedButtonThemeData(
    style: ElevatedButton.styleFrom(
      backgroundColor: tetherAccent,
      foregroundColor: Colors.white,
      minimumSize: const Size.fromHeight(48),
    ),
  ),
);

class ThemeNotifier extends ChangeNotifier {
  static const _key = 'tether:theme';
  ThemeMode _mode = ThemeMode.system;
  ThemeMode get mode => _mode;
  ThemeData get dark  => _buildDarkTheme();
  ThemeData get light => _buildLightTheme();

  Future<void> load() async {
    final prefs = await SharedPreferences.getInstance();
    final stored = prefs.getString(_key);
    _mode = stored == 'dark'  ? ThemeMode.dark
          : stored == 'light' ? ThemeMode.light
          : ThemeMode.system;
    notifyListeners();
  }

  Future<void> toggle() async {
    _mode = switch (_mode) {
      ThemeMode.system => ThemeMode.dark,
      ThemeMode.dark   => ThemeMode.light,
      ThemeMode.light  => ThemeMode.system,
    };
    final prefs = await SharedPreferences.getInstance();
    await prefs.setString(_key, _mode.name);
    notifyListeners();
  }
}

class LocaleNotifier extends ChangeNotifier {
  static const _key = 'tether:locale';
  Locale _locale = const Locale('zh');
  Locale get locale => _locale;

  Future<void> load() async {
    final prefs = await SharedPreferences.getInstance();
    final stored = prefs.getString(_key);
    _locale = stored == 'en' ? const Locale('en') : const Locale('zh');
    notifyListeners();
  }

  Future<void> toggle() async {
    _locale = _locale.languageCode == 'zh' ? const Locale('en') : const Locale('zh');
    final prefs = await SharedPreferences.getInstance();
    await prefs.setString(_key, _locale.languageCode);
    notifyListeners();
  }
}
```

**第二步：创建 native/flutter/lib/app_shell.dart（D-19/D-20 Bottom Tab Bar Shell）**

```dart
// lib/app_shell.dart
// D-19/D-20: BottomNavigationBar 主 Shell
// Tab 0: Sessions（会话列表）
// Tab 1: Settings（设置）
// IndexedStack 保持两个子页面的状态（不在切换时销毁）
import 'package:flutter/material.dart';
import 'package:flutter_gen/gen_l10n/app_localizations.dart';
import 'screens/session_list_screen.dart';
import 'screens/settings_screen.dart';

class AppShell extends StatefulWidget {
  const AppShell({super.key});
  @override
  State<AppShell> createState() => _AppShellState();
}

class _AppShellState extends State<AppShell> {
  int _selectedIndex = 0;

  final List<Widget> _pages = const [
    SessionListScreen(),
    SettingsScreen(),
  ];

  @override
  Widget build(BuildContext context) {
    final l = AppLocalizations.of(context)!;
    return Scaffold(
      body: IndexedStack(index: _selectedIndex, children: _pages),
      bottomNavigationBar: BottomNavigationBar(
        currentIndex: _selectedIndex,
        onTap: (i) => setState(() => _selectedIndex = i),
        items: [
          BottomNavigationBarItem(
            icon: const Icon(Icons.terminal),
            label: l.sessionsTab,
          ),
          BottomNavigationBarItem(
            icon: const Icon(Icons.settings),
            label: l.settingsTab,
          ),
        ],
      ),
    );
  }
}
```

**第三步：完整替换 native/flutter/lib/main.dart**

```dart
// lib/main.dart — Tether Flutter 客户端入口
// 导航: /login → /register → /shell（AppShell/BottomNav）→ /session/:id → /replay/:id
// 主题: ThemeNotifier (ThemeMode.system 默认) via SharedPreferences tether:theme
// 语言: LocaleNotifier (zh 默认) via SharedPreferences tether:locale
// 使用 go_router（D-19 BottomNav Shell 所需）
import 'package:flutter/material.dart';
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:go_router/go_router.dart';
import 'package:provider/provider.dart';
import 'theme.dart';
import 'app_shell.dart';
import 'screens/login_screen.dart';
import 'screens/register_screen.dart';
import 'screens/session_screen.dart';
import 'screens/replay_screen.dart';
import 'services/auth_service.dart';
import 'services/relay_client.dart';
import 'services/conversation_service.dart';
import 'package:flutter_gen/gen_l10n/app_localizations.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  final themeNotifier  = ThemeNotifier();
  final localeNotifier = LocaleNotifier();
  await Future.wait([themeNotifier.load(), localeNotifier.load()]);
  runApp(TetherApp(
    themeNotifier:  themeNotifier,
    localeNotifier: localeNotifier,
  ));
}

class TetherApp extends StatelessWidget {
  final ThemeNotifier  themeNotifier;
  final LocaleNotifier localeNotifier;
  const TetherApp({
    super.key,
    required this.themeNotifier,
    required this.localeNotifier,
  });

  @override
  Widget build(BuildContext context) {
    final router = GoRouter(
      initialLocation: '/login',
      routes: [
        GoRoute(
          path: '/login',
          builder: (ctx, state) => const LoginScreen(),
        ),
        GoRoute(
          path: '/register',
          builder: (ctx, state) => const RegisterScreen(),
        ),
        // AppShell: BottomNav wrapper for Sessions + Settings tabs
        GoRoute(
          path: '/shell',
          builder: (ctx, state) => const AppShell(),
        ),
        // 会话屏幕（Chat-first + Terminal 次级 Tab）— D-04a/D-04b
        GoRoute(
          path: '/session/:id',
          builder: (ctx, state) =>
              SessionScreen(sessionId: state.pathParameters['id']!),
        ),
        // 回放屏幕（只读 xterm）— D-37
        GoRoute(
          path: '/replay/:id',
          builder: (ctx, state) =>
              ReplayScreen(sessionId: state.pathParameters['id']!),
        ),
      ],
    );

    return MultiProvider(
      providers: [
        ChangeNotifierProvider.value(value: themeNotifier),
        ChangeNotifierProvider.value(value: localeNotifier),
        ChangeNotifierProvider(create: (_) => AuthService()),
        ChangeNotifierProvider(create: (_) => RelayClient()),
        ChangeNotifierProvider(create: (_) => ConversationService()),
      ],
      child: Consumer2<ThemeNotifier, LocaleNotifier>(
        builder: (ctx, theme, locale, _) => MaterialApp.router(
          title: 'Tether',
          theme: theme.light,
          darkTheme: theme.dark,
          themeMode: theme.mode,
          locale: locale.locale,
          localizationsDelegates: const [
            AppLocalizations.delegate,
            GlobalMaterialLocalizations.delegate,
            GlobalWidgetsLocalizations.delegate,
            GlobalCupertinoLocalizations.delegate,
          ],
          supportedLocales: const [Locale('zh'), Locale('en')],
          routerConfig: router,
        ),
      ),
    );
  }
}
```

为让 main.dart 通过 flutter analyze，需为尚不存在的屏幕创建 stub（session_screen.dart 是新文件）：

- `native/flutter/lib/screens/session_screen.dart` — `class SessionScreen extends StatelessWidget { const SessionScreen({super.key, required this.sessionId}); final String sessionId; @override Widget build(BuildContext context) => const Scaffold(body: SizedBox()); }`
- 确保 `native/flutter/lib/services/conversation_service.dart` stub 存在：`class ConversationService extends ChangeNotifier {}`

**第四步：创建 native/flutter/l10n.yaml**

```yaml
arb-dir: lib/l10n
template-arb-file: app_zh.arb
output-localization-file: app_localizations.dart
```

**第五步：创建 native/flutter/lib/l10n/app_zh.arb（含 Chat-first 键）**

```json
{
  "@@locale": "zh",
  "appName": "Tether",
  "agentConsoleSubtitle": "Agent 控制台",
  "sessionsTab": "会话",
  "settingsTab": "设置",
  "chatTab": "对话",
  "terminalTab": "终端",
  "loginButton": "登录",
  "registerButton": "注册",
  "noAccountLink": "还没有账号？注册",
  "hasAccountLink": "已有账号？登录",
  "emailLabel": "邮箱",
  "passwordLabel": "密码",
  "displayNameLabel": "显示名称",
  "confirmPasswordLabel": "确认密码",
  "confirmPasswordMismatch": "两次输入的密码不一致",
  "activeSessions": "活跃",
  "history": "历史",
  "gatewayList": "Gateway",
  "noSessions": "暂无 Session",
  "noSessionsDescription": "先用 CLI 启动一个 Session，然后在此刷新。",
  "gatewayNotConnected": "Gateway 未连接",
  "gatewayUnavailableDescription": "Gateway 尚未连接到 Relay，请先启动 tether gateway。",
  "sessionRunning": "运行中",
  "sessionStopped": "已停止",
  "sessionCompleted": "已完成",
  "sessionFailed": "失败",
  "sessionLost": "失联",
  "stopLabel": "停止",
  "replayLabel": "回放",
  "controlLabel": "控制",
  "observeLabel": "观察",
  "relay": "Relay",
  "relayConnectedPrefix": "Relay",
  "statusConnecting": "正在连接…",
  "statusRelayClosed": "Relay 已断开",
  "statusRelayError": "连接错误",
  "reconnectingPattern": "正在重连… {n}s 后重试",
  "@reconnectingPattern": {
    "placeholders": {
      "n": {"type": "int"}
    }
  },
  "authFailed": "认证失败，请重新登录。",
  "authErrorBadCredentials": "邮箱或密码错误，请重试。",
  "authErrorNetwork": "无法连接服务器，请检查网络或服务器地址。",
  "authErrorExpired": "登录已过期，请重新登录。",
  "relayAuthFailed": "认证失败，请重新登录。",
  "observeCannotSend": "观察模式不能输入",
  "sessionStopSent": "已发送停止请求",
  "sessionExited": "已退出",
  "loadingSessions": "加载中…",
  "settingsTitle": "设置",
  "languageToggleLabel": "语言",
  "themeToggleLabel": "主题",
  "throughRelay": "通过 Relay 连接",
  "logoutButton": "退出登录",
  "accountSection": "账号",
  "chatSendHint": "发消息…",
  "chatSendButton": "发送",
  "thinkingLabel": "思考中…",
  "toolCallLabel": "工具调用",
  "toolResultLabel": "工具结果",
  "selectPromptLabel": "请选择",
  "activityLabel": "活动",
  "unreadMessages": "{n} 条未读",
  "@unreadMessages": {
    "placeholders": {
      "n": {"type": "int"}
    }
  },
  "scrollToLatest": "滚动到最新",
  "copyToClipboard": "复制",
  "copiedToClipboard": "已复制",
  "cancelGeneration": "停止生成"
}
```

**第六步：创建 native/flutter/lib/l10n/app_en.arb（英文对应）**

```json
{
  "@@locale": "en",
  "appName": "Tether",
  "agentConsoleSubtitle": "Agent Console",
  "sessionsTab": "Sessions",
  "settingsTab": "Settings",
  "chatTab": "Chat",
  "terminalTab": "Terminal",
  "loginButton": "Sign in",
  "registerButton": "Create account",
  "noAccountLink": "Don't have an account? Register",
  "hasAccountLink": "Already have an account? Sign in",
  "emailLabel": "Email",
  "passwordLabel": "Password",
  "displayNameLabel": "Display name",
  "confirmPasswordLabel": "Confirm password",
  "confirmPasswordMismatch": "Passwords do not match",
  "activeSessions": "Active",
  "history": "History",
  "gatewayList": "Gateway",
  "noSessions": "No sessions",
  "noSessionsDescription": "Start a session with the CLI, then refresh here.",
  "gatewayNotConnected": "Gateway not connected",
  "gatewayUnavailableDescription": "The Gateway is not connected to Relay. Start tether gateway first.",
  "sessionRunning": "Running",
  "sessionStopped": "Stopped",
  "sessionCompleted": "Completed",
  "sessionFailed": "Failed",
  "sessionLost": "Lost",
  "stopLabel": "Stop",
  "replayLabel": "Replay",
  "controlLabel": "Control",
  "observeLabel": "Observe",
  "relay": "Relay",
  "relayConnectedPrefix": "Relay",
  "statusConnecting": "Connecting…",
  "statusRelayClosed": "Relay disconnected",
  "statusRelayError": "Connection error",
  "reconnectingPattern": "Reconnecting… retry in {n}s",
  "@reconnectingPattern": {
    "placeholders": {
      "n": {"type": "int"}
    }
  },
  "authFailed": "Authentication failed. Please log in again.",
  "authErrorBadCredentials": "Incorrect email or password. Please try again.",
  "authErrorNetwork": "Cannot reach server. Check network or server address.",
  "authErrorExpired": "Session expired. Please log in again.",
  "relayAuthFailed": "Authentication failed. Please log in again.",
  "observeCannotSend": "Observe mode cannot send input",
  "sessionStopSent": "Stop request sent",
  "sessionExited": "Exited",
  "loadingSessions": "Loading…",
  "settingsTitle": "Settings",
  "languageToggleLabel": "Language",
  "themeToggleLabel": "Theme",
  "throughRelay": "Connected via Relay",
  "logoutButton": "Sign out",
  "accountSection": "Account",
  "chatSendHint": "Send a message…",
  "chatSendButton": "Send",
  "thinkingLabel": "Thinking…",
  "toolCallLabel": "Tool call",
  "toolResultLabel": "Tool result",
  "selectPromptLabel": "Please select",
  "activityLabel": "Activity",
  "unreadMessages": "{n} unread",
  "@unreadMessages": {
    "placeholders": {
      "n": {"type": "int"}
    }
  },
  "scrollToLatest": "Scroll to latest",
  "copyToClipboard": "Copy",
  "copiedToClipboard": "Copied",
  "cancelGeneration": "Stop generating"
}
```

**第七步：创建 packages/protocol/scripts/gen-dart.sh（per D-32）**

```bash
#!/usr/bin/env bash
# gen-dart.sh — Dart 协议类型生成占位脚本
#
# 目的：当 packages/protocol/src/index.ts 发生变更时，用此脚本自动生成
#       native/flutter/lib/models/protocol.dart，取代手写类型。
#
# 当前协议版本：8+8 变体（RelayClientToServerFrame + RelayServerToClientFrame）
# 含 client.chat、replay.output、replay.done、RelayAuthScope、agentSessionId
#
# 预期工具：quicktype (https://quicktype.io)
#   安装：npm install -g quicktype
#   示例命令（待实现）：
#     quicktype --src-lang typescript --lang dart \
#       packages/protocol/src/index.ts \
#       -o native/flutter/lib/models/protocol.dart
#
# 当前状态：尚未实现。native/flutter/lib/models/protocol.dart 是手写版本（8+8 变体），
# 作为临时桥接。本脚本是 Phase 9 协议类型与 TypeScript 源同步的退出路径。
#
# 运行后请执行：cd native/flutter && flutter analyze && flutter test

echo "Dart codegen not yet implemented. See packages/protocol/src/index.ts for source types."
echo "Hand-written types (8+8 variants) are in native/flutter/lib/models/protocol.dart"
exit 0
```

然后 `chmod +x packages/protocol/scripts/gen-dart.sh`
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether/native/flutter && flutter gen-l10n && flutter analyze --no-pub 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze --no-pub` 输出无 error 级别问题
    - `grep -c "BottomNavigationBar" native/flutter/lib/app_shell.dart` 返回 1
    - `grep -c "IndexedStack" native/flutter/lib/app_shell.dart` 返回 1
    - `grep -c "go_router" native/flutter/lib/main.dart` 返回至少 1（import）
    - `grep -c "GoRouter" native/flutter/lib/main.dart` 返回 1
    - `grep -c "AppShell" native/flutter/lib/main.dart` 返回 1
    - `grep -c "tetherBackground" native/flutter/lib/theme.dart` 返回至少 2
    - `grep -c "TerminalTheme" native/flutter/lib/theme.dart` 返回 1
    - `grep -c "ThemeNotifier" native/flutter/lib/theme.dart` 返回 1
    - `grep -c "LocaleNotifier" native/flutter/lib/theme.dart` 返回 1
    - `grep -c "tether:theme" native/flutter/lib/theme.dart` 返回 1
    - `grep -c "tether:locale" native/flutter/lib/theme.dart` 返回 1
    - `grep -c "sessionsTab" native/flutter/lib/l10n/app_zh.arb` 返回 1
    - `grep -c "chatSendHint" native/flutter/lib/l10n/app_zh.arb` 返回 1
    - `test -f native/flutter/lib/l10n/app_en.arb` 文件存在
    - `test -x packages/protocol/scripts/gen-dart.sh` 文件可执行
  </acceptance_criteria>
  <done>flutter analyze 通过；main.dart 使用 GoRouter + AppShell（BottomNav）；theme.dart 含所有颜色常量和 TerminalTheme；ARB 文件含 Chat-first 文案键（sessionsTab/chatTab/terminalTab/chatSendHint 等）；gen-dart.sh 可执行且 exit 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pubspec → pub.dev | 依赖从 pub.dev 拉取；dependency_overrides 也从 pub.dev 拉取 |
| gen-dart.sh | 仅 echo + exit 0，无网络访问，无风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | pubspec.yaml dependency_overrides | accept | flutter_secure_storage_ohos 来自 pub.dev 官方；版本 ^1.0.0 已验证；无私有注册表 |
| T-09-01-02 | Tampering | protocol.dart fromJson | mitigate | 所有 fromString 和 fromJson 对未知 type 抛 FormatException（不静默 fallback）；16 个测试覆盖，含 Test 16 unknown type |
| T-09-01-03 | Information Disclosure | theme.dart SharedPreferences | accept | 只存储主题/语言偏好（dark/light/zh/en），无敏感数据；SharedPreferences 未加密是合理的 |
</threat_model>

<verification>
在 native/flutter/ 下执行：
```bash
flutter pub get
flutter gen-l10n
flutter analyze --no-pub
flutter test test/protocol_test.dart --no-pub
flutter build apk --debug
```

在 repo 根目录执行：
```bash
pnpm typecheck  # 必须不受影响
```

协议 variant 数量验证：
```bash
grep -c "final class Client" native/flutter/lib/models/protocol.dart  # 必须 = 8（8 个 C→S 变体）
grep -c "final class.*extends RelayServerToClientFrame" native/flutter/lib/models/protocol.dart  # 必须 = 8
```
</verification>

<success_criteria>
- flutter analyze 在 native/flutter/ 退出 0，无 error
- protocol_test.dart 16 个测试全部通过（8 client→server + 8 server→client）
- flutter build apk --debug 退出 0
- pnpm typecheck 不受影响
- protocol.dart 包含 ClientChat（8th client frame）和 ReplayOutput（6th server frame）
- main.dart 使用 GoRouter + AppShell（BottomNavigationBar），不是 MaterialApp.onGenerateRoute 纯堆栈
- ARB 含 sessionsTab、chatTab、terminalTab、chatSendHint 等 Chat-first 键
</success_criteria>

<output>
完成后创建 `.planning/phases/09-flutter-client-app/09-01-SUMMARY.md`
</output>
