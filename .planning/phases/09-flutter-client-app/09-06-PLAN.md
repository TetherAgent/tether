---
phase: 09-flutter-client-app
plan: 06
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03, 09-04, 09-05]
files_modified:
  - native/flutter/OHOS_SETUP.md
  - native/flutter/OHOS_NOTES.md
autonomous: false
requirements: [D-01, D-02, D-03, D-06, D-28, D-29, D-30, D-33, SC-1, SC-5, SC-6, SC-7]

must_haves:
  truths:
    - "flutter analyze 在 native/flutter/ 下退出 0，全部代码无 error"
    - "flutter test --no-pub 在 native/flutter/ 下全部通过"
    - "flutter build apk --debug 从 native/flutter/ 退出 0（Android smoke test）"
    - "pnpm typecheck 在 repo 根目录不受影响（pnpm 不解析 native/ 目录）"
    - "OHOS_SETUP.md 记录 DevEco Studio 版本要求、FVM 安装、flutter create 命令、OHOS 构建命令"
    - "OHOS_NOTES.md 记录 shared_preferences 和 dio 的 OHOS 兼容性测试结果（兼容 / 需 override / 需替换）"
    - "packages/protocol/scripts/gen-dart.sh 存在且以 exit 0 退出"
  artifacts:
    - path: "native/flutter/OHOS_SETUP.md"
      provides: "DevEco Studio 版本要求；FVM 命令；flutter create --platforms android,ios,ohos；OHOS 构建命令（flutter build hap --debug）；插件兼容性验证步骤"
    - path: "native/flutter/OHOS_NOTES.md"
      provides: "Phase 9 实际验证的 OHOS 插件兼容性结果；已知问题和临时解决方案；gap 文档（D-06 回退条件）"
  key_links:
    - from: "native/flutter/OHOS_SETUP.md"
      to: "packages/protocol/scripts/gen-dart.sh"
      via: "documented exit ramp for protocol codegen"
      pattern: "gen-dart.sh"
    - from: "native/flutter/OHOS_NOTES.md"
      to: "native/flutter/pubspec.yaml"
      via: "dependency_overrides: flutter_secure_storage_ohos"
      pattern: "flutter_secure_storage_ohos"
---

<objective>
执行跨平台兼容性验证和 OHOS 文档化。运行完整测试套件（flutter analyze + flutter test + flutter build apk），验证 pnpm 工作区隔离，记录 OHOS 插件兼容性测试结果。这是 Phase 9 的验收门控。

Purpose: D-02 要求 OHOS 兼容性风险在实现前被研究和记录。Phase 9 结束前需要一个 human-verify checkpoint 确认：Android 构建通过，OHOS 构建状态（通过/有文档化的 gap），pnpm 隔离验证，所有单元测试绿色。

Output: 完整测试通过报告；OHOS_SETUP.md（安装指南）；OHOS_NOTES.md（兼容性实际结果）；Phase 9 验收 checkpoint。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-flutter-client-app/09-CONTEXT.md
@.planning/phases/09-flutter-client-app/09-RESEARCH.md
@.planning/phases/09-flutter-client-app/09-VALIDATION.md

<interfaces>
<!-- 所有前置计划产出 -->
<!-- native/flutter/pubspec.yaml: 全部依赖 + dependency_overrides: flutter_secure_storage_ohos -->
<!-- native/flutter/lib/models/protocol.dart: 14 个协议类变体 -->
<!-- native/flutter/lib/services/auth_service.dart: AuthService + TokenRefreshInterceptor -->
<!-- native/flutter/lib/services/relay_client.dart: RelayClient 状态机 -->
<!-- native/flutter/lib/services/lan_client.dart: LanClient WS ticket 流 -->
<!-- native/flutter/lib/screens/: LoginScreen, RegisterScreen, SessionListScreen, TerminalScreen, ReplayScreen, SettingsScreen -->
<!-- native/flutter/lib/widgets/: KeyboardToolbar, SessionCard, StatsRow -->
<!-- native/flutter/test/: protocol_test, auth_service_test, relay_client_test, lan_client_test, session_list_test, terminal_screen_test -->
<!-- packages/protocol/scripts/gen-dart.sh: Dart codegen 占位脚本 -->

<!-- OHOS 构建要求（RESEARCH.md Environment Availability）-->
<!-- flutter (OHOS fork 3.22.0) — 必须从 gitee.com/openharmony-sig/flutter_flutter 安装 -->
<!-- DevEco Studio NEXT — OHOS 构建必需（若未安装则记录 gap，不阻断 Android CI）-->
<!-- flutter build hap --debug — OHOS 目标构建命令 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全量测试运行 + pnpm 隔离验证 + OHOS 文档创建</name>
  <read_first>
    - native/flutter/pubspec.yaml（确认所有依赖声明完整）
    - .planning/phases/09-flutter-client-app/09-VALIDATION.md（Sampling Rate 要求：phase gate 前需 full suite green）
    - .planning/phases/09-flutter-client-app/09-RESEARCH.md（Environment Availability 表：哪些工具可能未安装；Common Pitfalls 6 pnpm 命名冲突）
    - .planning/phases/09-flutter-client-app/09-CONTEXT.md（D-29 pnpm 工作区隔离；D-30 两套命令文档化；D-32 codegen 脚本；SC-1 骨架必须在 native/flutter/）
  </read_first>
  <files>
    native/flutter/OHOS_SETUP.md,
    native/flutter/OHOS_NOTES.md
  </files>
  <action>
**第一步：运行完整测试套件**

从 native/flutter/ 目录执行：
```bash
cd native/flutter
flutter pub get
flutter gen-l10n
flutter analyze --no-pub
flutter test --no-pub
flutter build apk --debug
```

记录每条命令的退出码和任何 warning/error 输出。

**第二步：验证 pnpm 工作区隔离（D-29）**

从 repo 根目录执行：
```bash
pnpm list -r 2>&1 | grep -i "native\|flutter\|dart"
pnpm typecheck
```

验证：
- `pnpm list -r` 输出不包含 native/flutter 相关包
- `pnpm typecheck` 正常完成，不出现 Dart/Flutter 相关错误

**第三步：OHOS 兼容性探测**

尝试运行（可能因环境未安装而失败——记录结果即可）：
```bash
flutter build hap --debug 2>&1 | head -30
```

根据结果分类：
- 退出 0 → OHOS 构建成功，记录到 OHOS_NOTES.md
- 退出非 0，错误是 "DevEco Studio not found" / "hvigor not found" → 环境未配置，记录 gap
- 退出非 0，错误是 MissingPluginException → 插件兼容问题，记录具体插件

**第四步：创建 native/flutter/OHOS_SETUP.md**

```markdown
# HarmonyOS / OHOS Flutter 开发环境配置

> 记录于 Phase 9 实施时（2026-05-04）。OHOS Flutter fork 版本：3.22.0。

## 必需工具

| 工具 | 用途 | 安装来源 |
|------|------|---------|
| OHOS Flutter fork 3.22.0 | SDK（支持 Android/iOS/HarmonyOS） | gitee.com/openharmony-sig/flutter_flutter |
| FVM（Flutter Version Manager）| 管理 fork SDK 版本 | fvm.app |
| DevEco Studio NEXT Beta3+ | HarmonyOS 构建 IDE | developer.harmonyos.com |
| Xcode 15+ | iOS 构建 | Mac App Store |
| Android Studio | Android 构建 | developer.android.com |
| node + ohpm + hvigor | OHOS 构建工具链 | DevEco Studio 内置 |

## FVM 安装 OHOS Fork

```bash
# 安装 FVM
dart pub global activate fvm

# 使用 OHOS fork（替代标准 Flutter）
# 参考：gitee.com/openharmony-sig/flutter_flutter/README.md
export FLUTTER_HOME=/path/to/ohos-flutter-fork
export PATH=$FLUTTER_HOME/bin:$PATH

# 验证 SDK
flutter --version  # 应显示 3.22.0 (OHOS fork)
```

## 项目初始化命令（D-02）

```bash
cd native
flutter create . --platforms android,ios,ohos --project-name tether
```

注意：Phase 9 已手动创建项目骨架。此命令仅供从头初始化使用。

## 构建命令

```bash
# 所有平台测试（从 native/flutter/ 执行）
flutter analyze --no-pub
flutter test --no-pub

# Android 构建（smoke test）
flutter build apk --debug

# iOS 构建（需 Xcode）
flutter build ios --debug --no-codesign

# HarmonyOS 构建（需 DevEco Studio + 完整 OHOS 工具链）
flutter build hap --debug
```

## pnpm 隔离确认（D-29/D-30）

```bash
# 在 repo 根目录 — 不得影响 Flutter
pnpm typecheck   # TypeScript 项目验证

# 在 native/flutter/ — 独立 Flutter 验证
flutter analyze --no-pub
flutter test --no-pub
```

## OHOS 插件兼容性验证

新增插件时，在 flutter build hap --debug 前运行：

```bash
# 查看 OHOS flutter_packages 中是否有 OHOS 适配版本
# gitee.com/openharmony-sig/flutter_packages

# 检查 pub.dev 插件页面是否有 HarmonyOS 平台标签
# 只有 pure-Dart 包或有 OHOS ArkTS 实现的包才安全

# 如需 override，添加到 pubspec.yaml dependency_overrides
```

## Dart codegen 退出路径（D-32/D-33）

```bash
# 当 packages/protocol/src/index.ts 发生变更时
bash packages/protocol/scripts/gen-dart.sh
# 当前输出: "Dart codegen not yet implemented"
# 目标工具: quicktype (https://quicktype.io)
# 手写类型位置: native/flutter/lib/models/protocol.dart
```
```

**第五步：创建 native/flutter/OHOS_NOTES.md**

根据第三步的实际测试结果填写。模板：

```markdown
# Phase 9 OHOS 兼容性说明

> 测试日期：2026-05-04
> OHOS Flutter fork 版本：3.22.0（gitee.com/openharmony-sig/flutter_flutter）
> 测试环境：[记录实际测试机器/模拟器]

## 插件兼容性测试结果

| 插件 | pub.dev 版本 | OHOS 状态 | 解决方案 |
|------|-------------|-----------|---------|
| xterm | ^4.0.0 | ✅ 兼容（纯 Dart，无 platform channel）| 无需 override |
| flutter_secure_storage | ^10.0.0 | ⚠️ 无内置 OHOS 支持 | dependency_overrides: flutter_secure_storage_ohos ^1.0.0 |
| flutter_secure_storage_ohos | ^1.0.0 | ✅ OHOS AES 实现 | 已在 pubspec.yaml dependency_overrides |
| dio | ^5.9.2 | [待测试] | [根据实际结果填写] |
| shared_preferences | ^2.3.0 | [待测试] | [根据实际结果填写] |
| web_socket_client | ^0.2.1 | [待测试] | [根据实际结果填写] |
| provider | ^6.1.2 | [待测试] | [根据实际结果填写] |

## flutter build hap --debug 结果

[填写实际结果：成功 / 失败原因 / 环境未配置]

## 已知 OHOS Gap（D-06 回退条件）

若以下问题无法解决，按 D-06 降级为文本日志 + 单行输入框：

- [ ] xterm TerminalView 在 OHOS 上渲染正常
- [ ] WebSocket 连接在 OHOS 上工作正常
- [ ] flutter_secure_storage_ohos token 读写正常

## 与 Android/iOS 的差异

[根据实际测试结果填写]

## 参考资料

- OHOS Flutter fork: gitee.com/openharmony-sig/flutter_flutter
- OHOS 插件仓库: gitee.com/openharmony-sig/flutter_packages
- flutter_secure_storage_ohos: pub.dev/packages/flutter_secure_storage_ohos
- DevEco Studio: developer.harmonyos.com/cn/develop/deveco-studio
```

注意：OHOS_NOTES.md 中标记 [待测试] 的项目需要在有 OHOS 工具链的环境中验证。Phase 9 可先填写 Android/iOS 实测结果，OHOS 具体结果留作后续验证。D-02 的验收标准是"xterm + WebSocket 在 HarmonyOS 真机或模拟器上运行正常"——这需要 DevEco Studio 环境，可作为 CI 或专项验证步骤。
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether/native/flutter && flutter pub get && flutter gen-l10n && flutter analyze --no-pub && flutter test --no-pub 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze --no-pub` 在 native/flutter/ 下输出 "No issues found!" 或仅 info 级别
    - `flutter test --no-pub` 在 native/flutter/ 下所有测试通过（protocol_test + auth_service_test + relay_client_test + lan_client_test + session_list_test + terminal_screen_test）
    - `flutter build apk --debug` 从 native/flutter/ 退出 0
    - `pnpm typecheck` 在 repo 根目录正常完成，不含 Flutter/Dart 错误
    - `pnpm list -r 2>&1 | grep -v '^$' | grep -i flutter` 返回空（pnpm 不解析 native/flutter）
    - `test -f native/flutter/OHOS_SETUP.md` 文件存在
    - `test -f native/flutter/OHOS_NOTES.md` 文件存在
    - `grep -c "flutter build hap" native/flutter/OHOS_SETUP.md` 返回至少 1
    - `grep -c "flutter_secure_storage_ohos" native/flutter/OHOS_NOTES.md` 返回至少 1
    - `test -f packages/protocol/scripts/gen-dart.sh && test -x packages/protocol/scripts/gen-dart.sh` 通过
    - `bash packages/protocol/scripts/gen-dart.sh; echo $?` 最后输出 0
  </acceptance_criteria>
  <done>flutter analyze + flutter test + flutter build apk --debug 全部通过；pnpm 隔离验证通过；OHOS_SETUP.md 和 OHOS_NOTES.md 存在；codegen 脚本可执行</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 9 Flutter 客户端 App 全部计划（09-01 ~ 09-06）已执行完成：
- 09-01: Flutter 项目骨架 + Dart 协议类型（14 个变体）+ ARB i18n 文件 + ThemeData 常量
- 09-02: AuthService（flutter_secure_storage token 存储 + QueuedInterceptor 刷新）+ RelayClient（Relay WS 状态机）
- 09-03: LanClient（GET /api/sessions 轮询 + WS ticket 流，access token 仅在 header 中）
- 09-04: LoginScreen + RegisterScreen + SessionListScreen（stats row + gateway_unavailable + Dismissible 停止 + i18n 切换）+ SettingsScreen
- 09-05: TerminalScreen（xterm TerminalView + KeyboardToolbar Ctrl/Esc/Tab + mode toggle + AppLifecycle 重连）+ ReplayScreen
- 09-06: OHOS_SETUP.md + OHOS_NOTES.md + 完整测试套件验证

所有单元测试通过，flutter analyze 干净，flutter build apk --debug 成功。
  </what-built>
  <how-to-verify>
**自动化验证（executor 已完成）：**
```bash
# 从 native/flutter/
flutter analyze --no-pub         # 必须 "No issues found!"
flutter test --no-pub            # 必须全部绿色
flutter build apk --debug        # 必须退出 0

# 从 repo 根目录
pnpm typecheck                   # 必须不受影响
```

**手动功能验证（用户执行）：**

1. 安装 APK 到 Android 设备/模拟器（`flutter build apk --debug` 输出路径）
2. 启动 App → 看到 LoginScreen（"Tether" + "Agent 控制台" + email/password 表单）
3. 用有效账号登录 → 跳转到 SessionListScreen
4. SessionListScreen 显示：
   - 顶部 stats row（活跃/历史/Gateway 数量）
   - 活跃 session 卡片（title || provider + 状态圆点）
   - 向左滑动活跃卡片 → 红色 "停止" 背景出现
   - AppBar 右侧语言/主题/设置图标
5. 点击活跃 session → TerminalScreen 打开 xterm 终端
6. 打开软键盘 → 看到 Ctrl/Esc/Tab 工具栏（48dp，tetherSurface 背景）
7. 点击 Ctrl → 按钮高亮（tetherAccent 背景），再按字母键确认控制字节发送
8. 捏合手势 → 字体大小在 10sp~24sp 区间缩放
9. 点击 AppBar mode toggle → 切换"控制"/"观察"；观察模式时 toolbar 按钮变灰
10. 点击历史 session → ReplayScreen，看到 "回放" 标签，无 KeyboardToolbar
11. Settings（设置图标）→ 切换 Relay/LAN 模式 → 返回 SessionList 自动刷新
12. AppBar 语言图标 → 切换中/英文
  </how-to-verify>
  <resume-signal>
输入 "approved" 表示功能验证通过；或描述发现的问题（executor 将修复后重新验证）
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OHOS 构建工具链 → APK/HAP 产出 | 构建工具来自 DevEco Studio 官方渠道 |
| native/flutter/ ↔ pnpm workspace | native/ 目录被 pnpm-workspace.yaml 排除在外 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Tampering | gen-dart.sh 占位脚本 | accept | 脚本当前只 echo + exit 0，无网络访问，无文件写入。chmod +x 由 Plan 01 任务完成 |
| T-09-06-02 | Information Disclosure | OHOS_NOTES.md 中的环境信息 | accept | OHOS_NOTES.md 记录工具版本和兼容性结果，不包含 token、密钥或生产配置。公开信息 |
| T-09-06-03 | Denial of Service | flutter build hap --debug 失败不阻断 CI | accept | OHOS 构建需要 DevEco Studio 环境，CI 只运行 Android 构建。OHOS 构建失败时记录 gap 到 OHOS_NOTES.md，不阻断 Phase 9 验收 |
</threat_model>

<verification>
全量验证命令序列（从 native/flutter/ 执行）：
```bash
flutter pub get
flutter gen-l10n
flutter analyze --no-pub
flutter test --no-pub
flutter build apk --debug
```

pnpm 隔离验证（从 repo 根目录）：
```bash
pnpm typecheck
pnpm list -r | grep -i flutter   # 必须无输出
```

文档存在性验证：
```bash
test -f native/flutter/OHOS_SETUP.md && echo "OK"
test -f native/flutter/OHOS_NOTES.md && echo "OK"
bash packages/protocol/scripts/gen-dart.sh; echo "exit: $?"  # 必须输出 "exit: 0"
```
</verification>

<success_criteria>
- flutter analyze 退出 0，无 error
- flutter test 全部通过（6 个 test 文件，25+ 个测试用例）
- flutter build apk --debug 退出 0
- pnpm typecheck 不受影响
- OHOS_SETUP.md + OHOS_NOTES.md 存在并记录兼容性状态
- 人工验证 checkpoint 通过（approved）
- Phase 9 所有 SC（SC-1~SC-7）覆盖：
  - SC-1: Flutter 骨架存在于 native/flutter/（✓ 09-01）
  - SC-2: Relay 连接/列表/订阅/重放/实时 session 流程（✓ 09-02/04/05）
  - SC-3: LAN Gateway 直连（✓ 09-03/04）
  - SC-4: xterm 终端交互面（✓ 09-05）
  - SC-5: Dart 协议类型从 packages/protocol 生成或桥接（✓ 09-01）
  - SC-6: OHOS 兼容性风险记录（✓ 09-06）
  - SC-7: 不发送任意命令/Provider/会话所有权操作（✓ 全程 architecture constraint）
</success_criteria>

<output>
完成后创建 `.planning/phases/09-flutter-client-app/09-06-SUMMARY.md`
</output>
