---
phase: 09-flutter-client-app
plan: 06
type: execute
wave: 5
depends_on: [09-01, 09-02, 09-03, 09-04, 09-05]
files_modified:
  - native/flutter/OHOS_SETUP.md
  - native/flutter/OHOS_NOTES.md
autonomous: false
requirements: [D-01, D-02, D-06, D-28, D-29, D-30, D-32, D-33]

must_haves:
  truths:
    - "flutter analyze --no-pub 在 native/flutter/ 下退出 0，无 error"
    - "flutter test --no-pub 在 native/flutter/ 下全部通过（包含 Plan 01~05 所有 test 文件）"
    - "flutter build apk --debug 从 native/flutter/ 退出 0（Android smoke test）"
    - "pnpm-workspace.yaml globs 不包含 native/flutter/ — pnpm 工作区隔离验证（D-29）"
    - "Dart 协议变体数量匹配 packages/protocol/src/index.ts（8 client frames + 8 server frames）"
    - "OHOS_SETUP.md 记录构建命令、FVM 安装路径、DevEco Studio 版本要求"
    - "OHOS_NOTES.md 使用三状态 bucket：Verified / Blocked by environment / Known incompatible（不使用 [待测试]）"
    - "packages/protocol/scripts/gen-dart.sh 存在、可执行、退出 0"
    - "human verify checkpoint 覆盖：登录 → Relay 会话列表 → Chat 视图（发送/接收/工具卡片/选项行）→ Terminal 标签 → 回放页 → 主题/语言切换 → OHOS chat+WS（若环境可用）"
    - "若无 OHOS DevEco 环境，Phase 9 保持 human_needed 状态，不标记为完成"
  artifacts:
    - path: "native/flutter/OHOS_SETUP.md"
      provides: "DevEco Studio 要求；FVM 命令；flutter create --platforms android,ios,ohos；OHOS 构建命令；插件兼容性验证步骤"
    - path: "native/flutter/OHOS_NOTES.md"
      provides: "Verified/Blocked/Known-incompatible 三状态；实际测试结果；已知 gap 清单"
  key_links:
    - from: "native/flutter/OHOS_NOTES.md"
      to: "native/flutter/pubspec.yaml"
      via: "dependency_overrides: flutter_secure_storage_ohos"
      pattern: "flutter_secure_storage_ohos"
    - from: "native/flutter/lib/protocol/protocol.dart"
      to: "packages/protocol/src/index.ts"
      via: "protocol drift check: 8+8 variant count match"
      pattern: "RelayClientToServerFrame|RelayServerToClientFrame"
---

<objective>
执行 Phase 9 全量验收门控：完整测试套件、pnpm 工作区隔离、协议漂移检查、OHOS 文档化，以及人工功能验收 checkpoint。

Purpose: Plans 01~05 实现了全部 Flutter 功能。本计划是 Phase 9 的硬性验收门控。OHOS 兼容性（D-02）必须以"Verified/Blocked/Known-incompatible"明确表达，不能用模糊的"待测试"。人工验收必须覆盖最新范围（Chat-first, Relay only, 无 KeyboardToolbar）。若 OHOS DevEco 环境不可用，Phase 9 保持 human_needed。

Output: OHOS_SETUP.md，OHOS_NOTES.md，全量通过报告，人工验收 checkpoint。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-flutter-client-app/09-CONTEXT.md
@.planning/phases/09-flutter-client-app/09-RESEARCH.md
@.planning/phases/09-flutter-client-app/09-VALIDATION.md

<interfaces>
<!-- pnpm-workspace.yaml glob check — correct isolation verification -->
<!-- Read pnpm-workspace.yaml at repo root and confirm native/** is not in packages glob -->

<!-- Protocol drift check — variant count from source of truth -->
<!-- packages/protocol/src/index.ts: count union members of RelayClientToServerFrame (=8) -->
<!-- and RelayServerToClientFrame (=8) -->
<!-- native/flutter/lib/protocol/protocol.dart: count sealed class subclasses of each -->
<!-- Must match exactly. If not, drift must be fixed before this plan proceeds. -->

<!-- All test files from Plans 01~05 -->
<!-- native/flutter/test/protocol_test.dart -->
<!-- native/flutter/test/auth_service_test.dart -->
<!-- native/flutter/test/relay_client_test.dart -->
<!-- native/flutter/test/conversation_service_test.dart -->
<!-- native/flutter/test/login_screen_test.dart -->
<!-- native/flutter/test/session_list_test.dart -->
<!-- native/flutter/test/chat_session_surface_test.dart -->
<!-- native/flutter/test/replay_screen_test.dart -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全量测试运行 + 协议漂移检查 + pnpm 隔离 + OHOS 文档</name>
  <files>
    native/flutter/OHOS_SETUP.md
    native/flutter/OHOS_NOTES.md
  </files>
  <action>
**第一步：协议漂移检查（D-33）**

统计 TypeScript 源头的 frame 变体数量：
```bash
# Count RelayClientToServerFrame variants in TS source
grep -c "| {" packages/protocol/src/index.ts || true
# More specific: count lines with 'type:' inside the union
grep "type:" packages/protocol/src/index.ts | grep -v "//" | wc -l
```

手动验证：`packages/protocol/src/index.ts` 定义：
- RelayClientToServerFrame: 8 variants (auth, list, subscribe, input, resize, stop, detach, chat)
- RelayServerToClientFrame: 8 variants (auth.ok, auth.failed, sessions, hello, event, replay.output, replay.done, error)

统计 Dart 实现的子类数量：
```bash
grep "class.*extends RelayClientToServerFrame" native/flutter/lib/protocol/protocol.dart | wc -l
grep "class.*extends RelayServerToClientFrame" native/flutter/lib/protocol/protocol.dart | wc -l
```

若两边不一致，立即停止并报告 drift。修复后继续。

**第二步：pnpm 工作区隔离验证（D-29）**

检查 pnpm-workspace.yaml glob 是否排除 native/：
```bash
cat pnpm-workspace.yaml
```
验证：`packages` 列表中无 `native/**` 或 `native/flutter/**` 条目。正确的 glob 只包含 TypeScript 包（如 `packages/*`、`apps/*`）。

如需双重确认（可选）：
```bash
pnpm -r exec pwd 2>/dev/null | grep native && echo "FAIL: native/ in workspace" || echo "pnpm isolation OK"
```

注意：不要用 `pnpm list -r | grep -i flutter`，该命令可能因描述文本误报。

从 repo 根目录验证 TypeScript 项目不受影响：
```bash
pnpm typecheck
```

**第三步：完整 Flutter 测试套件**

从 native/flutter/ 执行：
```bash
cd native/flutter
flutter pub get
flutter gen-l10n
flutter analyze --no-pub
flutter test --no-pub
flutter build apk --debug
```

记录每条命令退出码和 warning/error 输出。全部必须成功。

**第四步：OHOS 兼容性探测**

尝试 OHOS 构建（可能因环境未配置而失败，记录结果即可）：
```bash
flutter build hap --debug 2>&1 | head -30
echo "Exit code: $?"
```

结果分类：
- 退出 0 → OHOS 构建成功 → 状态: `Verified`
- "DevEco Studio not found" / "hvigor not found" → 环境未配置 → 状态: `Blocked by environment`
- MissingPluginException 包名 → 插件不兼容 → 状态: `Known incompatible`

**第五步：创建 native/flutter/OHOS_SETUP.md**

写入以下内容（使用 Write tool，不用 heredoc）：

```markdown
# HarmonyOS / OHOS Flutter 开发环境配置

> 记录于 Phase 9 实施（2026-05-08）。OHOS Flutter fork 版本：3.22.0。

## 必需工具

| 工具 | 用途 | 安装来源 |
|------|------|---------|
| OHOS Flutter fork 3.22.0 | SDK（支持 Android/iOS/HarmonyOS） | gitee.com/openharmony-sig/flutter_flutter |
| FVM（Flutter Version Manager）| 管理 fork SDK 版本 | fvm.app |
| DevEco Studio NEXT Beta3+ | HarmonyOS 构建 IDE | developer.harmonyos.com |
| Xcode 15+ | iOS 构建 | Mac App Store |
| Android Studio | Android 构建 | developer.android.com |
| node + ohpm + hvigor | OHOS 构建工具链 | DevEco Studio 内置 |

## FVM 安装 OHOS Fork

```bash
dart pub global activate fvm

# 使用 OHOS fork（替代标准 Flutter）
# 参见: gitee.com/openharmony-sig/flutter_flutter/blob/master/README.md
export FLUTTER_HOME=/path/to/ohos-flutter-fork
export PATH=$FLUTTER_HOME/bin:$PATH

# 验证 SDK
flutter --version  # 应显示 3.22.0 (OHOS fork)
```

## 构建命令

```bash
# 通用测试（从 native/flutter/ 执行）
flutter analyze --no-pub
flutter test --no-pub

# Android 构建（smoke test — 无需 DevEco Studio）
flutter build apk --debug

# iOS 构建（需 Xcode）
flutter build ios --debug --no-codesign

# HarmonyOS 构建（需 DevEco Studio NEXT + 完整 OHOS 工具链）
flutter build hap --debug
```

## pnpm 工作区隔离（D-29/D-30）

`native/flutter/` 完全不在 pnpm workspace 内。验证方式：

```bash
# 确认 pnpm-workspace.yaml globs 不包含 native/**
cat pnpm-workspace.yaml

# TypeScript 验证（repo 根目录）
pnpm typecheck

# Flutter 验证（native/flutter/ 目录）
flutter analyze --no-pub
flutter test --no-pub
```

两套命令分别独立运行，互不影响。

## OHOS 插件兼容性验证步骤

新增插件时，在 `flutter build hap --debug` 前运行：

```bash
# 检查 gitee OHOS flutter_packages 是否有对应适配
# gitee.com/openharmony-sig/flutter_packages

# 检查 pub.dev 插件是否有 HarmonyOS 平台标签
# 只有 pure-Dart 包或有 OHOS ArkTS 实现的包才安全

# 如需 override，添加到 pubspec.yaml dependency_overrides:
```

## Dart 协议 Codegen 退出路径（D-32/D-33）

```bash
# 当 packages/protocol/src/index.ts 发生协议变更时
bash packages/protocol/scripts/gen-dart.sh
# 当前输出: "Dart codegen not yet implemented. Hand-written bridge: native/flutter/lib/protocol/protocol.dart"
# 目标工具: quicktype (https://quicktype.io)
# 手写类型位置: native/flutter/lib/protocol/protocol.dart
# 漂移检查: RelayClientToServerFrame 和 RelayServerToClientFrame 各 8 个变体
```
```

**第六步：创建 native/flutter/OHOS_NOTES.md**

根据第四步的实际结果填写状态 bucket（绝不使用 [待测试]）：

```markdown
# Phase 9 OHOS 兼容性说明

> 测试日期：2026-05-08
> OHOS Flutter fork 版本：3.22.0
> 测试环境：[填写实际测试机器 / 模拟器 / 无 DevEco 环境]
> D-02 目标：chat view + WebSocket 在 HarmonyOS 真机或模拟器上运行正常

## 状态定义

| 状态 | 含义 |
|------|------|
| `Verified` | 在 OHOS 真机或模拟器上实测通过 |
| `Blocked by environment` | 无 DevEco Studio / hvigor / OHOS 设备，无法测试 |
| `Known incompatible` | 实测发现 MissingPluginException 或渲染错误 |

## 插件兼容性

| 插件 | pub.dev 版本 | OHOS 状态 | 解决方案 |
|------|-------------|-----------|---------|
| xterm | ^4.0.0 | [填写实测状态] | 纯 Dart，预期可兼容 |
| flutter_secure_storage | ^10.0.0 | Known incompatible (no native OHOS impl) | dependency_overrides: flutter_secure_storage_ohos ^1.0.0 |
| flutter_secure_storage_ohos | ^1.0.0 | [填写实测状态] | OHOS AES 实现 |
| dio | ^5.9.2 | [填写实测状态] | 纯 Dart HTTP，预期可兼容 |
| shared_preferences | ^2.3.0 | [填写实测状态] | 检查 OHOS platform channel 实现 |
| web_socket_client | ^0.2.1 | [填写实测状态] | 检查 dart:io WebSocket OHOS 支持 |
| provider | ^6.1.2 | [填写实测状态] | 纯 Dart，预期可兼容 |
| go_router | ^14.0.0 | [填写实测状态] | 纯 Dart，预期可兼容 |
| flutter_markdown | ^0.7.6 | [填写实测状态] | 纯 Dart，预期可兼容 |

## flutter build hap --debug 结果

[填写实际结果：成功退出 0 / 失败原因 / Blocked by environment: DevEco Studio 未安装]

## D-02 验收状态

D-02 要求：chat view + WebSocket 在 HarmonyOS 真机或模拟器上运行正常。

- [ ] ChatSessionSurface 渲染正常（发送、接收、工具卡片）
- [ ] WebSocket 连接到 Relay 成功（auth.ok → sessions frame）
- [ ] flutter_secure_storage_ohos token 读写正常
- [ ] xterm TerminalView 渲染正常（Terminal 次级标签）

若以上任一项为 `Blocked by environment`：Phase 9 保持 `human_needed` 状态，不标记为 complete。

## D-06 回退条件

若 xterm 在 OHOS 上无法渲染（Known incompatible）：
- 降级为文本日志（SelectableText + ListView）+ 单行 TextField 输入框
- 记录在本文档 "Known incompatible" 状态
- 不阻断 Android/iOS 构建

## 参考资料

- OHOS Flutter fork: gitee.com/openharmony-sig/flutter_flutter
- OHOS 插件仓库: gitee.com/openharmony-sig/flutter_packages
- flutter_secure_storage_ohos: pub.dev/packages/flutter_secure_storage_ohos
- DevEco Studio: developer.harmonyos.com/cn/develop/deveco-studio
```
  </action>
  <verify>
    <automated>
cd /Users/dream/code/tether/native/flutter &amp;&amp; flutter pub get &amp;&amp; flutter gen-l10n &amp;&amp; flutter analyze --no-pub &amp;&amp; flutter test --no-pub 2&gt;&amp;1 | tail -30
    </automated>
  </verify>
  <done>
    - flutter analyze --no-pub: 退出 0，无 error
    - flutter test --no-pub: 全部测试通过（8 个 test 文件）
    - flutter build apk --debug: 退出 0
    - pnpm-workspace.yaml 不包含 native/** glob
    - Dart protocol 变体数量 == TypeScript 源头变体数量（各 8）
    - OHOS_SETUP.md 存在，包含 flutter build hap --debug 命令
    - OHOS_NOTES.md 存在，使用 Verified/Blocked/Known-incompatible（不含 [待测试]）
    - packages/protocol/scripts/gen-dart.sh 存在且可执行（退出 0）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 9 Flutter 客户端 App 全部计划（09-01 ~ 09-06）已执行完成：

- 09-01: Flutter 项目骨架 + Dart 协议类型（8+8 变体，含 RelayAuthScope / agentSessionId / ClientChat / ReplayOutput）+ ARB i18n 文件 + ThemeData + GoRouter + AppShell（BottomNavigationBar）
- 09-02: AuthService（flutter_secure_storage + 独立 _refreshDio 避免拦截器递归）+ RelayClient（sendChat / replayOutputStream / _authFailed 停止重连 / _buildRelayUri 后缀检测 / 重连后重订阅）
- 09-03: ConversationService（替换 LanClient；sendMessage / ConversationTurn sealed class / stream for ChatSessionSurface）
- 09-04: LoginScreen（GoRouter context.go('/shell')）+ RegisterScreen（l.confirmPasswordMismatch）+ SessionListScreen（Relay only / DateFormat locale / context.push('/session/:id') + Chat-first）+ SettingsScreen（无 LAN 选项）
- 09-05: SessionScreen（TabBar Chat 默认 / Terminal 次级，无 KeyboardToolbar）+ ChatSessionSurface（气泡 / 工具卡片 / typing indicator / select options / unread FAB）+ TerminalScreen（dispose 生命周期修复）+ ReplayScreen（replayOutputStream + AppBar 显示 title/provider）
- 09-06: 全量测试 + 协议漂移检查 + OHOS 文档（三状态 bucket）

自动化验证已通过：flutter analyze 干净，flutter test 全绿，flutter build apk 成功，pnpm 隔离确认。
  </what-built>
  <how-to-verify>
**自动化验证（executor 已完成）：**
```bash
# 从 native/flutter/
flutter analyze --no-pub         # 必须 "No issues found!"
flutter test --no-pub            # 必须全部绿色（8 个 test 文件）
flutter build apk --debug        # 必须退出 0

# 从 repo 根目录
pnpm typecheck                   # 必须不受影响
```

**手动功能验证（用户执行）：**

安装 APK 到 Android 设备或模拟器，按以下流程验收：

1. 启动 App → LoginScreen（email + password 表单，无硬编码中文）
2. 用有效账号登录 → AppShell 底部 Tab Bar（会话列表 + 设置两个 Tab）
3. 会话列表：
   - stats row 显示活跃数量 / 历史数量 / "Relay" 标签
   - 活跃 session 卡片显示 title 或 provider（fallback）、状态圆点、时间
   - 向左滑动活跃卡片 → 红色停止背景出现
   - gateway_unavailable 时显示 WifiOff 图标 + 未连接提示
4. 点击活跃 session → SessionScreen 默认打开 Chat 标签
5. Chat 视图：
   - 发送文字消息 → 右侧用户气泡出现
   - 收到 assistant 回复 → 左侧 Markdown 气泡
   - 工具调用时显示 ToolCard（工具名 + 已完成标签）
   - 收到 select options → 选项按钮行；点击后选项禁用
   - 消息超出屏幕时，底部出现 FAB + unread count badge
6. 切换到 Terminal 标签 → xterm 终端渲染（无键盘工具栏）
7. 返回列表 → 点击历史 session → ReplayScreen
   - AppBar 显示 session.title 或 provider（非 sessionId fallback 优先）
   - AppBar 右侧显示"回放" Chip（非交互）
   - 终端回放内容自动播放
8. 底部 Tab "设置" → SettingsScreen
   - 只有语言切换、主题切换、账号信息/注销
   - 无 LAN/Relay 模式切换，无 Gateway URL 输入
9. AppBar 语言图标 → 切换中/英文（界面文案即时更新）
10. AppBar 主题图标 → 切换深色/浅色主题

**OHOS 验收（若有 DevEco Studio + 真机/模拟器）：**
11. flutter build hap --debug 退出 0
12. 安装 HAP 到 OHOS 真机或模拟器
13. Chat 视图发送消息 / 接收回复 / WebSocket 连接 → 正常工作
14. 在 OHOS_NOTES.md 中将相关项更新为 `Verified`

**若无 OHOS 环境：**
在 OHOS_NOTES.md 中确认 D-02 验收状态标记为 `Blocked by environment`，Phase 9 保持 human_needed。
  </how-to-verify>
  <resume-signal>
输入 "approved" 表示 Android 功能验证通过（OHOS 若可验证请一并确认）；或描述发现的问题（executor 将修复后重新验证）。
若无 OHOS 环境，输入 "approved-android-only"，Phase 9 将标记 human_needed 等待 OHOS 验证。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native/flutter/ ↔ pnpm workspace | native/ 目录被 pnpm-workspace.yaml 排除在外 |
| OHOS 构建工具链 → HAP 产出 | 构建工具来自 DevEco Studio 官方渠道 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Tampering | gen-dart.sh 占位脚本 | accept | 脚本当前只 echo + exit 0，无网络访问，无文件写入；chmod +x 由 Plan 01 完成 |
| T-09-06-02 | Information Disclosure | OHOS_NOTES.md | accept | 只记录工具版本和兼容性结果，不包含 token、密钥或生产配置 |
| T-09-06-03 | Denial of Service | flutter build hap 失败不阻断 CI | accept | OHOS 构建需 DevEco Studio 环境；CI 只运行 Android（flutter build apk）；OHOS 状态记录于 OHOS_NOTES.md |
| T-09-06-04 | Tampering | 协议漂移：Dart 类型与 TS 源不同步 | mitigate | 协议漂移检查：grep Dart subclass count == TS variant count；gen-dart.sh 作为未来 CI hook |
</threat_model>

<verification>
```bash
# 协议漂移检查
echo "=== TypeScript variants ==="
grep -c "type:.*'" packages/protocol/src/index.ts || echo "manual count needed"
echo "=== Dart client variants ==="
grep -c "class.*extends RelayClientToServerFrame" native/flutter/lib/protocol/protocol.dart
echo "=== Dart server variants ==="
grep -c "class.*extends RelayServerToClientFrame" native/flutter/lib/protocol/protocol.dart
# Both Dart counts must equal 8

# pnpm 隔离检查（read workspace yaml, not pnpm list grep）
echo "=== pnpm-workspace.yaml ==="
cat pnpm-workspace.yaml
grep "native" pnpm-workspace.yaml && echo "FAIL: native in pnpm workspace" || echo "pnpm isolation OK"

# Flutter 全量测试（从 native/flutter/）
cd native/flutter
flutter pub get
flutter gen-l10n
flutter analyze --no-pub
flutter test --no-pub
flutter build apk --debug

# 文档存在性
test -f native/flutter/OHOS_SETUP.md && echo "OHOS_SETUP.md OK"
test -f native/flutter/OHOS_NOTES.md && echo "OHOS_NOTES.md OK"
grep -c "flutter build hap" native/flutter/OHOS_SETUP.md
# Must be >= 1
grep -c "Blocked by environment\|Verified\|Known incompatible" native/flutter/OHOS_NOTES.md
# Must be >= 1 (three-bucket system in use)
grep "[待测试]" native/flutter/OHOS_NOTES.md && echo "FAIL: [待测试] found" || echo "No [待测试] OK"

# codegen 脚本
test -f packages/protocol/scripts/gen-dart.sh && test -x packages/protocol/scripts/gen-dart.sh
bash packages/protocol/scripts/gen-dart.sh; echo "gen-dart.sh exit: $?"
# Must output "exit: 0"

# pnpm TypeScript 不受影响（从 repo 根目录）
cd /Users/dream/code/tether && pnpm typecheck
```
</verification>

<success_criteria>
- flutter analyze 退出 0，无 error
- flutter test 全部通过（8 个 test 文件：protocol, auth_service, relay_client, conversation_service, login_screen, session_list, chat_session_surface, replay_screen）
- flutter build apk --debug 退出 0
- pnpm-workspace.yaml 不含 native/** glob（pnpm 隔离）
- Dart protocol 各 8 个变体与 TS 源一致（无漂移）
- OHOS_NOTES.md 使用 Verified/Blocked by environment/Known incompatible（无 [待测试]）
- packages/protocol/scripts/gen-dart.sh 存在、可执行、退出 0
- 人工验收 checkpoint 通过：
  - Android: 登录 → Relay 会话列表 → Chat 视图（发送/接收/工具卡片/选项）→ Terminal 标签 → 回放页 → 设置（无 LAN）→ 主题/语言切换
  - OHOS: 若可用，chat + WS 验证并更新 OHOS_NOTES.md 为 Verified；若不可用，记录 Blocked by environment，Phase 9 保持 human_needed
</success_criteria>

<output>
After completion, create `.planning/phases/09-flutter-client-app/09-flutter-client-app-06-SUMMARY.md` using the template at `@/Users/dream/.claude/get-shit-done/templates/summary.md`.
</output>
