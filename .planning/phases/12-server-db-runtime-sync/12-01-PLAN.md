---
phase: 12-server-db-runtime-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/sql/002_gateway_runtime_sync.sql
  - apps/server/app/service/db.ts
autonomous: true
requirements:
  - SYNC-01
must_haves:
  truths:
    - "Server 启动后 4 张新表（gateway_sessions / gateway_chat_messages / gateway_runtime_events / gateway_sync_cursors）自动存在于 MySQL"
    - "重复启动不会产生重复建表错误（IF NOT EXISTS 保证幂等）"
    - "apps/server/sql/ 目录下新增任何 .sql 文件后 ensureSchema 会自动加载，无需手动修改 db.ts"
  artifacts:
    - path: "apps/server/sql/002_gateway_runtime_sync.sql"
      provides: "4 张新表的 DDL"
      contains: "gateway_sessions"
    - path: "apps/server/app/service/db.ts"
      provides: "动态加载所有 sql/*.sql 的 ensureSchema 实现"
      exports: ["ensureSchema"]
  key_links:
    - from: "apps/server/app/service/db.ts"
      to: "apps/server/sql/002_gateway_runtime_sync.sql"
      via: "readdirSync + readFile 顺序加载"
      pattern: "readdirSync.*sql.*\\.sort"
---

<objective>
创建 4 张 Server DB 新表的 SQL migration 文件，并修复 db.ts 中硬编码单文件的 ensureSchema()，
改为动态按字母序加载 sql/ 目录下所有 .sql 文件。

Purpose: 这是 Phase 12 的基础层，后续所有写入和读取接口都依赖这 4 张表存在于 MySQL。
Output: 002_gateway_runtime_sync.sql（4 张表 DDL）+ 修改后的 db.ts（动态加载所有 migration 文件）。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/ROADMAP.md
@/Users/dream/code/tether/.planning/phases/12-server-db-runtime-sync/12-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/12-server-db-runtime-sync/12-RESEARCH.md

<interfaces>
<!-- apps/server/app/service/db.ts 当前 ensureSchema 实现（需修改） -->
```typescript
// 当前（硬编码）：
schemaReady = (async () => {
  const sqlPath = path.resolve(__dirname, '../../sql/001_init.sql');
  const sql = await readFile(sqlPath, 'utf8');
  await this.mysql().query(sql);
})();

// 目标（动态加载）：
import { readdirSync } from 'node:fs';

schemaReady = (async () => {
  const sqlDir = path.resolve(__dirname, '../../sql');
  const files = readdirSync(sqlDir).filter(f => f.endsWith('.sql')).sort();
  for (const file of files) {
    const sql = await readFile(path.join(sqlDir, file), 'utf8');
    await this.mysql().query(sql);
  }
})();
```

<!-- apps/server/sql/001_init.sql SQL 风格约定 -->
- 主键：VARCHAR(128) 或 BIGINT AUTO_INCREMENT
- 时间：DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP，自动更新用 ON UPDATE CURRENT_TIMESTAMP
- UNIQUE 约束命名：uq_<table>_<cols>；索引命名：idx_<table>_<col>
- 所有表用 CREATE TABLE IF NOT EXISTS
- payload 列用 MEDIUMTEXT
- multipleStatements: true（config.default.ts 已设置，整个文件可多语句执行）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 002_gateway_runtime_sync.sql — 4 张新表 DDL</name>
  <files>apps/server/sql/002_gateway_runtime_sync.sql</files>
  <read_first>
    - apps/server/sql/001_init.sql（SQL 风格对齐：DATETIME、命名约定、IF NOT EXISTS）
    - apps/server/app/service/db.ts（确认 multipleStatements 已开启，整个文件可作为一个 query 执行）
  </read_first>
  <action>
创建文件 apps/server/sql/002_gateway_runtime_sync.sql，内容如下（完整复制，不增减）：

```sql
CREATE TABLE IF NOT EXISTS gateway_sessions (
  id VARCHAR(128) NOT NULL,
  account_id VARCHAR(64) NOT NULL,
  workspace_id VARCHAR(64) NOT NULL,
  gateway_id VARCHAR(128) NOT NULL,
  user_id VARCHAR(64) DEFAULT NULL,
  provider VARCHAR(64) NOT NULL,
  title VARCHAR(255) DEFAULT NULL,
  project_path VARCHAR(1024) DEFAULT NULL,
  agent_session_id VARCHAR(255) DEFAULT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'running',
  transport VARCHAR(64) NOT NULL DEFAULT 'pty-event-stream',
  last_active_at DATETIME DEFAULT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  KEY idx_gateway_sessions_account_workspace (account_id, workspace_id),
  KEY idx_gateway_sessions_gateway_id (gateway_id)
);

CREATE TABLE IF NOT EXISTS gateway_chat_messages (
  id BIGINT NOT NULL AUTO_INCREMENT,
  session_id VARCHAR(128) NOT NULL,
  turn_index INT NOT NULL,
  role VARCHAR(16) NOT NULL,
  content MEDIUMTEXT NOT NULL,
  tools_json TEXT DEFAULT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY uq_chat_messages_session_turn (session_id, turn_index),
  KEY idx_chat_messages_session_id (session_id)
);

CREATE TABLE IF NOT EXISTS gateway_runtime_events (
  id BIGINT NOT NULL AUTO_INCREMENT,
  session_id VARCHAR(128) NOT NULL,
  event_id BIGINT NOT NULL,
  event_type VARCHAR(64) NOT NULL,
  payload_json MEDIUMTEXT NOT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY uq_runtime_events_session_event (session_id, event_id),
  KEY idx_runtime_events_session_id_id (session_id, id)
);

CREATE TABLE IF NOT EXISTS gateway_sync_cursors (
  id BIGINT NOT NULL AUTO_INCREMENT,
  gateway_id VARCHAR(128) NOT NULL,
  session_id VARCHAR(128) NOT NULL,
  last_event_id BIGINT DEFAULT NULL,
  last_turn_index INT DEFAULT NULL,
  last_synced_at DATETIME DEFAULT NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  UNIQUE KEY uq_sync_cursors_gateway_session (gateway_id, session_id)
);
```
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS" /Users/dream/code/tether/apps/server/sql/002_gateway_runtime_sync.sql</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在：apps/server/sql/002_gateway_runtime_sync.sql
    - grep -c "CREATE TABLE IF NOT EXISTS" 输出为 4（对应 4 张表）
    - 文件包含字符串 gateway_sessions、gateway_chat_messages、gateway_runtime_events、gateway_sync_cursors
    - 文件包含 UNIQUE KEY uq_chat_messages_session_turn（幂等键）
    - 文件包含 UNIQUE KEY uq_runtime_events_session_event（幂等键）
    - 文件包含 UNIQUE KEY uq_sync_cursors_gateway_session（幂等键）
  </acceptance_criteria>
  <done>4 张表的 IF NOT EXISTS DDL 已写入文件，与 001_init.sql 风格对齐。</done>
</task>

<task type="auto">
  <name>Task 2: 修改 db.ts ensureSchema — 动态加载所有 sql/*.sql</name>
  <files>apps/server/app/service/db.ts</files>
  <read_first>
    - apps/server/app/service/db.ts（当前 ensureSchema 实现，确认 import 语句和 schemaReady 变量位置）
  </read_first>
  <action>
修改 apps/server/app/service/db.ts：

1. 在文件顶部 import 区追加（与 readFile 相邻）：
   ```typescript
   import { readdirSync } from 'node:fs';
   import path from 'node:path';
   ```
   注意：readFile 已从 'node:fs/promises' 导入，path 可能已导入，先检查再追加，不重复导入。

2. 将 ensureSchema() 方法内的 schemaReady 赋值块从：
   ```typescript
   schemaReady = (async () => {
     const sqlPath = path.resolve(__dirname, '../../sql/001_init.sql');
     const sql = await readFile(sqlPath, 'utf8');
     await this.mysql().query(sql);
   })();
   ```
   替换为：
   ```typescript
   schemaReady = (async () => {
     const sqlDir = path.resolve(__dirname, '../../sql');
     const files = readdirSync(sqlDir)
       .filter(f => f.endsWith('.sql'))
       .sort();
     for (const file of files) {
       const sql = await readFile(path.join(sqlDir, file), 'utf8');
       await this.mysql().query(sql);
     }
   })();
   ```

不改动文件中任何其他内容（public mysql()、public query()、public transaction()、mysqlModeEnabled() 保持原样）。
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether && pnpm --filter @tether/server build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/server/app/service/db.ts 包含字符串 readdirSync
    - apps/server/app/service/db.ts 包含字符串 .filter(f => f.endsWith('.sql'))
    - apps/server/app/service/db.ts 不再包含字符串 001_init.sql（已移除硬编码路径）
    - apps/server/app/service/db.ts 包含 .sort()（确保按字母序加载）
    - pnpm --filter @tether/server build 成功（无 TypeScript 编译错误）
  </acceptance_criteria>
  <done>ensureSchema() 动态按序加载所有 sql/*.sql，新增 migration 文件无需改代码。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sql 文件 → MySQL | migration DDL 由源码控制，不来自外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | 002_gateway_runtime_sync.sql | accept | SQL migration 文件由 git 版本控制，不接受外部输入；IF NOT EXISTS 防止重复建表 |
| T-12-01-02 | DoS | db.ts readdirSync | mitigate | readdirSync 只读本地 sql/ 目录，路径硬编码于 __dirname，不接受外部输入；文件数量有限 |
</threat_model>

<verification>
```bash
# 确认 4 张表 DDL 存在
grep -c "CREATE TABLE IF NOT EXISTS" /Users/dream/code/tether/apps/server/sql/002_gateway_runtime_sync.sql
# 期望输出: 4

# 确认 ensureSchema 已改为动态加载
grep -n "readdirSync\|001_init" /Users/dream/code/tether/apps/server/app/service/db.ts
# 期望: 有 readdirSync，无 001_init.sql

# 确认 TypeScript 编译通过
cd /Users/dream/code/tether && pnpm --filter @tether/server build
```
</verification>

<success_criteria>
- 002_gateway_runtime_sync.sql 存在，包含 4 张表（gateway_sessions / gateway_chat_messages / gateway_runtime_events / gateway_sync_cursors）
- db.ts 的 ensureSchema 使用 readdirSync + sort() 动态加载所有 sql/*.sql
- db.ts 不再硬编码 001_init.sql 路径
- TypeScript 编译无错误
</success_criteria>

<output>
完成后创建 .planning/phases/12-server-db-runtime-sync/12-01-SUMMARY.md，记录：
- 创建的文件和修改的文件
- 4 张表的名称和唯一键
- ensureSchema 修改的具体内容
</output>
