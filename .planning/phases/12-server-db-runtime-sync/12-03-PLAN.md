---
phase: 12-server-db-runtime-sync
plan: 03
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - apps/relay/src/relay.ts
  - apps/relay/src/main.ts
autonomous: true
requirements:
  - SYNC-01
must_haves:
  truths:
    - "Relay handleGatewayFrame 在 gateway.sessions case 末尾 void 调用 syncToServer（不 await）"
    - "Relay handleGatewayFrame 在 gateway.conversation case 末尾 void 调用 syncToServer（不 await）"
    - "Relay handleGatewayFrame 在 gateway.event case 末尾，对白名单事件和 agent.turn 各 void 调用 syncToServer"
    - "syncToServer 失败（网络错误 / 非 200）时只 console.warn，不抛出，不阻塞转发"
    - "TETHER_RUNTIME_SYNC_SECRET 未设置时 syncToServer 立即 return（D-04）"
    - "syncToServer 使用 AbortSignal.timeout(3000) 防止长时间阻塞"
  artifacts:
    - path: "apps/relay/src/relay.ts"
      provides: "syncToServer 函数 + handleGatewayFrame 追加的 void 调用"
      contains: "void syncToServer"
    - path: "apps/relay/src/main.ts"
      provides: "serverSyncUrl / runtimeSyncSecret 从 env 读取并传入 startRelayServer"
  key_links:
    - from: "apps/relay/src/relay.ts handleGatewayFrame"
      to: "Server /api/runtime-sync/gateway/*"
      via: "void syncToServer() 内部 fetch POST"
      pattern: "void syncToServer"
---

<objective>
在 Relay handleGatewayFrame 的三个 case（gateway.sessions / gateway.conversation / gateway.event）
末尾追加异步非阻塞 syncToServer 调用，实现 Gateway frame 向 Server DB 的实时同步（D-02、D-04、D-05）。

Purpose: Relay 是所有 Gateway frame 的汇聚点，在转发完成后 void 调用 sync，不影响 Client 实时接收。
Output: relay.ts 新增 syncToServer 函数 + 三个 case 的 void 调用；main.ts 传入两个新 option 字段。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/ROADMAP.md
@/Users/dream/code/tether/.planning/phases/12-server-db-runtime-sync/12-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/12-server-db-runtime-sync/12-RESEARCH.md
@/Users/dream/code/tether/.planning/phases/12-server-db-runtime-sync/12-PATTERNS.md

<interfaces>
<!-- apps/relay/src/relay.ts 当前 RelayServerOptions type（lines 15-21） -->
```typescript
export type RelayServerOptions = {
  host: string;
  port: number;
  secret: string;
  allowLegacySecret?: boolean;
  validateToken?: (token: string) => Promise<RelayAuthScope | undefined>;
};
```

<!-- apps/relay/src/relay.ts 当前 handleGatewayFrame（lines 176-207） -->
```typescript
function handleGatewayFrame(frame: RelayGatewayToServerFrame, gatewayScope: RelayAuthScope): void {
  switch (frame.type) {
    case 'gateway.sessions':
      if (frame.sessions.length === 0 && hasCachedRunningSessions()) {
        broadcastSessionList();
        break;
      }
      dropSessionsForGateway(frame.gatewayId);
      for (const session of frame.sessions) {
        latestSessions.set(session.id, session);
      }
      broadcastSessionList();
      break;
    case 'gateway.replay':
      sendReplay(frame.clientId, frame.sessionId, frame.events, frame.done !== false, frame.latestEventId);
      break;
    case 'gateway.conversation':
      sendConversation(frame.clientId, frame.sessionId, frame.turns);
      break;
    case 'gateway.http.response':
      resolveHttpRequest(frame.requestId, frame.status, frame.body);
      break;
    case 'gateway.event':
      sendEventToSubscribers(frame.event);
      break;
    case 'gateway.error':
      sendGatewayError(frame);
      break;
    case 'gateway.auth':
      break;
  }
}
```

<!-- apps/relay/src/main.ts 当前读取 env 方式（参照 line 6 TETHER_SERVER_URL 已读取） -->
TETHER_SERVER_URL 已在 main.ts 读取并传给 startRelayServer（validateToken 依赖它）。
TETHER_RUNTIME_SYNC_SECRET 是新增变量，需在 main.ts 读取后传入 options。

<!-- 白名单事件类型 -->
RUNTIME_EVENT_WHITELIST = new Set(['terminal.output', 'terminal.input', 'session.error', 'session.exited', 'agent.status'])
agent.turn 单独处理（走 /api/runtime-sync/gateway/event，Server 侧区分写入目标表）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 relay.ts 中新增 syncToServer + 修改 RelayServerOptions + 追加 void 调用</name>
  <files>apps/relay/src/relay.ts</files>
  <read_first>
    - apps/relay/src/relay.ts（完整文件，理解 startRelayServer 函数作用域、handleGatewayFrame 当前结构、options 使用方式）
    - apps/relay/src/main.ts（了解当前向 startRelayServer 传入哪些 options，以确定追加位置）
  </read_first>
  <action>
对 apps/relay/src/relay.ts 进行以下**精确修改**（外科手术式，不改其他代码）：

**修改 1：RelayServerOptions type（lines 15-21）追加两个可选字段**
在 `validateToken?` 行之后追加：
```typescript
  serverSyncUrl?: string;
  runtimeSyncSecret?: string;
```
最终 RelayServerOptions：
```typescript
export type RelayServerOptions = {
  host: string;
  port: number;
  secret: string;
  allowLegacySecret?: boolean;
  validateToken?: (token: string) => Promise<RelayAuthScope | undefined>;
  serverSyncUrl?: string;
  runtimeSyncSecret?: string;
};
```

**修改 2：在 startRelayServer 函数体内（const server = createServer 行之前）新增 syncToServer 函数**

在 `export async function startRelayServer(options: RelayServerOptions): Promise<RunningRelayServer> {` 内部，
紧接在 `if (!options.secret && !options.validateToken)` 检查块之后（约 line 55-57 区域），添加：

```typescript
  const RUNTIME_EVENT_WHITELIST = new Set([
    'terminal.output',
    'terminal.input',
    'session.error',
    'session.exited',
    'agent.status'
  ]);

  async function syncToServer(endpoint: string, body: unknown): Promise<void> {
    if (!options.serverSyncUrl || !options.runtimeSyncSecret) return;
    try {
      const resp = await fetch(`${options.serverSyncUrl}${endpoint}`, {
        method: 'POST',
        headers: {
          'content-type': 'application/json',
          'x-tether-runtime-sync-secret': options.runtimeSyncSecret
        },
        body: JSON.stringify(body),
        signal: AbortSignal.timeout(3000)
      });
      if (!resp.ok) {
        console.warn(`[relay] sync failed: ${endpoint} HTTP ${resp.status}`);
      }
    } catch (err) {
      console.warn(`[relay] sync error: ${endpoint}`, String(err));
    }
  }
```

**修改 3：handleGatewayFrame 内三个 case 追加 void 调用**

在 `case 'gateway.sessions':` 的 `broadcastSessionList(); break;` 行之后（break 前），追加：
```typescript
        void syncToServer('/api/runtime-sync/gateway/sessions', {
          gatewayId: frame.gatewayId,
          sessions: frame.sessions,
          scope: gatewayScope
        });
```
注意：`frame.sessions.length === 0` 的 early-break 分支不触发 sync（避免空同步），只在正常 broadcastSessionList 之后追加。

在 `case 'gateway.conversation':` 的 `sendConversation(frame.clientId, frame.sessionId, frame.turns);` 行之后（break 前），追加：
```typescript
        void syncToServer('/api/runtime-sync/gateway/conversation', {
          sessionId: frame.sessionId,
          turns: frame.turns,
          scope: gatewayScope
        });
```

在 `case 'gateway.event':` 的 `sendEventToSubscribers(frame.event);` 行之后（break 前），追加：
```typescript
        if (frame.event.type === 'agent.turn' || RUNTIME_EVENT_WHITELIST.has(frame.event.type)) {
          void syncToServer('/api/runtime-sync/gateway/event', {
            gatewayId: frame.gatewayId,
            event: frame.event,
            scope: gatewayScope
          });
        }
```

**不修改**：gateway.replay、gateway.http.response、gateway.error、gateway.auth 这几个 case，不改动任何其他函数。
  </action>
  <verify>
    <automated>grep -c "void syncToServer" /Users/dream/code/tether/apps/relay/src/relay.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/relay/src/relay.ts 包含 serverSyncUrl?: string（RelayServerOptions 新字段）
    - apps/relay/src/relay.ts 包含 runtimeSyncSecret?: string（RelayServerOptions 新字段）
    - apps/relay/src/relay.ts 包含 async function syncToServer（内部函数定义）
    - apps/relay/src/relay.ts 包含 AbortSignal.timeout(3000)（超时保护）
    - apps/relay/src/relay.ts 包含 x-tether-runtime-sync-secret（header 名称正确）
    - grep -c "void syncToServer" 输出为 3（三个 case 各一次调用）
    - apps/relay/src/relay.ts 包含 RUNTIME_EVENT_WHITELIST
    - apps/relay/src/relay.ts 包含 frame.event.type === 'agent.turn' || RUNTIME_EVENT_WHITELIST.has（事件分类逻辑）
  </acceptance_criteria>
  <done>syncToServer 函数已实现，handleGatewayFrame 三个 case 各自在原有转发逻辑后追加 void 调用。</done>
</task>

<task type="auto">
  <name>Task 2: 修改 main.ts — 读取 TETHER_RUNTIME_SYNC_SECRET 并传入 startRelayServer</name>
  <files>apps/relay/src/main.ts</files>
  <read_first>
    - apps/relay/src/main.ts（完整文件，确认 TETHER_SERVER_URL 的读取位置和 startRelayServer 调用的 options 结构）
  </read_first>
  <action>
对 apps/relay/src/main.ts 进行以下**精确修改**：

1. 找到 TETHER_SERVER_URL 的读取位置（约 line 6），在其之后追加：
   ```typescript
   const runtimeSyncSecret = process.env.TETHER_RUNTIME_SYNC_SECRET;
   ```

2. 找到 startRelayServer({...}) 调用，在 options 对象中追加（与 validateToken 同级）：
   ```typescript
   serverSyncUrl: serverUrl,
   runtimeSyncSecret,
   ```
   其中 `serverUrl` 是 TETHER_SERVER_URL 已读取的变量名（以当前文件实际名称为准）。

3. 在 main.ts 中不做其他修改。

验证：relay 的 TypeScript 编译通过。
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether && pnpm --filter @tether/relay build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/relay/src/main.ts 包含字符串 TETHER_RUNTIME_SYNC_SECRET
    - apps/relay/src/main.ts 包含 runtimeSyncSecret（变量名）
    - apps/relay/src/main.ts 包含 serverSyncUrl（传入 startRelayServer options）
    - pnpm --filter @tether/relay build 成功（无 TypeScript 编译错误）
  </acceptance_criteria>
  <done>main.ts 读取 TETHER_RUNTIME_SYNC_SECRET 并通过 RelayServerOptions 传入，syncToServer 可在运行时访问。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relay → Server /api/runtime-sync/* | Relay 使用静态 secret 向 Server 发起内部 HTTP 请求 |
| Gateway frame → Relay syncToServer | Gateway frame 数据经 Relay 转发到 Server，不经 Gateway 直接写 MySQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering | relay.ts syncToServer | accept | syncToServer 只是 HTTP 客户端，不做数据校验（Server 侧做）；secret header 防止伪造调用方 |
| T-12-03-02 | DoS | syncToServer 超时 | mitigate | AbortSignal.timeout(3000) 限制 3 秒；catch 静默跳过，不阻塞转发（D-04）|
| T-12-03-03 | Repudiation | sync 失败静默跳过 | accept | console.warn 记录失败日志；靠 gateway_sync_cursors 重连补偿（D-04 设计决策）|
</threat_model>

<verification>
```bash
# 确认 void 调用次数（期望 3）
grep -c "void syncToServer" /Users/dream/code/tether/apps/relay/src/relay.ts

# 确认 syncToServer 不 await（无 await syncToServer）
grep "await syncToServer" /Users/dream/code/tether/apps/relay/src/relay.ts
# 期望: 无输出（空）

# 确认超时保护
grep "AbortSignal.timeout" /Users/dream/code/tether/apps/relay/src/relay.ts

# 确认编译成功
cd /Users/dream/code/tether && pnpm --filter @tether/relay build
```
</verification>

<success_criteria>
- relay.ts 包含 syncToServer 函数（fetch + AbortSignal.timeout(3000) + catch warn）
- handleGatewayFrame 三个 case 各自追加 void syncToServer 调用
- 没有任何 await syncToServer 调用（D-05：不阻塞转发）
- main.ts 传入 serverSyncUrl 和 runtimeSyncSecret
- pnpm --filter @tether/relay build 无报错
</success_criteria>

<output>
完成后创建 .planning/phases/12-server-db-runtime-sync/12-03-SUMMARY.md，记录：
- syncToServer 函数签名和失败处理方式
- 三个 case 的 void 调用位置
- RUNTIME_EVENT_WHITELIST 内容
- main.ts 追加的两个 option 字段名
</output>
