---
phase: 12-server-db-runtime-sync
plan: 07
type: execute
wave: 4
depends_on:
  - "12-04"
files_modified:
  - native/flutter/lib/services/conversation_service.dart
autonomous: true
requirements:
  - SYNC-01
must_haves:
  truths:
    - "_refreshConversationSnapshot 的 catch 分支不再调用 relayClient.requestConversation(sessionId)"
    - "authService.getSessionConversation 调用保留，直接从 Server HTTP 读取"
    - "Flutter App 不再发出 client.conversation WS 帧作为 fallback"
    - "Gateway 离线时 App 的 conversation 读取返回空数组，不发 WS 请求（D-11、D-12）"
  artifacts:
    - path: "native/flutter/lib/services/conversation_service.dart"
      provides: "移除 WS fallback 后的 ConversationService"
  key_links:
    - from: "native/flutter/lib/services/conversation_service.dart _refreshConversationSnapshot"
      to: "Server GET /api/sessions/:id/conversation"
      via: "relayClient.authService.getSessionConversation(sessionId)"
      pattern: "getSessionConversation"
---

<objective>
修改 Flutter ConversationService._refreshConversationSnapshot，移除 catch 分支中的
relayClient.requestConversation(sessionId) WS fallback 调用，完成 D-12 和 D-11 的切换。

Purpose: Flutter App 读取会话历史改为只走 Server HTTP，不再 fallback 到 Relay WS。
Output: 修改后的 conversation_service.dart（仅移除 catch 中的一行 WS 调用）。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/ROADMAP.md
@/Users/dream/code/tether/.planning/phases/12-server-db-runtime-sync/12-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/12-server-db-runtime-sync/12-RESEARCH.md
@/Users/dream/code/tether/.planning/phases/12-server-db-runtime-sync/12-PATTERNS.md

<interfaces>
<!-- 当前 _refreshConversationSnapshot（conversation_service.dart lines 206-225）-->
```dart
Future<void> _refreshConversationSnapshot(
  RelayClient relayClient,
  String sessionId,
) async {
  try {
    final data = await relayClient.authService.getSessionConversation(
      sessionId,
    );
    final turns = (data['turns'] as List<dynamic>? ?? const [])
        .map(
          (entry) => RelayConversationTurn.fromJson(
            entry as Map<String, dynamic>,
          ),
        )
        .toList();
    _replaceWithConversation(turns);
  } catch (_) {
    relayClient.requestConversation(sessionId);  // <-- 待删除的 WS fallback
  }
}
```

<!-- 修改后目标 -->
```dart
Future<void> _refreshConversationSnapshot(
  RelayClient relayClient,
  String sessionId,
) async {
  try {
    final data = await relayClient.authService.getSessionConversation(
      sessionId,
    );
    final turns = (data['turns'] as List<dynamic>? ?? const [])
        .map(
          (entry) => RelayConversationTurn.fromJson(
            entry as Map<String, dynamic>,
          ),
        )
        .toList();
    _replaceWithConversation(turns);
  } catch (_) {
    // D-12 / D-11: Server DB miss 时返回空数组（D-03），不回落 Relay WS。
    // 保持 _turns 不变。
  }
}
```

<!-- native/flutter/lib/services/auth_service.dart:139-145 — getSessionConversation 已指向 Server HTTP -->
getSessionConversation 已实现，指向 Server GET /api/sessions/:id/conversation，无需改动。
Phase 12 Server 接口建好后，try 分支将正常返回数据（不再走 catch）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除 _refreshConversationSnapshot catch 分支的 WS fallback 调用</name>
  <files>native/flutter/lib/services/conversation_service.dart</files>
  <read_first>
    - native/flutter/lib/services/conversation_service.dart（定位 _refreshConversationSnapshot 方法的确切行号，确认 catch 分支内容）
    - native/flutter/lib/services/auth_service.dart（确认 getSessionConversation 已实现，指向 Server HTTP）
  </read_first>
  <action>
对 native/flutter/lib/services/conversation_service.dart 进行以下**精确修改**：

找到 _refreshConversationSnapshot 方法（约 lines 206-225），将 catch 分支：
```dart
  } catch (_) {
    relayClient.requestConversation(sessionId);
  }
```
替换为：
```dart
  } catch (_) {
    // D-12 / D-11: Server DB miss 时返回空数组（D-03），不回落 Relay WS。
    // 保持 _turns 不变。
  }
```

**只改这一处**，不改动 try 分支、方法签名、其他方法，或文件中的任何其他内容。

修改后确认：
1. relayClient.requestConversation 在整个文件中的其他调用（如果有）不受影响
2. _refreshConversationSnapshot 方法体结构完整（try-catch 均有）
  </action>
  <verify>
    <automated>grep -c "requestConversation" /Users/dream/code/tether/native/flutter/lib/services/conversation_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "requestConversation" 输出为 0（conversation_service.dart 中无 requestConversation 调用）
    - native/flutter/lib/services/conversation_service.dart 仍然包含 _refreshConversationSnapshot 方法
    - native/flutter/lib/services/conversation_service.dart 仍然包含 getSessionConversation（try 分支保留）
    - native/flutter/lib/services/conversation_service.dart 仍然包含 catch (_)（catch 分支保留，只是删除内容）
    - flutter analyze native/flutter 无新增 error（如果 flutter 工具链可用）
  </acceptance_criteria>
  <done>_refreshConversationSnapshot 的 WS fallback 已移除，Flutter App 读取历史只走 Server HTTP（D-12）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter App → Server /api/sessions/:id/conversation | 外部读请求，使用普通用户 access token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Information Disclosure | Flutter conversation_service.dart | accept | 移除 WS fallback 简化攻击面；读请求由 Server requireNormalAccess 保护 |
| T-12-07-02 | Repudiation | catch 分支静默 | accept | D-03 明确规定 Server DB miss 返回空数组，catch 保持 _turns 不变是正确的 UX 行为；无需额外日志 |
</threat_model>

<verification>
```bash
# 确认 requestConversation 已移除
grep -c "requestConversation" /Users/dream/code/tether/native/flutter/lib/services/conversation_service.dart
# 期望: 0

# 确认 getSessionConversation 保留
grep "getSessionConversation" /Users/dream/code/tether/native/flutter/lib/services/conversation_service.dart
# 期望: 1 行（try 分支）

# 确认 catch 分支保留（不是移除整个 try-catch）
grep "catch" /Users/dream/code/tether/native/flutter/lib/services/conversation_service.dart
# 期望: 1 行以上（含 _refreshConversationSnapshot 的 catch）
```
</verification>

<success_criteria>
- conversation_service.dart 无 requestConversation 调用
- _refreshConversationSnapshot 保留 try-catch 结构，catch 分支为空（仅保留注释）
- getSessionConversation 调用保留在 try 分支中
- flutter analyze（如可用）无新增 error
</success_criteria>

<output>
完成后创建 .planning/phases/12-server-db-runtime-sync/12-07-SUMMARY.md，记录：
- 修改内容：移除 catch 分支中的一行 WS fallback 调用
- 修改原因：D-12 / D-11 切换到 Server HTTP 读路径
- 验证命令结果
</output>
