---
phase: 18-sqlite
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - apps/gateway/src/types.ts
  - apps/gateway/src/store.ts
  - apps/gateway/src/relay-client.ts
  - apps/gateway/src/daemon.ts
  - apps/gateway/src/pty.ts
  - apps/gateway/src/events.ts
  - apps/gateway/src/replay.ts
  - apps/gateway/src/chat-session-runner.ts
  - apps/gateway/src/index.ts
  - apps/gateway/src/session-runner-detach-fixture.ts
  - apps/gateway/test/store.test.ts
  - apps/cli/src/main.ts
autonomous: true
requirements: [SQLITE-03]

must_haves:
  truths:
    - "store.ts 文件已删除"
    - "Session / SessionEvent / SessionEventType / SessionStatus / ChatEvent / ChatEventType 等类型迁移到独立 apps/gateway/src/types.ts"
    - "apps/gateway 中不含 better-sqlite3 / DatabaseSync / store.ts / new Store() 引用"
    - "apps/cli 中不含 better-sqlite3 / DatabaseSync / store.ts / new Store() 引用"
    - "tether attach 命令已删除（D-04）"
    - "tether ls 的 SQLite fallback 已删除，gateway HTTP 不可达则报错退出（D-05）"
    - "tether stop 的 store.listSessions fallback 已删除，改用 gateway HTTP"
    - "tether url / tether send / tether stop 的 store.getSession 调用已删除"
    - "daemon.ts 不再接受 store: Store 参数，所有 store.* 调用已清理"
    - "relay-client.ts RelayClientOptions 中 store 字段已删除"
    - "startGatewayForeground 不再创建 Store 实例"
    - "replay.ts 改为空 stub（事件回放 deferred 到后续阶段）"
    - "chat-session-runner.ts 从 types.ts 而不是 store.ts import ChatEvent / ChatEventType"
    - "index.ts 不再 re-export Store；类型重导出从 types.ts"
    - "session-runner-detach-fixture.ts 不含 new Store() 调用"
    - "test/store.test.ts 文件已删除（随 store.ts 一并删除）"
    - "pnpm typecheck 全通过（gateway + cli + relay + protocol）"
    - "D-09 验收 grep 无命中"
  artifacts:
    - path: "apps/gateway/src/types.ts"
      provides: "Session / SessionEvent / SessionEventType / SessionStatus / ChatEvent / ChatEventType / AttachState / SessionTransport 类型定义（从 store.ts 迁移）"
      contains: "SessionEventType"
    - path: "apps/gateway/src/store.ts"
      provides: "已不存在（删除）"
    - path: "apps/gateway/src/replay.ts"
      provides: "空 stub，replayEvents 返回 Promise.resolve()（事件回放 deferred）"
      contains: "replayEvents"
    - path: "apps/gateway/src/chat-session-runner.ts"
      provides: "ChatEvent / ChatEventType 从 types.ts import，不再从 store.ts"
    - path: "apps/gateway/src/index.ts"
      provides: "不再 export Store；类型重导出路径改为 types.ts"
    - path: "apps/gateway/src/daemon.ts"
      provides: "DaemonOptions 不含 store 字段；所有 store 调用替换为 ptySessions"
    - path: "apps/gateway/src/relay-client.ts"
      provides: "RelayClientOptions 不含 store 字段"
    - path: "apps/cli/src/main.ts"
      provides: "无 new Store()；attach 命令已删；ls 无 fallback"
  key_links:
    - from: "apps/gateway/src/types.ts"
      to: "apps/gateway/src/pty.ts"
      via: "import type { Session, SessionEvent, SessionEventType, SessionStatus } from './types.js'"
      pattern: "from './types.js'"
    - from: "apps/gateway/src/types.ts"
      to: "apps/gateway/src/chat-session-runner.ts"
      via: "import type { ChatEvent, ChatEventType } from './types.js'"
      pattern: "from './types.js'"
    - from: "apps/gateway/src/types.ts"
      to: "apps/gateway/src/daemon.ts"
      via: "import type { Session, SessionStatus } from './types.js'"
      pattern: "from './types.js'"
    - from: "apps/gateway/src/daemon.ts"
      to: "apps/gateway/src/pty.ts"
      via: "DaemonOptions.ptySessions 替代所有原 store.getSession / store.listSessions 调用"
      pattern: "ptySessions"
    - from: "apps/cli/src/main.ts"
      to: "gateway HTTP /api/sessions"
      via: "fetchGatewaySessions 直接调用，不降级到 store"
      pattern: "fetchGatewaySessions"
---

<objective>
彻底删除 store.ts，移除所有 better-sqlite3 / DatabaseSync 依赖，清理 daemon.ts 和 relay-client.ts 中的 store 参数，删除 CLI 的 attach 命令和所有 Store 使用，通过 D-09 验收 grep 标准。

类型迁移目标：Session / SessionEvent / SessionEventType / ChatEvent / ChatEventType 等类型迁移到独立的 apps/gateway/src/types.ts（不迁移到 pty.ts，避免 pty.ts 成为循环依赖的中心节点）。

同时清理删除 store.ts 前会导致 typecheck 失败的所有依赖文件：replay.ts 改为空 stub；chat-session-runner.ts 的 ChatEvent/ChatEventType 迁入 types.ts；index.ts 更新导出路径；session-runner-detach-fixture.ts 和 test/store.test.ts 一并清理。

Purpose: Plan 3 是整个 Phase 18 的收尾门控。完成后 Gateway 完全不依赖本地 SQLite，代码库中无 better-sqlite3 残留。
Output: types.ts 新建，store.ts 被删，10 个文件清理完毕，D-09 grep 零命中，typecheck 全通过。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/PROJECT.md
@/Users/dream/code/tether/.planning/ROADMAP.md
@/Users/dream/code/tether/.planning/STATE.md
@/Users/dream/code/tether/.planning/phases/18-sqlite/18-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/18-sqlite/18-PATTERNS.md
@/Users/dream/code/tether/.planning/phases/18-sqlite/18-01-SUMMARY.md
@/Users/dream/code/tether/.planning/phases/18-sqlite/18-02-SUMMARY.md

<interfaces>
<!-- daemon.ts 中 DaemonOptions 当前含 store: Store（需要删除） -->

```typescript
// 当前 apps/gateway/src/daemon.ts 行 28-40
import { Store, type Session } from './store.js';

export type DaemonOptions = {
  host: string;
  port: number;
  store: Store;          // ← 删除此字段
  ptySessions?: PtySessionManager;
  allowApiSessionCreate?: boolean;
  relay?: RelayConfig;
  config?: TetherConfig;
};
```

<!-- relay-client.ts RelayClientOptions 当前含 store: Store（需要删除） -->

```typescript
// 当前 apps/gateway/src/relay-client.ts 行 25-37
export type RelayClientOptions = {
  url: string;
  secret: string;
  gatewayId: string;
  token?: string;
  scope?: RelayAuthScope;
  store: Store;          // ← 删除此字段
  ptySessions?: PtySessionManager;
  runnerClientForSession?: (session: Session) => SessionRunnerClient | undefined;
  ...
};
```

<!-- daemon.ts 中仍在使用 store 的地方（Plan 1/2 未清理，Plan 3 负责） -->

```typescript
// apps/gateway/src/daemon.ts 行 199-204（markSessionLost）
const markSessionLost = (session: Session, message: string): void => {
  options.store.updateSessionStatus(session.id, 'lost');   // → options.ptySessions.updateSessionStatus(session.id, 'lost')
  options.store.appendEvent(session.id, 'session.error', {...}); // → createSessionEvent + publishEvent
  session.status = 'lost';
};

// 行 251（/api/status ptySessions 列表）
options.store.listSessions().filter(...)   // → options.ptySessions.listSessions().filter(...)

// 行 300（/api/sessions GET）
const sessions = options.store.listSessions();  // → options.ptySessions.listSessions() ?? []

// 行 382、417、427、432、446、484...（/api/sessions/:id 的 store.getSession / store.updateSessionStatus / store.appendEvent）
// 全部改为 options.ptySessions.getSession / updateSessionStatus，appendEvent → createSessionEvent + publishEvent
```

<!-- CLI main.ts 中使用 Store 的所有位置（来自 18-PATTERNS.md）-->

```
行 382  tether attach: new Store().getSession(id)
行 398-399  tether ls: new Store() + store.listSessions()
行 408  tether ls: store.updateSessionStatus()
行 443  tether url: new Store().getSession(id)
行 457-458  tether send: new Store() + store.getSession(id)
行 474  tether send: store.touchSession()
行 485-488  tether stop: new Store() + store.listSessions() fallback
行 519  stopSession: store.getSession(id)
行 627  startGatewayForeground: new Store() + new PtySessionManager(store)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 types.ts（含 ChatEvent/ChatEventType 迁移）；清理 replay.ts/chat-session-runner.ts/index.ts/session-runner-spawn.ts；然后清理 daemon.ts 并删除 store.ts 前的所有阻塞依赖</name>
  <read_first>
    - apps/gateway/src/store.ts （完整文件，确认所有类型定义，含 ChatEvent / ChatEventType / Session / SessionEvent / SessionEventType / SessionStatus / AttachState / SessionTransport）
    - apps/gateway/src/chat-session-runner.ts （确认 ChatEvent / ChatEventType import 路径）
    - apps/gateway/src/replay.ts （确认当前实现，改为 stub）
    - apps/gateway/src/index.ts （确认所有 re-export，找到 export { Store }）
    - apps/gateway/src/daemon.ts （完整文件，确认所有 store.* 调用位置，共约 20 处）
    - apps/gateway/src/pty.ts （确认 Plan 1 已有 publishEvent / createSessionEvent / updateSessionStatus）
    - apps/gateway/src/session-runner-spawn.ts （如仍含 dbPath，确认 Plan 1 是否已清理）
  </read_first>
  <action>
    **1. 新建 apps/gateway/src/types.ts（类型迁移目标）：**

    将 store.ts 中的所有类型定义复制到独立的 types.ts，包含 ChatEvent 和 ChatEventType：
    - 独立的 types.ts 是纯类型文件，任何文件都可以安全 import，无循环依赖风险

    ```typescript
    // apps/gateway/src/types.ts
    // 从 store.ts 迁移的纯类型定义，不含任何实现代码

    export type SessionStatus = 'running' | 'stopped' | 'completed' | 'failed' | 'lost';

    export type SessionTransport = 'pty-event-stream' | 'tmux' | 'chat';

    export type AttachState = 'attached' | 'detached';

    export type SessionEventType =
      | 'session.started' | 'session.exited' | 'session.error'
      | 'terminal.output' | 'terminal.theme.detected' | 'user.input'
      | 'client.attached' | 'client.detached' | 'terminal.resize'
      | 'runner.started' | 'runner.heartbeat' | 'runner.exited'
      | 'client.control_changed' | 'approval.requested' | 'diff.detected'
      | 'agent.handoff' | 'agent.status' | 'agent.select';

    export type SessionEvent<TPayload extends Record<string, unknown> = Record<string, unknown>> = {
      id: number;
      sessionId: string;
      type: SessionEventType;
      ts: number;
      payload: TPayload;
    };

    export type Session = {
      id: string;
      provider: string;
      title: string;
      projectPath: string;
      accountId?: string;
      userId?: string;
      deviceId?: string;
      gatewayId?: string;
      status: SessionStatus;
      transport: SessionTransport;
      // 从 store.ts 的 Session 类型复制其余所有字段（agentSessionId、lastActiveAt 等）
    };

    // ChatEvent / ChatEventType 同步迁移（chat-session-runner.ts 依赖）
    export type ChatEventType =
      | 'chat.started' | 'chat.message' | 'chat.error' | 'chat.ended'
      // 从 store.ts 的 ChatEventType 定义完整复制
    ;

    export type ChatEvent<TPayload extends Record<string, unknown> = Record<string, unknown>> = {
      id: number;
      sessionId: string;
      type: ChatEventType;
      ts: number;
      payload: TPayload;
    };
    ```

    从 store.ts 读取完整的类型定义，确保所有字段都被复制（Session 所有字段、ChatEventType 所有枚举值）。

    **2. 更新所有文件的 import 路径，从 './store.js' 改为 './types.js'：**

    - apps/gateway/src/pty.ts：`from './store.js'` → `from './types.js'`（仅 type imports）
    - apps/gateway/src/events.ts：`import type { SessionEvent, SessionEventType } from './store.js'` → `from './types.js'`
    - apps/gateway/src/session-status-deriver.ts：`from './store.js'` → `from './types.js'`
    - apps/gateway/src/session-runner.ts：`from './store.js'` → `from './types.js'`
    - apps/gateway/src/relay-client.ts：type imports `from './store.js'` → `from './types.js'`
    - apps/gateway/src/daemon.ts：同上

    **3. chat-session-runner.ts — 迁移 ChatEvent / ChatEventType import：**

    将 `import type { ChatEvent, ChatEventType, Store } from './store.js'` 改为：
    ```typescript
    import type { ChatEvent, ChatEventType } from './types.js';
    ```
    Store 不再需要（chat-session-runner 本身不应持有 Store，确认文件中所有 Store 使用位置并清除）。

    验收：`grep -c "from './store.js'" apps/gateway/src/chat-session-runner.ts` 输出 0。

    **4. replay.ts — 改为空 stub（事件回放 deferred 到后续阶段）：**

    将整个 replay.ts 替换为：
    ```typescript
    // replay.ts — events replay deferred to Phase 15+ (MySQL-based)
    export function replayEvents(_sessionId: string): Promise<void> {
      return Promise.resolve();
    }
    ```
    删除原有的 Store / listEvents / latestEventId 等所有 import 和实现逻辑。

    验收：`grep -c "from './store.js'" apps/gateway/src/replay.ts` 输出 0。

    **5. index.ts — 更新导出路径，删除 Store re-export：**

    读取 index.ts 当前所有 export 语句，找到 `export { Store } from './store.js'` 行并删除。
    将所有类型重导出从 `'./store.js'` 改为 `'./types.js'`：
    ```typescript
    // 改前：export type { Session, SessionEvent, ... } from './store.js';
    // 改后：export type { Session, SessionEvent, ... } from './types.js';
    ```

    验收：`grep -c "from './store.js'" apps/gateway/src/index.ts` 输出 0。

    **6. daemon.ts 新增 createSessionEvent 导入（来自 events.ts）：**
    ```typescript
    import { createSessionEvent } from './events.js';
    ```

    **7. DaemonOptions 删除 store: Store 字段：**
    ```typescript
    export type DaemonOptions = {
      host: string;
      port: number;
      // store: Store  ← 删除
      ptySessions: PtySessionManager;  // 改为必填
      allowApiSessionCreate?: boolean;
      relay?: RelayConfig;
      config?: TetherConfig;
    };
    ```

    **8. markSessionLost 改为内存操作：**
    ```typescript
    const markSessionLost = (session: Session, message: string): void => {
      options.ptySessions.updateSessionStatus(session.id, 'lost');
      const errEvent = createSessionEvent(session.id, 'session.error', {
        code: 'session_lost',
        message
      });
      options.ptySessions.publishEvent(errEvent);
      session.status = 'lost';
    };
    ```

    **9. /api/status（行 251-292）：**
    `options.store.listSessions()` → `options.ptySessions.listSessions()`

    **10. /api/sessions GET（行 294-327）：**
    `const sessions = options.store.listSessions()` → `const sessions = options.ptySessions.listSessions()`

    **11. /api/sessions POST（行 329-410）：**
    spawnSessionRunnerProcess 调用中删除 `store: options.store` 参数。
    `options.store.getSession` → `options.ptySessions.getSession`

    **12. /api/sessions/:id 系列 HTTP 处理器（行 382-484 附近）：**
    - `options.store.getSession(...)` → `options.ptySessions.getSession(...)`
    - `options.store.updateSessionStatus(...)` → `options.ptySessions.updateSessionStatus(...)`
    - `options.store.appendEvent(...)` → `createSessionEvent(...) + options.ptySessions.publishEvent(...)`
    - `options.store.listSessions()` → `options.ptySessions.listSessions()`
    - `options.store.touchSession()` → 直接删除
    - `options.store.transcript(...)` → 返回 `[]` 或 `{ text: '' }`（snapshot 端点保留路由但返回空）
    - `options.store.listEvents(...)` 和 `options.store.listRecentEvents(...)` → 返回 `[]`（历史回放 deferred）
    - `options.store.updateAttachState(...)` → 直接删除

    **13. relay client 配置：**
    daemon.ts 中创建 relayClient 时，删除 `store: options.store` 参数。

    **14. 删除 import { Store } from './store.js'：**
    改为 `import type { Session, SessionStatus } from './types.js'`（不再有值 import）
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether && grep -c "options\.store\." apps/gateway/src/daemon.ts; pnpm --filter @tether/gateway typecheck 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `grep -c "options\.store\." apps/gateway/src/daemon.ts` 输出 0
    - `grep -c "new Store()" apps/gateway/src/daemon.ts` 输出 0
    - `grep -c "import.*Store" apps/gateway/src/daemon.ts` 输出 0（无 Store 值 import）
    - apps/gateway/src/types.ts 文件存在，含 `SessionEventType` 和 `ChatEventType` 定义
    - `grep -c "from './types.js'" apps/gateway/src/pty.ts` 输出至少 1
    - `grep -c "from './types.js'" apps/gateway/src/events.ts` 输出至少 1
    - `grep -c "from './store.js'" apps/gateway/src/chat-session-runner.ts` 输出 0
    - `grep -c "from './store.js'" apps/gateway/src/replay.ts` 输出 0
    - `grep -c "from './store.js'" apps/gateway/src/index.ts` 输出 0
    - `pnpm --filter @tether/gateway typecheck` 零错误
    - DaemonOptions 不含 `store` 字段
    - `grep -c "options\.ptySessions\." apps/gateway/src/daemon.ts` 输出至少 5
  </done>
</task>

<task type="auto">
  <name>Task 2: 清理 session-runner-detach-fixture.ts；删除 test/store.test.ts 和 store.ts；清理 relay-client.ts store 字段；清理 CLI；运行 D-09 验收</name>
  <read_first>
    - apps/gateway/src/store.ts （删除前确认已无值 import）
    - apps/gateway/src/relay-client.ts 行 25-80 （RelayClientOptions 含 store 字段，确认 Plan 1/2 改完后残留）
    - apps/gateway/src/session-runner-detach-fixture.ts （确认 new Store(payload.dbPath) 位置）
    - apps/gateway/test/store.test.ts （确认只测 SQLite Store，可安全删除）
    - apps/cli/src/main.ts 行 370-510 （attach / ls / url / send / stop 命令，确认所有 Store 使用位置）
    - apps/cli/src/main.ts 行 619-652 （startGatewayForeground，含 new Store() 和 PtySessionManager(store) 构造）
    - apps/gateway/src/daemon.ts 行 380-400 （startRelayClient 调用处，确认 store 字段已删后不再传入）
  </read_first>
  <action>
    **1. session-runner-detach-fixture.ts — 删除 Store 相关代码：**

    读取文件，找到 `import { Store } from './store.js'` 和 `const store = new Store(payload.dbPath)` 等相关行：
    - 删除 `import { Store } from './store.js'`（值 import）
    - 删除 `const store = new Store(payload.dbPath)` 或等效的 Store 实例化
    - 将 `new SessionRunner(store, payload.options)` 改为 `new SessionRunner(payload.options)`（与 Plan 1 对齐，SessionRunner 已无 store 参数）
    - 保留其他逻辑，只删除 Store 相关部分

    验收：`grep -c "new Store(" apps/gateway/src/session-runner-detach-fixture.ts` 输出 0。

    **2. 删除 apps/gateway/test/store.test.ts：**

    该测试文件仅测 SQLite Store 实现，store.ts 删除后无意义：
    ```bash
    rm apps/gateway/test/store.test.ts
    ```
    验收：`test -f apps/gateway/test/store.test.ts && echo FAIL || echo PASS` 输出 PASS（文件已删）。

    **3. relay-client.ts — 删除 store 字段和相关 import：**
    - RelayClientOptions 删掉 `store: Store` 字段
    - 删掉 `import type { ... Store } from './store.js'` 中的 Store（所有类型改从 './types.js' import）
    - 如果有残留的 `options.store.*` 调用（Plan 1/2 应已清完），现在一并清除

    **4. 删除 apps/gateway/src/store.ts 文件：**

    删除前先 grep 确认所有文件的 store import 都只剩 type import 且已改为 types.js：
    ```bash
    grep -rn "from './store\.js'" apps/gateway/src/ | grep -v "type "
    ```
    如果还有 `import { Store }` 等值 import，先修复再删文件。
    确认无误后删除 store.ts。

    **5. CLI main.ts — 删除 tether attach 命令（D-04）：**
    删除整个 `program.command('attach')...` 块（行 372-391 附近），包括 `.argument`、`.option`、`.description`、`.action` 整段。

    **6. CLI main.ts — tether ls 删除 SQLite fallback（D-05）：**
    改为：
    ```typescript
    program.command('ls').description('列出已知 session').action(async () => {
      const sessions = await fetchGatewaySessions().catch((err: unknown) => {
        console.error(`无法连接 Gateway：${String(err)}`);
        process.exit(1);
      });
      for (const session of sessions) {
        const alive = session.status === 'running';
        const status = alive ? session.status : 'stopped';
        console.log(`${session.id}\t${status}\t${session.transport}\t${session.projectPath}`);
      }
    });
    ```
    删掉 `const store = new Store()`、`store.listSessions()`、`store.updateSessionStatus()`。

    **7. CLI main.ts — tether url 删除 store.getSession（行 443）：**
    改为直接构造 URL，不验证 session 存在：
    ```typescript
    .action((id: string, options: { host?: string; port: number }) => {
      const host = options.host ?? localLanAddress() ?? '127.0.0.1';
      console.log(`http://${host}:${options.port}/remote/session/${id}`);
    });
    ```
    删掉 `new Store().getSession(id)` 和相关错误处理。

    **8. CLI main.ts — tether send 删除 store.getSession / store.touchSession（行 457-474）：**
    改为查 gateway HTTP GET /api/sessions/:id，使用现有 `gatewayAuthHeaders`、`findPersistentGateway` 等函数。

    **9. CLI main.ts — tether stop 删除 store fallback（行 485-519）：**
    `stopSession` 函数改为直接发 stop 请求，不再先查 session 类型。
    tether stop --all 的 sessions 列表来源改为 `await fetchGatewaySessions(gatewayUrl)`，不 fallback。

    **10. CLI main.ts — startGatewayForeground 删除 new Store()（行 627-628）：**
    ```typescript
    // 改造前
    const store = new Store();
    const ptySessions = new PtySessionManager(store);
    const daemon = await startDaemon({ ..., store, ptySessions, ... });

    // 改造后（PtySessionManager 在 Plan 1 已改为无参构造）
    const ptySessions = new PtySessionManager();
    const daemon = await startDaemon({ ..., ptySessions, ... });
    ```

    **11. 删除 CLI main.ts 顶部的 Store import：**
    删掉所有 Store 相关 import。
    删掉 `defaultDbPath`（如果只用于 `gatewayDbSummary` 函数），同时删掉整个 `gatewayDbSummary` 函数或改为返回空字符串。

    **12. apps/gateway/package.json — 删除 better-sqlite3 依赖：**
    ```bash
    cd /Users/dream/code/tether/apps/gateway && pnpm remove better-sqlite3
    ```
    验证 package.json 不再含 better-sqlite3。

    **13. D-09 验收 grep：**
    ```bash
    grep -r "better-sqlite3\|DatabaseSync\|store\.ts\|new Store()" \
      /Users/dream/code/tether/apps/gateway \
      /Users/dream/code/tether/apps/cli
    ```
    期望：无输出。
  </action>
  <verify>
    <automated>grep -v '^#' /Users/dream/code/tether/apps/gateway/src/relay-client.ts 2>/dev/null | grep -c "store:" || echo "0"; test -f /Users/dream/code/tether/apps/gateway/test/store.test.ts && echo "FAIL:store.test.ts exists" || echo "PASS:store.test.ts deleted"; grep -rn "better-sqlite3\|DatabaseSync\|new Store()" /Users/dream/code/tether/apps/gateway /Users/dream/code/tether/apps/cli 2>/dev/null | grep -v "node_modules" | grep -v ".md" | wc -l; cd /Users/dream/code/tether && pnpm --filter @tether/gateway typecheck 2>&1 | tail -10 && pnpm --filter @tether/cli typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>
    - apps/gateway/src/store.ts 文件不存在：`ls apps/gateway/src/store.ts` 返回错误
    - apps/gateway/src/types.ts 文件存在：`ls apps/gateway/src/types.ts` 成功
    - `test -f apps/gateway/test/store.test.ts && echo FAIL || echo PASS` 输出 PASS（文件已删）
    - D-09 验收 grep 输出行数为 0：`grep -r "better-sqlite3|DatabaseSync|store\.ts|new Store()" apps/gateway apps/cli` 无命中
    - `grep -c "new Store(" apps/gateway/src/session-runner-detach-fixture.ts` 输出 0
    - relay-client.ts RelayClientOptions 无 `store` 字段：`grep -c "store: Store" apps/gateway/src/relay-client.ts` 输出 0
    - CLI 无 `new Store()` 调用：`grep -c "new Store()" apps/cli/src/main.ts` 输出 0
    - tether ls 无 store fallback：`grep -c "store\.listSessions" apps/cli/src/main.ts` 输出 0
    - tether attach 命令已删：`grep -c "command('attach')" apps/cli/src/main.ts` 输出 0
    - typecheck @tether/gateway 和 @tether/cli 零错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Gateway HTTP | tether url/send/stop 改为直接调 gateway HTTP，不再读本地 SQLite；gateway 侧有认证 |
| Gateway 内存 Map | 重启后数据靠 sessions-restore 从 MySQL 恢复；无 SQLite 持久化层，崩溃丢失未同步的内存状态为已知风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Information Disclosure | store.ts 删除后敏感数据暴露 | accept | SQLite 文件中无高价值秘密（只有 session 元数据和终端输出摘要）；D-10 不要求删用户磁盘上的 tether.db，用户可自行决定 |
| T-18-03-02 | Denial of Service | tether ls 不可达时报错退出 | accept | D-05 明确要求此行为；用户能看到清晰错误信息 |
| T-18-03-03 | Tampering | 删掉 store.ts 但残留 import | mitigate | 用 D-09 grep 验收确保无残留 import；typecheck 失败会阻止继续 |
| T-18-03-04 | Information Disclosure | tether attach 命令删除后残留代码路径 | mitigate | 整段 program.command('attach') 删除，无残留 handler |
</threat_model>

<verification>
```bash
# D-09 验收标准（CONTEXT.md 原文）
grep -r "better-sqlite3\|DatabaseSync\|store\.ts\|new Store()" \
  /Users/dream/code/tether/apps/gateway \
  /Users/dream/code/tether/apps/cli
# 期望：无输出

# store.ts 文件已不存在
ls /Users/dream/code/tether/apps/gateway/src/store.ts 2>&1
# 期望：No such file or directory

# test/store.test.ts 文件已不存在
test -f /Users/dream/code/tether/apps/gateway/test/store.test.ts && echo FAIL || echo PASS
# 期望：PASS

# types.ts 文件存在且含 SessionEventType 和 ChatEventType
grep "SessionEventType\|ChatEventType" /Users/dream/code/tether/apps/gateway/src/types.ts
# 期望：有输出（类型定义）

# 类型来源已迁移到 types.ts（不在 pty.ts 中定义）
grep -c "export type SessionEventType" /Users/dream/code/tether/apps/gateway/src/pty.ts
# 期望：0（类型在 types.ts 中，不在 pty.ts 中）

# chat-session-runner.ts 不再从 store.ts import
grep -c "from './store.js'" /Users/dream/code/tether/apps/gateway/src/chat-session-runner.ts
# 期望：0

# replay.ts 是空 stub
grep -c "from './store.js'" /Users/dream/code/tether/apps/gateway/src/replay.ts
# 期望：0

# index.ts 不再 export Store
grep -c "from './store.js'" /Users/dream/code/tether/apps/gateway/src/index.ts
# 期望：0

# session-runner-detach-fixture.ts 无 Store 实例化
grep -c "new Store(" /Users/dream/code/tether/apps/gateway/src/session-runner-detach-fixture.ts
# 期望：0

# tether attach 已删
grep "command('attach')" /Users/dream/code/tether/apps/cli/src/main.ts
# 期望：无输出

# tether ls 无 fallback
grep "store\.listSessions\|new Store" /Users/dream/code/tether/apps/cli/src/main.ts
# 期望：无输出

# daemon.ts 主要 handler 验收
grep -c "options\.store\." /Users/dream/code/tether/apps/gateway/src/daemon.ts
# 期望：0

grep -c "options\.ptySessions\." /Users/dream/code/tether/apps/gateway/src/daemon.ts
# 期望：至少 5（说明 store 调用已全部替换）

# relay-client.ts store 字段已删
grep -c "store: Store" /Users/dream/code/tether/apps/gateway/src/relay-client.ts
# 期望：0

# typecheck 全通过
cd /Users/dream/code/tether && pnpm --filter @tether/gateway typecheck
cd /Users/dream/code/tether && pnpm --filter @tether/cli typecheck
```
</verification>

<success_criteria>
1. `grep -r "better-sqlite3\|DatabaseSync\|store\.ts\|new Store()" apps/gateway apps/cli` 无命中（D-09）
2. `ls apps/gateway/src/store.ts` 返回"不存在"
3. `test -f apps/gateway/test/store.test.ts && echo FAIL || echo PASS` 输出 PASS
4. `ls apps/gateway/src/types.ts` 成功，含 SessionEventType 和 ChatEventType 定义
5. `grep -c "export type SessionEventType" apps/gateway/src/pty.ts` 输出 0（类型不在 pty.ts）
6. `grep -c "from './store.js'" apps/gateway/src/chat-session-runner.ts` 输出 0
7. `grep -c "from './store.js'" apps/gateway/src/replay.ts` 输出 0
8. `grep -c "from './store.js'" apps/gateway/src/index.ts` 输出 0
9. `grep -c "new Store(" apps/gateway/src/session-runner-detach-fixture.ts` 输出 0
10. `grep "command('attach')" apps/cli/src/main.ts` 无命中（D-04）
11. `grep "store\.listSessions" apps/cli/src/main.ts` 无命中（D-05 fallback 已删）
12. `grep -c "options\.store\." apps/gateway/src/daemon.ts` 输出 0
13. `grep -c "options\.ptySessions\." apps/gateway/src/daemon.ts` 输出至少 5
14. `grep -c "store: Store" apps/gateway/src/relay-client.ts` 输出 0
15. `pnpm --filter @tether/gateway typecheck` 零错误
16. `pnpm --filter @tether/cli typecheck` 零错误
</success_criteria>

<output>
完成后创建 `.planning/phases/18-sqlite/18-03-SUMMARY.md`
</output>
