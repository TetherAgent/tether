---
phase: 18-sqlite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gateway/src/pty.ts
  - apps/gateway/src/session-runner.ts
  - apps/gateway/src/session-status-deriver.ts
  - apps/gateway/src/relay-client.ts
autonomous: true
requirements: [SQLITE-01]

must_haves:
  truths:
    - "PTY 事件（session.started / terminal.output / user.input / terminal.resize / session.exited）不再写入 SQLite"
    - "pty.ts 的所有 store.appendEvent / store.insertSession / store.touchSession 调用已被替换"
    - "session-runner.ts 的所有 store.appendEvent / store.insertSession / store.touchSession / store.touchRunnerHeartbeat 调用已被替换或删除"
    - "session-status-deriver.ts 不再调用 store.appendEvent，改用回调注入"
    - "relay-client.ts 的 getStoredSession / markSessionLost / agent.select 不再调用 store"
    - "createSessionEvent 函数按 D-01 使用 timestamp-based ID"
    - "PtySessionManager 新增 getSession / listSessions / updateSessionStatus 公开方法"
    - "编译全通过（pnpm typecheck --filter @tether/gateway）"
  artifacts:
    - path: "apps/gateway/src/pty.ts"
      provides: "PtySessionManager 无 store 依赖；提供 getSession / listSessions / updateSessionStatus"
      contains: "createSessionEvent"
    - path: "apps/gateway/src/session-runner.ts"
      provides: "SessionRunner 无 store 依赖；AgentStatusPublisher 用回调注入"
    - path: "apps/gateway/src/session-status-deriver.ts"
      provides: "AgentStatusPublisher 不依赖 Store，只用注入的 appendEvent 回调"
    - path: "apps/gateway/src/relay-client.ts"
      provides: "markSessionLost 改内存操作；agent.select 用 createSessionEvent"
  key_links:
    - from: "apps/gateway/src/pty.ts"
      to: "createSessionEvent"
      via: "文件内函数调用"
      pattern: "createSessionEvent"
    - from: "apps/gateway/src/pty.ts"
      to: "apps/gateway/src/relay-client.ts"
      via: "publishEvent 广播 + relay send 由上层负责"
      pattern: "publishEvent"
    - from: "apps/gateway/src/session-runner.ts"
      to: "AgentStatusPublisher"
      via: "构造函数注入 appendEvent 回调"
      pattern: "AgentStatusPublisher"
---

<objective>
删掉 Gateway 内所有 SQLite 写入路径（appendEvent / insertSession / touchSession），改用 timestamp-based 事件 ID 和内存 Map，同时让 PtySessionManager 暴露查询接口供后续 Plan 2 使用。

Purpose: Plan 1 是整个去 SQLite 改造的基础层。完成后 gateway 的 PTY 事件链路不再写磁盘，编译通过，relay-client 能用内存 Map 查 session。
Output: 四个 TypeScript 文件被修改，store 调用全部消失，typecheck 通过。
</objective>

<execution_context>
@/Users/dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dream/code/tether/.planning/PROJECT.md
@/Users/dream/code/tether/.planning/ROADMAP.md
@/Users/dream/code/tether/.planning/STATE.md
@/Users/dream/code/tether/.planning/phases/18-sqlite/18-CONTEXT.md
@/Users/dream/code/tether/.planning/phases/18-sqlite/18-PATTERNS.md

<interfaces>
<!-- 从 apps/gateway/src/store.ts 提取的类型（目标文件修改后不再 import Store，但需要这些类型） -->

```typescript
// SessionEventType — pty.ts 和 session-runner.ts 都要用
export type SessionEventType =
  | 'session.started' | 'session.exited' | 'session.error'
  | 'terminal.output' | 'terminal.theme.detected' | 'user.input'
  | 'client.attached' | 'client.detached' | 'terminal.resize'
  | 'runner.started' | 'runner.heartbeat' | 'runner.exited'
  | 'client.control_changed' | 'approval.requested' | 'diff.detected'
  | 'agent.handoff' | 'agent.status' | 'agent.select';

export type SessionEvent<TPayload extends Record<string, unknown> = Record<string, unknown>> = {
  id: number;
  sessionId: string;
  type: SessionEventType;
  ts: number;
  payload: TPayload;
};

export type Session = {
  id: string;
  provider: string;
  title: string;
  projectPath: string;
  accountId?: string;
  userId?: string;
  deviceId?: string;
  gatewayId?: string;
  status: SessionStatus;  // 'running' | 'stopped' | 'completed' | 'failed' | 'lost'
  ...
};
```

<!-- chat-session-runner.ts 中 createChatEvent 的参考实现（D-01 / D-02 pattern） -->

```typescript
// apps/gateway/src/chat-session-runner.ts 行 78-107
let chatEventSequence = 0;
function createChatEvent<TPayload extends Record<string, unknown>>(
  sessionId: string, type: ChatEventType, payload: TPayload, ts = Date.now()
): ChatEvent<TPayload> {
  chatEventSequence = (chatEventSequence + 1) % 1000;
  return { id: (ts * 1000) + chatEventSequence, sessionId, type, ts, payload };
}
```

<!-- relay-client.ts 中 toRelayEvent / send 的已有签名 -->

```typescript
// apps/gateway/src/relay-client.ts 行 270-288
const send = (frame: RelayGatewayToServerFrame) => {
  if (socket?.readyState === WebSocket.OPEN) socket.send(JSON.stringify(frame));
};

// toRelayEvent 已存在，把 SessionEvent 转为 RelayTerminalEvent
function toRelayEvent(event: SessionEvent): RelayTerminalEvent { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 pty.ts 中提取 createSessionEvent，替换所有 store 调用</name>
  <read_first>
    - apps/gateway/src/pty.ts （完整文件，了解所有 store 调用位置和现有 publish 模式）
    - apps/gateway/src/store.ts （确认 Session / SessionEvent / SessionEventType / SessionStatus 类型）
    - apps/gateway/src/chat-session-runner.ts 行 78-107 （createChatEvent 模式参考，D-01 / D-02）
  </read_first>
  <behavior>
    - createSessionEvent 接受 (sessionId, type, payload, ts?) 返回 SessionEvent，使用文件级 sessionEventSequence 计数器，ID = (ts * 1000) + sequence % 1000
    - PtySessionManager 构造函数去掉 store 参数（无参构造）
    - PtySessionManager.create() 中：删 this.store.insertSession，删 this.store.appendEvent，改为 this.publishEvent(createSessionEvent(...))
    - PtySessionManager.write() 中：删 this.store.appendEvent，删 this.store.touchSession，改为 this.publishEvent(createSessionEvent(...))
    - PtySessionManager.resize() 中：删 this.store.appendEvent，改为 this.publishEvent(createSessionEvent(...))
    - PtySessionManager.flushOutput() 中：删 this.store.appendEvent，删 this.store.touchSession，改为 this.publishEvent(createSessionEvent(...))
    - PtySessionManager term.onExit 中：删 this.store.updateSessionStatus，删 this.store.appendEvent，改为内存状态更新 + this.publishEvent(createSessionEvent(...))
    - 新增 getSession(id: string): Session | undefined
    - 新增 listSessions(): Session[]
    - 新增 updateSessionStatus(id: string, status: SessionStatus): void
    - 删除 import { Store } from './store.js'，只保留 import type { Session, SessionEvent, SessionStatus } from './store.js'
    - 注意：pty.ts 不直接调 relay send，publishEvent 只做本地广播；relay 层由 relay-client.ts 订阅事件后 send
  </behavior>
  <action>
    1. 在文件顶部（import 之后）添加：
       ```typescript
       let sessionEventSequence = 0;
       function createSessionEvent<TPayload extends Record<string, unknown>>(
         sessionId: string,
         type: SessionEventType,
         payload: TPayload,
         ts = Date.now()
       ): SessionEvent<TPayload> {
         sessionEventSequence = (sessionEventSequence + 1) % 1000;
         return { id: (ts * 1000) + sessionEventSequence, sessionId, type, ts, payload };
       }
       ```
    2. 修改 import：将 `import type { Session, SessionEvent } from './store.js'` 和 `import { Store } from './store.js'` 改为 `import type { Session, SessionEvent, SessionEventType, SessionStatus } from './store.js'`
    3. PtySessionManager 构造函数从 `constructor(private readonly store: Store)` 改为 `constructor() {}`
    4. create() 方法：
       - 删 `this.store.insertSession(session)`（session 已在 this.sessions.set 中保存）
       - 将 `this.publish(this.store.appendEvent(session.id, 'session.started', {...}))` 改为 `this.publishEvent(createSessionEvent(session.id, 'session.started', {...}))`
       - term.onExit 中：删 `this.store.updateSessionStatus(session.id, status)`，改为 `live.session.status = status`（live 通过 this.sessions.get 获取）；将 `this.publish(this.store.appendEvent(session.id, 'session.exited', {...}))` 改为 `this.publishEvent(createSessionEvent(session.id, 'session.exited', {...}))`
    5. write() 方法：
       - 将 `this.publish(this.store.appendEvent(sessionId, 'user.input', {...}))` 改为 `this.publishEvent(createSessionEvent(sessionId, 'user.input', {...}))`
       - 删 `this.store.touchSession(sessionId)`
    6. resize() 方法：
       - 将 `this.publish(this.store.appendEvent(sessionId, 'terminal.resize', {...}))` 改为 `this.publishEvent(createSessionEvent(sessionId, 'terminal.resize', {...}))`
    7. flushOutput() 方法：
       - 将 `const event = this.store.appendEvent(sessionId, 'terminal.output', {...})` 改为 `const event = createSessionEvent(sessionId, 'terminal.output', {...})`
       - 将 `this.publish(event)` 改为 `this.publishEvent(event)`
       - 删 `this.store.touchSession(sessionId)`
    8. 在 publishEvent 方法之后新增三个公开方法：
       ```typescript
       getSession(id: string): Session | undefined {
         return this.sessions.get(id)?.session;
       }
       listSessions(): Session[] {
         return [...this.sessions.values()].map(live => live.session);
       }
       updateSessionStatus(id: string, status: SessionStatus): void {
         const live = this.sessions.get(id);
         if (live) { live.session.status = status; }
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether && pnpm --filter @tether/gateway typecheck 2>&1 | grep -v "^$" | tail -20</automated>
  </verify>
  <done>
    - pty.ts 中不含 `this.store.` 字样
    - pty.ts 含 `createSessionEvent`、`getSession`、`listSessions`、`updateSessionStatus`
    - typecheck 通过（无 pty.ts 相关错误）
    - grep -c "this\.store\." apps/gateway/src/pty.ts 输出 0
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 session-status-deriver.ts 和 session-runner.ts，删除所有 store 依赖</name>
  <read_first>
    - apps/gateway/src/session-status-deriver.ts （确认 AgentStatusPublisher 当前的 store 依赖方式）
    - apps/gateway/src/session-runner.ts （确认所有 store 调用位置，含 heartbeat / insertSession / appendEvent）
    - apps/gateway/src/daemon.ts 行 115-116 （AgentStatusPublisher 构造调用现状，需要后续兼容）
  </read_first>
  <action>
    **session-status-deriver.ts:**
    1. 将构造函数签名从 `constructor(private readonly sessionId: string, private readonly store: Store, private readonly publishEvent: (event: SessionEvent) => void)` 改为 `constructor(private readonly sessionId: string, private readonly appendEvent: (type: SessionEventType, payload: Record<string, unknown>) => void, private readonly publishEvent: (event: SessionEvent) => void)`
    2. emit() 方法中：将 `const event = this.store.appendEvent(this.sessionId, 'agent.status', {...})` 改为：
       ```typescript
       this.appendEvent('agent.status', { status, previousStatus, reason, source });
       ```
       并删除直接调用 publishEvent（改由 appendEvent 回调内部完成，见 session-runner.ts 注入方式）
       实际上更简洁的方式是：appendEvent 回调负责构造 event + publishEvent，所以 emit 内只调用 this.appendEvent(...)
    3. 删除 `import type { SessionEvent, Store } from './store.js'`，改为 `import type { SessionEvent, SessionEventType } from './store.js'`

    **session-runner.ts:**
    1. 修改 import：将 `import { Store, type Session, type SessionEvent } from './store.js'` 改为 `import type { Session, SessionEvent, SessionEventType } from './store.js'`
    2. 在文件顶部（import 之后）添加与 pty.ts 完全相同的 createSessionEvent 实现（各自独立的文件级 sequence 计数器，D-02 说算法一致即可）：
       ```typescript
       let sessionEventSequence = 0;
       function createSessionEvent<TPayload extends Record<string, unknown>>(
         sessionId: string, type: SessionEventType, payload: TPayload, ts = Date.now()
       ): SessionEvent<TPayload> {
         sessionEventSequence = (sessionEventSequence + 1) % 1000;
         return { id: (ts * 1000) + sessionEventSequence, sessionId, type, ts, payload };
       }
       ```
    3. SessionRunner 构造函数从 `constructor(private readonly store: Store, private readonly options: ...)` 改为 `constructor(private readonly options: ...)`
    4. start() 方法中：
       - 删 `this.store.insertSession(session)`（session 对象仍然构造，但不写 SQLite）
       - `this.statusPublisher = new AgentStatusPublisher(session.id, this.store, ...)` 改为：
         ```typescript
         this.statusPublisher = new AgentStatusPublisher(
           session.id,
           (type, payload) => {
             const event = createSessionEvent(session.id, type, payload);
             this.publishEvent(event);
           },
           (event) => this.publishEvent(event)
         );
         ```
       - 删 `pollAgentSessionId(...() => this.store.getSession(...).agentSessionId...)`  该停止检测回调改为 `() => this.exited`（不再检查 SQLite，只检查退出状态）
       - 删 `.then((agentSessionId) => { if (agentSessionId) { this.store.updateAgentSessionId(...); } })`（agentSessionId 更新由上层 relay-client onAgentIdUpdate 处理，不在 runner 存）
       - 将 `this.publishEvent(this.store.appendEvent(session.id, 'session.started', {...}))` 改为 `this.publishEvent(createSessionEvent(session.id, 'session.started', {...}))`
       - 删 `this.publishEvent(this.store.appendEvent(session.id, 'runner.started', {...}))` （runner.started 不在 relay whitelist，直接删掉整段）
       - term.onData 中：将 `const event = this.store.appendEvent(session.id, 'terminal.output', {...})` 改为 `const event = createSessionEvent(session.id, 'terminal.output', {...})`；删 `this.store.touchSession(session.id)`
       - heartbeat setInterval 内：删 `this.store.touchRunnerHeartbeat(...)`；删 `this.publishEvent(this.store.appendEvent(...'runner.heartbeat'...))` 整行（heartbeat 不在 whitelist，不再发送）；heartbeat interval 整体仍保留空 setInterval 以维持 runner 存活检测，或直接删除整个 heartbeat（heartbeat 本来只更新 SQLite 时间戳，删掉即可）。**直接删掉 heartbeat setInterval 整段。**
    5. handleRequest write 分支：将 `const event = this.store.appendEvent(this.options.id, 'user.input', {...})` 改为 `const event = createSessionEvent(this.options.id, 'user.input', {...})`；删 `this.store.touchSession(...)`
    6. handleRequest resize 分支：将 `this.publishEvent(this.store.appendEvent(this.options.id, 'terminal.resize', {...}))` 改为 `this.publishEvent(createSessionEvent(this.options.id, 'terminal.resize', {...}))`
    7. handleTermExit：
       - 删 `this.store.updateSessionStatus(sessionId, ...)`
       - 将 `this.publishEvent(this.store.appendEvent(sessionId, 'session.exited', {...}))` 改为 `this.publishEvent(createSessionEvent(sessionId, 'session.exited', {...}))`
       - 删 `this.publishEvent(this.store.appendEvent(sessionId, 'runner.exited', {...}))` 整行（runner.exited 不在 whitelist）
    8. 删除 `import { AgentStatusPublisher } from './session-status-deriver.js'` 不需要删，但构造调用参数改变了，确保签名匹配新的 AgentStatusPublisher

    **relay-client.ts 中的 agent.select 和 markSessionLost:**
    1. 在 relay-client.ts 中也添加同样的 createSessionEvent 实现（文件级独立 sequence）
    2. markSessionLost（行 639-648）：
       ```typescript
       const markSessionLost = (sessionId: string): void => {
         const session = getStoredSession(sessionId);
         if (session?.status === 'running') {
           options.ptySessions?.updateSessionStatus(sessionId, 'lost');
           const errEvent = createSessionEvent(sessionId, 'session.error', {
             code: 'session_lost',
             message: 'Gateway relay client lost the session runner'
           });
           send({ type: 'gateway.event', gatewayId: effectiveGatewayId, event: toRelayEvent(errEvent) });
         }
       };
       ```
    3. agent.select（行 598-602）：将 `const selectEvent = options.store.appendEvent(subSession.id, 'agent.select', {...})` 改为 `const selectEvent = createSessionEvent(subSession.id, 'agent.select', {...})`
    4. stopPty 中 `options.store.updateSessionStatus(sessionId, 'lost')` 改为 `options.ptySessions?.updateSessionStatus(sessionId, 'lost')` 或直接删（runner 模式下没有 ptySessions，那行可保留 ptySessions?.updateSessionStatus，但 runner 模式无 store，所以两处都用 ptySessions）
    5. getStoredSession 保持现状（行 76），Plan 2 会改它
    6. listRelaySessions 保持现状，Plan 2 会改它
    7. RelayClientOptions 中的 store: Store 字段**先不删**（daemon.ts 还在传，Plan 3 才删），只是所有 options.store.appendEvent / options.store.updateSessionStatus 的直接调用改完
    8. 删除 relay-client.ts 顶部 `import type { ... Store } from './store.js'` 中的 Store（只保留 Session / SessionEvent）
  </action>
  <verify>
    <automated>cd /Users/dream/code/tether && pnpm --filter @tether/gateway typecheck 2>&1 | tail -30</automated>
  </verify>
  <done>
    - session-status-deriver.ts 不含 Store import
    - session-runner.ts 不含 this.store.appendEvent / this.store.insertSession / this.store.touchSession / this.store.touchRunnerHeartbeat
    - relay-client.ts 的 markSessionLost 和 agent.select 不含 options.store.appendEvent
    - typecheck 全通过（zero errors）
    - grep -rn "this\.store\.\(appendEvent\|insertSession\|touchSession\|touchRunnerHeartbeat\)" apps/gateway/src/session-runner.ts apps/gateway/src/pty.ts apps/gateway/src/session-status-deriver.ts 无命中
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PTY process → PtySessionManager | PTY 输出（terminal.output）从不可信进程流入；已有 maskSensitiveOutput 过滤 |
| PtySessionManager → relay-client | 事件通过内存 listeners 传播，同进程可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | createSessionEvent ID 生成 | accept | timestamp-based ID 不用于安全目的，只用于事件排序；不是安全边界，接受碰撞概率极低的风险 |
| T-18-01-02 | Information Disclosure | maskSensitiveOutput | mitigate | 保留现有 maskSensitiveOutput 调用，不得在 createSessionEvent 路径中绕过 |
| T-18-01-03 | Denial of Service | terminal.output 无限缓冲 | accept | 现有 bufferOutput 16KB 上限和 16ms timer 保留不变，Plan 1 只替换 store 调用 |
</threat_model>

<verification>
```bash
# 验收标准 D-09 的前置检查（Plan 3 才做完整 grep，Plan 1 只验证 pty/session-runner/relay-client）
grep -c "this\.store\." /Users/dream/code/tether/apps/gateway/src/pty.ts
# 期望: 0

grep -c "this\.store\." /Users/dream/code/tether/apps/gateway/src/session-runner.ts
# 期望: 0

grep -c "options\.store\.\(appendEvent\|updateSessionStatus\)" /Users/dream/code/tether/apps/gateway/src/relay-client.ts
# 期望: 0

# typecheck 全通过
cd /Users/dream/code/tether && pnpm --filter @tether/gateway typecheck
```
</verification>

<success_criteria>
1. `grep -c "this\.store\." apps/gateway/src/pty.ts` 输出 0
2. `grep -c "this\.store\." apps/gateway/src/session-runner.ts` 输出 0
3. `grep -c "options\.store\.\(appendEvent\|updateSessionStatus\)" apps/gateway/src/relay-client.ts` 输出 0
4. `pnpm --filter @tether/gateway typecheck` 零错误
5. pty.ts 含 `getSession`、`listSessions`、`updateSessionStatus` 方法定义
6. session-status-deriver.ts 不含 `Store` import
</success_criteria>

<output>
完成后创建 `.planning/phases/18-sqlite/18-01-SUMMARY.md`
</output>
