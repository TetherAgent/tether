// Node runtime flags for detached session runners.
// Keep this central so launcher/session-runner behavior stays consistent when a
// future runtime flag is needed.
export const NODE_RUNTIME_FLAGS = [] as const;
